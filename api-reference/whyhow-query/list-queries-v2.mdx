---
title: "List Queries (V2)"
api: "GET https://xh2o-yths-38lt.n7c.xano.io/api:8qSkh_B3/v2/whyhow-query/"
description: "Get a paginated list of all user queries."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField query="query" type="string">
  Search term to filter queries.
</ParamField>
<ParamField query="page" type="integer">
  Page number for pagination.
</ParamField>
<ParamField query="offset" type="integer">
  Number of items to skip.
</ParamField>
<ParamField query="limit" type="integer">
  Maximum number of items to return.
</ParamField>

<ResponseField name="itemsReceived" type="integer">
  Number of items in the current response.
</ResponseField>
<ResponseField name="curPage" type="integer">
  Current page number.
</ResponseField>
<ResponseField name="nextPage" type="integer">
  Next page number, or null if on the last page.
</ResponseField>
<ResponseField name="prevPage" type="integer">
  Previous page number, or null if on the first page.
</ResponseField>
<ResponseField name="offset" type="integer">
  The offset applied.
</ResponseField>
<ResponseField name="perPage" type="integer">
  Items per page.
</ResponseField>
<ResponseField name="itemsTotal" type="integer">
  Total number of items.
</ResponseField>
<ResponseField name="pageTotal" type="integer">
  Total number of pages.
</ResponseField>
<ResponseField name="items" type="object[]">
  Array of query objects.
</ResponseField>

<RequestExample>
```bash cURL
curl -G \
  https://xh2o-yths-38lt.n7c.xano.io/api:8qSkh_B3/v2/whyhow-query/ \
  -H "X-Data-Source: live" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d "query=Google" \
  -d "page=1" \
  -d "limit=10"
```
</RequestExample>

<ResponseExample>
```json 200
{
  "itemsReceived": 2,
  "curPage": 1,
  "nextPage": null,
  "prevPage": null,
  "offset": 0,
  "perPage": 10,
  "itemsTotal": 2,
  "pageTotal": 1,
  "items": [
    {
      "id": 1,
      "created_at": 1700000000000,
      "updated_at": null,
      "query": "Who do I know at Google?"
    }
  ]
}
```
</ResponseExample>

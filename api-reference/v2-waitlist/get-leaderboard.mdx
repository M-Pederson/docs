---
title: "Get Leaderboard"
api: "GET https://xh2o-yths-38lt.n7c.xano.io/api:g7_LZ7hE/leaderboard"
description: "Get top referrers leaderboard with masked emails."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

### Query Parameters

<ParamField query="limit" type="integer">
  Number of entries to return (max 50). Defaults to `10`.
</ParamField>

### Response

<ResponseField name="leaderboard" type="string">
  List of top referrers with masked emails.
</ResponseField>

<ResponseField name="total_shown" type="string">
  Total number of entries shown.
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET \
  'https://xh2o-yths-38lt.n7c.xano.io/api:g7_LZ7hE/leaderboard?limit=10' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "leaderboard": "[]",
  "total_shown": "10"
}
```
</ResponseExample>

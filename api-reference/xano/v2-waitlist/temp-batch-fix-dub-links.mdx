---
title: 'TEMPORARY: Batch create missing Dub links (delete after use)'
api: 'GET https://xh2o-yths-38lt.n7c.xano.io/api:g7_LZ7hE/temp/batch-fix-dub-links'
description: 'TEMPORARY: Batch create missing Dub links (delete after use) Authentication: not required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

## Query Parameters

<ParamField query="limit" type="integer">
  Total entries to process in this call
</ParamField>


## Response

<ResponseField name="processed" type="string">
  The processed field.
</ResponseField>

<ResponseField name="succeeded" type="string">
  The succeeded field.
</ResponseField>

<ResponseField name="failed" type="string">
  The failed field.
</ResponseField>

<ResponseField name="remaining" type="string">
  The remaining field.
</ResponseField>

<ResponseField name="message" type="string">
  The message field.
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET https://xh2o-yths-38lt.n7c.xano.io/api:g7_LZ7hE/temp/batch-fix-dub-links \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1"
```
</RequestExample>

<ResponseExample>
```json 200
{
  "processed": "example-processed",
  "succeeded": "example-succeeded",
  "failed": "example-failed",
  "remaining": "example-remaining",
  "message": "example-message"
}
```
</ResponseExample>

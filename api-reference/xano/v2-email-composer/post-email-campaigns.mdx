---
title: 'Create Email Campaigns'
api: 'POST https://xh2o-yths-38lt.n7c.xano.io/api:58PjJ6J9/email-campaigns'
description: 'Create a new email campaign from frontend JSON. Creates one email_campaign row per recipient, all sharing the same campa'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Request Body

<ParamField body="email_template_id" type="integer">
  Optional email template ID this campaign was created from
</ParamField>

<ParamField body="grant_id" type="string" required>
  Nylas grant ID to send from
</ParamField>

<ParamField body="subject" type="string" required>
  Email subject line
</ParamField>

<ParamField body="from" type="string" required>
  Sender email address
</ParamField>

<ParamField body="cc" type="array">
  The cc field.
</ParamField>

<ParamField body="bcc" type="array">
  The bcc field.
</ParamField>

<ParamField body="recipients" type="object" required>
  Array of recipient objects with name, email, body
</ParamField>

<ParamField body="scheduled_at" type="number">
  Optional scheduled send time. If null, rows are eligible to send immediately.
</ParamField>


## Response

<RequestExample>
```bash cURL
curl -X POST https://xh2o-yths-38lt.n7c.xano.io/api:58PjJ6J9/email-campaigns \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "email_template_id": 1,
      "grant_id": "example-grant_id",
      "subject": "example-subject",
      "from": "example-from",
      "cc": [],
      "bcc": [],
      "recipients": {},
      "scheduled_at": 1
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{}
```
</ResponseExample>

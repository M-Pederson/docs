---
title: "Get Date Descriptors"
api: "GET https://xh2o-yths-38lt.n7c.xano.io/api:mGnfV_UB/date-descriptors"
description: "Get all date descriptors for a certain user or generated by the system."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField query="descriptor" type="string">
  Optional filter to search for a specific descriptor.
</ParamField>

<ResponseField name="descriptors" type="object[]">
  Array of date descriptor objects.
</ResponseField>

<RequestExample>
```bash cURL
curl -G \
  https://xh2o-yths-38lt.n7c.xano.io/api:mGnfV_UB/date-descriptors \
  -H "X-Data-Source: live" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d "descriptor=birthday"
```
</RequestExample>

<ResponseExample>
```json 200
{}
```
</ResponseExample>

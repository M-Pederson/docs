---
title: "Get Widget Email Addresses"
api: "GET https://xh2o-yths-38lt.n7c.xano.io/api:3IvQ7jxX/widget/email-addresses"
description: "Get all connected email addresses for widget display."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ResponseField name="items" type="array">
  <Expandable>
    <ResponseField name="id" type="integer">Record ID.</ResponseField>
    <ResponseField name="grant_id" type="string">Nylas grant ID.</ResponseField>
    <ResponseField name="email_account" type="string">Email address.</ResponseField>
    <ResponseField name="show_in_email_widget" type="boolean">Widget visibility toggle.</ResponseField>
  </Expandable>
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET \
  'https://xh2o-yths-38lt.n7c.xano.io/api:3IvQ7jxX/widget/email-addresses' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN'
```
</RequestExample>

<ResponseExample>
```json 200
[
  {
    "id": 1,
    "user_id": 5,
    "grant_id": "grant_abc",
    "email_account": "me@example.com",
    "show_in_email_widget": true
  }
]
```
</ResponseExample>

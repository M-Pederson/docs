---
title: "Update Important Date"
api: "PUT https://xh2o-yths-38lt.n7c.xano.io/api:mGnfV_UB/important-dates/{important_date_id}"
description: "Update an existing important date record."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField path="important_date_id" type="integer" required>
  The ID of the important date to update.
</ParamField>

<ParamField body="date_descriptor_id" type="integer" required>
  The ID of the date descriptor.
</ParamField>
<ParamField body="annual_recurring" type="boolean">
  Whether this date recurs annually.
</ParamField>
<ParamField body="date" type="string" required>
  The date in `YYYY-MM-DD` format.
</ParamField>
<ParamField body="note" type="string">
  An optional note about the date.
</ParamField>
<ParamField body="reminder" type="integer">
  Number of days before the date to send a reminder.
</ParamField>

<ResponseField name="id" type="integer">
  The important date record ID.
</ResponseField>
<ResponseField name="date" type="string">
  The date value.
</ResponseField>
<ResponseField name="annual_recurring" type="boolean">
  Whether the date recurs annually.
</ResponseField>
<ResponseField name="note" type="string">
  The note.
</ResponseField>
<ResponseField name="reminder" type="integer">
  Days before to remind.
</ResponseField>

<RequestExample>
```bash cURL
curl -X PUT \
  https://xh2o-yths-38lt.n7c.xano.io/api:mGnfV_UB/important-dates/1 \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: live" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "date_descriptor_id": 1,
    "annual_recurring": true,
    "date": "2024-12-25",
    "note": "Updated note",
    "reminder": 3
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1700000000000,
  "updated_at": 1700100000000,
  "my_person_id": 42,
  "date_descriptor_id": 1,
  "annual_recurring": true,
  "date": "2024-12-25",
  "note": "Updated note",
  "reminder": 3
}
```
</ResponseExample>

---
title: 'Create Discover'
api: 'POST https://xh2o-yths-38lt.n7c.xano.io/api:rraxfaG7/discover'
description: 'Version: 1.0.2 (2026-06-05) POST /discover: accept a Discover query, resolve caller/team context, delegate semantic proc'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Request Body

<ParamField body="query" type="string" required>
  Natural-language Discover query submitted by the authenticated user.
</ParamField>

<ParamField body="discover_id" type="integer">
  Existing discover id means this request adds a follow-up question; null means start a new discover.
</ParamField>

<ParamField body="selected_nodes" type="array">
  The selected_nodes field.
</ParamField>

<ParamField body="team_ids" type="array">
  The team_ids field.
</ParamField>

<ParamField body="deep_research" type="boolean">
  Optional flag that lets the downstream pipeline fall back to deep research behavior.
</ParamField>


## Response

<RequestExample>
```bash cURL
curl -X POST https://xh2o-yths-38lt.n7c.xano.io/api:rraxfaG7/discover \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "query": "example-query",
      "discover_id": 1,
      "selected_nodes": [],
      "team_ids": [],
      "deep_research": true
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{}
```
</ResponseExample>

---
title: "Add Addresses"
api: "POST https://xh2o-yths-38lt.n7c.xano.io/api:_wjCLRXt/new-user/addresses"
description: "Add physical addresses during the onboarding flow."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField body="formatted_address" type="string" required>Full formatted address.</ParamField>
<ParamField body="address_label" type="string">Address label (e.g. `home`, `office`).</ParamField>
<ParamField body="address_note" type="string">Optional note.</ParamField>

<RequestExample>
```bash cURL
curl -X POST \
  'https://xh2o-yths-38lt.n7c.xano.io/api:_wjCLRXt/new-user/addresses' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -d '{"formatted_address": "123 Main St, New York, NY 10001", "address_label": "office"}'
```
</RequestExample>

<ResponseExample>
```json 200
{}
```
</ResponseExample>

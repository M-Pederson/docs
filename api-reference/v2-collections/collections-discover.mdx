---
title: 'Create Collections Discover'
api: 'POST https://xh2o-yths-38lt.n7c.xano.io/api:U_4ewelb/collections/discover'
description: 'Create a collection from a discover record. Copies every discover_response_node AND discover_context_node tied to the di'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Request Body

<ParamField body="discover_id" type="integer" required>
  The discover_id field.
</ParamField>

<ParamField body="title" type="string" required>
  The title field.
</ParamField>

<ParamField body="description" type="string">
  The description field.
</ParamField>

<ParamField body="icon_name" type="string">
  The icon_name field.
</ParamField>

<ParamField body="collection_color" type="string">
  The collection_color field.
</ParamField>


## Response

<ResponseField name="id" type="integer">
  The id field.
</ResponseField>

<ResponseField name="created_at" type="number">
  The created_at field.
</ResponseField>

<ResponseField name="updated_at" type="number">
  The updated_at field. Can be `null`.
</ResponseField>

<ResponseField name="user_id" type="integer">
  The user_id field.
</ResponseField>

<ResponseField name="title" type="string">
  The title field. Can be `null`.
</ResponseField>

<ResponseField name="description" type="string">
  The description field. Can be `null`.
</ResponseField>

<ResponseField name="icon_name" type="string">
  The icon_name field.
</ResponseField>

<ResponseField name="custom_icon_url" type="string">
  The custom_icon_url field. Can be `null`.
</ResponseField>

<ResponseField name="collection_color" type="string">
  The collection_color field.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST https://xh2o-yths-38lt.n7c.xano.io/api:U_4ewelb/collections/discover \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "discover_id": 1,
      "title": "example-title",
      "description": "example-description",
      "icon_name": "example-icon_name",
      "collection_color": "example-collection_color"
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1,
  "updated_at": 1,
  "user_id": 1,
  "title": "example-title",
  "description": "example-description",
  "icon_name": "example-icon_name",
  "custom_icon_url": "example-custom_icon_url",
  "collection_color": "example-collection_color"
}
```
</ResponseExample>

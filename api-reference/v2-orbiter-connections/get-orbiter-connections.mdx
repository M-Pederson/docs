---
title: "Get Orbiter Connections"
api: "GET https://xh2o-yths-38lt.n7c.xano.io/api:8Pr6-Q01/orbiter-connections"
description: "Returns all orbiter connections for the logged in user."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

### Query Parameters

<ParamField query="page" type="integer">
  Page number for pagination.
</ParamField>

<ParamField query="limit" type="integer">
  Number of items per page.
</ParamField>

<ParamField query="offset" type="integer">
  Offset for pagination.
</ParamField>

### Response

<ResponseField name="itemsReceived" type="integer">
  Number of items in the current response.
</ResponseField>

<ResponseField name="curPage" type="integer">
  Current page number.
</ResponseField>

<ResponseField name="nextPage" type="integer">
  Next page number, or null if no more pages.
</ResponseField>

<ResponseField name="prevPage" type="integer">
  Previous page number, or null if on first page.
</ResponseField>

<ResponseField name="offset" type="integer">
  Current offset.
</ResponseField>

<ResponseField name="perPage" type="integer">
  Items per page.
</ResponseField>

<ResponseField name="itemsTotal" type="integer">
  Total number of items.
</ResponseField>

<ResponseField name="pageTotal" type="integer">
  Total number of pages.
</ResponseField>

<ResponseField name="items" type="array">
  Array of connection objects containing `id`, `created_at`, `user_id`, `master_person_id`, `user_connected_to_id`, and `connected_to_mp_id`.
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET \
  'https://xh2o-yths-38lt.n7c.xano.io/api:8Pr6-Q01/orbiter-connections?page=1&limit=25' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "itemsReceived": 1,
  "curPage": 1,
  "nextPage": null,
  "prevPage": null,
  "offset": 0,
  "perPage": 25,
  "itemsTotal": 1,
  "pageTotal": 1,
  "items": [
    {
      "id": 1,
      "created_at": 1700000000000,
      "user_id": 42,
      "master_person_id": 10,
      "user_connected_to_id": 55,
      "connected_to_mp_id": 20
    }
  ]
}
```
</ResponseExample>

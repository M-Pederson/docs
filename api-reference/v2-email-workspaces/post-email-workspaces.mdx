---
title: 'Create Email Workspaces'
api: 'POST https://xh2o-yths-38lt.n7c.xano.io/api:dUJtZDlP/email-workspaces'
description: 'Create a new email workspace with one or more nylas_grant accounts. Position auto-increments. Provider is comma-separate'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Request Body

<ParamField body="name" type="string" required>
  The name field.
</ParamField>

<ParamField body="color" type="string">
  The color field.
</ParamField>

<ParamField body="emoji" type="string">
  The emoji field.
</ParamField>

<ParamField body="icon" type="string">
  The icon field.
</ParamField>

<ParamField body="nylas_grant_ids" type="array">
  The nylas_grant_ids field.
</ParamField>


## Response

<ResponseField name="id" type="integer">
  Primary key
</ResponseField>

<ResponseField name="created_at" type="number">
  Record creation timestamp
</ResponseField>

<ResponseField name="updated_at" type="number">
  Last update timestamp
</ResponseField>

<ResponseField name="position" type="integer">
  Sidebar display order — 0 is first
</ResponseField>

<ResponseField name="user_id" type="integer">
  Owner of this workspace
</ResponseField>

<ResponseField name="provider" type="string">
  The provider field.
</ResponseField>

<ResponseField name="name" type="string">
  Workspace display label e.g. Work, Personal, Side Project
</ResponseField>

<ResponseField name="color" type="string">
  Hex color for UI theming e.g. #4F46E5
</ResponseField>

<ResponseField name="icon" type="string">
  The icon field.
</ResponseField>

<ResponseField name="emoji" type="string">
  Single emoji identifier e.g. 💼
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST https://xh2o-yths-38lt.n7c.xano.io/api:dUJtZDlP/email-workspaces \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "name": "example-name",
      "color": "example-color",
      "emoji": "example-emoji",
      "icon": "example-icon",
      "nylas_grant_ids": []
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1,
  "updated_at": 1,
  "position": 1,
  "user_id": 1,
  "provider": "example-provider",
  "name": "example-name",
  "color": "example-color",
  "icon": "example-icon",
  "emoji": "example-emoji"
}
```
</ResponseExample>

---
title: 'Get Nylas Grants Threads'
api: 'GET https://xh2o-yths-38lt.n7c.xano.io/api:s_Mv_Fpv/nylas-grants/{nylas_grant_id}/threads'
description: 'Get email threads for a specific connected email account, with full inbox filters. nylas_grant_id is the Xano internal I'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Path Parameters

<ParamField path="nylas_grant_id" type="integer" required>
  The nylas_grant_id parameter.
</ParamField>


## Query Parameters

<ParamField query="limit" type="integer">
  Pagination
</ParamField>

<ParamField query="page_token" type="string">
  The page_token parameter.
</ParamField>

<ParamField query="folder_id" type="string">
  Folder / label filter (use Nylas folder ID, not keyword like "inbox")
</ParamField>

<ParamField query="unread" type="boolean">
  Group 1 filters (mutually exclusive with Group 2 on Google/Microsoft)
</ParamField>

<ParamField query="starred" type="boolean">
  The starred parameter.
</ParamField>

<ParamField query="thread_id" type="string">
  The thread_id parameter.
</ParamField>

<ParamField query="subject" type="string">
  Group 2 filters
</ParamField>

<ParamField query="to_email" type="string">
  The to_email parameter.
</ParamField>

<ParamField query="from_email" type="string">
  The from_email parameter.
</ParamField>

<ParamField query="cc" type="string">
  The cc parameter.
</ParamField>

<ParamField query="bcc" type="string">
  The bcc parameter.
</ParamField>

<ParamField query="any_email" type="string">
  The any_email parameter.
</ParamField>

<ParamField query="has_attachment" type="boolean">
  Universal filters
</ParamField>

<ParamField query="latest_message_before" type="integer">
  The latest_message_before parameter.
</ParamField>

<ParamField query="latest_message_after" type="integer">
  The latest_message_after parameter.
</ParamField>

<ParamField query="search_query_native" type="string">
  Native provider search query (Google/Microsoft native syntax)
</ParamField>


## Response

<ResponseField name="email" type="string">
  The email field.
</ResponseField>

<ResponseField name="next_cursor" type="string">
  The next_cursor field.
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET https://xh2o-yths-38lt.n7c.xano.io/api:s_Mv_Fpv/nylas-grants/1/threads \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN"
```
</RequestExample>

<ResponseExample>
```json 200
{
  "email": "example-email",
  "next_cursor": "example-next_cursor"
}
```
</ResponseExample>

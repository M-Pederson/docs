---
title: 'Update Team Questions'
api: 'PUT https://xh2o-yths-38lt.n7c.xano.io/api:idMy4x2v/team-questions/{team_question_id}'
description: 'Edit a team_question record - only the question text Edit the question text of a team_question. Only the creator can edi'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Path Parameters

<ParamField path="team_question_id" type="integer" required>
  The team_question_id parameter.
</ParamField>


## Request Body

<ParamField body="question" type="string" required>
  The question field.
</ParamField>


## Response

<ResponseField name="id" type="integer">
  The id field.
</ResponseField>

<ResponseField name="created_at" type="number">
  The created_at field.
</ResponseField>

<ResponseField name="updated_at" type="number">
  Timestamp of the last update to the team question.
</ResponseField>

<ResponseField name="user_id" type="integer">
  The user who asked the team question.
</ResponseField>

<ResponseField name="team_id" type="integer">
  The team associated with this question.
</ResponseField>

<ResponseField name="question" type="string">
  The question asked by the user.
</ResponseField>

<ResponseField name="llm_response" type="string">
  The response generated by the LLM.
</ResponseField>

<ResponseField name="team_question_id" type="integer">
  Reference to a related collection question.
</ResponseField>

<RequestExample>
```bash cURL
curl -X PUT https://xh2o-yths-38lt.n7c.xano.io/api:idMy4x2v/team-questions/1 \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "question": "example-question"
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1,
  "updated_at": 1,
  "user_id": 1,
  "team_id": 1,
  "question": "example-question",
  "llm_response": "example-llm_response",
  "team_question_id": 1
}
```
</ResponseExample>

---
title: "Get Travels"
api: "GET https://xh2o-yths-38lt.n7c.xano.io/api:WtHnIWRU/travels"
description: "Gets all travel events shared to the authenticated user and the user's personal travel events."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

### Response

<ResponseField name="travels" type="array">
  List of travel event objects for the authenticated user.
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET \
  'https://xh2o-yths-38lt.n7c.xano.io/api:WtHnIWRU/travels' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "travels": []
}
```
</ResponseExample>

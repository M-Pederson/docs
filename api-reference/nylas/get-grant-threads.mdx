---
title: "Get Grant Threads"
api: "GET https://xh2o-yths-38lt.n7c.xano.io/api:s_Mv_Fpv/nylas-grants/{nylas_grant_id}/threads"
description: "Get email threads for a specific connected email account with full inbox filters."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField path="nylas_grant_id" type="integer" required>Nylas grant ID (internal).</ParamField>
<ParamField query="limit" type="integer">Number of results (default 50).</ParamField>
<ParamField query="page_token" type="string">Pagination cursor.</ParamField>
<ParamField query="folder_id" type="string">Folder/label filter.</ParamField>
<ParamField query="unread" type="boolean">Filter by unread.</ParamField>
<ParamField query="starred" type="boolean">Filter by starred.</ParamField>
<ParamField query="subject" type="string">Filter by subject.</ParamField>
<ParamField query="from_email" type="string">Filter by sender.</ParamField>
<ParamField query="to_email" type="string">Filter by recipient.</ParamField>
<ParamField query="has_attachment" type="boolean">Filter by attachments.</ParamField>
<ParamField query="search_query_native" type="string">Native provider search query.</ParamField>

<ResponseField name="email" type="string">Email account.</ResponseField>
<ResponseField name="next_cursor" type="string">Pagination cursor for next page.</ResponseField>

<RequestExample>
```bash cURL
curl -X GET \
  'https://xh2o-yths-38lt.n7c.xano.io/api:s_Mv_Fpv/nylas-grants/1/threads?limit=20' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "email": "me@example.com",
  "next_cursor": "abc123"
}
```
</ResponseExample>

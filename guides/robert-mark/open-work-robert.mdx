---
title: "Open Work — Robert"
description: "Status landing for Robert's open work off the June 18 2026 Mark/Robert sync: How I Know X, the AI-SDK migration cross-check, the suggestion test harness, and the memory architecture."
---

Status landing for Robert's open work coming out of the **June 18, 2026 Mark/Robert sync**. The call had three threads plus one Mark tabled. This page is the index — each item links to the detailed page, the PR, or the live proof. **Nothing here is merged or sent on Mark's side; it is awaiting review.**

<Info>
  **Cadence.** Sync again **Monday June 22** (decisions at the bottom). Mark executes the AI-SDK migration **Thu/Fri** that week. **July 6** the GCP team migrates the enrichment engine + all chat business off Xano — so everything new is built additive and portable. The `my_person` → `contacts` table rename has **shipped** (live endpoints already query `contacts`).
</Info>

## Thread 1 — How I Know X (shipped, dogfooded)

Mark's end-of-call recap: *"add the button, punch up How I Know, try Sonnet 4.6, tighten the system prompt, make sure it only opens if it's in my person, save the my-person id back to Mem0."* All six shipped and **verified live against Xano 2026-06-22**.

| # | Item | Status | Verified |
| --- | --- | --- | --- |
| 1A | Pin "Save & Close" under the chat input — exit mid-interview | ✅ | Frontend `#454` |
| 1B | Interview model → `claude-sonnet-4-6` (was Haiku 4.5) | ✅ | Live `#8581` |
| 1C | Tighten the system prompt — one fact then move on, ~4–8 exchanges | ✅ | Live `#8581` |
| 1D | First open shows the saved summary, not a collapsed accordion | ✅ | Frontend `#454` |
| 1E | Section + pop-out render **only when the person is a contact** | ✅ | Frontend `#454` |
| 1F | Mem0 read/write keyed on the **contact id** (`contacts_id`), backend-derived (Option B) | ✅ | Live `#8581` / `#8583` |

- **PR (frontend):** [M-Pederson/orbiter-frontend #454](https://github.com/M-Pederson/orbiter-frontend/pull/454) — base `dev`, OPEN, per-item table + quality gates.
- **Live proof (screenshots + backend trace):** [orbiter-proofs / 2026-06-22 / how-i-know](https://robert-storage.tor1.digitaloceanspaces.com/orbiter-proofs/2026-06-22-how-i-know/index.html) — a full interview about a real contact, end-to-end against live Xano; the row landed with `mem_ingested: true`.
- **Docs:** the How I Know mode page + Xano→GCP backend reference — docs PR [#39](https://github.com/M-Pederson/docs/pull/39).

## Thread 2 — AI-SDK migration (cross-check, blueprint-first)

Mark built an AI-SDK Migration page in his Mintlify and asked Robert to read it, verify against the live repo, and send his own analysis before any code. Done.

<CardGroup cols={2}>
  <Card title="Agree — Scope A" icon="circle-check">
    Move the copilot turn to a streaming AI-SDK agent loop in a TanStack route, Crayon untouched, Zod card contract. Additive, flag-gated (`VITE_COPILOT_AI_SDK`, default off), reversible, repo-grounded. No changes.
  </Card>
  <Card title="One correction — OpenUI is live" icon="triangle-exclamation">
    The audit says "No OpenUI — `@openuidev/*` is absent." The repo shows it installed and rendering **four** live surfaces (Outcomes, Leverage Loops, Meeting Prep, How I Know). Scope B is **convergence** onto a renderer we already run, not a greenfield bet — lower marginal risk than the page implies. Sequencing unchanged: Scope A first.
  </Card>
</CardGroup>

**Cross-check write-up:** docs PR [#41](https://github.com/M-Pederson/docs/pull/41).

## Thread 3 — Suggestion test harness (Mark's "dashboard")

A standalone evaluation UI (its own page, outside the main app) to A/B-test the suggestion engine: separate **WHO** (does ranking surface the right people?) from **WHY** (is the generated explanation good?), with paste-a-system-prompt + model slots and a reviewer rating. **Starts after How I Know is as good as it can be.**

- **Page:** [Suggestion Test Harness](/guides/robert-mark/suggestion-test-harness) — Mark's design notes + the June 18 backend spec (endpoint reuse `#8490` / `#8497` / `#8491`+, net-new override-dispatch + `suggestion_eval_run`, GCP-portable design).

## Tabled thread — Memory architecture

At ~4:30 in the call Mark sketched a two-tier memory model (master-person facts shared across everyone vs my contact memories) and a Timeline/Memories tab, then tabled it. It is the *why* behind 1F, so it is written up before it evaporates.

- **Page:** [Two-Tier Memory + Timeline](/guides/open-work/mem0/two-tier-architecture) — the contact tier (live today, `contacts_id`) vs the proposed shared master-person tier, the read-only Timeline tab, and the two `subject_contact_id → contacts_id` / `haiku → sonnet-4-6` drifts it reconciles in [Migration Plan](/guides/open-work/mem0/migration-plan).

## For Monday — open decisions

<Steps>
  <Step title="Lock Scope A + agree the Scope-B reframe">
    Scope A as written (Robert is aligned), and agree that Scope B is **convergence onto OpenUI**, not first-time adoption.
  </Step>
  <Step title="Nuggets → Mem0 direct, or keep prose + infer?">
    The one How I Know design fork still open. Today `#8583` sends the prose summary with `infer: true`; the structured nuggets are already the highlighted facts. Mark @ 31:39 leans nuggets-direct. See [two-tier §5](/guides/open-work/mem0/two-tier-architecture).
  </Step>
  <Step title="Green-light the shared memory tier + Timeline tab?">
    Additive and GCP-portable (one search branch + a metadata field; one thin new read endpoint) — or keep it tabled until after the migration.
  </Step>
  <Step title="Harness rating scale">
    Reconcile awesome / mediocre / bad (June 18) vs 1–5 star (design notes) before the `suggestion_eval_run` table is built.
  </Step>
</Steps>

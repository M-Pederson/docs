---
title: "Update Master Link"
api: "PUT https://xh2o-yths-38lt.n7c.xano.io/api:rThjhjuK/master-links/{master_link_id}"
description: "Edit a master link for a company or person."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

### Path Parameters

<ParamField path="master_link_id" type="integer" required>
  The ID of the master link record to update.
</ParamField>

### Body Parameters

<ParamField body="link_url" type="string" required>
  The updated URL.
</ParamField>

### Response

<ResponseField name="id" type="integer">
  Unique identifier of the master link record.
</ResponseField>

<ResponseField name="master_person_id" type="integer">
  Associated master person ID.
</ResponseField>

<ResponseField name="master_company_id" type="integer">
  Associated master company ID.
</ResponseField>

<ResponseField name="service" type="string">
  The detected service (e.g. LinkedIn, Twitter).
</ResponseField>

<ResponseField name="service_label" type="string">
  Human-readable service label.
</ResponseField>

<ResponseField name="icon_url" type="string">
  URL of the service icon.
</ResponseField>

<ResponseField name="link_url" type="string">
  The link URL.
</ResponseField>

<RequestExample>
```bash cURL
curl -X PUT \
  'https://xh2o-yths-38lt.n7c.xano.io/api:rThjhjuK/master-links/1' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -d '{
    "link_url": "https://linkedin.com/in/janedoe"
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1700000000000,
  "updated_at": 1700000000000,
  "master_person_id": 1,
  "master_company_id": null,
  "service": "linkedin",
  "service_label": "LinkedIn",
  "icon_url": "https://example.com/linkedin-icon.png",
  "link_url": "https://linkedin.com/in/janedoe"
}
```
</ResponseExample>

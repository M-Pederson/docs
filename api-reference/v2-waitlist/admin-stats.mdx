---
title: "Admin: Get Stats"
api: "GET https://xh2o-yths-38lt.n7c.xano.io/api:g7_LZ7hE/admin/stats"
description: "Admin: Get aggregate waitlist statistics."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

### Query Parameters

<ParamField query="x_admin_key" type="string" required>
  Admin API key.
</ParamField>

### Response

<ResponseField name="total" type="string">
  Total number of waitlist entries.
</ResponseField>

<ResponseField name="by_status" type="string">
  Breakdown of entries by status.
</ResponseField>

<ResponseField name="referrals" type="string">
  Referral statistics.
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET \
  'https://xh2o-yths-38lt.n7c.xano.io/api:g7_LZ7hE/admin/stats?x_admin_key=YOUR_ADMIN_KEY' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "total": "1250",
  "by_status": "{}",
  "referrals": "{}"
}
```
</ResponseExample>

---
title: 'Delete Email Campaigns'
api: 'DELETE https://xh2o-yths-38lt.n7c.xano.io/api:58PjJ6J9/email-campaigns/{campaign_id}'
description: 'Delete all rows for a campaign. Only allowed if all rows are still pending (not yet sent). Authentication: required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Path Parameters

<ParamField path="campaign_id" type="string" required>
  The shared campaign UUID to delete
</ParamField>


## Response

<ResponseField name="deleted" type="string">
  The deleted field.
</ResponseField>

<ResponseField name="campaign_id" type="string">
  The campaign_id field.
</ResponseField>

<RequestExample>
```bash cURL
curl -X DELETE https://xh2o-yths-38lt.n7c.xano.io/api:58PjJ6J9/email-campaigns/1 \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN"
```
</RequestExample>

<ResponseExample>
```json 200
{
  "deleted": "example-deleted",
  "campaign_id": "example-campaign_id"
}
```
</ResponseExample>

---
title: "Initiate Parse"
api: "POST https://xh2o-yths-38lt.n7c.xano.io/api:C9Wg9y9e/initiate-parse"
description: "Initiate email parsing for the specified email address and time range."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField body="email_address" type="string">
  The email address to parse.
</ParamField>
<ParamField body="timerange" type="integer" required>
  The time range in days to parse emails from.
</ParamField>

<ResponseField name="result" type="object">
  Empty object on success.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST \
  https://xh2o-yths-38lt.n7c.xano.io/api:C9Wg9y9e/initiate-parse \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: live" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "email_address": "user@example.com",
    "timerange": 30
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{}
```
</ResponseExample>

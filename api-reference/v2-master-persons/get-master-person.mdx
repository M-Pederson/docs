---
title: 'Get Master Person'
api: 'GET https://xh2o-yths-38lt.n7c.xano.io/api:WKgay2AU/master-persons/{master_person_id}'
description: 'Get a single master person record by ID'
---

## Overview

Returns a single master person record by its ID.

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Path Parameters

<ParamField path="master_person_id" type="integer" required>
  The ID of the master person to retrieve.
</ParamField>

## Response

Returns the master person record.

<RequestExample>
```bash cURL
curl -X GET https://xh2o-yths-38lt.n7c.xano.io/api:WKgay2AU/master-persons/50 \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_TOKEN"
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 50,
  "orbiter_user": false,
  "name": "John Doe",
  "avatar": "https://example.com/avatar.png",
  "master_company_id": 100,
  "current_title": "CEO",
  "primary_location_id": 5,
  "bio": "Experienced technology leader..."
}
```
</ResponseExample>

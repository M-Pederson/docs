---
title: 'Create Delete My Company Address'
api: 'POST https://xh2o-yths-38lt.n7c.xano.io/api:KdpaUs_r/delete/my_company/address'
description: 'DELETE an address from a my_company record - and also delete "address note" if/any for that address Authentication: requ'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Request Body

<ParamField body="master_address_id" type="integer" required>
  The master_address_id field.
</ParamField>

<ParamField body="my_company_id" type="integer" required>
  The my_company_id field.
</ParamField>


## Response

<RequestExample>
```bash cURL
curl -X POST https://xh2o-yths-38lt.n7c.xano.io/api:KdpaUs_r/delete/my_company/address \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "master_address_id": 1,
      "my_company_id": 1
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{}
```
</ResponseExample>

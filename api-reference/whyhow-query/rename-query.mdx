---
title: "Rename Query"
api: "PATCH https://xh2o-yths-38lt.n7c.xano.io/api:8qSkh_B3/whyhow-query/{name}/{id}"
description: "Rename an existing query."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField path="name" type="string" required>
  The new name for the query.
</ParamField>

<ParamField body="whyhow_query_id" type="integer" required>
  The ID of the query to rename.
</ParamField>

<ResponseField name="result" type="object">
  Empty object on success.
</ResponseField>

<RequestExample>
```bash cURL
curl -X PATCH \
  https://xh2o-yths-38lt.n7c.xano.io/api:8qSkh_B3/whyhow-query/my-new-name/42 \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: live" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "whyhow_query_id": 42
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{}
```
</ResponseExample>

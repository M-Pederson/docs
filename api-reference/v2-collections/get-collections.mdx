---
title: 'get all collections (shared or private) for the User'
api: 'GET https://xh2o-yths-38lt.n7c.xano.io/api:U_4ewelb/collections'
description: 'get all collections (shared or private) for the User Authentication: required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Query Parameters

<ParamField query="query" type="string">
  The query parameter.
</ParamField>

<ParamField query="page" type="integer">
  The page parameter.
</ParamField>

<ParamField query="offset" type="integer">
  The offset parameter.
</ParamField>

<ParamField query="limit" type="integer">
  The limit parameter.
</ParamField>

<ParamField query="with_favourites" type="boolean">
  The with_favourites parameter.
</ParamField>


## Response

<RequestExample>
```bash cURL
curl -X GET https://xh2o-yths-38lt.n7c.xano.io/api:U_4ewelb/collections \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN"
```
</RequestExample>

<ResponseExample>
```json 200
{}
```
</ResponseExample>

---
title: 'Create Add My Company Phone'
api: 'POST https://xh2o-yths-38lt.n7c.xano.io/api:KdpaUs_r/add/my_company/phone'
description: 'create and ADD an master_phone record to a my_company record based on a master_company ID with an optional "phone note" '
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Request Body

<ParamField body="my_company_id" type="integer">
  The my_company_id field.
</ParamField>

<ParamField body="phone_number" type="string">
  The phone_number field.
</ParamField>

<ParamField body="phone_extension" type="integer">
  The phone_extension field.
</ParamField>

<ParamField body="phone_type" type="string">
  The phone_type field.
</ParamField>

<ParamField body="phone_note" type="string">
  The phone_note field.
</ParamField>


## Response

<ResponseField name="id" type="integer">
  The id field.
</ResponseField>

<ResponseField name="created_at" type="number">
  The created_at field.
</ResponseField>

<ResponseField name="my_company_id" type="integer">
  The my_company_id field.
</ResponseField>

<ResponseField name="master_phone_id" type="integer">
  The master_phone_id field.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST https://xh2o-yths-38lt.n7c.xano.io/api:KdpaUs_r/add/my_company/phone \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "my_company_id": 1,
      "phone_number": "example-phone_number",
      "phone_extension": 1,
      "phone_type": "example-phone_type",
      "phone_note": "example-phone_note"
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1,
  "my_company_id": 1,
  "master_phone_id": 1
}
```
</ResponseExample>

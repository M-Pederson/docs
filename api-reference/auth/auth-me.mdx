---
title: 'Get the record belonging to the authentication token'
api: 'GET https://xh2o-yths-38lt.n7c.xano.io/api:qMCc0ojP/auth/me'
description: 'Get the record belonging to the authentication token Authentication: required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Response

<ResponseField name="id" type="integer">
  The id field.
</ResponseField>

<ResponseField name="created_at" type="number">
  The created_at field.
</ResponseField>

<ResponseField name="master_person_id" type="integer">
  The master_person_id field. Can be `null`.
</ResponseField>

<ResponseField name="tiptap_collab_jwt" type="string">
  Jwt from tip tap
</ResponseField>

<ResponseField name="tiptap_ai_jwt" type="string">
  The tiptap_ai_jwt field. Can be `null`.
</ResponseField>

<ResponseField name="feature_base_jwt" type="string">
  The feature_base_jwt field. Can be `null`.
</ResponseField>

<ResponseField name="local_timezone" type="string">
  The local_timezone field.
</ResponseField>

<ResponseField name="copilot_enabled" type="boolean">
  The copilot_enabled field.
</ResponseField>

<ResponseField name="clerk_id" type="string">
  The clerk_id field. Can be `null`.
</ResponseField>

<ResponseField name="master_person" type="object">
  The master_person field. Can be `null`.
</ResponseField>

<ResponseField name="connected_emails" type="array">
  The connected_emails field.
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET https://xh2o-yths-38lt.n7c.xano.io/api:qMCc0ojP/auth/me \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN"
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1,
  "master_person_id": 1,
  "tiptap_collab_jwt": "example-tiptap_collab_jwt",
  "tiptap_ai_jwt": "example-tiptap_ai_jwt",
  "feature_base_jwt": "example-feature_base_jwt",
  "local_timezone": "example-local_timezone",
  "copilot_enabled": false,
  "clerk_id": "example-clerk_id",
  "master_person": "example-master_person",
  "connected_emails": []
}
```
</ResponseExample>

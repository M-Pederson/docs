---
title: "Get Connect Requests"
api: "GET https://xh2o-yths-38lt.n7c.xano.io/api:8Pr6-Q01/orbiter-connect-requests"
description: "Query all orbiter connect request records."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

### Query Parameters

<ParamField query="page" type="integer">
  Page number for pagination.
</ParamField>

<ParamField query="limit" type="integer">
  Number of items per page.
</ParamField>

<ParamField query="offset" type="integer">
  Offset for pagination.
</ParamField>

### Response

<ResponseField name="itemsReceived" type="integer">
  Number of items in the current response.
</ResponseField>

<ResponseField name="curPage" type="integer">
  Current page number.
</ResponseField>

<ResponseField name="nextPage" type="integer">
  Next page number, or null if no more pages.
</ResponseField>

<ResponseField name="prevPage" type="integer">
  Previous page number, or null if on first page.
</ResponseField>

<ResponseField name="items" type="array">
  Array of connect request objects containing `id`, `created_at`, `user_from_id`, `master_person_from_id`, `user_to_id`, `master_person_to_id`, and `request_message`.
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET \
  'https://xh2o-yths-38lt.n7c.xano.io/api:8Pr6-Q01/orbiter-connect-requests?page=1&limit=25' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "itemsReceived": 1,
  "curPage": 1,
  "nextPage": null,
  "prevPage": null,
  "offset": 0,
  "perPage": 25,
  "itemsTotal": 1,
  "pageTotal": 1,
  "items": [
    {
      "id": 1,
      "created_at": 1700000000000,
      "updated_at": null,
      "user_from_id": 42,
      "master_person_from_id": 10,
      "user_to_id": 55,
      "master_person_to_id": 20,
      "request_message": "Let's connect!"
    }
  ]
}
```
</ResponseExample>

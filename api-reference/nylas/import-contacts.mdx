---
title: "Import Contacts"
api: "POST https://xh2o-yths-38lt.n7c.xano.io/api:s_Mv_Fpv/nylas/contacts/import"
description: "Import contacts from a specific Nylas grant."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField body="grant_id" type="string" required>Nylas grant ID string.</ParamField>

<RequestExample>
```bash cURL
curl -X POST \
  'https://xh2o-yths-38lt.n7c.xano.io/api:s_Mv_Fpv/nylas/contacts/import' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -d '{"grant_id": "grant_abc123"}'
```
</RequestExample>

<ResponseExample>
```json 200
{}
```
</ResponseExample>

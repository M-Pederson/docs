---
title: 'Create Delete My Company Link'
api: 'POST https://xh2o-yths-38lt.n7c.xano.io/api:KdpaUs_r/delete/my_company/link'
description: 'DELETE a phone from a my_company record - and also delete "phone note" if/any for that phone Authentication: required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Request Body

<ParamField body="my_company_id" type="integer" required>
  The my_company_id field.
</ParamField>

<ParamField body="master_link_id" type="integer" required>
  The master_link_id field.
</ParamField>


## Response

<RequestExample>
```bash cURL
curl -X POST https://xh2o-yths-38lt.n7c.xano.io/api:KdpaUs_r/delete/my_company/link \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "my_company_id": 1,
      "master_link_id": 1
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{}
```
</ResponseExample>

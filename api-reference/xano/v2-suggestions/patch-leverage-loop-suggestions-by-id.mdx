---
title: 'Edit leverage_loop_suggestion record'
api: 'PATCH https://xh2o-yths-38lt.n7c.xano.io/api:MkA4QsNh/leverage-loop-suggestions/{leverage_loop_suggestion_id}'
description: 'Edit leverage_loop_suggestion record Authentication: required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Path Parameters

<ParamField path="leverage_loop_suggestion_id" type="integer" required>
  The leverage_loop_suggestion_id parameter.
</ParamField>


## Request Body

<ParamField body="updated_at" type="number">
  The updated_at field.
</ParamField>

<ParamField body="suggestion_request_id" type="integer">
  The suggestion_request_id field.
</ParamField>

<ParamField body="user_id" type="integer">
  The user_id field.
</ParamField>

<ParamField body="header_text" type="string">
  The header_text field.
</ParamField>

<ParamField body="suggestion_context" type="string">
  The suggestion_context field.
</ParamField>

<ParamField body="node_uuid" type="string">
  The node_uuid field.
</ParamField>

<ParamField body="archived" type="boolean">
  The archived field.
</ParamField>

<ParamField body="paths" type="object">
  The paths field.
</ParamField>


## Response

<ResponseField name="id" type="integer">
  The id field.
</ResponseField>

<ResponseField name="created_at" type="number">
  The created_at field.
</ResponseField>

<ResponseField name="updated_at" type="number">
  The updated_at field. Can be `null`.
</ResponseField>

<ResponseField name="suggestion_request_id" type="integer">
  The suggestion_request_id field. Can be `null`.
</ResponseField>

<ResponseField name="user_id" type="integer">
  The user_id field. Can be `null`.
</ResponseField>

<ResponseField name="header_text" type="string">
  The header_text field. Can be `null`.
</ResponseField>

<ResponseField name="suggestion_context" type="string">
  The suggestion_context field. Can be `null`.
</ResponseField>

<ResponseField name="node_uuid" type="string">
  The node_uuid field.
</ResponseField>

<ResponseField name="archived" type="boolean">
  The archived field.
</ResponseField>

<ResponseField name="paths" type="object">
  The paths field. Can be `null`.
</ResponseField>

<RequestExample>
```bash cURL
curl -X PATCH https://xh2o-yths-38lt.n7c.xano.io/api:MkA4QsNh/leverage-loop-suggestions/1 \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "updated_at": 1,
      "suggestion_request_id": 1,
      "user_id": 1,
      "header_text": "example-header_text",
      "suggestion_context": "example-suggestion_context",
      "node_uuid": "example-node_uuid",
      "archived": true,
      "paths": {}
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1,
  "updated_at": 1,
  "suggestion_request_id": 1,
  "user_id": 1,
  "header_text": "example-header_text",
  "suggestion_context": "example-suggestion_context",
  "node_uuid": "example-node_uuid",
  "archived": false,
  "paths": "example-paths"
}
```
</ResponseExample>

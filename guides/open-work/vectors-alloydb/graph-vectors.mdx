---
title: "Vectors in FalkorDB"
description: "Current state of vector creation in the graph — functions writing embeddings to nodes across all pipelines"
---

<Warning>
  **Standard change (2026-07-06): every Gemini embedding is now 1536-dim (was 3072).** All `gemini-embedding-001` vectors on every surface — the FalkorDB properties (`embeddings`, `expert_embeddings`), future AlloyDB columns, and every query-side embed — are requested with `dimensions: 1536` and **L2-normalized before storing or comparing** (Gemini pre-normalizes only its 3072 output; the shared wrapper `vectors/create-gemini-vectors` #13114 v2 handles both). The FalkorDB indexes were rebuilt @1536 and all ~12.3k stored Gemini vectors were prefix-truncated \+ renormalized on live, staging, and sandbox on 2026-07-06.
</Warning>

## Existing vector creation in nodes

Every function in the person/company/film-tv/funding/expertise/known Cypher pipelines that writes a `vecf32(...)` embedding onto a node — either on CREATE or on UPDATE/SET. Sourced from the [cyphers docs](/guides/cyphers). **13 functions total.**

### Person pipeline — [person-pipeline](/guides/cyphers/person-pipeline)

| Function | ID | Node(s) | Embedding prop |
| --- | --- | --- | --- |
| `mvp/node/add-person-node` | #2601 | `Person` (CREATE) | `name_embedding` |
| `mvp/node/update-person-node` | #2758 | `Person` (SET) | `name_embedding` |
| `mvp/resolve/resolve-edges-certifications` | #12578 | `Certification` (MERGE) | `name_embedding` |
| `mvp/node/primary-location-nodes-edges_v2` | #4690 | `Country`, `Region`, `City` (MERGE) | `name_embedding` |

### Company pipeline — [company-pipeline](/guides/cyphers/company-pipeline)

| Function | ID | Node(s) | Embedding prop |
| --- | --- | --- | --- |
| `mvp/node/add-company-node` | #2602 | `Company` / `School` / `VC_Firm` / `Organization` (CREATE) | `name_embedding` |
| `mvp/node/update-company-node` | #4659 | `Company` (SET) | `name_embedding` |
| `mvp/node/location-nodes-edges-company` | #4528 | `Country`, `Region`/`State`, `City` (MERGE) | `name_embedding` |

### Expertise — [expertise](/guides/cyphers/expertise)

| Function | ID | Node(s) | Embedding prop |
| --- | --- | --- | --- |
| `mvp/node/sync-domain-expertise-nodes` | #12663 | `DomainExpertise`, `SubDomainExpertise` (CREATE) | `expert_embeddings` (Gemini-only since v1.2, 2026-07-06) |
| `mvp/expertise/resolve-person-expertise` | #12668 | `SubDomainExpertise` (CREATE new on no-match) | `name_embedding` \+ `expertise_embedding` (legacy — uncalled since 2026-06-19; `expertise_embedding` retired) |
| `mvp/expertise/resolve-company-specialties` | #12746 | `SubDomainExpertise` (CREATE new on no-match) | `name_embedding` \+ `expert_embeddings` |

### Film & TV — [film-tv](/guides/cyphers/film-tv)

| Function | ID | Node(s) | Embedding prop |
| --- | --- | --- | --- |
| `mvp/node/add-film-tv-node` | #2320 | `Film_TV` (CREATE) | `name_embedding` |
| `mvp/edge/add-node-edge-imdb-award` | #2334 | `Film_TV_Award` (MERGE, both ON CREATE and ON MATCH) | `name_embedding` |

### Funding — [funding](/guides/cyphers/funding)

| Function | ID | Node(s) | Embedding prop |
| --- | --- | --- | --- |
| `mvp/funding/add-fundable-deal-node` | #12701 | `Funding_Round` (MERGE, both ON CREATE and ON MATCH) | `name_embedding` |

### Known — [known](/guides/cyphers/known)

| Function | ID | Node(s) | Embedding prop |
| --- | --- | --- | --- |
| `mvp/yc/process-yc-people` | #12700 | `contextFounded` (CREATE, multi-founder branch only) | `name_embedding` |

### Cross-reference

Every node label in this list matches a property entry in [Indexes](/guides/ontology/all-indexes#embedding-properties-by-node-type). The only node type declared in the vector index table that **no function in the Cypher docs writes** is `Startup` — worth confirming whether it's live anywhere or if it's stale from an earlier ontology decision.

---

## Locked decisions

- **Embedding model — `gemini-embedding-001` (1536-dim, cosine, L2-normalized) — updated 2026-07-06 (was 3072).** Same model on every surface: `google/gemini-embedding-001` via OpenRouter (Xano side, `dimensions: 1536` \+ in-function normalize via #13114 v2) ↔ `gemini-embedding-001` in AlloyDB (truncate the 3072 server output to the 1536 MRL prefix \+ `l2_normalize` in SQL). This resolves the earlier "dimension alignment" question — all embedding properties standardize on **1536-dim normalized** Gemini vectors. See the [Indexes](/guides/ontology/all-indexes) embedding-standard callout.
- **AlloyDB index type — ScaNN (`alloydb_scann`).** Every vector column uses ScaNN. HNSW and IVFFlat are **not** used: ScaNN is the only AlloyDB index that supports inline metadata filtering (the load-bearing requirement for the find-investors / find-people engines), and it clears pgvector's 2000-dim index cap. See [ScaNN Indexes](/guides/open-work/vectors-alloydb/scann).

## Open decisions

_To be filled as migration scoping progresses._

- Dual-write vs cutover strategy
- Sync cadence for node → AlloyDB after graph write
- Read-path routing (who queries AlloyDB vs who still calls `db.idx.vector.queryNodes`)
- Backfill plan for historical embeddings

---

## Related

- [Indexes](/guides/ontology/all-indexes) — current FalkorDB scalar \+ HNSW index inventory and scoring thresholds
- [All Indexes](/guides/ontology/all-indexes) — canonical list of every scalar \+ vector index (source of truth for `tool/create-indexes` #4669)
- [Cyphers](/guides/cyphers) — all Cypher queries, grouped by pipeline
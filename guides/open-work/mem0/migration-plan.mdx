---
title: "Migration Plan"
description: "Target Mem0 architecture for Orbiter AI chats: shared architecture first, then per-surface implementation details"
---

This page describes the target architecture for moving Orbiter AI chat memory onto Mem0. It starts with the shared developer rules that apply to every chat surface, then documents the per-surface details for Outcomes, Leverage Loop, Meeting Prep, and How I Know.

It builds on the deterministic Xano context functions and dispatch path documented in [Current Implementation](/guides/open-work/mem0/current-implementation), especially function `mvp/context/get-all-user-context_v2` (`#13076`) and function `mvp/meeting-prep/classify-participants-get-context` (`#13070`).

<Warning>
  **Deprecated function:** `mvp/context/get-all-user-context` (`#13069`) is deprecated. New Mem0 work must use function `mvp/context/get-all-user-context_v2` (`#13076`).
</Warning>

<div style={{border: "2px solid #16A34A", borderRadius: "10px", padding: "16px 18px", margin: "16px 0", background: "#0B1020", color: "#FFFFFF"}}>
  <div style={{fontSize: "0.76rem", fontWeight: 900, letterSpacing: "0.08em", textTransform: "uppercase", color: "#86EFAC", marginBottom: "8px"}}>Status: All four chat surfaces are live on Mem0 (June 10, 2026)</div>
  <div style={{fontSize: "1.02rem", fontWeight: 800, color: "#FFFFFF", lineHeight: 1.35}}>Outcomes shipped first inside live API `POST /openui05/dispatch` (`#8497`), verified end-to-end including through the app. The remaining three surfaces are now wired and server-verified the same day: Leverage Loop (read `#8584` / write `#8084`), How I Know (read `#8581` / write `#8583`), Meeting Prep (read+write `#8098`) — including cross-chat recall (facts saved in How I Know surfaced in Meeting Prep output).</div>
  <div style={{fontSize: "0.96rem", color: "#FFFFFF", marginTop: "8px", lineHeight: 1.55}}>Implementation notes vs this plan: the extractor converged on the email-POC benchmark winner — `deepseek/deepseek-v4-flash` via OpenRouter (with <code>reasoning.enabled: false</code>, otherwise reasoning tokens exhaust the budget and content comes back empty); extractor-gated adds use <code>infer: false</code> (the statement is already a distilled atomic sentence — matches the email POC), while the How I Know save (`#8583`) keeps <code>infer: true</code> because it feeds Mem0 a raw multi-fact relationship summary and Mem0's splitting does real work there; metadata follows the converged schema (`schema_version: "2026-06-10"`, `memory_hash`, `source_id`, `subject_contact_id`, `flow_type`, `source_type: "chat"`), with person-scope recall = user-scope search + `subject_contact_id` filter; the confidence gate accepts numeric ≥ 0.7 or "high"; `ai_memory_event_log` is a fast-follow — v1 idempotency is the deterministic `source_id` + dedup-against-retrieved gate described below. Every block on every surface is gated on `$env.MEM0_API_KEY` — unsetting it restores exact pre-migration behavior with no re-deploy (in `#8497` that means the dormant Zep blocks).</div>
</div>

## Implementation Readiness Callouts

Resolve these before building the Mem0 migration. They are implementation decisions, not product behavior changes. **Status (June 10, 2026):** all seven are resolved for the Outcomes surface — each callout below carries its resolution; two are deferred to their own surface migrations.

<div style={{border: "2px solid #DC2626", borderRadius: "10px", padding: "16px 18px", margin: "16px 0", background: "#0B1020", color: "#FFFFFF"}}>
  <div style={{fontSize: "0.76rem", fontWeight: 900, letterSpacing: "0.08em", textTransform: "uppercase", color: "#FFFFFF", marginBottom: "8px"}}>Open decision: transcript writes</div>
  <div style={{fontSize: "1.02rem", fontWeight: 800, color: "#FFFFFF", lineHeight: 1.35}}>Define which API writes user and assistant messages for each surface.</div>
  <div style={{fontSize: "0.96rem", color: "#FFFFFF", marginTop: "8px", lineHeight: 1.55}}>Do not leave this implicit. Decide whether API `POST /openui05/dispatch` (`#8497`) writes transcript rows itself, or whether each surface writes before and after dispatch. Outcomes uses `outcome_conversations` (`#689`), Leverage Loop uses `leverage_loop_conversations` (`#688`), and Meeting Prep uses `copilot_conversations` (`#636`) plus `copilot_chat_messages` (`#637`).</div>
  <div style={{fontSize: "0.92rem", color: "#86EFAC", marginTop: "10px", lineHeight: 1.55, borderTop: "1px solid #1F2937", paddingTop: "10px"}}>✅ <strong>Resolved (Outcomes, 2026-06-10):</strong> no change. `#8497` writes no transcript rows today and the Mem0 swap did not add any — Mem0 is the distilled-memory layer, never the transcript store. Frontend-side `outcome_conversations` persistence is untouched. First-party transcript persistence stays a separate, out-of-scope endpoint decision.</div>
</div>

<div style={{border: "2px solid #DC2626", borderRadius: "10px", padding: "16px 18px", margin: "16px 0", background: "#0B1020", color: "#FFFFFF"}}>
  <div style={{fontSize: "0.76rem", fontWeight: 900, letterSpacing: "0.08em", textTransform: "uppercase", color: "#FFFFFF", marginBottom: "8px"}}>Open decision: meeting prep identity</div>
  <div style={{fontSize: "1.02rem", fontWeight: 800, color: "#FFFFFF", lineHeight: 1.35}}>Bridge `meeting_prep_context_id` and `conversation_id` before wiring Mem0.</div>
  <div style={{fontSize: "0.96rem", color: "#FFFFFF", marginTop: "8px", lineHeight: 1.55}}>The Mem0 run key is `meeting_prep:{meeting_prep_context_id}`, but chat messages are stored under `copilot_conversations.id`. Add a reliable mapping, such as `meeting_prep_context_id` on `copilot_conversations`, a metadata field, or a deterministic lookup. Without this, Mem0 run memory and the visible chat transcript can drift.</div>
  <div style={{fontSize: "0.92rem", color: "#86EFAC", marginTop: "10px", lineHeight: 1.55, borderTop: "1px solid #1F2937", paddingTop: "10px"}}>⏸ <strong>Deferred (2026-06-10):</strong> non-Outcomes. Resolve during the Meeting Prep migration — the planned bridge is `meeting_prep_context_id` added onto `copilot_conversations` before wiring its run-scope Mem0.</div>
</div>

<div style={{border: "2px solid #F59E0B", borderRadius: "10px", padding: "16px 18px", margin: "16px 0", background: "#0B1020", color: "#FFFFFF"}}>
  <div style={{fontSize: "0.76rem", fontWeight: 900, letterSpacing: "0.08em", textTransform: "uppercase", color: "#FFFFFF", marginBottom: "8px"}}>Open decision: idempotency and retries</div>
  <div style={{fontSize: "1.02rem", fontWeight: 800, color: "#FFFFFF", lineHeight: 1.35}}>Define how repeated assistant turns, failed retries, and extractor reruns are deduplicated.</div>
  <div style={{fontSize: "0.96rem", color: "#FFFFFF", marginTop: "8px", lineHeight: 1.55}}>Each memory write should include a deterministic `source_id`, preferably the persisted Xano assistant-message row ID. Decide whether the extractor skips, upserts, or creates a new Mem0 memory when it sees the same `source_id` again, and persist the result in `ai_memory_event_log`.</div>
  <div style={{fontSize: "0.92rem", color: "#86EFAC", marginTop: "10px", lineHeight: 1.55, borderTop: "1px solid #1F2937", paddingTop: "10px"}}>✅ <strong>Resolved (Outcomes, 2026-06-10):</strong> `ai_memory_event_log` does not exist yet and was deliberately kept out of the demo-critical path. v1 idempotency = (a) deterministic `source_id` in add metadata, `outcome:{suggestion_request_id}:{hash(normalized query)}`; (b) dedup of extractor output against the retrieved memory set before writing (since the June 10 benchmark convergence, adds use <code>infer: false</code>, so gates (a)+(b) carry idempotency alone — verified: a re-sent identical turn dedup-skips). A re-dispatched identical turn is a no-op. The durable event-log table is an explicit fast-follow.</div>
</div>

<div style={{border: "2px solid #F59E0B", borderRadius: "10px", padding: "16px 18px", margin: "16px 0", background: "#0B1020", color: "#FFFFFF"}}>
  <div style={{fontSize: "0.76rem", fontWeight: 900, letterSpacing: "0.08em", textTransform: "uppercase", color: "#FFFFFF", marginBottom: "8px"}}>Open decision: self-context metadata</div>
  <div style={{fontSize: "1.02rem", fontWeight: 800, color: "#FFFFFF", lineHeight: 1.35}}>Store metadata with the Markdown returned by function `mvp/context/get-all-user-context_v2` (`#13076`).</div>
  <div style={{fontSize: "0.96rem", color: "#FFFFFF", marginTop: "8px", lineHeight: 1.55}}>For Outcomes and Leverage Loop, do not store only `self_markdown`. Also persist the context function name, function ID, contract version, generated timestamp, and optional source hash so stale snapshots can be detected without rerunning the function on every chat turn.</div>
  <div style={{fontSize: "0.92rem", color: "#86EFAC", marginTop: "10px", lineHeight: 1.55, borderTop: "1px solid #1F2937", paddingTop: "10px"}}>✅ <strong>Resolved (Outcomes, 2026-06-10):</strong> N/A to the Mem0 path as shipped. `self_yaml_context` arrives as a `#8497` input and fills the `[YOU]` slot only — it is never written to Mem0, so no staleness-metadata gap arises. This recommendation applies if/when self-context snapshots are persisted in Xano.</div>
</div>

<div style={{border: "2px solid #F59E0B", borderRadius: "10px", padding: "16px 18px", margin: "16px 0", background: "#0B1020", color: "#FFFFFF"}}>
  <div style={{fontSize: "0.76rem", fontWeight: 900, letterSpacing: "0.08em", textTransform: "uppercase", color: "#FFFFFF", marginBottom: "8px"}}>Open decision: streaming and failed turns</div>
  <div style={{fontSize: "1.02rem", fontWeight: 800, color: "#FFFFFF", lineHeight: 1.35}}>Pick one persistence rule for partial, failed, and successful LLM responses.</div>
  <div style={{fontSize: "0.96rem", color: "#FFFFFF", marginTop: "8px", lineHeight: 1.55}}>Recommended rule: persist the user message before the LLM call, persist the assistant message only after a final successful response, and run memory extraction only after that assistant message has a stable Xano row ID. Do not write partial streams or failed responses to Mem0.</div>
  <div style={{fontSize: "0.92rem", color: "#86EFAC", marginTop: "10px", lineHeight: 1.55, borderTop: "1px solid #1F2937", paddingTop: "10px"}}>✅ <strong>Resolved (Outcomes, 2026-06-10):</strong> `#8497` is non-streaming — one synchronous POST returning the full result. The extractor + Mem0 add run at end-of-stack after routing completes (the slot the Zep write occupied), so partial or failed turns are never written. Maps to the recommended rule with no restructuring.</div>
</div>

<div style={{border: "2px solid #D97706", borderRadius: "10px", padding: "16px 18px", margin: "16px 0", background: "#0B1020", color: "#FFFFFF"}}>
  <div style={{fontSize: "0.76rem", fontWeight: 900, letterSpacing: "0.08em", textTransform: "uppercase", color: "#FFFFFF", marginBottom: "8px"}}>Open decision: How I Know persistence and summary loop</div>
  <div style={{fontSize: "1.02rem", fontWeight: 800, color: "#FFFFFF", lineHeight: 1.35}}>Treat `master_person_relationship` (`#729`) as the active store unless the frontend still reads `how_i_know_x` (`#731`).</div>
  <div style={{fontSize: "0.96rem", color: "#FFFFFF", marginTop: "8px", lineHeight: 1.55}}>API `POST /openui05/how-i-know-interview` (`#8581`) generates interview output only. API `GET /relationships/{master_person_id}` (`#8582`) loads the row, and API `POST /relationships` (`#8583`) saves transcript, completion state, and the UI-facing relationship summary. Mem0 stores the individual durable memories behind the scenes.</div>
  <div style={{fontSize: "0.92rem", color: "#86EFAC", marginTop: "10px", lineHeight: 1.55, borderTop: "1px solid #1F2937", paddingTop: "10px"}}>✅ <strong>Resolved (How I Know live, 2026-06-10):</strong> built as positioned — `master_person_relationship` (`#729`) is the active store, `#8581` interview-only (now also reads Mem0 user-scope + `subject_contact_id` person-scope into its system prompt), `#8582` load, `#8583` save (now also writes to Mem0 when `interview_complete`, with <code>infer: true</code> over the raw relationship summary). Verified: the interviewer opens by citing previously saved relationship facts instead of re-asking.</div>
</div>

<div style={{border: "2px solid #F59E0B", borderRadius: "10px", padding: "16px 18px", margin: "16px 0 24px", background: "#0B1020", color: "#FFFFFF"}}>
  <div style={{fontSize: "0.76rem", fontWeight: 900, letterSpacing: "0.08em", textTransform: "uppercase", color: "#FFFFFF", marginBottom: "8px"}}>Required guardrail: ownership checks</div>
  <div style={{fontSize: "1.02rem", fontWeight: 800, color: "#FFFFFF", lineHeight: 1.35}}>Verify run ownership before any Mem0 search or write.</div>
  <div style={{fontSize: "0.96rem", color: "#FFFFFF", marginTop: "8px", lineHeight: 1.55}}>Every memory operation must verify that the source row belongs to `auth.id`: `suggestion_request` (`#529`) for Outcomes and Leverage Loop, `meeting_prep_context` (`#697`) for Meeting Prep, and the relationship row for How I Know. Do this before building Mem0 filters or writing `user_id` metadata.</div>
  <div style={{fontSize: "0.92rem", color: "#86EFAC", marginTop: "10px", lineHeight: 1.55, borderTop: "1px solid #1F2937", paddingTop: "10px"}}>✅ <strong>Implemented (Outcomes, 2026-06-10):</strong> when `suggestion_request_id &gt; 0`, dispatch loads `suggestion_request` (`#529`) and verifies `user_id == auth.id` (`#8497` is `auth = "user"`). On mismatch or missing row, all run-scope Mem0 search and write is skipped and the turn degrades to user scope only. User scope is keyed by the authenticated user's WorkOS id string (not the Xano PK), so it is inherently owned.</div>
</div>

## Decision

Use Mem0 as a **retrieval layer for distilled memories**, not as the transcript store and not as the canonical profile store.

For the current OpenUI05 implementation, Mem0 should replace or augment the Zep read/write steps inside `openui05/dispatch`. Keep the frontend contract the same: the frontend sends `query`, stable `thread_id`, and YAML context blocks to Xano; Xano owns memory retrieval, LLM/tool routing, and memory writes.

Xano remains the source of truth for:

- raw user and assistant messages, if first-party transcript persistence is added
- flow state for outcomes, leverage loops, meeting prep, and how-i-know chats
- deterministic context snapshots generated from Orbiter tables/functions
- meeting prep participant JSON, meeting memo Markdown, transcripts, and attachments
- relationship interview transcripts and display summaries

Mem0 stores:

- durable user preferences and constraints
- important decisions made during a flow
- run-specific working assumptions
- distilled memories from chat, email, calendar, meeting prep, and how-i-know context

Do not send every raw turn or full Xano context dump to Mem0.

## Shared Architecture

Use the same memory architecture for these surfaces:

| Surface | Flow type | Primary Xano run container | Detail section |
| --- | --- | --- | --- |
| Outcomes AI Chat | `outcome` | `suggestion_request` (`#529`) | [Outcomes AI Chat](#outcomes-ai-chat) |
| Leverage Loop (specific) AI Chat | `leverage_loop` | `suggestion_request` (`#529`) | [Leverage Loop (specific) AI Chat](#leverage-loop-specific-ai-chat) |
| Meeting Prep AI Chat | `meeting_prep` | `meeting_prep_context` (`#697`) | [Meeting Prep AI Chat](#meeting-prep-ai-chat) |
| How I Know AI Chat | `how_i_know` | `master_person_relationship` (`#729`) | [How I Know AI Chat](#how-i-know-ai-chat) |

Each surface has its own Xano thread or flow state, but they all use the same Mem0 read/write pattern.

### Entity Scope Rules

Use **one primary Mem0 entity per write**.

| Scope | Mem0 entity | Use for |
| --- | --- | --- |
| User memory | `user_id` | Durable facts that should follow the user across flows |
| Run memory | `run_id` | Facts that should only shape one outcome, leverage loop, meeting prep, or how-i-know chat |
| Agent memory | `agent_id` | Rare global behavior notes about an agent, not user-specific facts |

Do not write a single memory with both `user_id` and `run_id` unless you intentionally want Mem0's entity scoping behavior. For Orbiter, if a fact matters at both levels, write two memories: one user-scoped and one run-scoped.

Use metadata for everything else:

```json
{
  "flow_type": "leverage_loop",
  "thread_id": "suggestion_request:123",
  "source_type": "chat",
  "source_id": "ai_chat_message:456",
  "user_id": "15"
}
```

### Xano Persistence

Today, `openui05/dispatch` does not appear to store a complete transcript in Xano. It stores conversational memory in Zep and returns response metadata to the frontend.

If we add first-party transcript persistence for OpenUI05, either reuse the existing copilot tables or introduce a chat-specific set of tables. The existing copilot tables are:

```txt
copilot_conversations (#636)
- id
- created_at
- updated_at
- user_id
- mode
- title
- master_person_id
```

```txt
copilot_chat_messages (#637)
- id
- created_at
- conversation_id
- role
- content
- template_name
- template_props
```

For outcome and leverage-loop chats keyed by `suggestion_request_id`, the existing schema would need either a deterministic `mode/title` convention or an added/linking field. Do not assume `suggestion_request_id` exists on `copilot_chat_messages`; the live table schema checked on June 8, 2026 did not include it.

Recommended target table shape:

```txt
ai_chat_message
- id
- user_id
- flow_type              outcome | leverage_loop | meeting_prep | how_i_know
- thread_id              suggestion_request:123 | meeting_prep:88 | how_i_know:15:139
- role                   user | assistant | system | tool
- content
- metadata
- created_at
```

```txt
ai_chat_context_snapshot
- id
- user_id
- flow_type
- thread_id
- context_markdown
- context_source         xano_context_function | meeting_prep_context | how_i_know_context
- context_version
- generated_at
```

```txt
ai_memory_event_log
- id
- user_id
- flow_type
- thread_id
- source_message_id
- extractor_model
- extracted_json
- mem0_scope             user | run | skipped
- mem0_event_id
- mem0_status
- created_at
```

### Turn Lifecycle

This is the target lifecycle for every surface. For the current implementation, map "Mem0 search" to the place where `openui05/dispatch` now reads Zep thread context, and map "Mem0 write" to the place where it now posts messages back to Zep.

#### Start or resume a chat

1. Create or load the Xano thread/run state.
2. Load deterministic Xano context for the surface.
3. Store or reuse a stable context snapshot where needed.
4. Do not write the full deterministic context snapshot to Mem0.

For `outcome` and `leverage_loop`, the thread ID must be stable for the lifetime of the `suggestion_request`.

```txt
suggestion_request:{suggestion_request_id}
```

For `meeting_prep`:

```txt
meeting_prep:{meeting_prep_context_id}
```

For `how_i_know`:

```txt
how_i_know:{user_id}:{target_master_person_id}
```

#### Before each LLM response

Load:

1. System prompt for the surface.
2. Latest deterministic context snapshot from Xano.
3. Structured flow state from Xano.
4. Recent transcript from Xano, usually last `8-16` turns.
5. Relevant Mem0 memories from user scope.
6. Relevant Mem0 memories from run scope.

Build the model input in this order:

```txt
System instructions

Deterministic Orbiter context snapshot from Xano

Current flow state from Xano

Relevant durable user memories from Mem0

Relevant run memories from Mem0

Recent chat transcript from Xano

Latest user message
```

#### After each LLM response

1. Save user and assistant messages to Xano if first-party transcript persistence is enabled.
2. Run memory extraction asynchronously.
3. Save extractor output to `ai_memory_event_log`.
4. POST only approved high-confidence memories to Mem0.
5. Poll or receive the Mem0 event result and update `ai_memory_event_log`.

If memory extraction fails, the chat still succeeds.

### When Orbiter Calls Mem0

Use this rule across Orbiter:

```txt
Read from Mem0 right before an LLM response.
Write to Mem0 after the LLM turn, only if memory extraction finds something durable.
```

Mem0 is recall. It is not the place to store full Xano context snapshots, participant JSON, raw emails, raw meeting notes, or raw transcripts.

| Moment | Call Mem0? | Reason |
| --- | --- | --- |
| User opens calendar | No | Calendar browsing does not need memory recall. |
| Nylas webhook receives or updates a meeting | No | Webhooks should keep event data fresh; they should not trigger memory retrieval by themselves. |
| User chooses meeting prep | Usually no | Create/load `meeting_prep_context`, run function `mvp/meeting-prep/classify-participants-get-context` (`#13070`) if needed, and store the meeting memo. If this same action immediately asks an LLM to generate prep, search Mem0 right before that LLM call. |
| User sends the first meeting-prep chat message | Yes, search | Retrieve relevant user and run memories before building the LLM messages. |
| Every later chat turn | Yes, search | Each turn may need different memories depending on the user's latest question. |
| User opens a contact side panel | No | Load the stored relationship summary from Xano; memory recall is only needed when an AI turn starts. |
| User starts or continues a How I Know chat | Yes, search | Retrieve prior relationship memories before the interviewer asks the next question or summarizes. |
| After the assistant responds | Maybe write | Run extraction and write only durable, high-confidence memories. |
| Running function `mvp/context/get-all-user-context_v2` (`#13076`) user context | No | This is deterministic Xano context, not memory recall. |
| Running function `mvp/meeting-prep/classify-participants-get-context` (`#13070`) meeting participant context | No | This is deterministic meeting context, not memory recall. |

### Mem0 REST Calls

Mem0's current Platform API uses V3 endpoints for add/search/list. Entity IDs for search/list must go inside `filters`.

Search durable user memories before an LLM turn:

```bash
curl -X POST https://api.mem0.ai/v3/memories/search/ \
  -H "Authorization: Token $MEM0_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "query": "The user is defining a leverage loop around warm investor introductions.",
    "filters": {
      "user_id": "15"
    },
    "top_k": 8,
    "threshold": 0.1,
    "rerank": false
  }'
```

Search run-specific memories before an LLM turn:

```bash
curl -X POST https://api.mem0.ai/v3/memories/search/ \
  -H "Authorization: Token $MEM0_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "query": "The current chat is refining target investors and rejected paths.",
    "filters": {
      "run_id": "suggestion_request:123"
    },
    "top_k": 8,
    "threshold": 0.1,
    "rerank": false
  }'
```

Store a durable user memory when the extractor returns `scope = "user"`. Set `infer` to `false` because Orbiter already performed memory extraction and wants Mem0 to store the approved memory statement as-is.

```bash
curl -X POST https://api.mem0.ai/v3/memories/add/ \
  -H "Authorization: Token $MEM0_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": "15",
    "infer": false,
    "messages": [
      {
        "role": "user",
        "content": "User prefers warm introduction paths over cold outreach when pursuing investor conversations."
      }
    ],
    "metadata": {
      "flow_type": "leverage_loop",
      "thread_id": "suggestion_request:123",
      "source_type": "chat",
      "source_id": "ai_chat_message:456",
      "category": "preference",
      "confidence": "high"
    }
  }'
```

Store a run-specific memory when the extractor returns `scope = "run"`:

```bash
curl -X POST https://api.mem0.ai/v3/memories/add/ \
  -H "Authorization: Token $MEM0_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "run_id": "suggestion_request:123",
    "infer": false,
    "messages": [
      {
        "role": "user",
        "content": "For this leverage loop, the user rejected cold outbound and wants to focus on warm investor intro paths."
      }
    ],
    "metadata": {
      "user_id": "15",
      "flow_type": "leverage_loop",
      "thread_id": "suggestion_request:123",
      "source_type": "chat",
      "source_id": "ai_chat_message:456",
      "category": "decision",
      "confidence": "high"
    }
  }'
```

Mem0 add calls are asynchronous and return an `event_id`.

```bash
curl -X GET "https://api.mem0.ai/v1/event/$MEM0_EVENT_ID/" \
  -H "Authorization: Token $MEM0_API_KEY"
```

### Memory Extraction

Run memory extraction after the assistant response is saved to Xano.

Recommended model:

```txt
gpt-5-nano
```

Why: memory extraction is a focused classification and summarization task. Use `gpt-5-mini` only if QA shows `gpt-5-nano` misses relationship nuance or produces unstable scopes.

Recommended settings:

```json
{
  "model": "gpt-5-nano",
  "reasoning": {
    "effort": "minimal"
  },
  "max_output_tokens": 1200
}
```

Extractor system prompt:

```txt
You are Orbiter's memory extraction service.

Your job is to decide whether the latest chat turn contains durable memory worth saving to Mem0.

You do not write chat responses. You only return structured JSON.

Save a memory only when it is:
- durable beyond the current message
- useful for future recommendations, meeting prep, introductions, or relationship reasoning
- specific to the user, the active run, or a meaningful relationship
- high confidence because the user stated it, confirmed it, or the app state makes it clear
- not already just raw transcript
- not better stored only as structured Xano state

Do not save:
- greetings, thanks, or filler
- generic assistant advice
- full raw emails, full meeting notes, or full context snapshots
- duplicate facts already present in the retrieved memory list
- guesses about the user that were not stated or strongly implied
- sensitive information unless it is necessary for the product experience and the user provided it

Scope rules:
- Use scope "user" for durable facts that should follow the user across outcomes, leverage loops, meeting prep, and how-i-know chats.
- Use scope "run" for facts that should only affect the active thread.
- Use scope "skip" when nothing should be saved.

Categories:
- preference
- goal
- constraint
- decision
- rejected_path
- relationship
- meeting_strategy
- workflow_state
- source_fact

Memory style:
- Write one short standalone sentence.
- Include the relevant entity names.
- Avoid IDs unless the ID is the only stable label.
- Prefer "User prefers..." or "For this run..." phrasing.
- Do not include private implementation details.
```

Pass only the material needed to judge memory:

```json
{
  "flow_type": "leverage_loop",
  "thread_id": "suggestion_request:123",
  "user_id": "15",
  "latest_user_message": "No cold outreach. I only want intros through people who actually know the investor.",
  "assistant_response": "Got it. I will constrain this leverage loop to warm paths...",
  "current_flow_state": {
    "objective": "Find investor intro paths",
    "selected_mode": "leverage_loop"
  },
  "recent_messages": [
    {
      "role": "user",
      "content": "I want to find investors for the seed round."
    },
    {
      "role": "assistant",
      "content": "Should we include cold outbound or only warm paths?"
    }
  ],
  "retrieved_memories": [
    "User prefers tactical recommendations over broad strategy."
  ]
}
```

Structured output schema:

```json
{
  "type": "object",
  "additionalProperties": false,
  "required": ["memories"],
  "properties": {
    "memories": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "scope",
          "memory",
          "category",
          "confidence",
          "reason",
          "source_type"
        ],
        "properties": {
          "scope": {
            "type": "string",
            "enum": ["user", "run", "skip"]
          },
          "memory": {
            "type": "string"
          },
          "category": {
            "type": "string",
            "enum": [
              "preference",
              "goal",
              "constraint",
              "decision",
              "rejected_path",
              "relationship",
              "meeting_strategy",
              "workflow_state",
              "source_fact"
            ]
          },
          "confidence": {
            "type": "string",
            "enum": ["low", "medium", "high"]
          },
          "reason": {
            "type": "string"
          },
          "source_type": {
            "type": "string",
            "enum": [
              "chat",
              "email",
              "calendar",
              "meeting_prep",
              "how_i_know",
              "xano_context"
            ]
          }
        }
      }
    }
  }
}
```

Backend save policy:

- Save `confidence = "high"` automatically.
- Queue `confidence = "medium"` for QA or save only if the category is low risk.
- Never save `confidence = "low"`.
- Skip `scope = "skip"`.
- Deduplicate against retrieved memories before POSTing to Mem0.

### Source Save Rules

| Source | Save | Do not save |
| --- | --- | --- |
| Outcome and leverage loop | Selected target or goal, rejected paths, strong constraints, preferred relationship path, confirmed decision criteria | Every draft suggestion, generated candidate lists, transient brainstorm text |
| Meeting prep | Durable meeting strategy, relationship context confirmed by the user, follow-up preferences | Raw transcript, full meeting notes, full function `mvp/meeting-prep/classify-participants-get-context` (`#13070`) participant context or memo, unconfirmed guesses |
| How I Know | How the user met or knows the person, relationship strength, intro fit, user-confirmed boundaries | Speculative labels, raw relationship chat transcript |
| Email and calendar | Distilled facts that shape future recommendations, user-confirmed preferences or commitments, important relationship or meeting signals | Raw email bodies, raw calendar descriptions, private content that is not needed later |

## Chat Surface Details

Outcomes AI Chat is the canonical implementation pattern. The other surfaces should follow the same turn lifecycle, Mem0 read/write rules, extractor contract, and save policy unless their section explicitly calls out a difference.

## Outcomes AI Chat

<div style={{border: "2px solid #2563EB", borderRadius: "12px", padding: "18px 22px", margin: "16px 0 18px", background: "#0B1020", color: "#FFFFFF"}}>
  <div style={{fontSize: "0.78rem", fontWeight: 800, letterSpacing: "0.08em", textTransform: "uppercase", color: "#FFFFFF", marginBottom: "6px"}}>AI chat surface</div>
  <div style={{fontSize: "1.2rem", fontWeight: 800, color: "#FFFFFF", lineHeight: 1.25}}>Outcome definition, refinement, and plan state</div>
  <div style={{fontSize: "0.95rem", color: "#FFFFFF", marginTop: "6px"}}>Flow type: `outcome` · Run key: `suggestion_request:{suggestion_request_id}` · Primary table: `suggestion_request` `#529`</div>
  <div style={{fontSize: "0.95rem", color: "#FFFFFF", marginTop: "6px"}}><strong>Conversation store:</strong> `outcome_conversations` `#689`, keyed by `suggestion_request_id`.</div>
  <div style={{fontSize: "0.95rem", color: "#FFFFFF", marginTop: "6px"}}><strong>Context generated:</strong> function `mvp/context/get-all-user-context_v2` (`#13076`) at draft creation or when stored self context is missing; reuse the stored snapshot on later turns.</div>
</div>

<div style={{border: "2px solid #16A34A", borderRadius: "10px", padding: "16px 18px", margin: "16px 0 18px", background: "#0B1020", color: "#FFFFFF"}}>
  <div style={{fontSize: "0.76rem", fontWeight: 900, letterSpacing: "0.08em", textTransform: "uppercase", color: "#86EFAC", marginBottom: "8px"}}>Shipped — live in `#8497` since June 10, 2026</div>
  <div style={{fontSize: "0.96rem", color: "#FFFFFF", lineHeight: 1.55}}>As built, inside API `POST /openui05/dispatch` (`#8497`), portable primitives only (`api.request` + lambdas + `db` + conditionals):</div>
  <ol style={{margin: "8px 0 0 1.2rem", padding: 0, color: "#FFFFFF", lineHeight: 1.55}}>
    <li><strong>Identity:</strong> Mem0 `user_id` = the user's WorkOS id string from `user` (`#125`), never the Xano PK. `run_id` = `suggestion_request:{suggestion_request_id}`, used only when the row is owned by `auth.id`.</li>
    <li><strong>Read:</strong> every turn searches Mem0 V3 (`top_k: 8`, `threshold: 0.1`) — user scope always, run scope when owned — and joins hits into the `[NETWORK MEMORY]` slot of `$mem_context`, exactly where Zep recall sat. The YOU/TARGET/COMPANY wrapping is unchanged.</li>
    <li><strong>Write:</strong> end-of-stack after routing, a `deepseek/deepseek-v4-flash` extractor via OpenRouter (this plan's extractor prompt and schema; <code>reasoning.enabled: false</code> — required, or reasoning tokens exhaust `max_tokens` and content is empty) judges the turn; high-confidence (≥ 0.7 numeric or "high"), non-duplicate memories are added with <code>infer: false</code>, metadata `source_id = outcome:{srid}:{hash(query)}` + `memory_hash` + `schema_version: "2026-06-10"`, scope-routed user vs run (run only when owned). (Shipped on `claude-haiku-4-5` + <code>infer: true</code>; converged to the benchmark stack later the same day.)</li>
    <li><strong>Failure posture:</strong> `api.request` never throws — Mem0 or extractor failures degrade gracefully and the chat always succeeds.</li>
    <li><strong>Rollback:</strong> the three Zep blocks remain in the script, re-gated on `$env.zep != "" &amp;&amp; $env.MEM0_API_KEY == ""`. Unset `MEM0_API_KEY` to restore exact pre-cutover Zep behavior.</li>
  </ol>
  <div style={{fontSize: "0.96rem", color: "#FFFFFF", marginTop: "10px", lineHeight: 1.55, borderTop: "1px solid #1F2937", paddingTop: "10px"}}>The same pattern now runs on the other three surfaces (June 10): <strong>Leverage Loop</strong> — read in `#8584` (`/interview`, now `auth = "user"`; query = `{target_name}: {last 3 transcript lines}`; user-scope + `subject_contact_id` person-scope searches feed a `[NETWORK MEMORY]` section in the system prompt) and write in `#8084` (`/dispatch`, deepseek extractor over the submitted loop summary, `source_id = leverage_loop:{srid}:{hash}`). <strong>Meeting Prep</strong> — `#8098` overrides `$memoryBlock` with Mem0 hits on read and extracts from goal+transcript+prep on write (`source_id = meeting_prep:{mp}:{hash}`). <strong>How I Know</strong> — `#8581` reads Mem0 into the interviewer prompt; `#8583` writes the relationship summary with <code>infer: true</code> on `interview_complete` (`source_id = how_i_know:{mp}:{hash}`). All env-gated on `MEM0_API_KEY`; none of these three ever had a memory layer, so unset = exact pre-wiring behavior.</div>
</div>

Outcomes AI Chat is the flow for creating, refining, and turning a user's desired outcome into structured plan state and outcome suggestions. Treat the `suggestion_request` row as the durable run container, and treat the outcome chat transcript as Xano-owned state.

<div style={{border: "2px solid #2563EB", borderRadius: "10px", padding: "16px 18px", margin: "16px 0 18px", background: "#0B1020", color: "#FFFFFF"}}>
  <div style={{fontSize: "0.76rem", fontWeight: 900, letterSpacing: "0.08em", textTransform: "uppercase", color: "#FFFFFF", marginBottom: "8px"}}>Implementation summary</div>
  <ol style={{margin: "0 0 0 1.2rem", padding: 0, color: "#FFFFFF", lineHeight: 1.55}}>
    <li>Create or load `suggestion_request` and use it as the durable run container.</li>
    <li>Generate or reuse function `mvp/context/get-all-user-context_v2` (`#13076`) user context and store it in `suggestion_request.llm_summary_context.openui_context.self_markdown`.</li>
    <li>Store and load outcome transcript rows from `outcome_conversations` (`#689`) keyed by `suggestion_request_id`.</li>
    <li>Load outcome state from `outcome_properties`, plan rows, suggestions, and recent outcome transcript rows.</li>
    <li>Search Mem0 user scope and run scope immediately before each LLM response.</li>
    <li>Persist the assistant response, run the memory extractor, and write only approved durable memories to Mem0.</li>
  </ol>
</div>

<Info>
  **Tables used**

  | Table | ID | Role |
  | --- | --- | --- |
  | `suggestion_request` | `529` | Primary outcome run record, draft status, request text, `copilot_mode = outcome`, and stored `llm_summary_context`. |
  | `outcome_conversations` | `689` | Outcome-specific user/assistant transcript rows keyed by `suggestion_request_id`. |
  | `copilot_messages` | `530` | Legacy/copilot message context keyed by `suggestion_request_id`; keep separate from the target `ai_chat_message` design. |
  | `outcome_properties` | `528` | Structured outcome fields such as due date, timeframe, and location. |
  | `outcome_plan_step` | `527` | Plan/task rows generated or refined for the outcome. |
  | `outcome_suggestion` | `531` | Generated outcome suggestion cards for the request. |
  | `outcome_suggestion_node` | `534` | Nodes attached to outcome suggestions for graph/path display. |
</Info>

Mem0 shape:

```txt
flow_type: outcome
thread_id: suggestion_request:{suggestion_request_id}
run_id: suggestion_request:{suggestion_request_id}
```

Detailed outcome lifecycle:

```txt
1. Create or load suggestion_request.
2. Run function mvp/context/get-all-user-context_v2 (#13076), if the suggestion_request self context is missing.
3. Store the Markdown snapshot in suggestion_request.llm_summary_context.openui_context.self_markdown.
4. Load outcome_properties and current outcome draft state.
5. Load recent outcome_conversations or equivalent transcript rows.
6. Search Mem0 user scope right before the LLM response.
7. Search Mem0 run scope for suggestion_request:{suggestion_request_id}.
8. Call the LLM with Xano context first, then Mem0 recall, then recent transcript.
9. Persist the assistant response.
10. Run the memory extractor.
11. Write only approved memories to Mem0.
```

Prompt order:

```txt
System prompt

function mvp/context/get-all-user-context_v2 (#13076) user context, when available

Current outcome draft state from suggestion_request

Structured outcome_properties

Relevant durable user memories from Mem0

Relevant run memories from Mem0

Recent outcome chat transcript

Latest user message
```

Search examples:

```txt
Search user_id: 15
Query: "outcome planning preferences constraints rejected paths for this user's goal"
```

```txt
Search run_id: suggestion_request:123
Query: "current outcome draft constraints decisions rejected paths"
```

Save to Mem0:

- a durable user preference about how they define outcomes
- a recurring constraint the user explicitly confirms
- a run-specific decision that should shape the rest of this outcome
- a rejected path for this outcome

Do not save:

- every generated outcome suggestion
- every draft task
- model-generated candidate lists
- full `suggestion_request.llm_summary_context`

## Leverage Loop (specific) AI Chat

<div style={{border: "2px solid #7C3AED", borderRadius: "12px", padding: "18px 22px", margin: "16px 0 18px", background: "#0B1020", color: "#FFFFFF"}}>
  <div style={{fontSize: "0.78rem", fontWeight: 800, letterSpacing: "0.08em", textTransform: "uppercase", color: "#FFFFFF", marginBottom: "6px"}}>AI chat surface</div>
  <div style={{fontSize: "1.2rem", fontWeight: 800, color: "#FFFFFF", lineHeight: 1.25}}>Specific-person leverage loop and active objective</div>
  <div style={{fontSize: "0.95rem", color: "#FFFFFF", marginTop: "6px"}}>Flow type: `leverage_loop` · Run key: `suggestion_request:{suggestion_request_id}` · Primary table: `suggestion_request` `#529`</div>
  <div style={{fontSize: "0.95rem", color: "#FFFFFF", marginTop: "6px"}}><strong>Conversation store:</strong> `leverage_loop_conversations` `#688`, keyed by `suggestion_request_id`.</div>
  <div style={{fontSize: "0.95rem", color: "#FFFFFF", marginTop: "6px"}}><strong>Context generated:</strong> function `mvp/context/get-all-user-context_v2` (`#13076`) at draft creation or when stored self context is missing; target and relationship context are loaded before each LLM turn.</div>
</div>

Same as [Outcomes AI Chat](#outcomes-ai-chat), with these deltas: `flow_type = leverage_loop`, search queries should include the target person and active objective, and run-specific memories should capture rejected paths or decisions for that specific loop.

<div style={{border: "2px solid #7C3AED", borderRadius: "10px", padding: "16px 18px", margin: "16px 0 18px", background: "#0B1020", color: "#FFFFFF"}}>
  <div style={{fontSize: "0.76rem", fontWeight: 900, letterSpacing: "0.08em", textTransform: "uppercase", color: "#FFFFFF", marginBottom: "8px"}}>Implementation summary</div>
  <ol style={{margin: "0 0 0 1.2rem", padding: 0, color: "#FFFFFF", lineHeight: 1.55}}>
    <li>Start with the Outcomes AI Chat implementation pattern.</li>
    <li>Use `suggestion_request` as the run container and `leverage_loop_conversations` (`#688`) as the loop-specific transcript source.</li>
    <li>Generate or reuse function `mvp/context/get-all-user-context_v2` (`#13076`) at draft creation or when stored self context is missing.</li>
    <li>Load the target `master_person`, user relationship context, target company context when relevant, and current leverage-loop draft state.</li>
    <li>Search Mem0 with the target person's name plus the active objective; avoid broad person-name-only retrieval.</li>
    <li>Save only durable loop constraints, rejected paths, relationship facts, and confirmed decisions.</li>
  </ol>
</div>

<Info>
  **Tables used**

  | Table | ID | Role |
  | --- | --- | --- |
  | `suggestion_request` | `529` | Primary leverage-loop run record, usually `copilot_mode = loop`, plus stored user context in `llm_summary_context`. |
  | `leverage_loop_conversations` | `688` | Leverage-loop-specific transcript rows keyed by `suggestion_request_id`. |
  | `leverage_loop_suggestion` | `535` | Generated or refined leverage-loop suggestions, paths, and node target references. |
  | `copilot_messages` | `530` | Legacy/copilot message context keyed by `suggestion_request_id`; do not treat this as the canonical future transcript table. |
  | `master_person` | `139` | Target person context when the leverage loop is about helping a specific person. |
  | `my_person` | `128` | User-owned person row used when relationship context is keyed through the user's network. |
</Info>

Mem0 shape:

```txt
flow_type: leverage_loop
thread_id: suggestion_request:{suggestion_request_id}
run_id: suggestion_request:{suggestion_request_id}
```

Before the LLM turn, prepare deterministic Xano context:

```txt
1. Create or load suggestion_request.
2. Run function mvp/context/get-all-user-context_v2 (#13076), if self context is missing.
3. Load target master_person context.
4. Load relationship context for the user and target person.
5. Load target company context, if relevant.
6. Load current leverage-loop draft state.
```

Then search Mem0:

```txt
Search user_id: 15
Query: "{target person} leverage loop {active objective}"
```

```txt
Search run_id: suggestion_request:123
Query: "{target person} leverage loop rejected paths decisions constraints"
```

Do not retrieve every memory that mentions the target person. Retrieve only memories relevant to the latest user turn and active objective.

## Meeting Prep AI Chat

<div style={{border: "2px solid #059669", borderRadius: "12px", padding: "18px 22px", margin: "16px 0 18px", background: "#0B1020", color: "#FFFFFF"}}>
  <div style={{fontSize: "0.78rem", fontWeight: 800, letterSpacing: "0.08em", textTransform: "uppercase", color: "#FFFFFF", marginBottom: "6px"}}>AI chat surface</div>
  <div style={{fontSize: "1.2rem", fontWeight: 800, color: "#FFFFFF", lineHeight: 1.25}}>Meeting-specific prep, participant context, and strategy</div>
  <div style={{fontSize: "0.95rem", color: "#FFFFFF", marginTop: "6px"}}>Flow type: `meeting_prep` · Run key: `meeting_prep:{meeting_prep_context_id}` · Primary table: `meeting_prep_context` `#697`</div>
  <div style={{fontSize: "0.95rem", color: "#FFFFFF", marginTop: "6px"}}><strong>Conversation store:</strong> `copilot_conversations` `#636` and `copilot_chat_messages` `#637`; `meeting_prep_context` stores prep context and memo, not the chat transcript.</div>
  <div style={{fontSize: "0.95rem", color: "#FFFFFF", marginTop: "6px"}}><strong>Context generated:</strong> participant context and the user's team context both come from function `mvp/meeting-prep/classify-participants-get-context` (`#13070`) during prep-initiation paths, API `POST /meeting-prep-context/create` (`#8325`) or widget bridge API `POST /conversations/{conversation_id}/messages` (`#8154`).</div>
  <div style={{fontSize: "0.95rem", color: "#FFFFFF", marginTop: "8px"}}>Do not call function `mvp/context/get-all-user-context_v2` (`#13076`) for Meeting Prep.</div>
</div>

Same as [Outcomes AI Chat](#outcomes-ai-chat) for Mem0 read/write timing, with a different Xano prep step before chat begins.

Meeting Prep AI Chat is the flow for preparing around a specific meeting. The canonical run container is `meeting_prep_context`, not the calendar event itself, because the prep context stores the desired outcome, participant memo, deep-research state, attachments, and chat-relevant meeting strategy.

<div style={{border: "2px solid #059669", borderRadius: "10px", padding: "16px 18px", margin: "16px 0 18px", background: "#0B1020", color: "#FFFFFF"}}>
  <div style={{fontSize: "0.76rem", fontWeight: 900, letterSpacing: "0.08em", textTransform: "uppercase", color: "#FFFFFF", marginBottom: "8px"}}>Implementation summary</div>
  <ol style={{margin: "0 0 0 1.2rem", padding: 0, color: "#FFFFFF", lineHeight: 1.55}}>
    <li>Create or load `meeting_prep_context` by `user_id + nylas_event_id`; refreshes must update the existing row.</li>
    <li>Do not call function `mvp/context/get-all-user-context_v2` (`#13076`) for Meeting Prep; function `mvp/meeting-prep/classify-participants-get-context` (`#13070`) already returns `my_team` and the user's side of the meeting context from `user_email`.</li>
    <li>Run function `mvp/meeting-prep/classify-participants-get-context` (`#13070`) only from prep-initiation paths, primarily API `POST /meeting-prep-context/create` (`#8325`), when participant context is missing, stale, forced, or contract-versioned stale.</li>
    <li>Store raw `participant_context` and derived `meeting_prep_memo_markdown` on table `#697`.</li>
    <li>Store chat threads/messages in `copilot_conversations` (`#636`) and `copilot_chat_messages` (`#637`); do not use `meeting_prep_context` as a transcript table.</li>
    <li>During chat, load the saved memo and stored participant context; do not call function `mvp/meeting-prep/classify-participants-get-context` (`#13070`) from chat turns.</li>
    <li>Search Mem0 right before the LLM response and save only durable meeting strategy, relationship facts, or follow-up preferences.</li>
  </ol>
</div>

<Info>
  **Tables used**

  | Table | ID | Role |
  | --- | --- | --- |
  | `meeting_prep_context` | `697` | Primary meeting-prep run record; stores `desired_outcome`, raw `participant_context`, `meeting_prep_memo_markdown`, participant context status/freshness/error metadata, email thread summaries, deep-research state, and attachments. |
  | `copilot_conversations` | `636` | Meeting-prep chat conversation thread records used by the CrayonChat/widget path. |
  | `copilot_chat_messages` | `637` | Individual meeting-prep chat message rows keyed by `conversation_id`. |
  | `meeting_transcriptions` | `698` | Meeting transcript and summary storage when available; do not inject full transcripts into every chat turn. |
  | `event` | `221` | Internal calendar event reference for the meeting. |
  | `user` | `125` | Owner of the meeting prep context and Mem0 `user_id` scope. |
  | `master_person` | `139` | Resolved participant/person context produced by meeting-prep classification. |
  | `master_company` | `142` | Resolved company context for attendees and organizers. |
</Info>

Mem0 shape:

```txt
flow_type: meeting_prep
thread_id: meeting_prep:{meeting_prep_context_id}
run_id: meeting_prep:{meeting_prep_context_id}
```

Meeting-prep context storage decision:

| Concern | Decision |
| --- | --- |
| Function hook | Add function `mvp/meeting-prep/classify-participants-get-context` (`#13070`) only to prep-initiation paths: API `POST /meeting-prep-context/create` (`#8325`) and the widget bridge inside API `POST /conversations/{conversation_id}/messages` (`#8154`) when the message resolves to a calendar event. |
| Function input | The prep path loads the calendar event and user's calendar email, extracts attendee emails, then passes `user_email` and `participant_emails` into function `mvp/meeting-prep/classify-participants-get-context` (`#13070`). `participant_emails` may exclude `user_email`; function `mvp/meeting-prep/classify-participants-get-context` (`#13070`) still seeds `my_team` from `user_email`. |
| Raw function result | Store the raw function `mvp/meeting-prep/classify-participants-get-context` (`#13070`) JSON in `meeting_prep_context.participant_context`. |
| Prompt-ready memo | Store the derived Markdown in `meeting_prep_context.meeting_prep_memo_markdown`. |
| Refresh metadata | Store `participant_context_status`, `participant_context_generated_at`, `participant_context_source_hash`, `participant_context_source_event_updated_at`, `participant_context_function_id`, `participant_context_function_name`, and `participant_context_error`. |
| Refresh rule | Reuse the stored package when the source hash matches; refresh only on first prep open, explicit `force_refresh`, attendee/organizer/event-version changes, or a context-contract version change. |
| Duplicate protection | Create/load by `user_id + nylas_event_id`. Refreshing participant context edits the existing `meeting_prep_context` row; it must not create a duplicate row for the same user's event. |

<div style={{border: "3px solid #DC2626", borderRadius: "10px", padding: "18px 20px", margin: "20px 0", background: "#0B1020", color: "#FFFFFF"}}>
  <div style={{fontSize: "0.78rem", fontWeight: 900, letterSpacing: "0.08em", textTransform: "uppercase", color: "#FFFFFF", marginBottom: "8px"}}>Do not move this downstream</div>
  <div style={{fontSize: "1.08rem", fontWeight: 850, color: "#FFFFFF", lineHeight: 1.35}}>Meeting-prep chat does not call function `mvp/meeting-prep/classify-participants-get-context` (`#13070`).</div>
  <div style={{fontSize: "0.96rem", color: "#FFFFFF", marginTop: "8px", lineHeight: 1.55}}>The chat layer must read the saved `meeting_prep_context.meeting_prep_memo_markdown` and stored `participant_context`. Running function `mvp/meeting-prep/classify-participants-get-context` (`#13070`) belongs only in prep-initiation paths, where refresh is gated by `force_refresh`, source-hash changes, or context-contract changes.</div>
</div>

Prompt order:

```txt
System prompt

meeting_prep_context.meeting_prep_memo_markdown, including my_team from function mvp/meeting-prep/classify-participants-get-context (#13070)

Relevant Mem0 memories

Recent meeting-prep chat transcript

Latest user message
```

Do not save the full function `mvp/meeting-prep/classify-participants-get-context` (`#13070`) memo, full participant JSON, raw meeting notes, or the complete chat transcript to Mem0.

## How I Know AI Chat

<div style={{border: "2px solid #D97706", borderRadius: "12px", padding: "18px 22px", margin: "16px 0 18px", background: "#0B1020", color: "#FFFFFF"}}>
  <div style={{fontSize: "0.78rem", fontWeight: 800, letterSpacing: "0.08em", textTransform: "uppercase", color: "#FFFFFF", marginBottom: "6px"}}>AI chat surface</div>
  <div style={{fontSize: "1.2rem", fontWeight: 800, color: "#FFFFFF", lineHeight: 1.25}}>Relationship interview, summary, and durable relationship memory</div>
  <div style={{fontSize: "0.95rem", color: "#FFFFFF", marginTop: "6px"}}>Flow type: `how_i_know` · Run key: `how_i_know:{user_id}:{target_master_person_id}` · Primary table: `master_person_relationship` `#729`</div>
  <div style={{fontSize: "0.95rem", color: "#FFFFFF", marginTop: "6px"}}><strong>Conversation store:</strong> `master_person_relationship` (`#729`) stores the raw `transcript`, `relationship_summary`, and `interview_complete` state. API `GET /relationships/{master_person_id}` (`#8582`) loads it, and API `POST /relationships` (`#8583`) creates or updates it.</div>
  <div style={{fontSize: "0.95rem", color: "#FFFFFF", marginTop: "6px"}}><strong>Context generated:</strong> before API `POST /openui05/how-i-know-interview` (`#8581`), load target `master_person` (`#139`), existing `master_person_relationship.relationship_summary`, recent `master_person_relationship.transcript`, and relevant Mem0 relationship memories.</div>
</div>

Same as [Outcomes AI Chat](#outcomes-ai-chat) for Mem0 read/write timing, with an interview-specific prompt and Xano relationship-summary store.

How I Know AI Chat asks the user how they know a selected person, what they think about them, when they met, how strong the relationship is, and where the relationship is useful or sensitive. Xano owns the transcript and the short UI summary. Mem0 owns the individual durable memories behind the scenes.

<div style={{border: "2px solid #D97706", borderRadius: "10px", padding: "16px 18px", margin: "16px 0 18px", background: "#0B1020", color: "#FFFFFF"}}>
  <div style={{fontSize: "0.76rem", fontWeight: 900, letterSpacing: "0.08em", textTransform: "uppercase", color: "#FFFFFF", marginBottom: "8px"}}>Target behavior</div>
  <div style={{fontSize: "1.02rem", fontWeight: 800, color: "#FFFFFF", lineHeight: 1.35}}>The UI shows one compact relationship summary, while Mem0 stores the individual relationship memories.</div>
  <div style={{fontSize: "0.96rem", color: "#FFFFFF", marginTop: "8px", lineHeight: 1.55}}>After the chat yields new durable memories, run a low-cost summary model to refresh `master_person_relationship.relationship_summary` in table `#729`. The summary should be display text for the UI, not the source of truth for memory. The source of truth for recall is the approved individual memories saved to Mem0.</div>
</div>

<div style={{border: "2px solid #D97706", borderRadius: "10px", padding: "16px 18px", margin: "16px 0 18px", background: "#0B1020", color: "#FFFFFF"}}>
  <div style={{fontSize: "0.76rem", fontWeight: 900, letterSpacing: "0.08em", textTransform: "uppercase", color: "#FFFFFF", marginBottom: "8px"}}>Implementation callouts</div>
  <ol style={{margin: "0 0 0 1.2rem", padding: 0, color: "#FFFFFF", lineHeight: 1.55}}>
    <li>`master_person_relationship` (`#729`) is the active transcript and summary store; `how_i_know_x` (`#731`) should be treated as legacy unless a frontend audit proves it is still read.</li>
    <li>API `POST /openui05/how-i-know-interview` (`#8581`) is LLM-only; it does not persist the relationship row.</li>
    <li>API `POST /relationships` (`#8583`) must persist each accepted transcript update, latest summary, and `interview_complete` state.</li>
    <li>Because table `#729` stores transcript as one text field, define a canonical transcript format, truncation rule, and summary fallback before long interviews ship.</li>
    <li>Add or confirm a unique constraint for `user_id + master_person_id`; lookup-before-write in API `POST /relationships` (`#8583`) is not enough protection against duplicate rows under retries.</li>
    <li>Use a deterministic Mem0 `source_id` such as `master_person_relationship:{id}:transcript_hash:{hash}` or add first-party message rows before extracting memories.</li>
    <li>Decide whether context blocks for API `POST /openui05/how-i-know-interview` (`#8581`) remain frontend-supplied or move server-side into Xano.</li>
    <li>Define reopen behavior when `interview_complete = true`: append and resummarize, restart, or preserve the old interview and create a new version.</li>
  </ol>
</div>

<div style={{border: "2px solid #D97706", borderRadius: "10px", padding: "16px 18px", margin: "16px 0 18px", background: "#0B1020", color: "#FFFFFF"}}>
  <div style={{fontSize: "0.76rem", fontWeight: 900, letterSpacing: "0.08em", textTransform: "uppercase", color: "#FFFFFF", marginBottom: "8px"}}>Implementation summary</div>
  <ol style={{margin: "0 0 0 1.2rem", padding: 0, color: "#FFFFFF", lineHeight: 1.55}}>
    <li>Load the relationship row with API `GET /relationships/{master_person_id}` (`#8582`) before the chat turn.</li>
    <li>Call API `POST /openui05/how-i-know-interview` (`#8581`) to ask the next question or produce a closing reflection.</li>
    <li>Persist the updated transcript, latest UI summary, and completion flag with API `POST /relationships` (`#8583`).</li>
    <li>Extract durable relationship memories from the accepted transcript update and save approved memories to Mem0.</li>
    <li>When new Mem0 memories are created or updated, run a cheap summary refresh and save the updated `relationship_summary` back to `master_person_relationship` (`#729`) through API `POST /relationships` (`#8583`).</li>
    <li>Search Mem0 only when asking or summarizing, not when rendering the contact side panel.</li>
    <li>Keep raw transcript in Xano and individual durable memories in Mem0.</li>
  </ol>
</div>

<Info>
  **Tables used**

  | Table | ID | Role |
  | --- | --- | --- |
  | `master_person_relationship` | `729` | Active How I Know store with `user_id`, `master_person_id`, raw `transcript`, UI-facing `relationship_summary`, and `interview_complete`. |
  | `how_i_know_x` | `731` | Legacy/pending-confirmation side-panel table keyed by `my_person_id`; do not use as the Mem0 migration canonical store unless frontend still depends on it. |
  | `my_person` | `128` | User-owned person row used by the legacy `how_i_know_x.my_person_id` design. |
  | `master_person` | `139` | Target person profile and stable target identifier for the relationship interview. |
  | `user` | `125` | Owner of the relationship summary and Mem0 `user_id` scope. |
</Info>

Mem0 shape:

```txt
flow_type: how_i_know
thread_id: how_i_know:{user_id}:{target_master_person_id}
run_id: how_i_know:{user_id}:{target_master_person_id}
```

Do not call Mem0 just to render the contact side panel. Load the latest `master_person_relationship.relationship_summary` from Xano. Search Mem0 only when the AI interviewer is about to ask or summarize.

Current endpoints:

```txt
API POST /openui05/how-i-know-interview (#8581)
Xano API group: Robert OpenUI 0.5 (#1276)
Auth: user
Model: claude-haiku-4-5
Max tokens: 700
Temperature: 0.3

API GET /relationships/{master_person_id} (#8582)
Xano API group: 🚀 Robert API (#1261)
Auth: user
Purpose: load master_person_relationship (#729) by auth.id + master_person_id

API POST /relationships (#8583)
Xano API group: 🚀 Robert API (#1261)
Auth: user
Purpose: create or update master_person_relationship (#729)
```

Input contract for API `POST /openui05/how-i-know-interview` (`#8581`):

```txt
master_person_id: int
transcript: text
current_summary?: text
target_yaml_context?: text
target_name?: text
self_yaml_context?: text
self_company_yaml_context?: text
```

Response contract:

```txt
ready: boolean
summary: text
next_question: text
options: array
allow_freetext: boolean
multi_select: boolean
exchange_count: int
raw: text
```

Interview flow:

```txt
1. API GET /relationships/{master_person_id} (#8582) loads the master_person_relationship (#729) row, if one exists.
2. Load target person context from Xano.
3. Load existing master_person_relationship.relationship_summary, if present.
4. Load recent master_person_relationship.transcript.
5. Search Mem0 for relevant relationship memories.
6. API POST /openui05/how-i-know-interview (#8581) asks one focused question or produces a closing reflection.
7. API POST /relationships (#8583) persists the updated transcript, latest display summary, and interview_complete state.
8. Run memory extraction on the accepted transcript update.
9. Write approved individual memories to Mem0.
10. If new memories were created or updated, run a cheap relationship-summary refresh and save the new relationship_summary through API POST /relationships (#8583).
```

Save durable relationship memories to Mem0, such as origin, trust level, strengths, boundaries, and intro fit. Keep the raw transcript and UI display summary in Xano.

## Implementation Checklist

1. Wire function `mvp/context/get-all-user-context_v2` (`#13076`) into `suggestion_request` draft creation.
2. Store the Markdown snapshot in `suggestion_request.llm_summary_context.openui_context.self_markdown`.
3. Feed that stored snapshot into every API `POST /openui05/dispatch` (`#8497`) turn as `self_yaml_context`, or load it server-side in dispatch from `suggestion_request_id`.
4. Wire function `mvp/meeting-prep/classify-participants-get-context` (`#13070`) into prep-initiation paths: API `POST /meeting-prep-context/create` (`#8325`) and the widget bridge inside API `POST /conversations/{conversation_id}/messages` (`#8154`).
5. Store the function `mvp/meeting-prep/classify-participants-get-context` (`#13070`) result in `meeting_prep_context.participant_context`.
6. Derive `meeting_prep_context.meeting_prep_memo_markdown` from the function `mvp/meeting-prep/classify-participants-get-context` (`#13070`) company/person Markdown fields.
7. Feed that stored meeting prep memo into every meeting-prep chat turn before Mem0 memories.
8. Add Xano tables for `ai_chat_message`, `ai_chat_context_snapshot`, and `ai_memory_event_log` if we want first-party transcript/context persistence beyond the existing JSON fields.
9. Add a shared `thread_id` convention for all four chat surfaces.
10. Add Mem0 search before LLM response generation.
11. Add memory extraction after response persistence.
12. Add Mem0 writes for approved extractor candidates.
13. Add event polling or webhook handling to update write status.
14. Add an admin QA view showing extracted memories, skipped candidates, Mem0 event IDs, and source messages.
15. Add deletion/privacy workflow so a user memory can be removed from Mem0 and the local Xano event log.

## References

- [Mem0 Add Memories API](https://docs.mem0.ai/api-reference/memory/add-memories)
- [Mem0 Search Memories API](https://docs.mem0.ai/api-reference/memory/search-memories)
- [Mem0 Get Memories API](https://docs.mem0.ai/api-reference/memory/get-memories)
- [Mem0 Get Event API](https://docs.mem0.ai/api-reference/events/get-event)
- [Mem0 Entity-Scoped Memory](https://docs.mem0.ai/platform/features/entity-scoped-memory)
- [OpenAI GPT-5 nano model](https://platform.openai.com/docs/models/gpt-5-nano)
- [OpenAI Structured Outputs](https://platform.openai.com/docs/guides/structured-outputs)

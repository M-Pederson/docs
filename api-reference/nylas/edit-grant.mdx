---
title: "Edit Grant Settings"
api: "PATCH https://xh2o-yths-38lt.n7c.xano.io/api:s_Mv_Fpv/nylas-grants/{nylas_grant_id}"
description: "Update settings for a Nylas grant (AI, widget visibility, signature)."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField path="nylas_grant_id" type="integer" required>Grant ID to edit.</ParamField>
<ParamField body="ai_enabled" type="boolean">Enable/disable AI features.</ParamField>
<ParamField body="show_in_email_widget" type="boolean">Show in email widget.</ParamField>
<ParamField body="email_signature_html" type="string">HTML email signature.</ParamField>
<ParamField body="signature_enabled" type="boolean">Auto-append signature.</ParamField>
<ParamField body="signature_on_replies" type="boolean">Include signature on replies.</ParamField>
<ParamField body="orbiter_signature_enabled" type="boolean">Enable Orbiter branding signature.</ParamField>

<RequestExample>
```bash cURL
curl -X PATCH \
  'https://xh2o-yths-38lt.n7c.xano.io/api:s_Mv_Fpv/nylas-grants/1' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -d '{"ai_enabled": true, "signature_enabled": true}'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "email_account": "me@example.com",
  "ai_enabled": true,
  "signature_enabled": true
}
```
</ResponseExample>

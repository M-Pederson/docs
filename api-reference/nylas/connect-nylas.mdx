---
title: "Connect to Nylas"
api: "POST https://xh2o-yths-38lt.n7c.xano.io/api:s_Mv_Fpv/nylas/connect"
description: "Initiate a new Nylas connection for the authenticated user."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField body="test" type="boolean" required>Whether this is a test connection.</ParamField>

<RequestExample>
```bash cURL
curl -X POST \
  'https://xh2o-yths-38lt.n7c.xano.io/api:s_Mv_Fpv/nylas/connect' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -d '{"test": false}'
```
</RequestExample>

<ResponseExample>
```json 200
{}
```
</ResponseExample>

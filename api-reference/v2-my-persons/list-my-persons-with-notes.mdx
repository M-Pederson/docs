---
title: 'List My Persons With Notes'
api: 'GET https://xh2o-yths-38lt.n7c.xano.io/api:Et3oQAtI/my-persons/notes'
description: 'Get all my_person records that have at least one note'
---

## Overview

Returns all my_person records that have at least one note for the authenticated user.

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Response

<ResponseField name="itemsReceived" type="integer">
  Number of items returned in the current response.
</ResponseField>

<ResponseField name="curPage" type="integer">
  The current page number.
</ResponseField>

<ResponseField name="nextPage" type="integer">
  The next page number, or null if no more pages.
</ResponseField>

<ResponseField name="prevPage" type="integer">
  The previous page number, or null if on the first page.
</ResponseField>

<ResponseField name="items" type="array">
  Array of my_person records with notes, containing id, master_person_id, and full_name.
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET "https://xh2o-yths-38lt.n7c.xano.io/api:Et3oQAtI/my-persons/notes" \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_TOKEN"
```
</RequestExample>

<ResponseExample>
```json 200
{
  "itemsReceived": 1,
  "curPage": 1,
  "nextPage": null,
  "prevPage": null,
  "offset": 0,
  "itemsTotal": 1,
  "pageTotal": 1,
  "items": [
    {
      "id": 1,
      "master_person_id": 50,
      "full_name": "John Doe"
    }
  ]
}
```
</ResponseExample>

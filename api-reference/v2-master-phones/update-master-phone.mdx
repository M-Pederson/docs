---
title: "Update Master Phone"
api: "PUT https://xh2o-yths-38lt.n7c.xano.io/api:KJyr4R-l/master-phones/{master_phone_id}"
description: "Update a master phone record."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

### Path Parameters

<ParamField path="master_phone_id" type="integer" required>
  The ID of the master phone record to update.
</ParamField>

### Body Parameters

<ParamField body="phone_number" type="string">
  The updated phone number.
</ParamField>

<ParamField body="phone_extension" type="integer">
  Phone extension. Nullable.
</ParamField>

<ParamField body="phone_type" type="string">
  Type of phone. One of: `mobile`, `office`, `other`, `home`. Defaults to `mobile`.
</ParamField>

<ParamField body="phone_note" type="string">
  Optional note for the phone record. Nullable.
</ParamField>

### Response

Returns the updated master phone record.

<RequestExample>
```bash cURL
curl -X PUT \
  'https://xh2o-yths-38lt.n7c.xano.io/api:KJyr4R-l/master-phones/1' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -d '{
    "phone_number": "+1-555-987-6543",
    "phone_type": "office"
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{}
```
</ResponseExample>

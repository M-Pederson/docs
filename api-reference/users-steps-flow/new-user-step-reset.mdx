---
title: 'Create New User Step Reset'
api: 'POST https://xh2o-yths-38lt.n7c.xano.io/api:_wjCLRXt/new-user/step/reset'
description: 'Authentication: required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Request Body

<ParamField body="step" type="integer">
  The step field.
</ParamField>


## Response

<ResponseField name="id" type="integer">
  The id field.
</ResponseField>

<ResponseField name="created_at" type="number">
  The created_at field.
</ResponseField>

<ResponseField name="updated_at" type="number">
  The updated_at field. Can be `null`.
</ResponseField>

<ResponseField name="oldest_activity" type="number">
  The oldest_activity field. Can be `null`.
</ResponseField>

<ResponseField name="master_person_id" type="integer">
  The master_person_id field. Can be `null`.
</ResponseField>

<ResponseField name="orbiter_url" type="string">
  The orbiter_url field.
</ResponseField>

<ResponseField name="workos_id" type="string">
  The workos_id field. Can be `null`.
</ResponseField>

<ResponseField name="workos_profile" type="object">
  The workos_profile field. Can be `null`.
</ResponseField>

<ResponseField name="tiptap_collab_jwt" type="string">
  Jwt from tip tap
</ResponseField>

<ResponseField name="tiptap_ai_jwt" type="string">
  The tiptap_ai_jwt field. Can be `null`.
</ResponseField>

<ResponseField name="feature_base_jwt" type="string">
  The feature_base_jwt field. Can be `null`.
</ResponseField>

<ResponseField name="local_timezone" type="string">
  The local_timezone field.
</ResponseField>

<ResponseField name="use_primary_timezone" type="boolean">
  The use_primary_timezone field.
</ResponseField>

<ResponseField name="active_account" type="boolean">
  Is the account a active - we will use this later when user's can cancel account, etc.
</ResponseField>

<ResponseField name="enrich_complete" type="boolean">
  The enrich_complete field.
</ResponseField>

<ResponseField name="role" type="string">
  The role field.
</ResponseField>

<ResponseField name="user_login_name" type="string">
  The user_login_name field.
</ResponseField>

<ResponseField name="copilot_enabled" type="boolean">
  The copilot_enabled field.
</ResponseField>

<ResponseField name="settings" type="object">
  The settings field.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST https://xh2o-yths-38lt.n7c.xano.io/api:_wjCLRXt/new-user/step/reset \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "step": 1
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1,
  "updated_at": 1,
  "oldest_activity": 1,
  "master_person_id": 1,
  "orbiter_url": "example-orbiter_url",
  "workos_id": "example-workos_id",
  "workos_profile": "example-workos_profile",
  "tiptap_collab_jwt": "example-tiptap_collab_jwt",
  "tiptap_ai_jwt": "example-tiptap_ai_jwt",
  "feature_base_jwt": "example-feature_base_jwt",
  "local_timezone": "example-local_timezone",
  "use_primary_timezone": false,
  "active_account": false,
  "enrich_complete": false,
  "role": "profile_only",
  "user_login_name": "example-user_login_name",
  "copilot_enabled": false,
  "settings": "example-settings"
}
```
</ResponseExample>

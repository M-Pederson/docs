---
title: 'Add master address record for a company or person'
api: 'POST https://xh2o-yths-38lt.n7c.xano.io/api:Lnhgpj_M/master-addresses'
description: 'Add master address record for a company or person Authentication: required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Request Body

<ParamField body="master_company_id" type="integer">
  The master_company_id field.
</ParamField>

<ParamField body="master_person_id" type="integer">
  The master_person_id field.
</ParamField>

<ParamField body="address_label" type="string">
  The address_label field.
</ParamField>

<ParamField body="formatted_address" type="string" required>
  The formatted_address field.
</ParamField>

<ParamField body="address_note" type="string">
  The address_note field.
</ParamField>


## Response

<RequestExample>
```bash cURL
curl -X POST https://xh2o-yths-38lt.n7c.xano.io/api:Lnhgpj_M/master-addresses \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "master_company_id": 1,
      "master_person_id": 1,
      "address_label": "example-address_label",
      "formatted_address": "example-formatted_address",
      "address_note": "example-address_note"
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{}
```
</ResponseExample>

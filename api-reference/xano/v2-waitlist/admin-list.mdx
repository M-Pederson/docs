---
title: 'Admin: Get paginated list of waitlist entries'
api: 'GET https://xh2o-yths-38lt.n7c.xano.io/api:g7_LZ7hE/admin/list'
description: 'Admin: Get paginated list of waitlist entries Authentication: not required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

## Query Parameters

<ParamField query="x_admin_key" type="string" required>
  Admin API key (X-Admin-Key header)
</ParamField>

<ParamField query="page" type="integer">
  Page number
</ParamField>

<ParamField query="per_page" type="integer">
  Items per page
</ParamField>

<ParamField query="status" type="string">
  Filter by status (pending, invited, converted, rejected)
</ParamField>


## Response

<RequestExample>
```bash cURL
curl -X GET https://xh2o-yths-38lt.n7c.xano.io/api:g7_LZ7hE/admin/list \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1"
```
</RequestExample>

<ResponseExample>
```json 200
{}
```
</ResponseExample>

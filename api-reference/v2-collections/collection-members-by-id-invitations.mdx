---
title: 'Accept or reject a collection invitation'
api: 'PUT https://xh2o-yths-38lt.n7c.xano.io/api:U_4ewelb/collection-members/{collection_join_id}/invitations'
description: 'Accept or reject a collection invitation Authentication: required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Path Parameters

<ParamField path="collection_join_id" type="integer" required>
  The collection_join_id parameter.
</ParamField>


## Request Body

<ParamField body="action" type="string" required>
  The action field.
</ParamField>


## Response

<ResponseField name="id" type="integer">
  The id field.
</ResponseField>

<ResponseField name="created_at" type="number">
  The created_at field.
</ResponseField>

<ResponseField name="updated_at" type="number">
  When the user accepted or rejected Can be `null`.
</ResponseField>

<ResponseField name="collection_id" type="integer">
  The collection_id field. Can be `null`.
</ResponseField>

<ResponseField name="user_id" type="integer">
  The user_id field. Can be `null`.
</ResponseField>

<ResponseField name="invited_by_user_id" type="integer">
  User who shared the collection
</ResponseField>

<ResponseField name="via_team_id" type="integer">
  Team through which collection was shared (null for direct invites) Can be `null`.
</ResponseField>

<ResponseField name="is_favourite" type="boolean">
  The is_favourite field.
</ResponseField>

<ResponseField name="favourite_position" type="integer">
  The favourite_position field. Can be `null`.
</ResponseField>

<ResponseField name="master_person" type="object">
  The master_person field. Can be `null`.
</ResponseField>

<RequestExample>
```bash cURL
curl -X PUT https://xh2o-yths-38lt.n7c.xano.io/api:U_4ewelb/collection-members/1/invitations \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "action": "example-action"
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1,
  "updated_at": 1,
  "collection_id": 1,
  "user_id": 1,
  "invited_by_user_id": 1,
  "via_team_id": 1,
  "is_favourite": false,
  "favourite_position": 1,
  "master_person": "example-master_person"
}
```
</ResponseExample>

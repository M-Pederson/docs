---
title: "Add Phone Numbers"
api: "POST https://xh2o-yths-38lt.n7c.xano.io/api:_wjCLRXt/new-user/phone-numbers"
description: "Add phone numbers during the onboarding flow."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField body="phone_number" type="string" required>Phone number.</ParamField>
<ParamField body="phone_type" type="string" required>Type (e.g. `mobile`, `office`).</ParamField>
<ParamField body="phone_extension" type="integer">Extension.</ParamField>

<RequestExample>
```bash cURL
curl -X POST \
  'https://xh2o-yths-38lt.n7c.xano.io/api:_wjCLRXt/new-user/phone-numbers' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -d '{"phone_number": "+1234567890", "phone_type": "mobile"}'
```
</RequestExample>

<ResponseExample>
```json 200
{}
```
</ResponseExample>

---
title: 'Add a single share (travel_user_join) to a travel.'
api: 'POST https://xh2o-yths-38lt.n7c.xano.io/api:WtHnIWRU/travels/{travel_id}/shares'
description: 'Add a single share (travel_user_join) to a travel. Authentication: required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Path Parameters

<ParamField path="travel_id" type="integer" required>
  The travel_id parameter.
</ParamField>


## Request Body

<ParamField body="shared_to_user" type="integer" required>
  The shared_to_user field.
</ParamField>

<ParamField body="team_id" type="integer">
  The team_id field.
</ParamField>


## Response

<ResponseField name="id" type="integer">
  The id field.
</ResponseField>

<ResponseField name="created_at" type="number">
  The created_at field.
</ResponseField>

<ResponseField name="travel_id" type="integer">
  The travel_id field.
</ResponseField>

<ResponseField name="shared_to_user" type="integer">
  The shared_to_user field.
</ResponseField>

<ResponseField name="team_id" type="integer">
  The team_id field. Can be `null`.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST https://xh2o-yths-38lt.n7c.xano.io/api:WtHnIWRU/travels/1/shares \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "shared_to_user": 1,
      "team_id": 1
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1,
  "travel_id": 1,
  "shared_to_user": 1,
  "team_id": 1
}
```
</ResponseExample>

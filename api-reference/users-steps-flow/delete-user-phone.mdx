---
title: "Delete User Phone Number"
api: "DELETE https://xh2o-yths-38lt.n7c.xano.io/api:_wjCLRXt/user-delete/phone-numbers/{master_phone_id}"
description: "Delete a user phone number record."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField path="master_phone_id" type="integer" required>The phone ID to delete.</ParamField>

<RequestExample>
```bash cURL
curl -X DELETE \
  'https://xh2o-yths-38lt.n7c.xano.io/api:_wjCLRXt/user-delete/phone-numbers/1' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN'
```
</RequestExample>

<ResponseExample>
```json 200
{}
```
</ResponseExample>

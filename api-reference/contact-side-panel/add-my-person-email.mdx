---
title: 'Create Add My Person Email'
api: 'POST https://xh2o-yths-38lt.n7c.xano.io/api:iTL91sBE/add/my_person/email'
description: 'create and ADD an master_email record to a my_person record based on a my_person ID Authentication: required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Request Body

<ParamField body="my_person_id" type="integer" required>
  The my_person_id field.
</ParamField>

<ParamField body="email_address" type="string" required>
  The email_address field.
</ParamField>

<ParamField body="email_type" type="string">
  The email_type field.
</ParamField>

<ParamField body="active" type="boolean">
  The active field.
</ParamField>

<ParamField body="email_note" type="string">
  The email_note field.
</ParamField>


## Response

<ResponseField name="id" type="integer">
  The id field.
</ResponseField>

<ResponseField name="created_at" type="number">
  The created_at field.
</ResponseField>

<ResponseField name="my_person_id" type="integer">
  The my_person_id field.
</ResponseField>

<ResponseField name="master_email_id" type="integer">
  The master_email_id field.
</ResponseField>

<ResponseField name="primary" type="boolean">
  The primary field.
</ResponseField>

<ResponseField name="user_id" type="integer">
  The user_id field.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST https://xh2o-yths-38lt.n7c.xano.io/api:iTL91sBE/add/my_person/email \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "my_person_id": 1,
      "email_address": "user@example.com",
      "email_type": "example-email_type",
      "active": true,
      "email_note": "example-email_note"
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1,
  "my_person_id": 1,
  "master_email_id": 1,
  "primary": false,
  "user_id": 1
}
```
</ResponseExample>

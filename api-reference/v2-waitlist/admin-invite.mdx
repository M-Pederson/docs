---
title: 'Admin: Invite a user from the waitlist'
api: 'POST https://xh2o-yths-38lt.n7c.xano.io/api:g7_LZ7hE/admin/invite'
description: 'Admin: Invite a user from the waitlist Authentication: not required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

## Request Body

<ParamField body="x_admin_key" type="string" required>
  Admin API key (X-Admin-Key header)
</ParamField>

<ParamField body="email" type="string" required>
  Email of user to invite
</ParamField>


## Response

<ResponseField name="success" type="string">
  The success field.
</ResponseField>

<ResponseField name="message" type="string">
  The message field.
</ResponseField>

<ResponseField name="email" type="string">
  The email field.
</ResponseField>

<ResponseField name="referral_count" type="string">
  The referral_count field.
</ResponseField>

<ResponseField name="invited_at" type="string">
  The invited_at field.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST https://xh2o-yths-38lt.n7c.xano.io/api:g7_LZ7hE/admin/invite \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -d '{
      "x_admin_key": "example-x_admin_key",
      "email": "user@example.com"
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "success": "example-success",
  "message": "example-message",
  "email": "example-email",
  "referral_count": "example-referral_count",
  "invited_at": "example-invited_at"
}
```
</ResponseExample>

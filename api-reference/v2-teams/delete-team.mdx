---
title: 'Delete Team'
api: 'DELETE https://xh2o-yths-38lt.n7c.xano.io/api:idMy4x2v/teams/{team_id}'
description: 'Delete a team and all its members'
---

## Overview

Delete a team and all its members.

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Path Parameters

<ParamField path="team_id" type="integer" required>
  The ID of the team to delete.
</ParamField>

## Response

<ResponseField name="success" type="boolean">
  Whether the deletion was successful.
</ResponseField>

<RequestExample>
```bash cURL
curl -X DELETE "https://xh2o-yths-38lt.n7c.xano.io/api:idMy4x2v/teams/1" \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_TOKEN"
```
</RequestExample>

<ResponseExample>
```json 200
{
  "success": true
}
```
</ResponseExample>

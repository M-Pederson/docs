---
title: 'Create Enrich Profile'
api: 'POST https://xh2o-yths-38lt.n7c.xano.io/api:nFBFWRKy/enrich-profile'
description: 'Receives the enrichment job result from Cloud Run and updates the matching profile_enrichment_job row. v1.37 (2026-07-16'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

## Response

<ResponseField name="id" type="integer">
  The id field.
</ResponseField>

<ResponseField name="created_at" type="number">
  The created_at field.
</ResponseField>

<ResponseField name="updated_at" type="number">
  The updated_at field. Can be `null`.
</ResponseField>

<ResponseField name="request_type" type="string">
  The request_type field.
</ResponseField>

<ResponseField name="job_id" type="string">
  The job_id field.
</ResponseField>

<ResponseField name="master_person_id" type="integer">
  The master_person_id field. Can be `null`.
</ResponseField>

<ResponseField name="master_company_id" type="integer">
  The master_company_id field. Can be `null`.
</ResponseField>

<ResponseField name="data" type="object">
  The data field. Can be `null`.
</ResponseField>

<ResponseField name="biography" type="array">
  The biography field. Can be `null`.
</ResponseField>

<ResponseField name="response" type="object">
  The response field. Can be `null`.
</ResponseField>

<ResponseField name="processing" type="boolean">
  The processing field.
</ResponseField>

<ResponseField name="error" type="string">
  The error field. Can be `null`.
</ResponseField>

<ResponseField name="model" type="string">
  The model field.
</ResponseField>

<ResponseField name="tokens" type="integer">
  The tokens field. Can be `null`.
</ResponseField>

<ResponseField name="temperature" type="number">
  The temperature field. Can be `null`.
</ResponseField>

<ResponseField name="llm_response" type="string">
  The llm_response field.
</ResponseField>

<ResponseField name="provider_attempt_count" type="integer">
  Monotonic Cloud Run/provider LLM attempt count for this logical job. Null on legacy rows; new jobs start at 0. Can be `null`.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST https://xh2o-yths-38lt.n7c.xano.io/api:nFBFWRKy/enrich-profile \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1"
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1,
  "updated_at": 1,
  "request_type": "example-request_type",
  "job_id": "example-job_id",
  "master_person_id": 1,
  "master_company_id": 1,
  "data": "example-data",
  "biography": [],
  "response": "example-response",
  "processing": false,
  "error": "example-error",
  "model": "example-model",
  "tokens": 1,
  "temperature": 1,
  "llm_response": "example-llm_response",
  "provider_attempt_count": 1
}
```
</ResponseExample>

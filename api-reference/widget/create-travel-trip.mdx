---
title: 'Create Travel Trip'
api: 'POST https://xh2o-yths-38lt.n7c.xano.io/api:P-1RmkSI/travel/trip'
description: 'Create a travel event'
---

## Overview

Create a new travel event with departure, destination, and sharing options.

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Request Body

<ParamField body="departing_from" type="string" required>
  The departure location.
</ParamField>

<ParamField body="destination" type="string" required>
  The destination location.
</ParamField>

<ParamField body="start_date" type="date" required>
  The trip start date.
</ParamField>

<ParamField body="end_date" type="date">
  The trip end date.
</ParamField>

<ParamField body="note" type="string">
  Optional note about the trip.
</ParamField>

<ParamField body="share_to_all" type="boolean">
  Whether to share the trip with all users.
</ParamField>

<ParamField body="share_to_connections" type="boolean">
  Whether to share the trip with connections only.
</ParamField>

<ParamField body="groups" type="array">
  List of group IDs to share the trip with.
</ParamField>

<ParamField body="formatted_address" type="string" required>
  The formatted address of the destination.
</ParamField>

## Response

Returns the created travel trip object.

<RequestExample>
```bash cURL
curl -X POST https://xh2o-yths-38lt.n7c.xano.io/api:P-1RmkSI/travel/trip \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "departing_from": "New York",
    "destination": "San Francisco",
    "start_date": "2026-04-01",
    "end_date": "2026-04-05",
    "note": "Business trip",
    "share_to_all": false,
    "share_to_connections": true,
    "formatted_address": "San Francisco, CA, USA"
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1
}
```
</ResponseExample>

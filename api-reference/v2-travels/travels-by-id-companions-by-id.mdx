---
title: 'Delete Travels Companions'
api: 'DELETE https://xh2o-yths-38lt.n7c.xano.io/api:WtHnIWRU/travels/{travel_id}/companions/{companion_id}'
description: 'Remove a single companion (travel_companion) from a travel. Note: this intentionally does NOT remove the matching travel'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Path Parameters

<ParamField path="travel_id" type="integer" required>
  The travel_id parameter.
</ParamField>

<ParamField path="companion_id" type="integer" required>
  The companion_id parameter.
</ParamField>


## Response

<RequestExample>
```bash cURL
curl -X DELETE https://xh2o-yths-38lt.n7c.xano.io/api:WtHnIWRU/travels/1/companions/1 \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN"
```
</RequestExample>

<ResponseExample>
```json 200
{}
```
</ResponseExample>

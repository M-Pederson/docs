---
title: "Refactor Edge Descriptions"
description: "Migrate all LLM-generated edge descriptions to OpenRouter with the standard model config."
---

## Goal

Every function that calls an LLM to generate a graph edge `description` property should be updated to use the standard OpenRouter config below. All five functions currently use non-standard models (`gemini-3.1-flash-lite` or `deepseek-v4-flash`) that need to be swapped out.

## Standard OpenRouter Config

```json
{
  "model": "openai/gpt-oss-120b",
  "provider": {
    "sort": "throughput",
    "allow_fallbacks": true,
    "data_collection": "deny"
  }
}
```

## Why

- `gpt-oss-120b` via OpenRouter with `sort: throughput` maximises parallelism during backfills
- `data_collection: deny` keeps edge content out of provider training pipelines
- `allow_fallbacks: true` prevents silent failures when the primary model is rate-limited

## Functions to update

These are the LLM-calling functions that write a `description` to a graph edge. Each makes an `api.request` to OpenRouter — swap the `model` param and add the `provider` block.

<Card icon="wrench">

```
enrich/create-work-edge-description — 13000
```

Reads from the `work_experience` Xano table (not graph). Generates a 2–3 sentence description of a person's role at a company. **Current model:** `google/gemini-3.1-flash-lite`

</Card>

<Card icon="wrench">

```
enrich/create-work-edge-description-from-graph — 13003
```

Fetches `WORKS_AT` / `WORKED_AT` edge by `edge_uuid` from FalkorDB, generates a 2–3 sentence work history paragraph. **Current model:** `google/gemini-3.1-flash-lite`

</Card>

<Card icon="wrench">

```
enrich/create-relationship-edge-description-from-graph — 13009
```

Fetches relationship edges (`BOARD_MEMBER_OF`, `ADVISOR_TO`, `INVESTED_IN`, `MEMBER_OF`) by `edge_uuid` + `edge_label`. Generates a 1–3 sentence relationship description. **Current model:** `deepseek/deepseek-v4-flash`

</Card>

<Card icon="wrench">

```
enrich/create-worked-on-edge-description-from-graph — 13016
```

Fetches `WORKED_ON` (Film/TV credit) edge by `(person_uuid, film_uuid)` — no `edge_uuid` stamped on these edges. Generates a single sentence describing the person's credit. Called by `#13017 write-worked-on-edge-description-to-graph`, backfilled via `#13018 tool/backfill-worked-on-edge-descriptions`. **Current model:** `google/gemini-3.1-flash-lite`

</Card>

<Card icon="wrench">

```
enrich/create-expertise-edge-description-from-graph — 13019
```

Fetches `HAS_EXPERTISE` edge by `(person_uuid, expertise_uuid)`. Generates a 15–30 word sentence grounded in the edge's evidence fields (`identified_as`, `signal_type`, `depth`, `recency`, etc.). **Current model:** `google/gemini-3.1-flash-lite`

</Card>

<Card icon="filter">

```
mvp/resolve/resolve-edges-certifications — 12578
```

Handles the full certification resolution pipeline — LLM call → Xano table write → FalkorDB Cypher. The LLM generates `issuer_domain` + `description` for each certification record, then the Cypher sets `has_cert.description` directly on the `HAS_CERTIFICATION` edge. This is the only edge-description function where the LLM call and the graph write are **in the same function** (not split into create/write pairs). **Current model:** `moonshotai/kimi-k2-0905:online`

</Card>

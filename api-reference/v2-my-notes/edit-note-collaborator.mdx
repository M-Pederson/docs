---
title: "Edit Note Collaborator Permission"
api: "PUT https://xh2o-yths-38lt.n7c.xano.io/api:iuc7DvAP/my-note-collaborators/{my_note_collaborator_id}"
description: "Update the permission level of a note collaborator."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField path="my_note_collaborator_id" type="integer" required>
  The collaborator record ID.
</ParamField>

<ParamField body="permission_level" type="string" required>
  New access level: `viewer`, `editor`, or `commentor`.
</ParamField>

<ResponseField name="id" type="integer">Collaborator record ID.</ResponseField>
<ResponseField name="permission_level" type="string">Updated permission level.</ResponseField>

<RequestExample>
```bash cURL
curl -X PUT \
  'https://xh2o-yths-38lt.n7c.xano.io/api:iuc7DvAP/my-note-collaborators/1' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -d '{
    "permission_level": "viewer"
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1700000000,
  "updated_at": 1700100000,
  "my_note_id": 1,
  "shared_with_user": 5,
  "permission_level": "viewer"
}
```
</ResponseExample>

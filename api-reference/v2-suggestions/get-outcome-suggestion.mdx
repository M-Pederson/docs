---
title: 'Get Outcome Suggestion'
api: 'GET https://xh2o-yths-38lt.n7c.xano.io/api:MkA4QsNh/outcome-suggestions/{outcome_suggestion_id}'
description: 'Get a single outcome_suggestion record'
---

## Overview

Retrieve a single outcome suggestion record by its ID.

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Path Parameters

<ParamField body="outcome_suggestion_id" type="integer" required>
  The ID of the outcome suggestion to retrieve.
</ParamField>

## Response

<ResponseField name="id" type="integer">
  The outcome suggestion ID.
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET https://xh2o-yths-38lt.n7c.xano.io/api:MkA4QsNh/outcome-suggestions/123 \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_TOKEN"
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 123
}
```
</ResponseExample>

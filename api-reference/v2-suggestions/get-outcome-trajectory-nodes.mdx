---
title: 'Get Outcome Trajectory Nodes'
api: 'GET https://xh2o-yths-38lt.n7c.xano.io/api:MkA4QsNh/outcome-trajectory-nodes'
description: 'Query all outcome trajectory node records'
---

## Overview

Retrieve all outcome trajectory node records for a given outcome trajectory.

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Query Parameters

<ParamField body="outcome_trajectory_id" type="integer" required>
  The ID of the outcome trajectory to get nodes for.
</ParamField>

<ParamField body="page" type="integer">
  Page number for pagination.
</ParamField>

<ParamField body="limit" type="integer">
  Number of items per page.
</ParamField>

<ParamField body="offset" type="integer">
  Number of items to skip.
</ParamField>

## Response

<ResponseField name="itemsReceived" type="integer">
  Number of items returned in the current page.
</ResponseField>

<ResponseField name="curPage" type="integer">
  Current page number.
</ResponseField>

<ResponseField name="items" type="array">
  Array of outcome trajectory node records.
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET "https://xh2o-yths-38lt.n7c.xano.io/api:MkA4QsNh/outcome-trajectory-nodes?outcome_trajectory_id=123&page=1" \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_TOKEN"
```
</RequestExample>

<ResponseExample>
```json 200
{
  "itemsReceived": 5,
  "curPage": 1,
  "nextPage": null,
  "prevPage": null,
  "items": []
}
```
</ResponseExample>

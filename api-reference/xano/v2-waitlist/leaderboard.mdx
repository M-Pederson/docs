---
title: 'Get top referrers leaderboard with masked emails'
api: 'GET https://xh2o-yths-38lt.n7c.xano.io/api:g7_LZ7hE/leaderboard'
description: 'Get top referrers leaderboard with masked emails Authentication: not required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

## Query Parameters

<ParamField query="limit" type="integer">
  Number of entries to return (max 50)
</ParamField>


## Response

<ResponseField name="leaderboard" type="string">
  The leaderboard field.
</ResponseField>

<ResponseField name="total_shown" type="string">
  The total_shown field.
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET https://xh2o-yths-38lt.n7c.xano.io/api:g7_LZ7hE/leaderboard \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1"
```
</RequestExample>

<ResponseExample>
```json 200
{
  "leaderboard": "example-leaderboard",
  "total_shown": "example-total_shown"
}
```
</ResponseExample>

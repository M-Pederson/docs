---
title: 'Update birthday_join record'
api: 'PUT https://xh2o-yths-38lt.n7c.xano.io/api:mGnfV_UB/birthdays/{birthday_join_id}'
description: 'Update birthday_join record Authentication: required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Path Parameters

<ParamField path="birthday_join_id" type="integer" required>
  The birthday_join_id parameter.
</ParamField>


## Request Body

<ParamField body="date" type="string" required>
  The date field.
</ParamField>

<ParamField body="birthplace_location" type="string">
  The birthplace_location field.
</ParamField>


## Response

<ResponseField name="id" type="integer">
  The id field.
</ResponseField>

<ResponseField name="created_at" type="number">
  The created_at field.
</ResponseField>

<ResponseField name="updated_at" type="number">
  The updated_at field. Can be `null`.
</ResponseField>

<ResponseField name="master_person_id" type="integer">
  The master_person_id field. Can be `null`.
</ResponseField>

<ResponseField name="birthdate" type="string">
  The birthdate field. Can be `null`.
</ResponseField>

<ResponseField name="birthday_month" type="integer">
  The birthday_month field. Can be `null`.
</ResponseField>

<ResponseField name="birthday_day" type="integer">
  The birthday_day field. Can be `null`.
</ResponseField>

<ResponseField name="birthplace_location" type="string">
  The birthplace_location field. Can be `null`.
</ResponseField>

<ResponseField name="created_by_user_id" type="integer">
  The created_by_user_id field. Can be `null`.
</ResponseField>

<RequestExample>
```bash cURL
curl -X PUT https://xh2o-yths-38lt.n7c.xano.io/api:mGnfV_UB/birthdays/1 \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "date": "example-date",
      "birthplace_location": "example-birthplace_location"
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1,
  "updated_at": 1,
  "master_person_id": 1,
  "birthdate": "example-birthdate",
  "birthday_month": 1,
  "birthday_day": 1,
  "birthplace_location": "example-birthplace_location",
  "created_by_user_id": 1
}
```
</ResponseExample>

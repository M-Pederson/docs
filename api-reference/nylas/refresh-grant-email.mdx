---
title: "Refresh Grant Email"
api: "POST https://xh2o-yths-38lt.n7c.xano.io/api:s_Mv_Fpv/nylas/refresh-grant-email"
description: "Refresh invalid emails to valid in the Orbiter database for a user's grant."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField body="grant_id" type="string">Nylas grant ID to refresh.</ParamField>

<RequestExample>
```bash cURL
curl -X POST \
  'https://xh2o-yths-38lt.n7c.xano.io/api:s_Mv_Fpv/nylas/refresh-grant-email' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -d '{"grant_id": "grant_abc123"}'
```
</RequestExample>

<ResponseExample>
```json 200
{}
```
</ResponseExample>

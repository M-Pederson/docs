---
title: 'Create Add My Person Address'
api: 'POST https://xh2o-yths-38lt.n7c.xano.io/api:iTL91sBE/add/my_person/address'
description: 'create and ADD an master_address record to a my_person record based on a my_person ID with an optional "address note" Au'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Request Body

<ParamField body="my_person_id" type="integer" required>
  The my_person_id field.
</ParamField>

<ParamField body="address_note" type="string">
  The address_note field.
</ParamField>

<ParamField body="address_label" type="string">
  The address_label field.
</ParamField>

<ParamField body="formatted_address" type="string" required>
  The formatted_address field.
</ParamField>


## Response

<ResponseField name="id" type="integer">
  The id field.
</ResponseField>

<ResponseField name="created_at" type="number">
  The created_at field.
</ResponseField>

<ResponseField name="my_person_id" type="integer">
  The my_person_id field.
</ResponseField>

<ResponseField name="master_address_id" type="integer">
  The master_address_id field.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST https://xh2o-yths-38lt.n7c.xano.io/api:iTL91sBE/add/my_person/address \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "my_person_id": 1,
      "address_note": "example-address_note",
      "address_label": "example-address_label",
      "formatted_address": "example-formatted_address"
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1,
  "my_person_id": 1,
  "master_address_id": 1
}
```
</ResponseExample>

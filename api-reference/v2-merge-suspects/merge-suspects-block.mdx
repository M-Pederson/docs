---
title: 'Update Merge Suspects Block'
api: 'PATCH https://xh2o-yths-38lt.n7c.xano.io/api:6VkM3aHx/merge-suspects/block'
description: 'Block a merge suspect. Sets do_not_merge = true for the given merge suspect Authentication: required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Request Body

<ParamField body="merge_suspect_id" type="integer" required>
  ID of the merge suspect record to block
</ParamField>


## Response

<ResponseField name="id" type="integer">
  The id field.
</ResponseField>

<ResponseField name="created_at" type="number">
  The created_at field.
</ResponseField>

<ResponseField name="user_id" type="integer">
  The user_id field.
</ResponseField>

<ResponseField name="master_person_id" type="integer">
  The master_person_id field.
</ResponseField>

<ResponseField name="merge_with_master_person_id" type="integer">
  The merge_with_master_person_id field.
</ResponseField>

<ResponseField name="merge_context" type="string">
  The merge_context field. Can be `null`.
</ResponseField>

<ResponseField name="merged" type="boolean">
  The merged field.
</ResponseField>

<ResponseField name="do_not_merge" type="boolean">
  The do_not_merge field.
</ResponseField>

<ResponseField name="kind" type="string">
  'person' or 'company' — discriminator for which pair of FKs is populated. Existing rows backfill to 'person'.
</ResponseField>

<ResponseField name="needs_rescore" type="boolean">
  Set true after a related merge collapses other suspects' inputs; cron picks these up to re-run Tier 1/Tier 2 scoring
</ResponseField>

<ResponseField name="master_company_id" type="integer">
  Populated when kind='company'
</ResponseField>

<ResponseField name="merge_with_master_company_id" type="integer">
  Populated when kind='company'
</ResponseField>

<RequestExample>
```bash cURL
curl -X PATCH https://xh2o-yths-38lt.n7c.xano.io/api:6VkM3aHx/merge-suspects/block \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "merge_suspect_id": 1
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1,
  "user_id": 1,
  "master_person_id": 1,
  "merge_with_master_person_id": 1,
  "merge_context": "example-merge_context",
  "merged": false,
  "do_not_merge": false,
  "kind": "person",
  "needs_rescore": false,
  "master_company_id": 1,
  "merge_with_master_company_id": 1
}
```
</ResponseExample>

---
title: "Get Widget Emails"
api: "GET https://xh2o-yths-38lt.n7c.xano.io/api:3IvQ7jxX/widget/emails"
description: "Get all unread emails for the authenticated user within a time period."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField query="time_period" type="string" required>
  Time range: `today`, `48_hours_ago`, or `7_days_ago`.
</ParamField>

<RequestExample>
```bash cURL
curl -X GET \
  'https://xh2o-yths-38lt.n7c.xano.io/api:3IvQ7jxX/widget/emails?time_period=today' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN'
```
</RequestExample>

<ResponseExample>
```json 200
{}
```
</ResponseExample>

---
title: "Get Emails"
api: "GET https://xh2o-yths-38lt.n7c.xano.io/api:U2vqGzGG/emails"
description: "Gets all unread emails for an authenticated user."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

### Response

<ResponseField name="emails" type="array">
  List of unread email objects for the authenticated user.
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET \
  'https://xh2o-yths-38lt.n7c.xano.io/api:U2vqGzGG/emails' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "emails": []
}
```
</ResponseExample>

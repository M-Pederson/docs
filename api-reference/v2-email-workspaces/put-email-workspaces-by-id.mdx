---
title: 'Update Email Workspaces'
api: 'PUT https://xh2o-yths-38lt.n7c.xano.io/api:dUJtZDlP/email-workspaces/{email_workspace_id}'
description: 'Update the display settings (name, color, emoji) of an email workspace profile. Auth user must own the workspace. Authen'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Path Parameters

<ParamField path="email_workspace_id" type="integer" required>
  ID of the workspace to update
</ParamField>


## Request Body

<ParamField body="name" type="string">
  New display name
</ParamField>

<ParamField body="color" type="string">
  New hex color e.g. #4F46E5
</ParamField>

<ParamField body="emoji" type="string">
  New emoji identifier e.g. 💼
</ParamField>


## Response

<ResponseField name="id" type="integer">
  Primary key
</ResponseField>

<ResponseField name="created_at" type="number">
  Record creation timestamp
</ResponseField>

<ResponseField name="updated_at" type="number">
  Last update timestamp
</ResponseField>

<ResponseField name="position" type="integer">
  Sidebar display order — 0 is first
</ResponseField>

<ResponseField name="user_id" type="integer">
  Owner of this workspace
</ResponseField>

<ResponseField name="provider" type="string">
  The provider field.
</ResponseField>

<ResponseField name="name" type="string">
  Workspace display label e.g. Work, Personal, Side Project
</ResponseField>

<ResponseField name="color" type="string">
  Hex color for UI theming e.g. #4F46E5
</ResponseField>

<ResponseField name="icon" type="string">
  The icon field.
</ResponseField>

<ResponseField name="emoji" type="string">
  Single emoji identifier e.g. 💼
</ResponseField>

<RequestExample>
```bash cURL
curl -X PUT https://xh2o-yths-38lt.n7c.xano.io/api:dUJtZDlP/email-workspaces/1 \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "name": "example-name",
      "color": "example-color",
      "emoji": "example-emoji"
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1,
  "updated_at": 1,
  "position": 1,
  "user_id": 1,
  "provider": "example-provider",
  "name": "example-name",
  "color": "example-color",
  "icon": "example-icon",
  "emoji": "example-emoji"
}
```
</ResponseExample>

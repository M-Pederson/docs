---
title: 'Token exchange for new user on create'
api: 'POST https://xh2o-yths-38lt.n7c.xano.io/api:s_Mv_Fpv/nylas/token-exchange'
description: 'Token exchange for new user on create Authentication: required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Request Body

<ParamField body="code" type="string" required>
  The code field.
</ParamField>

<ParamField body="state" type="string">
  The state field.
</ParamField>

<ParamField body="redirect_url" type="string">
  The redirect_url field.
</ParamField>


## Response

<RequestExample>
```bash cURL
curl -X POST https://xh2o-yths-38lt.n7c.xano.io/api:s_Mv_Fpv/nylas/token-exchange \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "code": "example-code",
      "state": "example-state",
      "redirect_url": "example-redirect_url"
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{}
```
</ResponseExample>

---
title: "Company Waterfall Functions"
description: "Every function and tool in the live company enrichment waterfall, grouped by role — orchestrator, LLM stage, gated pre-phases, phase chain, deal cascade, specialties, graph writers, relational fanout, queue infrastructure, and the Fundable substrate. Entry gate and cron live on their own pages."
---

<Note>
  **Status: LIVE** — workspace 3, branch `v1` (last full live audit 2026-07-02). The waterfall is entry-gate-driven: `master-company-new` #12558 resolves get-or-create synchronously and dispatches the 15-step v4.17 orchestrator `enrich-master-company` #12992 async at seed depth; queued leaves land in `queue_enrich_company`, but the drain cron **Task #127 ships `active = false`** — nothing drains the queue automatically today (see [Company Waterfall Crons](/guides/enrichment/waterfall/company-crons)). Architecture spec: [Company Waterfall overview](/guides/enrichment/waterfall/company-waterfall) · table schemas: [Company Waterfall Tables](/guides/enrichment/waterfall/company-tables) · graph writes: [Company Nodes](/guides/enrichment/waterfall/company-nodes) \+ [Company Edges](/guides/enrichment/waterfall/company-edges).
</Note>

Every function and task in the live company waterfall, grouped by role. Every path \+ ID is one-click copyable.

## Quick copy — all active paths

```text All active company paths
# Entry gate
mvp/get-add/master-company-new                #12558
# Domain helpers (entry-gate input cleanup)
mvp/format/domain-extractor                   #4587
mvp/format/domain-resolver                    #12696
# Orchestrator
mvp/enrich/enrich-master-company              #12992
# LLM stage + shared fanout helper
mvp/enrich/new-company-enrichment             #12974
mvp/enrich/apply-company-llm-payload          #12989
# Gated pre-phase (orchestrator steps 5-7)
mvp/investor/get-signal-nfx-data-company      #12991
mvp/investor/parse-signal-nfx-company         #12990
thesis/build-investment-thesis-in-gcp         #12978
thesis/gather-investor-context-v3             #12911
mvp/enrich/get-exa-company-c-suite            #12988
mvp/get-add/master-person-from-exa            #12997
# Phase chain (orchestrator steps 8-10)
mvp/enrich/process-company-phase-3            #12799
mvp/yc/process-yc-people                      #12700
mvp/yc/scrape_yc_data                         #4628
mvp/format/yc-batch-name                      #4516
mvp/enrich/add-yc-stats                       #10503
mvp/founders/find-founder-linkedin            #12563
mvp/enrich/process-company-phase-5            #12809
mvp/enrich/process-company-phase-6            #12810
# Deal fanout + cascade (orchestrator step 11)
mvp/enrich/process-company-phase-7            #12813
mvp/funding/add-all-fundable-deals            #12703
mvp/investor/cascade-deal-participants        #12856
mvp/funding/add-fundable-deal-node            #12701
mvp/fundable/resolve-investors-edges          #12702
# Specialties + taxonomy (orchestrator step 12)
mvp/expertise/resolve-company-specialties     #12746
mvp/expertise/attach-subdomain-parent         #13142
mvp/expertise/resolve-parent-domain           #13141
# About + industry classification (orchestrator step 13)
mvp/about/llm-company-about                   #2545
mvp/about/add-company-about                   #2538
mvp/industry/industry-specialties-llm         #4517
# Locations (orchestrator step 14 + upstream geocode)
mvp/address/add-company-locations             #1924
mvp/address/add-company-address               #2451
mvp/address/create-company-address-join       #4653
mvp/node/location-nodes-edges-company         #4528
mvp/maps/radar-maps-search-address            #2000
# School upsert (via add-company-node #2602)
mvp/enrich/add-school                         #4689
# SPECIALIZES_IN edge descriptions
enrich/create-company-specialty-description-from-graph  #13143
enrich/write-company-specialty-description-to-graph  #13144
# Graph writers
mvp/node/add-company-node                     #2602
mvp/node/update-company-node                  #4659
# Relational fanout helpers
mvp/industry/add-company-industry-array       #2547
mvp/industry/add-company-specialty-array      #2548
mvp/link/add-bulk-company-links               #2446
mvp/email/add-company-contact-email           #2710
mvp/phone/add-company-phones                  #2450
mvp/phones/llm-phone-format                   #2540
banner/add-update-co-banner                   #281
mvp/format/clean-my-json2                     #2806
# Queue + gate infrastructure
mvp/queue/upsert-enrich-company               #12855
mvp/stop/check-kill-switch-company            #12789
mvp/queue/process-enrichment-queue            #12816
# Fundable substrate (BigQuery mirror + lookup)
mvp/funding/fundable-lookup                   #12698
mvp/funding/get-add-fundable-records          #12689
mvp/funding/_bq-execute                       #12690
mvp/funding/_upsert-org                       #12691
mvp/funding/_upsert-person                    #12692
mvp/fundable/link-fundable-org                #12794
# Cron processor (inactive on live — see Company Waterfall Crons page)
process-company-queue                         Task #127
```

## LLM calls at a glance

Every LLM-bearing function and its live model. Full temp / token / timeout / pricing config lives on the [Company model summary](/guides/enrichment/company-pipeline/model-summary) and the prompts on [Company system prompts](/guides/enrichment/company-pipeline/llm-system-prompts) — linked, not duplicated.

| Function | Live model | Purpose |
| --- | --- | --- |
| `new-company-enrichment` #12974 | `google/gemini-3.1-flash-lite` → `anthropic/claude-sonnet-4.6` (escalation) | Two-stage web-grounded company research (`openrouter:web_search` tool) \+ Serper profile merge |
| `parse-signal-nfx-company` #12990 | `$env.LLM_MODEL_JSON_PARSE` (env-configured; resolves to `google/gemini-3.1-flash-lite-preview` per the [company model summary](/guides/enrichment/company-pipeline/model-summary)) | Signal NFX firm-profile markdown → JSON extract (temp 0.1 / 4000 tok / 60s, single attempt) |
| `process-yc-people` #12700 | `google/gemini-2.5-flash-lite` | founding-context narrative (temp 0, 500 tok) \+ 1536-dim embedding upstream of the FOUNDED/MENTOR edges |
| `scrape_yc_data` #4628 | `google/gemini-2.5-flash-lite` (×2) | YC company-page \+ partner-page markdown → JSON extraction |
| `yc-batch-name` #4516 | `google/gemini-2.5-flash-lite` | batch string → `W##` / `S##` / `INVALID` |
| `find-founder-linkedin` #12563 | `groq/compound` — **direct Groq API, not OpenRouter; absent from the model summary** | founder LinkedIn web-search lookup (empty result drops the founder entirely) |
| `resolve-company-specialties` #12746 | `google/gemini-3.1-flash-lite` (×1–2 per specialty) | specialty blurb generation (temp 0.2) \+ guard-band merge adjudication (temp 0) |
| `resolve-parent-domain` #13141 (via #13142) | `google/gemini-3.1-flash-lite` (reasoning off, 3-attempt retry, fail-loud) | parent DomainExpertise pick over the curated roster (0 = NONE, never auto-creates) |
| `create-company-specialty-description-from-graph` #13143 | `deepseek/deepseek-v4-flash` (temp 0.3, 300 tok, reasoning off) | SPECIALIZES\_IN edge NL description (one company-named sentence) |
| `llm-company-about` #2545 | `moonshotai/kimi-k2.5` (throughput-sorted, 2 attempts) | tagline \+ `about` \+ `about_500` generation |
| `industry-specialties-llm` #4517 | `openai/gpt-4o` (temp 0, single shot) | industry (fixed 30-item list) \+ 2–6 specialties classification |
| `location-nodes-edges-company` #4528 | `google/gemini-2.5-flash` (×3, uncaught) | region-code completion \+ region/city descriptions |
| `llm-phone-format` #2540 | `google/gemini-3.1-flash-lite` (temp 0.2 / 30 s; catch retries once on the SAME model) | raw phone → E.164 \+ calling code \+ local display — the E.164 output is the `master_phone` dedup key |

## Entry gate

THE entry point — `mvp/get-add/master-company-new` #12558 — is documented on [Company Waterfall Entry Points](/guides/enrichment/waterfall/company-entry-points): the quick-reference card, calling contract, and the full section-by-section walkthrough from placeholder guard to enrichment dispatch.

## Domain helpers (entry-gate input cleanup)

<Card icon="wrench">
  ```text
  mvp/format/domain-extractor — #4587
  ```

  **v1.1 (2026-05-31):** PSL registrable-domain canonicalizer — `www.resend.com` → `resend.com` (subdomains stripped as a side effect of registrable-domain extraction). Returns **null** on ANY parse failure: protocol/path inputs like `https://www.resend.com/pricing` fail psl label validation and yield null (the old page's "becomes `resend.com`" example was wrong — only bare hostnames are reduced), as do bare TLDs. The v1.1 null-on-error guard exists so psl error text can never leak into `company_domain`/logo/website. Called twice in #12558 §2 — before and after the resolver. _(The old page's ID #2639 was wrong — that is a Brave Signal-NFX helper; the live ID is #4587.)_
</Card>

<Card icon="wrench">
  ```text
  mvp/format/domain-resolver — #12696
  ```

  **Unversioned (2026-03-23, never modified):** follows the redirect chain of a forced-https `HEAD https://<domain>` probe (8 s abort inside a 15 s lambda, single attempt, any HTTP status accepted) and compares PSL **registrable** domains — only cross-registrable-domain hops set `was_redirected`; http→https, subdomain, and path redirects do not. Returns `{resolved_domain, original_domain, was_redirected}`; any network failure silently echoes the input back as "no redirect" (indistinguishable from success — no error field, no logging). #12558 consumes the output only when `was_redirected` is true, keeping both `$varOriginalDomain` and the resolved domain for the dedup ladders. _(The old page's ID #2640 was wrong — the live ID is #12696.)_
</Card>

## Orchestrator

<Card icon="layer-group">
  ```text
  mvp/enrich/enrich-master-company — #12992
  ```

  **v4.17 (2026-06-29) — the 15-step gated orchestrator.** Pre-flight: 60 s debounce (logs `qa_passed: true`, returns null) → base `enrich_history_company` row → defensive depth repair (**effective depth = max(input depth, deepest queue-row depth)**, v4.7 — feeds every seed-only gate and phases 3/5/6/7 \+ specialties) → VC-via-funding-rounds suppression early return (investor companies reached through deal resolution skip the whole pipeline) → `company_enrich_data` guard. Then the chain, each step crash-isolated in its own `try_catch` → `log_crash` `CRASH: <step>`: **1\+2** LLM preflight #12974 (only when `company_llm.data` empty) → **3** apply payload #12989 (only when the LLM freshly ran) → **4** Fundable re-lookup \+ conservative VC-evidence classification → **4.5** investor-classification repair — the ONLY place `is_vc` can be **downgraded** to false → **5** Signal NFX #12991 → post-5 signal-proof `is_vc` repair → **6** thesis #12978 (no row / empty `derived_thesis_summary` / \>30 d stale) → **7** Exa C-suite #12988 (response saved at any depth; people expansion seed-only, ending in the function's single async-shared dispatch: `enrich-master-person` at depth\+1 with `current_company_only: true`) → **8–11** phases 3/5/6/7 → **12** specialties #12746 → **13** about #2545 → **14** locations #1924 → **15** node sync #4659 → **15.5** `is_vc` re-assert after the node update.

  **As-built (flagged):** the inner per-person C-suite catch and the avatar WARN catch do NOT set `$hasCrash` — a run with per-person failures still finalizes with the bare `qa_passed: true` success marker. A seed company whose `exa_c_suite` is already saved silently no-ops the people branch (expansion happens only on the run that freshly fetches the Exa response). **Xano artifact — do not reimplement:** the dead `data_source_id` input, the dead `$modelUsed` variable, and the duplicated VC-evidence lambda (implement once). Full step table: [Company Waterfall overview](/guides/enrichment/waterfall/company-waterfall).
</Card>

## LLM stage

<Card icon="cloud-arrow-up">
  ```text
  mvp/enrich/new-company-enrichment — #12974
  ```

  **v1.17 (2026-06-04) — two-stage LLM company research; pure compute, zero writes.** Stage 1: `google/gemini-3.1-flash-lite` via OpenRouter with the `openrouter:web_search` tool (temp 0, 8000 tokens, 180 s, `provider: {sort: "throughput", allow_fallbacks: true, data_collection: "deny"}`); escalates to `anthropic/claude-sonnet-4.6` (temp 0.2, 240 s) when tier-1 confidence is missing, non-numeric, or unparseable — not only when `< 0.7`. Stage 2: one Serper batch call (7 `site:` queries across LinkedIn/X/Facebook/Instagram/YouTube/Crunchbase/Wellfound) merged into `data.profiles` through a canonical-URL identity filter — one URL per platform, the seed LinkedIn always wins and suppresses every other LinkedIn candidate; Serper failure silently yields `[]`. `source_urls`/`source_facts` inputs (v1.10–v1.15) feed the prompt verbatim AND drive deterministic post-LLM overrides (identity facts, official-website domain, `_meta.sources` rewrite).

  Returns `{data, model_used, escalated, gemini_confidence}` — a tier-2 parse failure still replaces the tier-1 result, so callers MUST check `data._error`. Precondition: at least one of `company_domain` / `linkedin_url` / `source_urls` / `source_facts` (name alone throws `inputerror`). Prompts: [Company system prompts](/guides/enrichment/company-pipeline/llm-system-prompts) · models: [Company model summary](/guides/enrichment/company-pipeline/model-summary).
</Card>

## Shared fanout helper

<Card icon="wrench">
  ```text
  mvp/enrich/apply-company-llm-payload — #12989
  ```

  **v1.8 (2026-07-02) — the shared LLM→relational fanout**, called sync from #12558 §10 (fresh creates) and #12992 Step 3 (backfills whose LLM payload just ran). Reads `company_enrich_data.company_llm.data`, then: `master_company` scalars preserve-existing (`existing |first_notempty: LLM` — LLM only fills blanks) with two deliberate exceptions — the v1.7 VC parent-bleed `company_name` repair (overwrite) and escalate-only `is_school`/`is_vc` flips; the single `company_financial` row upsert; then filtered profile links → #2446, industries → #2547, specialties → #2548, up to two abouts → #2538, HQ \+ office addresses → #2451, contact email → #2710, phones → #2450. Effective source is the constant `Company_LLM` (the `source` input is dead). No try\_catch — errors bubble to the caller.

  The v1.8 `company_financial` upsert is **preserve-existing per field**: `company_type` coalesces new → stored row → the `"Privately Held"` constant; `ticker` / `stock_label` / `primary_exchange` / `stock_link` / `went_public_on` coalesce against the existing row; `is_public` is escalate-preserving; the `funding_total` and `revenue` objects coalesce against the existing values, with the v1.6 VC-payload suppression applied **after** the coalesce (so a `"Venture Capital Firm"` payload still forces `funding_total` null — fund size is not company financing). `total_rounds_raised` comes exclusively from Fundable `num_funding_rounds` (no LLM rounds field is ever read); `funding_total` = LLM `funding.total_raised_usd` with a deterministic Fundable fallback (`first_notempty` → `round(fundable.total_raised × 1,000,000)` when finite and \> 0, v1.5). v1.8 also fixed the link filter's angel.co platform regex (`(?:wellfound\.com|angel\.co)`) — angel.co profiles are no longer silently dropped. *History: v1.7 → v1.8 the §5 upsert was LLM-wins overwrite (null LLM fields clobbered stored columns) and the angel.co branch could never pass — both repaired 2026-07-02.*
</Card>

## Signal NFX (orchestrator Step 5)

<Card icon="cloud-arrow-up">
  ```text
  mvp/investor/get-signal-nfx-data-company — #12991
  ```

  **v1.3 (2026-06-30):** company-side Signal NFX scrape. 30-day debounce on `enrich_history_company` (`source: "Signal NFX"` — ANY prior attempt in the window blocks a retry, failures included) → Brave URL discovery via `brave/get-signalnfx_domain` #2640 (top hit only; accepted only when it contains `signal.nfx.com/investors/`) → Firecrawl scrape via `mvp/firecrawl/scrape` #4515 (`soft_fail: true`; 4 attempts, constant 2s sleep, 60s each, 48h cache) → raw scrape into `company_enrich_data.signal_nfx_scrape` → LLM parse via #12990 → parsed JSON into `signal_nfx_json` → `investor_profile_company` upsert (`location` / `exits` / `investment_range` / `investment_sweet_spot` / `investment_stages`, with legacy `stages` key fallback) → **`master_company.is_vc = true`** flip on successful parse → `permalink_company` `signal_page`/`signal_json` mirror (edit-only, never creates the row). Called sync from `enrich-master-company` #12992 Step 5 only when `fundable_org_id` is set AND `is_vc` AND `signal_nfx_json` is empty — **the step has no depth gate**: any-depth VC with a Fundable org id triggers the external scrape (the only shield is the orchestrator's earlier depth\>0 early-return for funding-round-sourced VCs).

  **As-built (flagged):** the URL filter accepts only `/investors/` (person-profile) URLs — never `/firms/` — yet the parse prompt extracts VC-firm fields, and unlike the person-side twin `mvp/investor/get-signal-nfx-data` #2769 (Serper discovery \+ identity validation; person pipeline, cross-ref only) there is no identity check before persisting and flipping `is_vc`. Since #4515 v1.2, a failed scrape persists a structured failure object (`{success: false, markdown: null, …}`) into `signal_nfx_scrape` — consumers must tolerate that shape. Failure trap: markdown scraped but parse null ⇒ `signal_nfx_json` stays empty (the caller gate stays open) while the 30-day debounce defers the retry.
</Card>

<Card icon="cloud-arrow-up">
  ```text
  mvp/investor/parse-signal-nfx-company — #12990
  ```

  **v1.1 (2026-06-22):** stateless markdown → JSON extractor for a Signal NFX firm profile — one OpenRouter chat completion on `$env.LLM_MODEL_JSON_PARSE` (temp 0.1, `max_tokens` 4000, 60s, throughput-sorted provider routing, single user-role message, no retry), with defensive code-fence stripping before `JSON.parse`. Returns the parsed object or `null` on ANY failure (empty markdown, HTTP throw, missing content, unparseable JSON) — a null parse leaves `signal_nfx_json` unwritten and #12991 reports `success: false`. Zero DB/graph writes. Prompt on [Company system prompts](/guides/enrichment/company-pipeline/llm-system-prompts); model config on the [model summary](/guides/enrichment/company-pipeline/model-summary). Note: the `links` input is YAML-embedded into the prompt and the LLM (not code) filters out signal.nfx.com hosts — non-deterministic by design.
</Card>

## Investment thesis (orchestrator Step 6 — GCP Cloud Run)

<Card icon="cloud-arrow-up">
  ```text
  thesis/build-investment-thesis-in-gcp — #12978
  ```

  **Unversioned (latest change 2026-06-30 — retry-once):** gathers investor context via #12911 (`max_deals: 100`, `recency_months: 36`), canonicalizes the firm name (2026-06-29 deterministic fallback chain: `legal_name` → `company_name` → Fundable legal/name → domain), then POSTs `{master_person_id?, master_company_id?, context, data_source}` to the **GCP Cloud Run thesis builder** at `{gcp_backend_url}/v1/outcomes/investment-thesis/build` — **180s timeout, exactly one immediate retry on any non-200, no auth header sent** (base URL read at runtime from the `environment_variables` TABLE via #12977, per-datasource — not a Xano `$env` var). Non-200 on both attempts ⇒ `badrequest`, nothing written. On 200: firm-name match check against a normalized alias pool (mismatch ⇒ rejection sentinel as the derived summary, nulled `node_uuid`/declared summary, `REJECTED: GCP thesis firm_name mismatch` log\_crash — the row is still upserted); investor-type preservation (`is_vc` ⇒ `"vc_fund"`, `is_angel` ⇒ `"angel"` — overrides GCP); zero-deals companies get a deterministic no-evidence sentinel in place of GCP's derived summary. Upserts `investment_thesis` (#709 — one row per person OR company via query-then-add\_or\_edit; no unique index backs the key) with recomputed all-time deal stats (`total_deals_count` / `lead_deals_count` / `lead_ratio` / `last_lead_date` / `last_investment_date`), then mirrors a non-rejected derived summary into `investor_profile_person` / `investor_profile_company`. Three try\_catch'd `TRACE:` log\_crash rows (request / response / retry) — trace failures never break the build.

  Caller contract: **omit** the unused id — an explicit `0` passes this function's precondition but sends #12911 down the company branch (its branch pick is `!= null` only); never pass both ids (mixed person/company behavior). The Step 6 gate lives in `enrich-master-company` #12992: `fundable_org_id` set AND `is_vc` AND (no `investment_thesis` row OR empty `derived_thesis_summary` OR stale \> 30 days, `updated_at → created_at` fallback). The narrative / vector / check-size columns of #709 are written by the Cloud Run service directly (it receives `data_source` for exactly that purpose), not by this function.
</Card>

<Card icon="layer-group">
  ```text
  thesis/gather-investor-context-v3 — #12911
  ```

  **v3.6 (2026-06-30):** builds the `{entity_type, entity_context, deals, stats}` payload for the GCP thesis builder. Company branch (checked FIRST, `master_company_id != null`): `master_company` \+ `permalink_company` \+ `company_enrich_data` context with the v3.2 Signal preference (`company_enrich_data.signal_nfx_json` over legacy `permalink_company.signal_json`); when `fundable_org_id` is set, walks `fundable_institutional_investments` (fixed page 1 × 200 rows, `created_at desc` as a recency PROXY — investors with more than 200 rows silently truncate) into a 36-month recent-window deal sample capped at 100, collecting a parallel all-time sample; **sparse-window fallback (v3.4/v3.6):** fewer than 3 recent deals ⇒ the all-time sample ships instead (`stats.fallback_to_all_time: true`). All-time deal stats are computed either way, so thesis rows never lose stats when the recent window empties.

  Person branch: **force-refreshes Signal first** — sync `mvp/investor/get-signal-nfx-data` #2769 then `mvp/investor/person-extract-cb-signal` #2687 (person-pipeline helpers, cross-refs only) inside a best-effort try\_catch (`qa_passed: true` on both arms — never a QA failure), then reads `permalink_person` context and walks `fundable_angel_investments` through the same loop. Otherwise pure read \+ recompute: no external calls of its own, no queue/graph writes. A company with no `fundable_org_id` silently returns zero deals.
</Card>

## Exa C-suite \+ founders (orchestrator Step 7)

<Card icon="cloud-arrow-up">
  ```text
  mvp/enrich/get-exa-company-c-suite — #12988
  ```

  **v1.1 (2026-06-25):** Exa `people`-category search for founders / C-suite / senior stakeholders. POST `/search` (`numResults: 6`, `type: "auto"`, highlights \+ professional-bio summary query, 60s, no retry, no status check) → LinkedIn `/in/` slug filter (empty slug keeps all results) → **v1.1 title filter** (founder family always passes; junior titles — intern/analyst/associate/… — disqualify before the senior checks; then C-x officers, president/chair, and partner/MD families; context fallback requires the seed name in the bio blob) → optional `/contents` expansion of every distinct work-history company \+ education-institution id (**one unbatched call, all ids at once**, 60s), replacing org references in place with `{id, name, linkedinUrl, website, image, description}`. Pure fetch — zero DB/graph/queue writes; returns `{matches, matchCount, rawMatchCount, slug, cost, expansionCost}`.

  **No cost gates** — every invocation spends Exa credits (cost is only reported). Once-per-company semantics live entirely in the caller's `exa_c_suite|is_empty` gate, and a zero-match run still saves a non-empty envelope — an empty Exa result permanently satisfies the gate. What lands in `company_enrich_data.exa_c_suite` since v1.1 is this filtered \+ enriched envelope, **not the raw Exa response** (rows saved before 2026-06-25 keep the older broader payload — the field is fetch-once). People expansion is seed-only: at effective depth \> 0, #12992 saves the envelope and logs `SAVED: C-suite/founder Exa response only; people expansion gated to seed depth`.
</Card>

<Card icon="play">
  ```text
  mvp/get-add/master-person-from-exa — #12997
  ```

  **v1.9 (2026-06-25):** seed-scoped upsert of ONE `master_person` per Exa match. Filters the match's work history against seed signals (exact domain, root domain, LinkedIn company profile, exact name — aliases/legal/LLM/Fundable names included — plus two conservative prefix rules); seed-scoped with zero accepted rows ⇒ `skipped_not_seed_company`, which fires **before** dedup (an existing person is not updated on a non-seed match). Dedup key: normalized `linkedin_url` (`https://www.linkedin.com/in/…`, no trailing slash) — hit ⇒ non-destructive `first_notempty` merge \+ `is_c_suite: true`; miss with the person kill switch ON ⇒ `blocked_by_kill_switch`; else `db.add` with **`visibility: false`** (v1.7 — created ≠ card-ready), person node \+ `master_link`, and the v1.9 `person_enrich_data` shell invariant (`exa` = the ORIGINAL unfiltered match, 13 sibling payload columns `{}`, fill-only-when-empty). Current-employer resolution: seed context reuses the seed `master_company_id` outright (v1.5); non-seed context calls #12558 **lookup-only** (`allow_create: false, queue: false, hold_downstream: true` — may link an existing company, never mints one, v1.8). No queueing, no edges, no bios/avatar.

  Everything else runs caller-side in #12992's Step-7 per-match loop AFTER this helper returns (the old doc's cascade tree mis-nested these): bios ×2 → avatar **only when `action == "updated"`** (fresh inserts get none here) → seed-scoped `work_experience` rows (all stamped with the seed `master_company_id`) → `mvp/edges/create-work-edges` #2800, which MERGEs **`WORKS_AT`** (weight = seniority\_rank, \+20 if past) plus FOUNDED / BOARD\_MEMBER\_OF / ADVISOR\_TO / INVESTED\_IN / MEMBER\_OF — **there is no `HAS_WORKED_AT` edge on this path** (catalog: [Company edges](/guides/enrichment/waterfall/company-edges)) → `deep-person-basic` → async `enrich-master-person` at depth 1 with `current_company_only: true` (no `queue_enrich_person` row is written). **As-built (flagged):** `cascade_depth` is a dead input — passed by the caller, never read; and a match with an empty `url` skips dedup entirely, so re-runs insert duplicate person rows.
</Card>

## Phase chain — YC subsystem (orchestrator step 8)

<Card icon="layer-group">
  ```text
  mvp/enrich/process-company-phase-3 — #12799
  ```

  **v1.2 (2026-06-03) — YC detection \+ fanout.** Detects YC backing by intersecting the company's `fundable_deals` with Y Combinator's institutional-investment rows (hard-coded YC org uuid in `fundable_institutional_investments`); a hit runs `scrape_yc_data` **#4628** synchronously. Detection \+ scrape re-run whenever `yc_data` is empty **or** holds the `"no_data"` sentinel — unlike phase 5, a YC miss retries on every run. The processing block then fans `yc_data` out: industries, two about rows, HQ address, bulk links, `yc_batch` via **#4516** (the `"INVALID"` sentinel is written verbatim, unguarded), `yc_status`, and a `company_financial` upsert that **force-overwrites `company_type: "Private - VC Funded"`** on every run where a batch exists.

  **Seed-only gate (v1.2):** people resolution (**#12700**) runs only at `cascade_depth == 0`; deeper runs write a `qa_passed: true` SKIP row (`"SKIP: YC people gated to seed company depth"`) instead. A YC-people crash does **not** flip `$hasCrash` — phase 3 still finishes with its `qa_passed: true` heartbeat. **As-built (flagged):** the processing block reads yc-oss-era keys (`tags`, `one_liner`, `long_description`, `url`, `ycdc_company_url`, `status`) that #4628's extraction never emits — on Firecrawl-scraped companies those writes are null no-ops; only `batch` and `location` overlap the live extraction shape.
</Card>

<Card icon="cloud-arrow-up">
  ```text
  mvp/yc/process-yc-people — #12700
  ```

  **v3.2 (2026-07-02) — YC founders \+ partner → people \+ graph edges.** Merges founders from two sources — Fundable associations (`is_founder` or a "founder" title, via `master_company.fundable_org_id`) and `yc_data.founders` — keyed on `lowercase(trim(name))`, Fundable winning field-wise. Founders missing a LinkedIn URL go through `find-founder-linkedin` **#12563**; any founder still without one is **dropped entirely** — they never become a `master_person`. Survivors are created via `master-person-new` with `queue: true`, **tier 1**, `deep_research: true`, `is_c_suite: true`, at depth\+1; the primary YC partner gets the same flags at **tier 2** plus biography, avatar, `add-yc-stats` **#10503**, LLM bios (silent-fail), and a person-node update.

  Section 6 generates a founding-context narrative (`google/gemini-2.5-flash-lite`, temp 0) \+ a 1536-dim embedding — a failure here returns early and **no edges are written**. Section 7 writes the graph: multi-founder companies get a delete-then-CREATE `contextFounded` hub (`WAS_FOUNDED` w1 — **v3.2 (2026-07-02)** fixed the illegal w0; the hub itself is accepted by design as the vector-searchable founding event, superseding the §3.6 purge intent; per-founder `FOUNDED` w5); a single founder gets a direct `FOUNDED` MERGE (w5); the partner gets a `MENTOR` (w14) MERGE per founder whose key includes `valid_from` derived from `founded ?? 1970` — a later `founded` change mints a duplicate edge (never swept). Weights: [Company Edges](/guides/enrichment/waterfall/company-edges) · Cypher: [Cyphers → Company pipeline](/guides/cyphers/company-pipeline).
</Card>

<Card icon="cloud-arrow-up">
  ```text
  mvp/yc/scrape_yc_data — #4628
  ```

  **Unversioned (2026-06-04) — Firecrawl scrape \+ two-stage LLM extract.** Searches Firecrawl `/v2/search` (limit 1, 48-hour cache, markdown \+ links) for the company's ycombinator.com page — exactly one immediate retry inside the catch; a second failure throws to phase 3's catch. The YC partner link is regex-pre-extracted from the raw markdown **before** cleaning strips links; the cleaned page is then LLM-extracted to structured JSON (founders, batch, location, about, partner stub). When a partner URL resolves, the partner page is scraped and a second extraction **replaces the `primary_yc_partner` stub with the rich object** (biography, image, links, `yc_stats`) that #12700 §5 depends on — a name-only partner has none of those fields.

  Persists `yc_data` (or the `"no_data"` sentinel on a miss — the exact value phase 3's gates test), seeds an about row \+ bulk links, and writes `enrich_history_company` (`source: "YC"` on success; the failure row carries **no source**). Both extractions: `google/gemini-2.5-flash-lite`, temp 0 — config on the [model summary](/guides/enrichment/company-pipeline/model-summary), prompts on [system prompts](/guides/enrichment/company-pipeline/llm-system-prompts).
</Card>

<Card icon="cloud-arrow-up">
  ```text
  mvp/format/yc-batch-name — #4516
  ```

  **Unversioned (2026-06-04) — LLM batch normalizer.** Normalizes any YC batch string to `W##` / `S##` — or `INVALID` when unparseable — via `google/gemini-2.5-flash-lite` (temp 0, system message only). Returns `choices[0].message.content` **verbatim**: no trim, no fence-strip, no validation — and no caller guards the `INVALID` sentinel, so #12799 writes it straight into `master_company.yc_batch` and `company_financial.yc_batch`. API failures throw to the caller (no try\_catch). A Go port could be a deterministic regex, but parity means matching the `INVALID` sentinel behavior exactly.
</Card>

<Card icon="wrench">
  ```text
  mvp/enrich/add-yc-stats — #10503
  ```

  **Unversioned (2026-03-28) — YC-partner stats upsert.** Upserts `investor_profile_person.yc_stats` keyed on `master_person_id` (get-then-edit-else-add; first row wins if duplicates exist). The write is an **unconditional overwrite** — a null/empty `yc_stats` input stomps an existing populated value. No try\_catch: errors bubble to #12700 §5's catch, which logs and continues.
</Card>

<Card icon="cloud-arrow-up">
  ```text
  mvp/founders/find-founder-linkedin — #12563
  ```

  **Unversioned (2026-03-19) — Groq Compound founder-LinkedIn lookup.** Stateless per-founder web search on the **direct Groq API — not OpenRouter**: model `groq/compound` (agentic built-in search; temp 0.2, top\_p 0.5, 1024 tok, 30 s timeout), one immediate retry, second failure logs to `log_crash` and returns null. Returns the raw model string with **zero validation** — no trim, no URL check; anything non-empty is stored as the founder's `linkedin_url` by #12700, and an empty/null return means the founder is **dropped from person resolution entirely**. A first-attempt failure recovered by the retry is silent, and a "not found" outcome is indistinguishable from success in observability.

  ⚠️ The only pipeline LLM call outside OpenRouter routing — it appears on **neither** the [company model summary](/guides/enrichment/company-pipeline/model-summary) nor any [llm-system-prompts](/guides/enrichment/company-pipeline/llm-system-prompts) page.
</Card>

## Phase chain — Fundable backfill \+ financials (orchestrator steps 9–10)

<Card icon="layer-group">
  ```text
  mvp/enrich/process-company-phase-5 — #12809
  ```

  **Unversioned (2026-06-01) — once-ever Fundable backfill.** The entire work block is gated on `company_enrich_data.fundable` being empty: a hit stores the mapped payload, stamps `master_company.fundable_org_id`, and back-links the `fundable_organizations` row (`master_company_id` \+ `master_company_node_uuid`); a miss writes the `"no_data"` sentinel. **Either way the phase permanently retires for that company** — a miss never retries, even if the mirror later gains a matching org (the orchestrator's Step-4 re-lookup only backfills `fundable_org_id`; it does not re-open this fanout). Lookup runs through `fundable-lookup` **#12698** — Xano mirror only, never BigQuery: domain → LinkedIn → first `crunchbase.com/organization` link.

  Live fanout on a hit: up to three about rows (`short_description`, `long_description`, `crunchbase_about`) \+ bulk profile links (six mapped keys; `suppress_downstream` is **not** passed, so link-triggered downstream work is ungoverned here). **As-built (flagged):** the industries, specialties, HQ-address, banner, founded-year, and LinkedIn-follower branches are dead code — they read keys absent from #12698 v1.1's mapped shape and never fire (see [deviations](#deviations-deprecated--orphaned)). Crash leak: the catch never closes the `enrich_history_company` row (stays `processing: true`); the `qa_passed: true` heartbeat fires even on no-op skip runs.
</Card>

<Card icon="layer-group">
  ```text
  mvp/enrich/process-company-phase-6 — #12810
  ```

  **v1.1 (2026-07-02) — Fundable link repair \+ financials.** Two independent try blocks, inline `db` only — zero external calls, zero `function.run` dispatches. Block 1: when `fundable_org_id` is empty and a domain exists, exact-domain-match a `fundable_organizations` row (no sort — arbitrary first on duplicate domains), stamp `fundable_org_id`, and mirror `master_company_id` \+ `node_uuid` onto the org row **only if unclaimed** (first-claimer-wins, never re-pointed). This is a strict subset of orchestrator Step 4's re-lookup (domain only — no LinkedIn/Crunchbase fallback).

  Block 2 upserts `company_financial` from the **reverse-linked** fundable row (lookup by `fundable_organizations.master_company_id`, not `fundable_org_id`) — **fill-empty/escalate-only since v1.1**: `funding_total` (USD-millions → whole-USD object) is written only when the Fundable total is \> 0 **and** the existing value is empty **and** the company is not `is_vc` (no 0-stamps, no overwrite of non-empty values, no undo of #12989's VC suppression); `total_rounds_raised` is fill-forward; `is_public` (strict `ipo_status == "public"`) is escalate-only, so it never reverts phase-7 IPO flips. The early `$masterCompany` fetch is load-bearing (the `is_vc` read). It contains **no profile-URL logic of any kind** — the old page's "canonicalize profile URLs" claim was wrong (the closest live behavior is #12989's deterministic URL filter). *History: unversioned (2026-04-13) → v1.1 the write was an unconditional overwrite that re-stomped all three fields — including 0 and false — every run; repaired 2026-07-02.* Writer precedence: [Company Tables](/guides/enrichment/waterfall/company-tables).
</Card>

## Deal fanout (Phase 7 — orchestrator step 11)

<Card icon="layer-group">
  ```text
  mvp/enrich/process-company-phase-7 — #12813
  ```

  **v1.5 (2026-06-22):** Step 11 of the orchestrator — the seed-marker gate in front of all company-side deal fanout. Fanout requires effective depth 0 **AND** a seed marker (`is_vc` truthy, `seeded_by_user == 1`, or source `"User Input"` / `"ScrapeCreators Linkedin"`); a plain depth-0 company without a marker writes a `qa_passed: true` SKIP row instead (v1.4). v1.3 depth repair: the deepest `queue_enrich_company.cascade_depth` row for the company can only RAISE effective depth — stale direct callers passing 0 cannot trigger seed-only fanout. Gated-in runs open a `source: "Funding Rounds"` history row and sync-run **#12703** against the company's fundable org (first match on `master_company_id`); no fundable org → silent no-op (heartbeat only). Nuance: an inner #12703 crash logs a CRASH row and still writes the `qa_passed: true` heartbeat — the inner catch never sets the crash flag. The `data_source_id` input is dead: declared, never read — do not implement.
</Card>

<Card icon="wrench">
  ```text
  mvp/funding/add-all-fundable-deals — #12703
  ```

  **v2.3 (2026-07-02):** Walks both sides of a fundable org's deal graph — rounds it raised (`fundable_deals`, now sorted **`date asc`**, oldest first — v2.3) and rounds it invested in (`fundable_institutional_investments`, unsorted, no DISTINCT on `deal_id`) — dispatching each deal sync into **#12856** inside a per-deal `try_catch`, so one bad deal never aborts the batch. Deliberately no `processed_at` filter here: every fetched deal dispatches and #12856 self-gates on staleness. `cascade_depth` passes through raw (always 0 from Phase 7). The v2.3 chronological order lets #12702's earlier-round detection see prior rounds already in the graph — out-of-order processing had misclassified genuine follow-ons as first-time `INVESTED_IN`.

  **v2.2 list+paging+output trap (2026-07-02):** both `db.query` reads returned a `{type: "list", paging: {…}}` **with** an `output=[…]` projection, which silently yields a **bare empty array** — so #12703 had been walking **zero deals since 2026-06-25** (dispatching nothing to #12856; a silent no-op behind Phase 7's success heartbeat). v2.2 removed the projections and set `totals: true`. **Xano artifact — do not reimplement:** the list+paging+output empty-array behavior is a Xano quirk a normal-SQL (AlloyDB) port does not inherit.

  **Fan-out governor:** 25 target-side \+ 25 investor-side deals per invocation — caps arrive via `first_notempty:25`, so 0, null, and omitted ALL coerce to 25 (you cannot pass 0 to mean "none" or "uncapped"). Overflow writes the `qa_passed: true` log row `"fanout capped — processed first batch only"`; there is no continuation mechanism — remaining deals wait for a future invocation, and the target-side `date asc` walk means a re-run re-covers the oldest 25 unless rows changed.
</Card>

## Deal cascade (per-deal processing)

<Card icon="layer-group">
  ```text
  mvp/investor/cascade-deal-participants — #12856
  ```

  **v1.8 (2026-07-02):** Cascades one `fundable_deals` row end-to-end; called per deal from **#12703** (both sides) and from the person pipeline's investor fanout. Order: staleness gate — skip when `processed_at` is set and neither the deal, its institutional investments, nor their investment-person links carry a newer `updated_at` (angel investments are a blind spot: a new angel on a processed deal never re-triggers) → ensure the portfolio company exists as a `master_company` (sync **#12558** with `queue: false`, `hold_downstream: true`, `priority_tier: 2`, depth `effectiveDepth+1` — **1 from seed deals** — created local-only, NOT queued; v1.5 coerces Fundable `is_investor` to a native bool for the `is_vc` input) → IPO signal (flips an existing `company_financial` row `is_public` false→true when `ipo_status == "public"`; never creates a row; catch is silent) → **#12701** `Funding_Round` \+ RAISED (unconditional, all depths) → gated **#12702** → stamp `processed_at` **only when #12701 materialized the `Funding_Round` node** (v1.8 — a node-write failure leaves `processed_at` null \+ logs a crash so #12703 retries; ops can still null it to force a re-cascade).

  **v1.7 staleness recount (2026-07-02) — Xano list+paging+output trap.** The "any newer `updated_at`?" recheck runs a `db.query` returning `{type: "list", paging: {…}}`; through v1.6 it also carried an `output=[…]` projection, which silently returned a **bare empty array** (`itemsTotal` always 0), so the gate always read "nothing newer" and a deal that had gained an investor was never re-cascaded. v1.7 dropped the projection and set `totals: true`, so the recount is real. **Xano artifact — do not reimplement:** a `db.query` carrying BOTH a `list`/`paging` return AND an `output` projection yields `[]` / `itemsTotal: 0` with no error — a normal-SQL (AlloyDB) port does not inherit this and needs no `totals` flag.

  **v1.6 depth repair (sandbox-verified 2026-07-02):** `cascade_depth` is a **nullable input read via `first_notnull`** — an explicit seed `0` survives, the §4 `== 0` gate is satisfiable, #12702 fires from seed deals, and the portfolio get-add receives depth **1**; an omitted depth still fails closed to 1. *History: v1.5 (2026-06-24) → v1.6 the depth was read `first_notempty:1`, coercing seed `0` to `1` — #12702 was never invoked and portfolio companies were created at depth 2; repaired 2026-07-02.*
</Card>

<Card icon="cloud-arrow-up">
  ```text
  mvp/funding/add-fundable-deal-node — #12701
  ```

  **v1.9 (2026-07-02):** Graph writer for one deal. MERGEs the `Funding_Round` node keyed `uuid = fundable_deals.id` (the Fundable deal id string — not a generated uuid), re-embedding the description via `vectors/create-vectors-string` on every run, with investor counts and Twig-gated props (sizes/counts are never written as 0 and never zeroed out on match); writes `funding_round_node_uuid` back onto the deal row; then — only when the org's Company node uuid exists — MERGEs `(Company)-[:RAISED]->(Funding_Round)` at **weight 10**, `created_at` coalesced to the announced date (epoch millis floored to true UTC midnight) else write time, deterministic edge uuid `<companyNodeUuid>-<dealId>`. No `try_catch` — failures bubble to #12856's `CRASH: add-fundable-deal-node` catch. Weights on [edge weights](/guides/ontology/edge-weights) (lower = stronger); Cypher on [Cyphers → Funding](/guides/cyphers/funding).

  **v1.9 fixes (2026-07-02), all sandbox-verified:** (a) **quote-safety** — every Cypher string property (node name, descriptions, org name) is now JSON-encoded/double-quoted, so an apostrophe (`Deel's`, `company's`) no longer terminates the string literal and silently kills the node CREATE; this was the proven cause of 2 of 6 Deel `Funding_Round`s failing while #12701 still returned `"success"`. (b) **embedding-splice guard** (mirrors #2602 v1.8) — `name_embedding` is normalized to a bracket-string-or-null and **omitted** when unavailable, so the node is created vector-less rather than crashing on a malformed vector splice. (c) **response guard** — a missing `node_uuid` in the graph response now logs a `log_crash` and leaves `funding_round_node_uuid` **falsy** (the signal #12856 v1.8's `processed_at` gate reads) instead of silently reporting success. (d) **RAISED `created_at` floored to true UTC midnight** — the announced-date epoch no longer leaks the run's time-of-day. *History: the earlier v1.8-dated date hardening floored the ms→s division so a `fundable_deals.date` carrying non-zero milliseconds can't crash the Twig `date()` render.* **Xano artifact — do not reimplement:** the JSON-encode-string-into-Twig-Cypher approach is a Xano quote-injection workaround — parameterize the query in a port.
</Card>

<Card icon="cloud-arrow-up">
  ```text
  mvp/fundable/resolve-investors-edges — #12702
  ```

  **v1.13 (2026-07-02):** The investor half of the deal cascade (v1.13 floors the ms→s date division — fractional-millisecond deal dates no longer crash the Twig date render): for one deal's `Funding_Round` node it resolves every institutional investor org (bounded local-only creation via **#12558** — `hold_downstream: true`, `is_vc: false` per the conservative-classification rule — then queues depth 1 / tier 2 via **#12855** for fresh AND already-linked orgs; orgs the kill switch/containment refuses are skipped entirely, v1.7), each investment's deal partners and every angel (local-only `master-person-new`, then `upsert-enrich-person` at depth 1 / tier 1 — note it also unconditionally overwrites an existing partner's `master_person.master_company_id` / `current_title` with this deal's firm context), and MERGEs the investor edge set with `VC_Firm` / `Angel` label writes: `LEAD_INVESTED_IN` w15 · `INVESTED_IN` w20 institutional / w25 angel · `FOLLOW_ON_INVESTED_IN` w20 (earlier-round detection via announced-date compare) · `INVESTMENT_PARTNER_IN` w15 · `INVESTMENT_PARTNER_AT` w20. Catalog on [Company Edges](/guides/enrichment/waterfall/company-edges); Cypher on [Cyphers → Funding](/guides/cyphers/funding).

  **Repaired (v1.12, sandbox-verified end-to-end 2026-07-02):** the depth guard now reads `cascade_depth` as a **nullable input via `first_notnull`**, so the explicit seed `0` its caller passes survives and the function no longer self-skips — a synthetic sandbox seed deal produced `Funding_Round` \+ `RAISED` w10 \+ `LEAD_INVESTED_IN` w15 \+ `INVESTED_IN` w20, `queue_enrich_company` / `queue_enrich_person` upserts (count-increment, no duplicate rows), zero SKIP rows, and idempotent MERGEs on re-run. *History: v1.11 (2026-06-23) → v1.12 a `first_notempty:1` coercion made every invocation return `"skipped_depth_limited"`; repaired 2026-07-02.* **Historical window:** deals processed between 2026-06-23 and 2026-07-02 got NO investor edges and have `processed_at` stamped — re-cascading them requires nulling `fundable_deals.processed_at` (backfill **declined 2026-07-02** — accepted as historical, per Mark; see [deviations](#deviations-deprecated--orphaned)).
</Card>

## Specialties → expertise taxonomy (orchestrator step 12)

<Card icon="cloud-arrow-up">
  ```text
  mvp/expertise/resolve-company-specialties — #12746
  ```

  **v1.8 (2026-07-02) — locked expertise-match spec** (promoted from the v3 experiment #13138 on 2026-06-18). Orchestrator step 12, sync, every depth. Reads the company's specialties from `speciality_join` (frequency-ranked, capped to the top 15 distinct strings), then **destructively clears ALL of the company's `SPECIALIZES_IN` edges** and rebuilds per specialty: a blurb LLM writes a generic 2–3-sentence domain description (`google/gemini-3.1-flash-lite`; decode failure falls back to the specialty name), `"{specialty}: {blurb}"` is Gemini-embedded (3072-dim) and KNN-searched against the `Entity`-label `expert_embeddings` index (Domain nodes can match too), and a 3-way guard band decides (score is a distance — lower = closer): `< 0.15` auto-match, `0.15–0.25` LLM adjudication (`{"same": bool}`; decode failure → create), `≥ 0.25` or no result → create. A v1.8 exact-name guard (lowercased name over SubDomain \+ Domain labels) rescues matches when the vector path fails or the index is stale.

  Matched edges MERGE with `weight = min(round(10 + match_score*160), 50)` — ON MATCH pipe-appends `r.specialty` and keeps the LOWER weight/score. The create branch CREATEs the node (dual embeddings; deliberately no `expertise_embedding`), **verifies the returned uuid** (v1.8 — failure yields a `create_failed` result with no row, edges, or parent attach), inserts the `sub_domain_expertise` row, attaches the taxonomy parent via **#13142**, dual-writes the relational FK (`domain_expertise_id` via exact-name lookup on `domain_expertise` — silent no-op on miss, v1.8d), and MERGEs the edge at **fixed weight 10** (a new node perfectly represents its domain). Both branches call **#13144** after every MERGE for the edge description. Only the KNN and name-guard are try\_caught — an OpenRouter or #13141 throw aborts mid-loop **after** the destructive clear, leaving a partial specialty set until the next run. Weights: [Company Edges](/guides/enrichment/waterfall/company-edges) · prompts: [system prompts](/guides/enrichment/company-pipeline/llm-system-prompts) · config: [model summary](/guides/enrichment/company-pipeline/model-summary).
</Card>

<Card icon="cloud-arrow-up">
  ```text
  mvp/expertise/attach-subdomain-parent — #13142
  ```

  **Unversioned (2026-06-19) — taxonomy parent attach.** For each freshly created SubDomainExpertise node: sync-runs `mvp/expertise/resolve-parent-domain` **#13141** — an LLM pick over the full curated DomainExpertise roster fetched live from the graph (deterministic index→uuid map, `0 = NONE`, **never auto-creates a domain**; v1.3 added a 3-attempt retry \+ fail-loud throw on OpenRouter exhaustion, v1.4 refreshed the prompt for the 136-domain roster). A hit MERGEs `(DomainExpertise)-[:HAS_SUBDOMAIN {weight: 100}]->(SubDomainExpertise)` — weight 100 is deliberate hub damping for path-distance semantics, not closeness (see [edge weights](/guides/ontology/edge-weights)). A NONE verdict flags the node `parent_unresolved` \+ `parent_unresolved_at` for human review (flagged nodes are skipped by the orphan drainer). Returns `{attached, parent_domain}` — the parent by NAME, enabling the caller's relational FK dual-write.

  **As-built (flagged):** send-cypher results are never inspected — a wrong data source or not-yet-visible node writes nothing and still returns `attached: true`. Dedup is per-pair: a later run with a different resolver verdict can ADD a second parent edge, and nothing clears `parent_unresolved` on a subsequent attach.
</Card>

## About \+ industry classification (orchestrator step 13)

<Card icon="cloud-arrow-up">
  ```text
  mvp/about/llm-company-about — #2545
  ```

  **2026-05-30 (prompt `// v4`) — tagline \+ about generation.** Orchestrator step 13, sync, any depth. Reads ALL `about_company` rows for the company (newest first, YAML-encoded) and asks `moonshotai/kimi-k2.5` (throughput-sorted providers, single system message, reasoning off) for `{tagline, description_short, description_long}` — exactly 2 attempts (temp 0.2 → 0.3), then `log_crash` \+ return `""`. A deterministic clamp backstop trims to 174 / 229 / 500 chars at sentence/paragraph boundaries before writing `master_company.tagline` / `about` / `about_500` \+ `last_about_processed` — an **unconditional overwrite** of all three. Skip gate: already processed AND no newer about rows AND `about_500` populated → no-op (an empty `about_500` forces regeneration even with zero new rows). **As-built (flagged):** the old page's `master_company.company_about` column does not exist — the live writes are the three fields above.

  Section 5 tail (own try\_catch, non-fatal): runs the gpt-4o classifier **#4517** and fans its output into `add-company-industry-array` #2547 \+ `add-company-specialty-array` #2548 — so step 13 is a two-LLM step, not one. Prompt: [system prompts](/guides/enrichment/company-pipeline/llm-system-prompts) · config: [model summary](/guides/enrichment/company-pipeline/model-summary).
</Card>

<Card icon="filter">
  ```text
  mvp/industry/industry-specialties-llm — #4517
  ```

  **v1.1 (2026-06-30) — gpt-4o industry \+ specialties classifier.** Single-shot `openai/gpt-4o` (temp 0, 4000 tok, no retry, no fallback model) over ALL `about_company` rows — the full unprojected rows (ids, hashes, source strings included) are YAML-dumped into the system prompt. Returns `{industry, specialties}`: industry constrained to a fixed 30-item canonical list or `null`; 2–6 non-generic specialties. v1.1 made missing/empty `choices` non-fatal — every parse/shape failure degrades to `{industry: null, specialties: []}` with **no log row anywhere**; only a transport-level throw escapes, and #2545 §5's catch swallows that too. Pure compute, zero writes — persisting is the caller's job via #2547/#2548. Called only from #2545 §5 (= step 13). Prompt `// v4`: [system prompts](/guides/enrichment/company-pipeline/llm-system-prompts).
</Card>

<Card icon="wrench">
  ```text
  mvp/about/add-company-about — #2538
  ```

  **Unversioned — deduped about-row writer.** The feedstock writer for everything step 13 consumes: validates (empty or URL-only text → skip), strips emojis (shared helper #2523 — hash parity requires bit-identical stripping), then inserts one `about_company` row deduped **per company** on the hex md5 of the emoji-stripped text. The `source` string is a verbatim caller passthrough and NOT part of the key — the first source to submit a given text wins (canonical values: `Company_LLM` from #12989, `Fundable` from phase 5, `YC` from phase 3). Returns `"success"` only on a real insert, `""` on any skip — callers can't tell dedup from rejection without checking. No unique-index backstop: concurrent identical submissions can double-insert. The `data_source_id` input is dead legacy (declared, never written) post source-text migration.
</Card>

## Locations (orchestrator step 14 \+ upstream geocode)

<Card icon="wrench">
  ```text
  mvp/address/add-company-locations — #1924
  ```

  **Unversioned (2026-03-19) — the step-14 graph fanout pass.** Resolves nothing — it drains the company's `master_address` rows with `path_triples_added == false` into FalkorDB via **#4528** (sync, per row), then flips `path_triples_added: true` unconditionally on return. Always responds `"success"`, even with zero unprocessed rows. No try\_catch: a #4528 throw mid-loop leaves the current row unflagged (retried next run) and bubbles to #12992's step-level catch. **As-built (flagged):** #4528 "succeeds" even when its Cypher MATCH misses (e.g. empty `master_company.node_uuid`) and zero edges are written — the row is still flagged processed and never retried into the graph. Address _resolution_ (Radar geocode → `master_address` rows) happens upstream in **#2451**, invoked from the LLM-payload / Fundable / Exa steps — the old page's "resolves HQ \+ office addresses" claim was wrong.
</Card>

<Card icon="cloud-arrow-up">
  ```text
  mvp/node/location-nodes-edges-company — #4528
  ```

  **§3.6 (2026-05-31 weight audit) — shared person/company location graph writer.** Per address: get-or-create Country, Region|State, and City nodes (MERGE-keyed `{name}` / `region_key` / `city_key`; the State-vs-Region label pivots on a hard-coded `country.id == 2` US check) plus side-effect relational `region`/`city` rows and a `country.node_uuid` backfill, then exactly one of four edge variants keyed on which inputs are present: `LOCATED_IN` w70 (to city or region) or **w80 direct-to-country**, `IN_REGION` w25, `IN_COUNTRY` w50. All endpoints MATCH by uuid first — any missing uuid silently no-ops the whole variant (send-cypher string errors are swallowed). Three inline `google/gemini-2.5-flash` calls fill a missing region\_state/region\_code pair and write region/city descriptions — uncaught, so an LLM failure aborts the whole function. `LOCATED_IN` alone stamps `valid_from` (`Y-m`).

  **As-built (flagged):** the Country MERGE's Twig conditionals reference keys that don't exist on the `country` row — only `flag` \+ `name_embedding` ever land, and a country-table miss mints a junk `{name: ""}` node; the city-description prompt's region/country slots render empty (undefined template vars); the `$falkorObject`/`$falkorString` build is dead code. The person-only `primary_location` tail never fires from #1924 (no `master_person_id` passed). Weights: [Company Edges](/guides/enrichment/waterfall/company-edges) \+ [edge weights](/guides/ontology/edge-weights) · Cypher: [Cyphers → Company pipeline](/guides/cyphers/company-pipeline).
</Card>

<Card icon="wrench">
  ```text
  mvp/address/add-company-address — #2451
  ```

  **v1.1 (2026-05-23, added `source` provenance) — geocode \+ `master_address` upsert.** Geocodes free-text via Radar (**#2000**; `null` → early return, no row) and upserts one `master_address` row deduped per company on **Radar-normalized city \+ state** — two distinct street addresses in the same city/state collapse to one row. Inserts leave `path_triples_added` at default `false`, so the row is picked up by the next #1924 run; a row that already has `address_line_1` is skipped; otherwise the row is upgraded in place (`address_label`/`source` keep-existing via `first_notempty` — and `path_triples_added` is NOT reset, so an already-fanned row never re-enters the graph even if its fields changed). Radar `latitude`/`longitude` land in **text** columns; `timezone` stores Radar `timeZone.name` while #4653 stores `timeZone.id` — mixed formats downstream. The `data_source_id` input is declared but never read.
</Card>

<Card icon="wrench">
  ```text
  mvp/address/create-company-address-join — #4653
  ```

  **Unversioned (2026-02-23) — user-facing `my_company` address attach.** Not pipeline-wired: attaches an address to a `my_company` record — reuses an existing `master_address` by **exact `formatted_address` match** (no city/state collapse) or geocodes via #2000 and inserts one (null-safe if Radar fails: the row keeps the caller's verbatim string with all geocoded columns null; hard-codes `data_source_id: 5`; explicit `path_triples_added: false`; timezone = Radar `timeZone.id`). Optionally saves a note — under `$auth.id`, NOT the `user_id` input (live split, reproduce as-is) — then writes the `my_company_address_join` row. Duplicate join → early return `""` with the note skipped. New rows wait for the next #1924 run for that master\_company.
</Card>

<Card icon="cloud-arrow-up">
  ```text
  mvp/maps/radar-maps-search-address — #2000
  ```

  **Unversioned — Radar forward geocode.** GET `api.radar.io/v1/geocode/forward` with the raw live secret as the `Authorization` value (no Bearer prefix, no test-key or data-source switch — sandbox runs hit live Radar). Single attempt, no status handling: any non-2xx, transport error, or zero-result response funnels through one catch to `null` — indistinguishable from "no match". Returns the **first** address object verbatim (Radar-native keys: `formattedAddress`, `city`, `state`, `stateCode`, `latitude`, `longitude`, `timeZone{...}`, …) or `""` on an empty query. Preserve the `""`-vs-`null` asymmetry: #2451's failure check is `== null` only. **Xano artifact — do not reimplement:** the try\_catch doubles as the no-results guard (dot-path raise on `addresses.0`) — a Go port should check HTTP status \+ array length explicitly.
</Card>

## School upsert

<Card icon="wrench">
  ```text
  mvp/enrich/add-school — #4689
  ```

  **Unversioned (2026-01-20) — relational school-row upsert.** Called sync from `add-company-node` **#2602** when the company domain ends `.edu` or `is_school` is set (the mirror call in `update-company-node` #4659 is `!`-disabled — node updates never re-touch the table). Finds an existing `school` row by OR across four identity keys (`master_company_id` / `school_name` / `domain` / `linkedin_url`) and fills only its **empty** fields (existing-first `first_notempty` — it never corrects a wrong non-empty value), else inserts. **As-built (flagged):** empty inputs participate in the OR, so a sparse call can bind and "fill" an unrelated sparse row; no unique constraint backstops the read-then-write; and the `school` table (#300) is tagged "depreciated" in Xano yet actively written by this live path. The #2602 call site is not try\_caught — a crash here aborts node creation before the FalkorDB write. The graph `School` label is the caller's job; this function is relational-only.
</Card>

## SPECIALIZES\_IN edge descriptions

<Card icon="wrench">
  ```text
  enrich/create-company-specialty-description-from-graph — #13143
  ```

  **v1.0 (2026-06-19) — edge-description generator.** Reads one `(Company)-[SPECIALIZES_IN]->(Entity)` edge \+ endpoint properties from FalkorDB — keyed on the `(company_uuid, expertise_uuid)` pair because these edges carry no `e.uuid`, unlike the music trio — and generates a single standalone sentence (15–30 words, always leading with the company name) via `deepseek/deepseek-v4-flash` (temp 0.3, 300 tok, reasoning off, single system message). Responses run through the `check-openrouter` guard #13136: `llm_error` / `empty_content` degrade silently to `null`, but **out-of-credits (402) throws** — and propagates through #13144 into #12746, aborting the specialty run mid-loop. **As-built (flagged):** there is no missing-edge guard — zero rows still invokes the LLM on an empty record and returns whatever it produces. Prompt: [system prompts](/guides/enrichment/company-pipeline/llm-system-prompts) · config: [model summary](/guides/enrichment/company-pipeline/model-summary).
</Card>

<Card icon="wrench">
  ```text
  enrich/write-company-specialty-description-to-graph — #13144
  ```

  **v1.0 (2026-06-19) — idempotent skip-if-described wrapper.** Called sync from **#12746** after every SPECIALIZES\_IN MERGE (matched AND created branches; `create_failed` skips it). Empty uuid → null; `e.description_generated_at` already set → returns the stored sentence with no LLM spend; otherwise generates via #13143 and MATCH-SETs `e.description` \+ `e.description_generated_at` (ISO-8601 string, unlike the epoch-millis node timestamps). GOOD-or-null: an empty generation writes neither field, so the edge stays retry-eligible. Because #12746 opens every run by deleting all of the company's SPECIALIZES\_IN edges, the marker's practical effect is **within-run only** — every resolve run regenerates every description, and when 2\+ raw specialties collapse onto one edge the second call skips (the stored sentence reflects the edge at first write, not the appended `specialty` list). The SET result is never inspected — a no-op graph write is invisible to the caller. Standalone re-runs (backfill-style) are safe and cheap: described edges cost one read.
</Card>

## Graph writers (FalkorDB Company node)

<Card icon="cloud-arrow-up">
  ```text
  mvp/node/add-company-node — #2602
  ```

  **v1.7 (2026-07-02) — Company node CREATE with dual embeddings.** Called sync from `master-company-new` #12558 §10 on every fresh `master_company` create. Loads the row \+ `company_enrich_data.company_llm.data`, derives type and labels — `.edu` domain or `is_school` input runs `add-school` #4689 **sync (active here, `!`-disabled in #4659)**; `.org` → Organization; `"venture capital"` in the about flips `is_vc: true` on the row — then builds `Entity:Company[:School|:VC_Firm|:Organization]` (School wins over VC), embeds `company_name` twice (OpenAI 1536 `name_embedding` \+ Gemini 3072 `embeddings` via #13125, dual-write since v1.5 2026-06-15), and fires a **`CREATE` — not MERGE**: uniqueness is caller-side, and a re-run against the same id mints a duplicate node and re-stamps `node_uuid` (orphaning the previous one). `send-cypher` runs in a **3-attempt retry**; three failures log a needs-backfill `log_crash` row and return early. On success the response node's `uuid` is **stamped back onto `master_company.node_uuid`** (a success with no uuid in the response shape logs a crash row but still returns `"node added"`). v1.6 optional LLM props — `business_model`, primary/other product name \+ category, `target_customers`, `is_acquired: true` only-when-true, `acquired_by`/`_date`/`_ticker` — are null-omitted from the envelope; **no `description` property is written at create** (that arrives via #4659's `about_500` sync). Envelope \+ key: [Company Nodes](/guides/enrichment/waterfall/company-nodes) · Cypher: [Cyphers → Company pipeline](/guides/cyphers/company-pipeline).

  **Repaired (v1.7, 2026-07-02):** the null-domain fallback **no longer sets `is_vc: true`** — a company without a domain now gets only the placeholder logo. *History: through v1.6 the fallback also flipped `is_vc: true` on every null-domain company; rows mislabeled by the old branch are NOT backfilled (backfill **declined 2026-07-02** — accepted as historical, per Mark; see [deviations](#deviations-deprecated--orphaned)).* Still live (flagged): the `.edu` branch writes the RAW `is_school` input back to the row, so an `.edu` domain with `is_school` omitted writes `false` and skips the School LABEL while still running add-school and the `"school"` node type. **Xano artifact — do not reimplement:** the `!`-disabled v3 template, the `$nodeMcId` var copy (the template engine can't resolve `$input.*`), the JSON-stringify string-prop lambda (a port should parameterize the query), and the `for (3)` \+ `is_empty` retry shape (XanoScript has no break).
</Card>

<Card icon="cloud-arrow-up">
  ```text
  mvp/node/update-company-node — #4659
  ```

  **v1.8 (2026-06-23) — full property SET sync on the existing node.** The orchestrator's Step 15 (#12992). `MATCH (n:Entity {uuid: master_company.node_uuid})` \+ `SET` — **single attempt** inside a try\_catch (contrast #2602's 3 retries); a missing node is a **silent no-op**, and labels are never updated here. Builds the full envelope from the row \+ addons (name, domain, website, logo, founded, employees, tagline, industry list, `summary` = about, `description` = about\_500, specialties, `company_financial` fields incl. `went_public_on`/`stock_link`, investor-profile fields, v1.6 LLM props, profiles), prunes it through `clean-my-json2` **#2806**, then string-builds the SET via the `jsonToCypherSet` lambda: **nulls \+ empty arrays dropped — a blank value never CLEARS a stale node property — arrays deduped case-insensitively, booleans/numbers written raw, so `n.is_acquired = false` IS written** (verified through #2806, 2026-07-02; contrast #2602, which omits it when not true). Both vectors are re-embedded every run from the `about_500` → `company_domain` → `company_name` fallback text (**different embed text than #2602's name-only**), plus `n.updated_at = timestamp()`; v1.7 (2026-06-15) added the Gemini dual-embed. v1.8 guard: missing `node_uuid`, empty property string, or empty embed text → `{skipped: ...}` early return.

  Relational side effects a port must preserve: **forces `master_company.visibility = true` on every run**, re-writes `is_school`, and backfills an empty `company_name` from the domain (dots → underscores). Failure posture: only Section 3 (prune → SET → send) is try\_catch'd (`log_crash` note `"FalkorDB company node update failed"`); Sections 1–2 bubble to the caller. **Xano artifact — do not reimplement:** the `!`-disabled `add-school` call (never run from the update path), the `|!join` disabled comma-join on industries (the live contract is industry-as-list), the `!`-disabled v1 template, and the dead `$nodeLabel`/`"Personal Website"` label computation (never reaches the Cypher).
</Card>

## Relational fanout helpers (relational tables only)

All synchronous `function.run` callees of the fanout writers (#12989, #12558 §10c, phases 3/5) — none touch FalkorDB, queues, or cascade depth, and every defaulted `master_company_id` is consumed raw (callers must pass a real id; `0` is silently accepted as a key).

<Card icon="wrench">
  ```text
  mvp/industry/add-company-industry-array — #2547
  ```

  **v1.1 (2026-07-02) — industry upsert \+ join.** Lowercases each name, get-or-creates the global `industry` row (names stored lowercased), and adds an `industry_join` row per company; returns null on every path. **Dedup repaired in v1.1:** the existing-industry extraction now reads the real addon nesting via a lambda (the same shape sister function #2548 uses), and intra-batch duplicates are tracked — re-runs and repeated names within one call no longer duplicate `industry_join` rows. There is still no DB-level join dedup (app-level only). *History: unversioned (2026-05-12) → v1.1 the extraction (`|get:"industry"`) resolved null and the dedup gate never fired, duplicating join rows on every re-run; repaired 2026-07-02. Historical duplicate rows are NOT cleaned (backfill **declined 2026-07-02** — accepted as historical, per Mark; see [deviations](#deviations-deprecated--orphaned)).*
</Card>

<Card icon="wrench">
  ```text
  mvp/industry/add-company-specialty-array — #2548
  ```

  **Unversioned (2026-04-10) — specialty upsert \+ join.** Case-preserving twin of #2547 against `specialty` \+ `speciality_join` (British-spelling table name, American-spelling `specialty_id` column — replicate the schema names exactly, do not "fix" them). Dedup works here: a lambda extracts existing specialty strings from the `_specialty` addon nesting, plus an intra-batch added-set tracker — re-runs with identical strings are safe, but matching is **case-sensitive** ("Fintech"/"fintech" mint duplicate `specialty` rows). Returns the array of strings newly joined this run.
</Card>

<Card icon="wrench">
  ```text
  mvp/link/add-bulk-company-links — #2446
  ```

  **v1.1 (2026-06-23) — bulk link fanout.** Normalizes each URL — rtrim `/`, `twitter.com` → `x.com`, lowercase; **this normalization is part of the `master_link` dedup-key contract** — then delegates each to the shared `create-master-link`, per-link try\_catch with silent swallow (one bad URL never aborts the batch; nothing is logged). v1.1 added the `suppress_downstream` pass-through, correctly coerced `first_notnull:false`, so fresh creates can persist links without link-triggered downstream work. Returns null always; `data_source_id` is a dead input (see [deviations](#deviations-deprecated--orphaned)).
</Card>

<Card icon="wrench">
  ```text
  mvp/email/add-company-contact-email — #2710
  ```

  **v1.1 (2026-05-23) — contact-email insert-if-missing.** Dedup key `(lowercased email_address, master_company_id)` — the lowercasing lives in the input validator. A miss adds a `master_email` row with hardcoded `email_type: "other"` \+ `share_publicly: true` and the v1.1 `source` stamp (insert-only; never backfilled onto existing rows). Response-shape asymmetry callers must tolerate: existing rows return as a LIST, a fresh insert as an OBJECT, empty input as the string `"no email"`.
</Card>

<Card icon="wrench">
  ```text
  mvp/phone/add-company-phones — #2450
  ```

  **v1.1 (2026-05-23) — phone fanout via LLM formatter.** Raw-string dedups the input array, then per phone (own try\_catch): `llm-phone-format` **#2540** → E.164 → `master_phone` insert-if-missing keyed `(master_company_id, formatted phone_number)`, with hardcoded `phone_type: "main"` \+ the v1.1 `source` stamp. Idempotency rests on the LLM formatting the same raw string to the same E.164 every run (temp 0.2 — near-deterministic, not guaranteed). A per-item crash logs a `log_enrichment_company` row (`stack_status: "add-company-phones CRASH"` — not `log_crash`) and the batch continues; the response is `"success"` regardless.
</Card>

<Card icon="cloud-arrow-up">
  ```text
  mvp/phones/llm-phone-format — #2540
  ```

  **Unversioned (active prompt `// v4`) — LLM phone normalizer.** One OpenRouter call on `google/gemini-3.1-flash-lite` (temp 0.2, 30 s, system-message-only, `data_collection: "deny"`, no JSON mode — a fence-strip lambda owns parsing) returning `{country_calling_code, phone (E.164), phone_local_display}`, or `{"error": ...}` for unparseable input (\<7 or \>15 digits). The catch logs `log_crash` (`"primary failed, retrying again"`) and retries once on the **same model** — the "fallback" var is identical, not an escalation; a second failure throws to the caller (#2450's per-item catch). The `country` hint input is never passed by the company pipeline, so ambiguous 10-digit numbers default to US `+1`. Empty input early-returns `{phone: null, phone_local_display: null}` with the `country_calling_code` key entirely absent — callers must tolerate the missing key. Prompt: [Company system prompts](/guides/enrichment/company-pipeline/llm-system-prompts) · config: [model summary](/guides/enrichment/company-pipeline/model-summary). **As-built (flagged):** the Xano tag (`moonshotai/kimi-k2-0905`) and the "Kimi K2 primary, Claude Sonnet fallback" code comment are both stale — the live model vars are the contract.
</Card>

<Card icon="cloud-arrow-up">
  ```text
  banner/add-update-co-banner — #281
  ```

  **Unversioned (2026-06-01) — banner convert → GCS → `master_banner`.** Converts the remote image to WebP (SVG passthrough), uploads to GCS under `banners/{master_company_id}/…` with a random path segment — **every call mints a new storage object**, and only the GCS URL is stored, never the source URL — then **appends** a `master_banner` row with zero dedup: idempotency lives entirely in the callers' gates (#12558 §10c once per fresh create; #12809's once-ever Fundable block). Promotion: the new row becomes `main` (demoting the previous main and mirroring the URL onto `master_company.banner`) only when a non-protected main already exists (protected sources: Scrapin / LinkedIn / Owner) or when `source == "Enrich Company"` — a legacy override with no identified live caller.

  **As-built (flagged): a company's FIRST Fundable banner lands `main = false`** and `master_company.banner` stays null (no existing main ⇒ no promotion; live callers pass `source: "Fundable"`) — code unambiguous, product intent unconfirmed. No try\_catch anywhere: in #12558 §10c the call sits outside any try\_catch, so a banner crash aborts the entry gate mid-section (in #12809 the phase try\_catch contains it). `data_source` is a dead input — every row lands `data_source_id = 0` (see [deviations](#deviations-deprecated--orphaned)).
</Card>

<Card icon="wrench">
  ```text
  mvp/format/clean-my-json2 — #2806
  ```

  **v1.1 (2026-06-30) — recursive JSON pruner ahead of Cypher SET.** Single lambda, zero I/O: depth-first deletes object keys whose values are null, blank/whitespace-only strings, empty arrays, or — post-recursion, cascading bottom-up in one pass — empty objects; **`false` and `0` deliberately survive** (this is what lets `is_acquired: false` reach the graph from #4659). Array ELEMENTS are never removed, only object keys; null and non-object scalars pass through unchanged. In #4659 it is load-bearing for stripping empty strings and empty objects (the downstream SET-builder would otherwise emit `n.key = ""` / `"[object Object]"`); its null/empty-array stripping is redundant belt-and-braces there. A port must preserve exactly: strict `=== null`, `.trim()`-based string emptiness, elements-never-removed, and the single-pass bottom-up cascade. **Xano artifact — do not reimplement:** the v1.1 indexed-`for`-loop style exists only to dodge a Xano lambda callback-binding quirk — loop style is not semantics.
</Card>

## Queue \+ gate infrastructure

<Card icon="wrench">
  ```text
  mvp/queue/upsert-enrich-company — #12855
  ```

  **v1.1 (2026-06-03):** single source of truth for ALL `queue_enrich_company` writes — callers never branch on "already queued?"; that logic lives here. One row per `master_company_id`, enforced only by app-level get-then-branch (the table has NO unique index on it — concurrent callers can race duplicate inserts). Update path: `count`\+\+ on every duplicate request (a request tally, not a processing count), `priority_tier` and `cascade_depth` only ever improve (min; fallbacks 4 / 99), and source\_\* metadata refreshes only when the incoming request strictly improves depth (or ties depth with strictly better tier), per-field `first_notempty` against the existing values. Insert path: `processing: false, count: 1`, raw source\_\* fields. Guard: missing/zero id → `skipped_no_id` no-op.

  **As-built (flagged):** `priority_tier |first_notempty:4` coerces an explicit tier 0 → 4 — tier 0 is unreachable through this function (the table's tier vocabulary is 1–4). A Go port must reproduce 0→4, not honor 0. Table schema: [Company Waterfall Tables](/guides/enrichment/waterfall/company-tables).
</Card>

<Card icon="wrench">
  ```text
  mvp/stop/check-kill-switch-company — #12789
  ```

  **Unversioned (2026-04-11):** returns `true` when the company-creation kill switch is active. NOT a Xano env-var boolean — it reads the `kill_switch_company` **row in the `environment_variables` TABLE** and compares the live `master_company` row COUNT against that cap (`count >= value` ⇒ ON; set the value to 0 to force ON, very high to force OFF; each data source has its own count and its own threshold row). Fail-open: any error in the check logs one `log_crash` row and returns `false` — creation proceeds. When ON, #12558 answers from existing local rows only (zero external calls) and blocks ALL new company creation, including direct Fundable org seeds; #12816 additionally skips its entire company section.
</Card>

<Card icon="clock-rotate-left">
  ```text
  mvp/queue/process-enrichment-queue — #12816
  ```

  **v1.1 (2026-06-03) — operator batch drain serving BOTH queues** (`entity_type: "person" | "company" | "both"`, default both — the person batch runs to completion before the company batch starts). Always checks both kill switches (#4617 person, #12789 company); an active switch skips that queue's section and stamps a `*_kill_switch` note in the response. Company section: first 100 unprocessed rows sorted `priority_tier asc` only → in-memory `max_tier`/`max_depth`/`min_count` filter → `max_items` batch (default 10) → per row: lock → **sync** #12992 → delete on success; the catch releases the lock and bumps `companies_errors` only — **no log\_crash row**. `dry_run: true` is fully read-only and returns the would-be batch in `items` (while still incrementing the processed counters, by design). v1.1 routed dispatch through the gated v4 orchestrators so `cascade_depth` gates apply to queued leaves.

  **As-built (flagged):** the threshold filter is a string-built predicate — an omitted `min_count` may render it invalid, so callers should always pass `min_count: 0` explicitly (**Xano artifact — do not reimplement** the string-built filter; in Go these are three numeric comparisons). `*_remaining` counters are approximate: pre-filter and capped at the 100-row candidate window.
</Card>

## Cron processor (queue drain)

The queue-drain cron **Task #127** (`process-company-queue`, ships `active = false` on live) is documented on [Company Waterfall Crons](/guides/enrichment/waterfall/company-crons).

## Fundable substrate (BigQuery mirror \+ lookup)

<Card icon="wrench">
  ```text
  mvp/funding/fundable-lookup — #12698
  ```

  **v1.1 (2026-06-03):** Precedence lookup of a company in the local Xano `fundable_organizations` mirror — it never queries BigQuery. Tier order: exact `fundable_org_id` → `domain` → `original_domain` → `linkedin_url` → `crunchbase_url`, each lower tier running only while the result is still empty (v1.1 added the exact-id tier so direct-deal investors never bind to sibling Fundable rows sharing a domain). Returns bare `null` on a total miss, else a single-element array shaped like the legacy BigQuery response with `funding_rounds` / `founders` hard-nulled. **Xano artifact — do not reimplement:** the array wrapper \+ hard nulls exist only so legacy callers can keep doing `|get:0:null` — return row-or-nil and adapt callers. Read-only, zero network, zero writes. Called from the entry gate **#12558** §8 and phase 5 **#12809**; the orchestrator's Step-4 Fundable re-lookup is inline db, not this function.
</Card>

<Card icon="cloud-arrow-up">
  ```text
  mvp/funding/get-add-fundable-records — #12689
  ```

  **Unversioned (2026-03-22):** The BigQuery mirror ingester — pulls ONE organization plus its full funding subgraph (deals, valuations, org associations, institutional \+ angel investments, investment-person links, people, articles, industries) from `orbiter-io.fundable_data_feed_production` via up to 12 sequential `jobs.query` calls (one service-account token fetched up front from the internal `google-auth-service`; 180 s HTTP / 60 s BQ wait; no retry, no pagination — first response page only) and mirrors it into the `fundable_*` tables, resolving BQ uuids → Xano row ids through `_upsert-org` **#12691** and `_upsert-person` **#12692**. Re-run-safe by construction (upserts \+ check-then-add on natural keys) but has no `try_catch` — a mid-run failure leaves a silently partial mirror. Off the sync chain: nothing in the live #12558/#12992 waterfall invokes it. Mirror schemas on [Company Waterfall Tables](/guides/enrichment/waterfall/company-tables).

  **As-built (flagged):** (1) with all three inputs empty, Q1 renders with no `WHERE` clause — `SELECT * FROM organizations LIMIT 1` mirrors an arbitrary org; there is no all-empty guard. (2) PHASE 13 always writes the raw BQ institutional-investment uuid into `fundable_institutional_investments_person.institutional_investment_id` — its id map is keyed by Xano deal id and can never match, so every row is a permanently dangling reference. A Go port must decide explicitly: bit-for-bit parity or fix \+ backfill.
</Card>

<Card icon="cloud-arrow-up">
  ```text
  mvp/funding/_bq-execute — #12690
  ```

  **Unversioned (2026-03-22):** Generic self-authenticating BigQuery runner — raw SQL in, typed rows array out (`jobs.query`, project `orbiter-io`, 180 s HTTP / 60 s BQ wait, no retry, first response page only). The zero-row guard is a string compare on `totalRows == "0"` whose default also fires on any HTTP/auth failure, so errors are indistinguishable from empty results — a port should separate the two. **Xano artifact — do not reimplement:** it re-fetches a token on every call to dodge `function.run` variable-passing issues; use one cached token provider. Not called by anything in the company extraction (see the deviations table).
</Card>

<Card icon="wrench">
  ```text
  mvp/funding/_upsert-org — #12691
  ```

  **Unversioned (2026-03-22):** Upserts one typed BQ organizations row into `fundable_organizations`, keyed on the `crunchbase` permalink — a 32-field verbatim, BQ-wins overwrite (nulls included; no preserve-existing semantics), so every re-mirror stomps local edits. Load-bearing exclusion: the backlink columns `master_company_id` / `master_company_node_uuid` are deliberately OUT of the write set, so re-mirrors never clear established master links — keep them out of any port's write set too. Table 666 has no index (and no uniqueness backstop) on `crunchbase`, so the match is an unindexed scan and concurrent upserts can duplicate.
</Card>

<Card icon="wrench">
  ```text
  mvp/funding/_upsert-person — #12692
  ```

  **Unversioned (2026-03-22):** Fundable-people twin of #12691 — mirrors one BQ `people` row into `fundable_people` (25 fields written verbatim; nulls overwrite): match on `crunchbase_person_id`, else `linkedin_person_id` (both unique-indexed), else a blind insert — id-less people mint a duplicate row on every re-mirror. `master_person_id` / `master_person_node_uuid` are never written, preserving person backlinks across re-upserts. Called sync from **#12689** PHASE 8, one call per person row. Known failure mode: a linkedin-only row later re-arriving from BQ with a crunchbase id takes the insert path and collides with the linkedin unique index — with no `try_catch` anywhere, one such person aborts the entire #12689 run.
</Card>

<Card icon="wrench">
  ```text
  mvp/fundable/link-fundable-org — #12794
  ```

  **Unversioned (2026-04-12):** Backlink writer — stamps `master_company_id` onto the single `fundable_organizations` row matching a domain (unspecified pick when duplicate rows share the domain). Writes `master_company_id` only — never `master_company_node_uuid` — and returns the literal string `"linked"` unconditionally, even when the gate skipped (empty domain, or `master_company_id` 0/omitted — 0 counts as "empty" to the gate) or no row matched; callers get no success signal. **Xano artifact — do not reimplement:** the function exists only to dodge a Xano `db.edit` UUID-validation bug on inline edits of this table; in Go it is a one-line `UPDATE fundable_organizations SET master_company_id = ? WHERE domain = ?`.
</Card>

---

## Deviations, deprecated & orphaned

Everything below is live in workspace 3 but must NOT be treated as part of the active waterfall contract — deprecated chains, orphaned phases, parked backups, dead branches, and dead inputs a Go rebuild should not port.

| Path | ID | Status | Note |
| --- | :-: | --- | --- |
| `mvp/enrich/run-base-company-enrich` | **#4513** | Deprecated (tagged in Xano) | **v2.1 (2026-04-13)** legacy pre-v3/v4 sequential orchestrator: PDL phase-1 **#12797** → Enrich Layer phase-2 **#12798** → phase-3 → a dead `function.run ""` phase-4 slot (empty stored callee target — no `process-company-phase-4` exists; phase numbering runs 1, 2, 3, 5, 6, 7) → about → name correction → locations → node sync. No `cascade_depth` input — every legacy invocation behaves as seed/depth 0 to its callees; hardcodes `data_source_id: 78` on history rows. Still invocable via tool #12672. A Go rebuild implements #12992, not this. |
| `mvp/enrich/run-base-company-enrich-v3` | **#12814** | Deprecated, still callable | The v3 orchestrator between #4513 and the current v4 #12992 (which replaced it 2026-05-12; #12558 v2.6 switched its §11 dispatch off it the same day). Zero live callers. Legacy fingerprints in the data: `enrich_history_company` rows keyed `data_source_id: 101` with an empty `source` string, and — unlike the v4 chain's emptiness gates — it **unconditionally overwrites `company_enrich_data.exa_c_suite`** when the row exists. Do not run; do not port. |
| `mvp/get-add/master-company_BACKUP` | **#12986** | Parked backup — zero live callers | Snapshot of the entry gate with the same 10 queue-upsert call sites plus legacy PDL-era log milestones (`"start PDL"`, `"PDL complete"`, `"SKIP PDL+EL — Fundable data exists"`); besides #12558 it is the only other writer of `log_enrichment_company`. Verified uncalled in the queue-producer sweep — do not implement; safe-to-drop candidate. |
| `mvp/enrich/process-company-phase-1` | **#12797** | Orphaned | PDL processing — dropped from the active pipeline (orchestrator v2.4 removed PDL/Enrich Layer entirely); reachable only through deprecated #4513. |
| `mvp/enrich/process-company-phase-2` | **#12798** | Orphaned | Enrich Layer processing — same v2.4 removal; reachable only through deprecated #4513. |
| `tool/run-company_queue` | **#12672** | Legacy — do not run | Pre-v4 operator tool (last touched 2026-03-16). Flushes the ENTIRE unprocessed queue through deprecated #4513 (not #12992), passes no `cascade_depth` (all depth gating lost), locks rows AFTER processing, and never deletes or releases them — successfully processed rows become permanent `processing: true` tombstones invisible to Task #127 and #12816. No try\_catch, no kill switch, no batch cap. Operators should use `process-enrichment-queue` #12816 instead. |
| `log_queue_company` (table) | **#640** | Orphaned table — no live writer | Legacy queue log: a body grep of every queue producer/consumer, both orchestrators, and Task #127 found zero references. Schema documented on [Company Waterfall Tables](/guides/enrichment/waterfall/company-tables); do not port. |
| `mvp/fundable/resolve-investors-edges` (2026-06-23 → 07-02 deal window) | **#12702** | Repaired v1.12 (2026-07-02) — backfill declined | The v1.11 `first_notempty` depth coercion self-skipped the function from 2026-06-23 until the v1.12 repair (nullable `cascade_depth` read via `first_notnull`; sandbox-verified 2026-07-02). Deals processed in that window got **no investor edges** yet have `processed_at` stamped. **Backfill declined 2026-07-02 (per Mark)** — accepted as historical; a re-cascade would require nulling `fundable_deals.processed_at`. |
| `mvp/funding/add-fundable-deal-node` \+ `resolve-investors-edges` (deal-date milliseconds) | **#12701 / #12702** | Fixed (2026-07-02: #12701 date hardening \+ #12702 v1.13) | Both crashed on any `fundable_deals.date` carrying non-zero milliseconds — the `divide:1000` on the deal date yielded a fractional epoch and Twig's `date()` threw (found in sandbox testing 2026-07-02). Fixed same day: the division is now floored in both functions. |
| `mvp/funding/add-fundable-deal-node` (pre-v1.9 apostrophe silent-node-failure) | **#12701** | Repaired v1.9 (2026-07-02) | Through v1.8 every Cypher string property was spliced single-quoted with no escaping — an apostrophe in an org name/description terminated the literal and silently killed the node CREATE while the function still returned `"success"` (proven cause of 2/6 Deel `Funding_Round`s failing). v1.9 JSON-encodes/double-quotes every string prop, guards the `name_embedding` splice (vector-less node instead of crash), and on a missing `node_uuid` logs a crash \+ leaves `funding_round_node_uuid` falsy so #12856 v1.8 retries. |
| `add-all-fundable-deals` / `cascade-deal-participants` / `tool/backfill-fundable-org-companies` (db.query list+paging+output trap) | **#12703 / #12856 / #13066** | Fixed (2026-07-02) | A `db.query` returning `{type: "list", paging: {…}}` **with** an `output=[…]` projection silently returns a bare empty array (no items, no error), and `itemsTotal` needs `totals: true`. #12703 had walked **zero deals since 2026-06-25**; #12856's staleness recount always read 0 newer rows (a grown investor set never re-cascaded); #13066's batch backfill silently no-op'd. All three dropped the projection \+ added `totals: true`. A workspace sweep of 70 functions found only these three affected. **Xano artifact — do not reimplement:** a normal-SQL (AlloyDB) port does not inherit this. |
| `cascade-deal-participants` (processed_at-on-materialization) | **#12856** | Repaired v1.8 (2026-07-02) | Through v1.7 `processed_at` was stamped unconditionally, even after a logged crash, so a #12701 node-write failure was un-reprocessable. v1.8 stamps it **only** when #12701 reports the `Funding_Round` materialized (`funding_round_node_uuid` non-null); failures leave it null \+ log a crash so #12703 retries. |
| `mvp/node/add-company-node` (pre-v1.7 `is_vc` mislabels) | **#2602** | Repaired v1.7 (2026-07-02) — backfill declined | Through v1.6 the null-domain fallback set `is_vc: true` on every company without a domain. v1.7 removed the flip (placeholder logo unchanged). **Backfill declined 2026-07-02 (per Mark)** — rows mislabeled by the old branch are accepted as historical. |
| `mvp/enrich/process-company-phase-5` (dead branches) | **#12809** | Dead branches (live) | The industries / specialties / HQ-address / banner / founded-year / LinkedIn-follower branches read keys (`industry`, `categories`, `specialities`, `description`, `tagline`, `background_cover_image_url`, `hq.*`, `founded_year`, `follower_count`) that `fundable-lookup` #12698 v1.1's mapped return never contains — provably never fire. Effective live behavior: lookup \+ 3 abouts \+ bulk links \+ `fundable_org_id` stamp/backlink only. |
| `mvp/node/location-nodes-edges-company` (dead branches) | **#4528** | Dead code (live) | Country-MERGE Twig conditionals (`summary`/`alpha_2_code`/`population`/`capital_city`/`currency_*`) reference keys absent from the `country` row → only `flag` \+ `name_embedding` ever set; the `$falkorObject`/`$falkorString` build and the post-MERGE `db.get region` clobber are computed and never used; the city-description prompt's region/country Twig slots render empty. Port effective behavior only. |
| `mvp/industry/industry-specialties-llm` (inert blocks) | **#4517** | Dead code (live) | A disabled `// v1` free-text-industry prompt and a disabled `"Backed by YC Winter 2024"` test user-message sit `!`-prefixed in the body — the live payload is the `// v4` system message only. Do not implement either. |
| `mvp/industry/add-company-industry-array` (historical duplicates) | **#2547** | Repaired v1.1 (2026-07-02) — backfill declined | Pre-v1.1, the existing-industry extraction resolved null and the dedup gate never fired — every re-run duplicated `industry_join` rows. v1.1 reads the real addon nesting via a lambda and tracks intra-batch duplicates. **Backfill declined 2026-07-02 (per Mark)** — historical duplicate join rows are accepted as-is. |
| `mvp/node/update-company-node` (dead \+ disabled blocks) | **#4659** | Dead code \+ disabled call site (live) | The `$nodeLabel` / `"Personal Website"` label computation never reaches the Cypher — the function does not update node labels; only its `is_school` db.edits matter. The `add-school` #4689 call inside its .edu/is\_school branch is `!`-disabled — only the node-create path (#2602) writes school rows. Do not port either block. |
| `school` (table) | **#300** | Tagged "depreciated", still live-written | Actively written by #4689 via #2602 (plus the person-flow writer `mvp/education/create-school` #1900). Confirm with the team whether the table survives a port or the graph `School` label \+ `master_company.is_school` is the surviving contract. |
| `mvp/phones/llm-phone-format` (`// v1` prompt) | **#2540** | Disabled block \+ stale metadata | The entire `// v1` prompt block is `!`-disabled dead code (only `// v4` runs), and the `moonshotai/kimi-k2-0905` tag \+ "Kimi K2 primary, Claude Sonnet fallback" comment contradict the live `google/gemini-3.1-flash-lite` model vars — trust the vars, not the labels. |
| `mvp/get-add/master-person-from-exa` | **#12997** | Dead input | `cascade_depth` is declared but never read anywhere in the body — #12992 passes `effective_depth+1` to no effect; containment comes from the hard-coded lookup-only employer flags instead. A Go port should accept and ignore it (or drop it with caller agreement). |
| `mvp/investor/get-signal-nfx-data-company` | **#12991** | Dead branch | The `{"skipped": "firecrawl returned empty"}` early return is unreachable since `mvp/firecrawl/scrape` #4515 v1.2 (2026-06-30) — failed scrapes now return a structured non-empty object. Do not carry the guard into a port; branch on `markdown` presence (the code already does, next). |
| `mvp/enrich/get-exa-company-c-suite` | **#12988** | Unexercised path | Explicit `expand_companies: false` is passed by no live caller (#12992 always sends `true`); the false path is implementable from code but has zero production coverage. |
| `mvp/link/add-bulk-company-links` | **#2446** | Dead input | `data_source_id` is declared, defaulted 0, and never read anywhere in the stack — do not wire it up in a port (matching behavior = ignored). |
| `banner/add-update-co-banner` | **#281** | Dead input \+ legacy override | `data_source` is never read — every row lands `data_source_id = 0`, so the temp backfill `temp/backfill-enrich-layer-banner` #12892 (which dedups on `data_source_id == 94` and passes `94` in) can never see this function's writes and re-adds banners on re-runs. The `source == "Enrich Company"` main-promotion override has no identified live caller (Enrich Layer era). |
| `mvp/funding/_bq-execute` | **#12690** | Active — no caller identified | Generic self-authenticating BQ SQL runner; nothing in the company extraction calls it (#12689 inlines its own BQ requests, #12703 reads landed tables only). Identify the live caller before porting — rebuild-spec gap. |
| `mvp/funding/get-add-fundable-records` | **#12689** | Active — caller outside the live waterfall chain | The BQ→Xano mirror ingester is not invoked from #12558/#12992 or any phase function; its production trigger was not identified in this extraction (rebuild-spec gap). The PHASE 13 dangling institutional-investment-id bug stands regardless of caller. |
| `mvp/financial/get-company-financial-data` | **#12708** | Not pipeline-wired (read-only) | Serving/aggregation helper for the side-panel financial tab (`company_financial` \+ Fundable deals/investors read tree; zero writes). The old page implied phase-6 involvement — verified: #12810 contains no `function.run` statements at all and never calls it. Not part of the enrichment write path; no port needed for waterfall parity. |
| company-side specialty-description backfill | — | Claimed, not found | #13144's description header claims a backfill drain for existing edges, but no company-side backfill function exists in workspace 3 (the person analog #13021 does). May be an API endpoint or unbuilt — open question. |
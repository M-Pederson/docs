---
title: "Get Shared Travels"
api: "GET https://xh2o-yths-38lt.n7c.xano.io/api:WtHnIWRU/travels/shared"
description: "Gets all travel events shared to the user."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

### Response

<ResponseField name="travels" type="array">
  List of travel events shared to the authenticated user by other users.
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET \
  'https://xh2o-yths-38lt.n7c.xano.io/api:WtHnIWRU/travels/shared' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "travels": []
}
```
</ResponseExample>

---
title: "Broadway Co-Producer Lists"
description: "Scope & build plan for ingesting Broadway and West End producing credits and materializing co-production edges: decisions, source landscape, credit-extraction + identity-resolution pipeline, storage, and rollout."
icon: "ticket"
---

**Status: scope & planning, decisions locked 2026-08-21.** Nothing on this page is built. Goal: ingest the full producing block of every Broadway and West End production, resolve producers to `master_person` / `master_company` records, and materialize production credits as graph edges so co-producing relationships become queryable co-investment paths.

## Decisions (2026-08-21)

| Question | Decision |
| --- | --- |
| Edge weights | Fixed §3.6 tier bands: lead producer closest, then producer, co-producer, associate/executive, "in association with" furthest. Band values still to be set against §3.6. |
| Co-production | Derived through the shared production node; no materialized `CO_PRODUCED_WITH`. Revisit selective materialization only if the leverage-loop perf test demands it. |
| Node label | Extend the film/TV credit node family with a stage discriminator; no new label family. Existing credit-walking scoring picks up stage productions for free. |
| Couple credits | One `PRODUCED` edge per spouse plus a shared `couple_credit` marker. Household-level treatment stays in the Known layer. |
| Scrape posture | ToS review runs parallel to Phase 0. Fetcher built as a source adapter, raw HTML cached to GCS, Playbill as the fallback backbone. |
| v1 scope | **Broadway \+ West End.** Off-Broadway and national tours stay out. |
| Runtime | Go on GCP, native: Pub/Sub → Cloud Run → AlloyDB, the avatar-pipeline shape. No Xano, throwaway Phase 0 scripts excepted. |

## Why

Modern Broadway shows carry 20 to 40\+ names above the title. Co-producer billing is granted at investment thresholds (typically low-to-mid six figures, varying by capitalization and era; verify in Phase 0), so the credit block functions as a public, ordered proxy for the show's cap table. The pool skews precisely toward the Orbiter ICP: finance operators, real estate principals, media executives, and family offices co-investing outside their day jobs. Each production is a shared deal; two people on the same credit block wrote checks into the same LLC. That yields person-to-person co-investment edges that PDL and Crunchbase-class sources never see, with billing tier as a check-size signal and opening/closing dates for bitemporal validity. It composes naturally with the existing film/TV credit nodes (heavy person overlap) and the Fundable investor graph.

The West End is the same investor class with heavily overlapping producing teams (Broadway transfers share lead producers and shingles), so including it in v1 adds transatlantic co-investment edges for the cost of one extra source adapter.

## The key structural fact

Every Broadway production is financed as its own single-purpose entity (LLC or LP), and most raise under Reg D, so a **Form D usually exists on EDGAR for the show entity**, naming the entity and its related persons (typically the lead producers) plus offering size. The public data therefore splits two ways:

- **IBDB / Playbill credits**: the complete, ordered producing block, every co-producer included, with stable IBDB person and production IDs. Breadth.
- **SEC Form D**: government-grade corroboration of the show entity and its lead tier, plus offering amounts. Precision anchor, lead tier only.

The UK inverts this. West End productions are financed through UK entities, and **Companies House discloses officers and persons with significant control for free via API**: a stronger unmasking layer than Form D (full officer lists, not lead-tier related persons). The UK gap is on the credits side instead: no IBDB equivalent with stable IDs, so credit sourcing leans on SOLT listings, Theatricalia, programmes, and press, with coverage depth to validate in Phase 0.

Design accordingly: credits backbone per market (IBDB / SOLT\+Theatricalia); entity corroboration per market (Form D / Companies House).

<Tip>
  **The producing-shingle bridge.** A large share of credits are entities, not people: producing shingles, family LLCs, and grouped "in association with" blocks. Resolving a shingle's principals once (shingle site, Form D related persons, Companies House officers, OpenCorporates, press) converts every credit that entity holds across shows and both markets into person-level edges. Same leverage as the family-foundation bridge in the donations plan.
</Tip>

## Data-source landscape

| # | Source | Role | Access | Notes |
| --- | --- | --- | --- | --- |
| 1 | **IBDB** (ibdb.com, The Broadway League) | US primary backbone | Free, scrape | Canonical production pages with the full ordered producing block; person pages aggregate credits under stable IDs (the anchoring pattern Discover Deep Research uses for IMDb URLs and MBIDs). No public API. ToS/scrape posture in Phase 0 review; throttle hard, cache everything. |
| 2 | **Playbill Vault** (playbill.com) | US cross-check / fallback | Free, scrape | Independent credit listings plus opening/closing dates; the fallback backbone if IBDB access posture changes. |
| 3 | **Tony Awards db** (tonyawards.com) | US high-value subset | Free, scrape | Producers are the named recipients on Best Play/Musical nominations; award co-recipient signal for the most successful cohort. |
| 4 | **SEC EDGAR Form D** (full-text search) | US corroboration \+ unmasking | Free API | Show-entity filings: related persons (lead producers), offering amounts, entity addresses. |
| 5 | **Official London Theatre / SOLT** | UK production index | Free, scrape | The League's UK counterpart; season listings and Olivier Awards administration. |
| 6 | **Theatricalia** (theatricalia.com) | UK credits | Free, scrape | Community production/person database; producer-credit coverage depth and ID stability to validate in Phase 0. |
| 7 | **Companies House API** | UK entity unmasking | Free API | Officers \+ PSC registers for show entities; richer than Form D. |
| 8 | **Olivier Awards** | UK high-value subset | Free, scrape | Producer credits on nominated/winning productions; the Tony-sweep counterpart. |
| 9 | **The Broadway League** | US season index, grosses | Free | Production universe per season; grosses give recoupment context if ever wanted. |
| 10 | **OpenCorporates** | Shingle principals, both markets | Freemium API | Officer lookups for producing LLCs where the market filing is silent. |
| 11 | **Wikipedia/Wikidata, BroadwayWorld, WhatsOnStage** | Corroboration | Free | Producer bios, producing-team announcements, historic context. |

## Edges (per decisions above)

| Aspect | Design |
| --- | --- |
| Pattern | `(Person)-[:PRODUCED]->(Production)` and `(Company)-[:PRODUCED]->(Production)`, with a person-level derivation carrying `via_entity_uuid` when the credit is held by a shingle. |
| Node | Stage productions extend the film/TV credit node family with a stage discriminator, keyed by IBDB production ID (US) or the UK anchor chosen in Phase 0: title, theatre, opening/closing, revival flag, season, `market`. |
| Cardinality | One edge per producer-production pair. Co-production is derived through the shared production node at query/score time. |
| Weight (§3.6 bands, lower = closer) | Fixed tier bands per the decision; values TBD against §3.6. Same bands both markets pending the UK tier-mapping question below. |
| Metadata envelope | Per the 2026-07-04 house standard: `uuid`, `created_at`, `updated_at`, `weight`, deterministic `description` ("X produced Y"), `description_generated_at`. |
| Other properties | `billing_tier`, `billing_order`, `couple_credit`, `market` (`broadway` \| `west_end`), `valid_from` / `valid_to` (opening/closing per the bitemporal convention), `source_production_id`, `via_entity_uuid`, `source_urls`, `confidence`, `award_winner` (Tony/Olivier sweep). |

## What the graph already has (leverage, not build)

- **IMDb & MusicBrainz waterfalls**: the queue drain contract, tiering, and promotion-audit machinery this lane should clone rather than reinvent. Born onto the unified contract from the IMDb & Music Queue Refactor, not a fourth bespoke queue.
- **Heavy person overlap with film/TV credits**: many Broadway and West End producers already exist as enriched persons via the IMDb waterfall, so existing-graph matching starts warm.
- **Identity machinery**: `master-person-new` #13039 (kill switch #4617), `master-company-new` #12558 (kill switch #12789), Exa profile search #12936, `create-master-link` #2541, queue upsert #12854.
- **Proven agent patterns to clone**: tiered resolver `resolve_org_identity` #12984 (cheap model first, confidence-gated write-back, append-only attempts audit per #723); deep-research config `deep-person-basic` #12833 for shingle-principal research; adjudication-with-corroboration from `verify-imdb-url` #12677 v5 and `find-artist` #13168 (same-name/different-profession rejection, name-only matches capped, auto-link only at high confidence).
- **Fundable Stage-1 gating**: edges only for identities that resolve to an existing record or pass the auto-link threshold; everything else parks as `needs_review` with its evidence. Releasable later, never lost.
- **LLM Test Harness** (API group 42) for extraction and adjudication prompt evals before anything ships.

## Pipeline design: three stages

Go on GCP end to end: per-market source adapters behind one fetcher interface, raw HTML cached to GCS, Pub/Sub → Cloud Run workers, mentions and attempts in AlloyDB, edges to FalkorDB.

### Stage A: production discovery \+ credit extraction

Input: one season per market (backfill) or the current season (cron).

1. Season index sweep per market (IBDB cross-checked against the League list; SOLT listings cross-checked against Theatricalia) → production queue rows keyed by the market anchor.
2. Production-page fetch through the source adapter → deterministic HTML parse of the producing block. No LLM on the happy path.
3. LLM pass only for compound credit strings: splitting "A, B & C", couple credits ("John and Jane Smith"), and grouped "in association with" blocks into typed tokens. Contract below.
4. Awards sweep (annual): mark producer credits on Tony and Olivier nominated/winning productions.

Output: structured **credit mentions**, never edges directly.

| Role | Model | Why |
| --- | --- | --- |
| Compound-string splitting (step 3) | The #12984 tier-1 config (strict JSON, reasoning off) | High-volume, low-ambiguity token typing at tier-1 economics. |
| Weak-signal retry | The #12984 tier-2 config | Strings the cheap model fumbles. |
| Shingle-principal research (Stage B) | The #12833 deep-research config | The only deep-research surface in this lane. |
| Adjudication (Stage B) | The #12677 v5 config, temp 0.1 | Full-context match adjudication with corroboration guards. |

Pinned by reference so this lane inherits house model-config changes.

### Stage B: identity resolution (per mention)

Deterministic first, LLM last:

1. **Source-ID match**: previously resolved IBDB (and, if Phase 0 validates, Theatricalia) IDs map straight through, the MBID / IMDb-URL anchoring pattern.
2. **Existing-graph match**: `master_person` by normalized name where a film/TV credit, a Fundable investor record, or a prior stage credit corroborates; existing `master_link` URL match when the mention carried one.
3. **Shingle-principal path**: entity credits → Form D related persons (US) / Companies House officers and PSCs (UK) / OpenCorporates / shingle site → person candidates.
4. **Exa LinkedIn search** on the mention's identity points (co-credit overlap, era, geography, entity affiliation), #13121-style.
5. **LLM adjudication**: the #12677 v5 shape with full mention context plus candidate profile, explicit same-name/different-era-or-industry rejection. **Auto-link only at `confidence: high` / ≥0.85**; 0.6–0.85 → `needs_review`; below → park. Name-only matches are capped below the auto-link threshold regardless of model output (the #13168 guard).

Minimum to attempt auto-link: name plus **two** independent corroborating points (e.g., co-credit overlap \+ entity affiliation, or era \+ geography \+ known producing partner).

### Stage C: edge writer

MERGE `PRODUCED` with the metadata envelope; idempotent per producer-production pair. New-person policy mirrors the investor cascade: local-only leaves plus queue rows (tier TBD), kill switches respected, no recursive enrichment from a credit sweep. Closing-date updates rewrite `valid_to` only.

## Draft system prompt v0.1: credit-string splitting (not deployed)

```text
You split ONE stage-production producing-credit string into typed producer tokens.

Production: {{title}} ({{season}}, {{market}}). Credit line as printed: {{credit_string}}
Billing tier for this line: {{billing_tier}}

Return ONLY valid JSON: {"credits": [...]}. Each credit object:
- name: exactly as written for this token
- entity_type: "person" | "couple" | "company" | "unknown"
- persons: for couples, the individual names as written, else null
- billing_order: integer position within the line, 1-based
- association_group: true when the token sits inside an "in association with" block
- evidence_quote: the verbatim fragment for this token
- confidence: "high" | "medium" | "low"

Rules:
1. Preserve names exactly; never expand initials or normalize.
2. A couple credit ("John and Jane Smith") is one token with entity_type "couple" and both persons listed.
3. Suffixes like "Inc.", "LLC", "Ltd", "Productions", "Entertainment", "Group" imply entity_type "company"; a bare personal name implies "person"; ambiguous brand-like names are "unknown".
4. Do not merge tokens across lines or tiers; emit every token with its own evidence_quote.
5. If the string contains no producer tokens, return {"credits": []}.
```

The Stage-B adjudication prompt follows #12677 v5 (match yes/no/none, confidence, reasoning, same-name rejection) and gets drafted against real cases in the LLM Test Harness before deployment.

## Proposed storage (not built)

| Table | Purpose |
| --- | --- |
| `stage_production` | One row per production, `market` column plus the market anchor (IBDB ID / UK anchor): title, theatre, opening/closing, revival flag, season, award status, source URLs. Relational home for per-production detail the graph doesn't need. |
| `stage_credit_mention` | One row per extracted credit token: production ref, raw contract fields, billing tier/order, resolution state (`unresolved` / `auto_linked` / `needs_review` / `rejected`), resolved `master_person_id` / `master_company_id`, `via_entity` ref, edge state. Audit-first per #723. |
| `stage_credit_resolution_attempts` | Append-only log of every Stage-B attempt: candidates, model, confidence, outcome, cost. Mirrors `fundable_organization_enrichment_attempts`. |

## Rollout order

1. **Phase 0 spike (3-4 days)**: US: scrape ~20 productions across eras, validate page stability and the deterministic parse rate, measure compound-string ambiguity, sample-match ~200 credits against the existing graph for warm-start overlap, confirm the co-producer billing-threshold claim. UK: validate SOLT/Theatricalia producer-credit coverage and ID stability on ~10 productions, run the Companies House officer join on ~5 show entities, pick the UK anchor. ToS review for both markets.
2. **Backfill extraction**: Broadway 2000-present (~900-1,000 productions, the co-producer-inflation era) plus the West End window Phase 0 supports (2000-present proposed). All landing in `stage_credit_mention`; no edges yet.
3. **Shingle unmasking \+ person resolution**, prioritized by productions with the highest existing-graph overlap; edges start writing behind the Stage-1 gate.
4. **Recurring**: weekly new-openings cron per market, closing-date updates, annual Tony \+ Olivier sweeps.

## Remaining open questions

1. **§3.6 band values**: final numbers per tier.
2. **West End tier mapping**: UK above-title billing conventions differ; map onto the same five tiers or define a UK-specific set. Decide from Phase 0 samples.
3. **West End backfill window**: 2000-present proposed, pending Phase 0 coverage findings.
4. **UK anchor**: Theatricalia IDs if stable enough, else a (title, theatre, opening-year) composite key.

## References

- [IBDB](https://www.ibdb.com) (The Broadway League)
- [Playbill Vault](https://playbill.com/vault)
- [Tony Awards database](https://www.tonyawards.com)
- [SEC EDGAR full-text search](https://www.sec.gov/edgar/search/) (Form D filings)
- [Official London Theatre / SOLT](https://officiallondontheatre.com)
- [Theatricalia](https://theatricalia.com)
- [Companies House API](https://developer.company-information.service.gov.uk)
- [The Broadway League](https://www.broadwayleague.com)
- [OpenCorporates](https://opencorporates.com)
---
title: "Update Master Email"
api: "PUT https://xh2o-yths-38lt.n7c.xano.io/api:ds4G40TG/master-emails/{master_email_id}"
description: "Edits master email records that the authenticated user created."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

### Path Parameters

<ParamField path="master_email_id" type="integer" required>
  The ID of the master email record to update.
</ParamField>

### Body Parameters

<ParamField body="email_address" type="string">
  The updated email address.
</ParamField>

<ParamField body="email_type" type="string">
  Type of email (`personal`, `work`, `other`).
</ParamField>

<ParamField body="active" type="boolean">
  Whether the email is active.
</ParamField>

<ParamField body="email_note" type="string">
  Optional note for the email. Nullable.
</ParamField>

### Response

<ResponseField name="id" type="integer">
  Unique identifier of the master email record.
</ResponseField>

<ResponseField name="email_address" type="string">
  The email address.
</ResponseField>

<ResponseField name="email_type" type="string">
  Type of email (`personal`, `work`, `other`).
</ResponseField>

<ResponseField name="active_status" type="boolean">
  Whether the email is active.
</ResponseField>

<ResponseField name="master_person_id" type="integer">
  Associated master person ID.
</ResponseField>

<ResponseField name="master_company_id" type="integer">
  Associated master company ID.
</ResponseField>

<RequestExample>
```bash cURL
curl -X PUT \
  'https://xh2o-yths-38lt.n7c.xano.io/api:ds4G40TG/master-emails/1' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -d '{
    "email_address": "updated@example.com",
    "email_type": "personal",
    "active": true
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1700000000000,
  "updated_at": 1700000000000,
  "master_person_id": 1,
  "master_company_id": null,
  "email_address": "updated@example.com",
  "email_type": "personal",
  "active_status": true
}
```
</ResponseExample>

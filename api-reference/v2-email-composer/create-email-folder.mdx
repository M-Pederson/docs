---
title: "Create Email Folder"
api: "POST https://xh2o-yths-38lt.n7c.xano.io/api:58PjJ6J9/email-folders"
description: "Create a new folder/label in the connected email account."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField body="nylas_grant_id" type="integer" required>Nylas grant ID.</ParamField>
<ParamField body="name" type="string" required>Folder name.</ParamField>
<ParamField body="text_color" type="string">Label text color (Gmail only).</ParamField>
<ParamField body="background_color" type="string">Label background color (Gmail only).</ParamField>
<ParamField body="parent_id" type="string">Parent folder ID (Microsoft/EWS only).</ParamField>

<RequestExample>
```bash cURL
curl -X POST \
  'https://xh2o-yths-38lt.n7c.xano.io/api:58PjJ6J9/email-folders' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -d '{"nylas_grant_id": 1, "name": "Important Deals"}'
```
</RequestExample>

<ResponseExample>
```json 200
{}
```
</ResponseExample>

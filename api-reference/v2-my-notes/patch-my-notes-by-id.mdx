---
title: 'Update My Notes'
api: 'PATCH https://xh2o-yths-38lt.n7c.xano.io/api:iuc7DvAP/my-notes/{my_note_id}'
description: 'Edit my_note record, will mostly be used to patch if the note is a doc or just a note. Authentication: required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Path Parameters

<ParamField path="my_note_id" type="integer" required>
  The my_note_id parameter.
</ParamField>


## Request Body

<ParamField body="title" type="string">
  The title field.
</ParamField>

<ParamField body="note" type="string">
  The note field.
</ParamField>

<ParamField body="is_document" type="boolean">
  The is_document field.
</ParamField>


## Response

<ResponseField name="id" type="integer">
  The id field.
</ResponseField>

<ResponseField name="created_at" type="number">
  The created_at field.
</ResponseField>

<ResponseField name="updated_at" type="number">
  The updated_at field. Can be `null`.
</ResponseField>

<ResponseField name="user_id" type="integer">
  The user_id field.
</ResponseField>

<ResponseField name="title" type="string">
  The title field.
</ResponseField>

<ResponseField name="note" type="string">
  The note field.
</ResponseField>

<ResponseField name="is_document" type="boolean">
  The is_document field.
</ResponseField>

<ResponseField name="tiptap_reference" type="string">
  Auto generated tiptap references for docs
</ResponseField>

<RequestExample>
```bash cURL
curl -X PATCH https://xh2o-yths-38lt.n7c.xano.io/api:iuc7DvAP/my-notes/1 \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "title": "example-title",
      "note": "example-note",
      "is_document": true
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1,
  "updated_at": 1,
  "user_id": 1,
  "title": "example-title",
  "note": "example-note",
  "is_document": false,
  "tiptap_reference": "example-tiptap_reference"
}
```
</ResponseExample>

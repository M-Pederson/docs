---
title: "Persona Build Design"
description: "Build spec for persona/build-persona #13349 and the Simulated Person builder #13372 — evidence envelope, both LLM prompts verbatim, table specs (754 / 779 / 783), and the production trigger design. Comparing and ranking lives under Suggestion Recipes → loop_generic."
icon: "masks-theater"
---

The **build spec for `persona/build-persona` (#13349)** — the function that turns one `master_person` into the nine persona narratives and persists them to `persona` (754). This page carries the final specs a Go developer needs: the evidence envelope and eligibility gate, the v0.6 system prompt, the LLM call and retry, the table specs, and the production trigger design. The canonical nine-dimension design is [Personas](/guides/open-work/personas); evidence sourcing is [New Build Plan](/guides/open-work/personas/new-build-plan); scoring is [Persona Scoring](/guides/open-work/personas/scoring). The iteration history behind these specs — prompt versions, test logs, and the model bake-off — lives in this page's git history.

<Info>
  **Production trigger design (decided 2026-08-15 — to build).** The public POST endpoint that triggers persona creation checks the `persona` (754) table first: a row built within the **last 90 days** (tunable — we may adjust this number) means the endpoint simply returns that the persona exists, and nothing is generated.

  With no fresh persona, it generates — but generation keeps the hard prerequisite the build gate already enforces: **deep bio AND social insights must both exist**. When either is missing, the endpoint initiates the missing prerequisite(s) (Deep Bio launch and/or social insights run) and adds the person to the **`queue_persona` table (779)** — built 2026-08-15: upsert-only on `master_person_id` per house queue convention, enforced by a unique index; never duplicated, re-requests bump `count`. Full spec in the Go-port copy-paste blocks below.

  A polling CRON drains the queue: when a queued person's deep bio and social insights are both done, it processes the persona (build \+ dequeue); otherwise it ignores the row and the **next CRON pass picks it up**.
</Info>

<Warning>
  **Behavior change shipped to `mvp/node/update-person-node` (#2758, v1.20 — live, 2026-08-14).** The Person node's `interests` property is now sourced from **`master_person.interests`** (the native array written by social-insights v2/v3 and deep-person-basic) instead of the legacy `interest_join` taxonomy, which produced a comma-joined string. The dead `interest_join` query was removed. `social_media_influence` was already being projected from `master_person.social_media_influence` — verified, no change needed.

  Both properties ride the existing `clean-my-json2` (#2806) gate: **when the source value is null or empty, the parameter is NOT added to the node.** Two consequences to keep in mind: (1) because Cypher `SET` only touches provided keys, a node keeps its **last non-empty value** if the table field is later emptied — there is no clear-on-null; (2) there is **no backfill** — nodes pick up array-shaped interests the next time `update-person-node` runs for that person.
</Warning>

## What the builder does

`persona/build-persona` (#13349, **v0.3**) takes a `master_person_id`, runs two stages, and **persists the build into the `persona` table (754)** — an upsert keyed on `master_person_id` (skip with `persist: false`). A gated or invalid synthesis never writes, so a bad run cannot blank a good row.

<Note>
  **The `persona` table already existed** — table **754** (2026-06-25, modeled on `investment_thesis` #709) with `master_person_id`, `node_uuid`, and the 8 narrative/vector column pairs — so the builder reuses it rather than creating a duplicate. Three columns were added 2026-08-14 via the safe incremental schema route: `persona_sequence_summary` (text), `synthesis` (json telemetry), `graph_context` (json evidence snapshot). Vector columns stay null until the embedding stage.
</Note>

**Stage 1 — `graphContext`.** Resolves `master_person.node_uuid`, then sends one Cypher through `mvp/falkor/send-cypher` (#2815). Because send-cypher reads its connection from `environmental_variables_falkordb` in the **active data source**, calling with `X-Data-Source: sandbox` automatically targets the sandbox graph.

```cypher
MATCH (p:Entity {uuid: "{{$var.masterPerson.node_uuid}}"})
OPTIONAL MATCH (p)-[r]-(n)
RETURN p AS person, r AS edge, n AS neighbor, CASE WHEN r IS NULL THEN NULL ELSE id(startNode(r)) = id(p) END AS outbound
```

A shaping lambda folds the rows into the `graphContext` response object:

- `person` — the person's node (labels \+ properties), with **every `*embedding*` property stripped** (they are vector noise for an LLM and huge).
- `connections[]` — one entry per one-hop edge: `edge_type`, `direction` (`outbound`/`inbound`, computed in-Cypher), `edge_properties` (embeddings stripped — the LLM-written edge `description` and `valid_from`/`valid_to` ride along as evidence), and the packed `neighbor` node.
- `stats` — `edge_count`, `neighbor_count`, `raw_rows`.

**Stage 2 — synthesis (skip with `synthesize: false`).** The system prompt is rendered in a `util.template_engine` block — it is the [Build Persona](/guides/open-work/personas/build-persona) prompt adapted for a three-section evidence envelope (deltas below).

<Warning>
  **Deep bio \+ social insights are REQUIRED evidence (decided 2026-08-14).** Synthesis hard-gates on both: the latest completed `profile_enrichment_job` deep-bio payload AND non-empty `master_person.social_insights`. When either is missing the function returns `graphContext` but skips the LLM call and the persist entirely, with `synthesis.reason = "missing_required_evidence"` plus `has_deep_bio` / `has_social_insights` flags. Do not build personas for ineligible people.
</Warning>

The user message is one JSON envelope with three sections, bounded to a 100,000-char evidence budget:

1. `graphContext` — as above, **slimmed in v0.3**: property telemetry that carries no persona signal is dropped (`match_score`, `semantic_*`, `description_generated_at`, `created_at`/`updated_at`, avatar/logo/image URLs, `display_label`, `visibility`) while `description`, `valid_from`/`valid_to`, `evidence`, `depth`, `recency`, titles, and role history stay.
2. `deepBio` — the **full deep-bio research payload minus sources**: `subject_name`, `subject_aliases`, `biography`, `biography_sections`, `interests`, `relationships`, `unverified_claims`, `extracted_entities` from `profile_enrichment_job.response.result`. **Excluded: `citations`, `verification`, `metadata`** (source lists and research telemetry). The prompt instructs the model to treat `unverified_claims` as unconfirmed — calibrated inference only, never hard facts. Provenance (`profile_enrichment_job_id`, `job_id`) is returned in `evidence_check`.
3. `socialInsights` — `master_person.social_insights` verbatim (dated observations with quotes and engagement counts).

The call goes to **`moonshotai/kimi-k3`** on OpenRouter with the house rules: strict ZDR (`data_collection: deny`, `zdr: true`), `require_parameters: true`, `sort: "throughput"`, `response_format: json_object`, temperature 0.2, `reasoning_effort: medium`, `max_tokens` 8000 (headroom for K3 reasoning tokens — the K2048 empty-verdict trap). Kimi is tool-free here, so the moonshotai provider pin is not required (same reasoning as the social-insights helper v2.3). A `model` input overrides the default (and disables the retry, so smoke tests measure a model raw).

### The system prompt (v0.6, verbatim)

Extracted from the live `util.template_engine` block in #13349 — `{{...}}` placeholders are Twig, rendered server-side per person.

<Accordion title="System prompt — copy/paste">
  ```text
  You are Persona, an extraction engine that reads a graph context about a single person and produces a fixed set of nine narratives used to match people for warm introductions.
  
  The narratives drive a matchmaking system, so accuracy matters more than completeness: a fabricated detail can cause a bad introduction. When the input does not support a narrative, you leave it null rather than guessing.
  
  Why these narratives exist
  
  Each narrative is embedded as its own vector and matched by similarity against equally-embedded descriptions of an ideal person, to power warm introductions. Dense, distinctive, concrete language — actual companies, technologies, sectors, communities, metrics, and dates — makes a narrative match well; generic praise and stock phrasing degrade it. Every narrative must stand alone: no cross-references to other dimensions.
  
  Coverage varies — that is expected
  
  Input richness varies enormously from person to person: a public figure may arrive with a deep biography, years of posts, and dozens of documented relationships; a private operator may arrive with little more than a title and a work history. Narrative depth must scale with the evidence. Write more only where the evidence is rich; write one or two tight sentences where it is thin but real; write null where it is absent. A persona with three null dimensions and six sharp ones matches better than nine diluted ones — fabricated or padded coverage causes bad introductions, while an honest gap merely narrows the match surface. Never stretch thin evidence to reach a length, and never let one rich dimension bleed into a thin one to fill it.
  
  For every dimension, also grade the evidence behind it as evidence_coverage:
  - "rich" — multiple corroborating signals, ideally dated, from more than one evidence section.
  - "moderate" — solid but partial support; real signals with limited corroboration or freshness.
  - "thin" — a single or weak signal; the narrative is still written, tightly, with calibrated language.
  - "none" — no usable signal; the narrative is null.
  
  Input
  
  You receive one JSON document with three evidence sections describing one person:
  - graphContext: extracted from a knowledge graph. graphContext.person is the person's graph node — labels plus properties assembled from enrichment sources (name, description, biographies, current title and company, roles, skills, expertise, interests, social_media_influence, profiles, and so on). graphContext.connections is every edge within one hop of the person; each connection carries edge_type (for example WORKS_AT, FOUNDED, HAS_EXPERTISE, KNOWS, RECEIVED_HONOR), direction ("outbound" = from the person, "inbound" = toward the person), edge_properties (often including a description of the relationship and valid_from/valid_to dates — valid_to null means currently valid), and the neighbor node (labels + properties).
  - deepBio: a researched biography payload — biography paragraphs, biography_sections, relationships (people and organizations with relationship context), interests, extracted_entities, and unverified_claims. Treat unverified_claims strictly as unconfirmed: they may inform calibrated inference but never hard facts.
  - socialInsights: dated observations synthesized from the person's public social-media activity, often with quotes and engagement counts.
  
  Field names vary by source — interpret the graph by meaning, not by exact key names, and map whatever is present onto the signal types listed for each dimension below.
  
  The nine dimensions
  
  Produce exactly these nine narratives, always in this order. For each, the dimension_order, question, role, and primary_source_signals are FIXED — reproduce them verbatim. You generate only the narrative value.
  
  identity_profile — role valence — "Who are they right now?"
  Signals: title, company, founder_status, headline, signature_project.
  Their current professional identity — what they are and how they present themselves today.
  domain_focus — role driver — "What do they know?"
  Signals: expertise_edges, industry_niche, company_focus, published_topics.
  Their areas of expertise and the specific industry/niche they operate in — their sharpest knowledge edges, stated concretely.
  soft_skills — role modifier — "What can they do?"
  Signals: recommendations, peer_mentions, collaboration_patterns, track_record_evidence.
  Interpersonal and operational capabilities (e.g., synthesis, alignment, discovery, dealmaking) inferred from social and track-record evidence.
  track_record — role valence — "What have they done?"
  Signals: employment_history, founding_history, product_launches, authorship, honors.
  The concrete trail of accomplishments — roles held, companies founded, things shipped, written, or recognized.
  public_engagement — role driver — "What do they talk about?"
  Signals: social_posts, podcast_topics, conference_talks, newsletter_themes.
  The themes and topics they engage with publicly.
  communication_style — role modifier — "How do they talk about it?"
  Signals: tone_analysis, writing_format, audience, content_corpus.
  Their tone, format, and intended audience, inferred from their content. HARD FLOOR: this dimension requires first-party content in the input — their own posts, quotes, or recorded talks. Platform presence, follower counts, or descriptions of where they post do not support it; null in that case.
  community_affiliation — role structural — "Who are they with?"
  Signals: professional_networks, startup_ecosystems, industry_groups, open_source_or_advisory_roles.
  The tribes, scenes, ecosystems, and organizations they belong to. When the input documents named relationships (co-founders, collaborators, mentors, investors), name the two to four strongest with their context here.
  passions_interests — role driver — "What beyond work?"
  Signals: side_projects, causes, hobbies, advocacy, personal_content.
  Causes, hobbies, side projects, and advocacy outside their core profession.
  current_needs — role driver — "What are they focused on and seeking right now?"
  Signals: recent_posts, stated_asks, current_role_initiatives, funding_activity, hiring_signals, career_transitions.
  Their present-tense goals and needs — what they are building, raising, hiring for, seeking, or navigating right now. Strictly time-anchored: use only current or recent evidence (roughly the last twelve months when dates are available). Superseded signals do not count — a job search that ended in a new role is no longer a need. HARD FLOOR: this dimension requires dated recent activity; a current job title or role description alone is not a present-tense need. When nothing current and dated is documented, this narrative is null.
  
  Grounding and the null rule
  
  Base every narrative only on what the input evidence provides. Do not use outside knowledge about the named person, no matter how famous they are — if the input doesn't say it, you don't know it.
  Hard facts (employers, titles, launches, honors, named communities, numbers, dates) must be directly supported by the input evidence — never invent them. Every sentence you write must be traceable to a specific input element; if you cannot point to the evidence behind a sentence, delete the sentence.
  Interpretive dimensions (soft_skills, communication_style, and inferences within domain_focus) may reason beyond the literal text, but only as a reasonable read of the signals present. When inferring, use calibrated language ("appears", "likely", "suggests") and keep it tethered to the evidence.
  Set narrative to null (the JSON literal — not the string "null", not "") when the input evidence carries no signal for that dimension, or only a signal too thin or ambiguous to support a defensible statement. Null is a correct, common, and expected answer — many people will legitimately have several null dimensions — and a null is always better than a guess. When in doubt between writing and null, choose null. Never pad a dimension with generic filler that the data doesn't earn.
  Always emit all nine dimension objects with their fixed metadata; only the narrative value becomes null.
  Do not assert sensitive personal attributes (health, religion, political affiliation, sexual orientation, etc.) unless they are explicitly and clearly part of the person's public professional identity.
  Never mention the evidence apparatus. Do not reference the graph, the graph context, the researched biography, the social-insights review, captured posts, or any other pipeline artifact. Describe the person's actual public footprint ("his observable public presence is thin") or set the narrative to null — never narrate what the input data did or did not contain.
  Null or commit: when the honest content of a dimension would mostly be a disclaimer about thin or ambiguous evidence, return null instead. Calibrated language belongs inside substantive claims, never as standalone commentary about uncertainty.
  Two dimensions carry hard evidence floors (restated for emphasis): communication_style needs the person's own words in the input; current_needs needs dated recent activity. Absent those specific signals, those narratives are null — no matter how much adjacent evidence exists.
  
  Voice and length
  
  Third person, present tense for current identity and ongoing facts.
  1-4 sentences per narrative, scaled to the evidence: rich evidence earns 3-4 sentences; thin-but-real evidence gets 1-2 tight sentences. Concrete and specific — name the actual companies, topics, communities, and skills rather than gesturing at them.
  Prefer concrete evidence — dates, metrics, engagement counts, and short quotes — over abstractions whenever the input provides them.
  Do not open narratives with stock formulas (for example "His track record suggests" or "His sharpest expertise is"), and do not reuse the same sentence template across dimensions. Lead each narrative with the person's distinctive substance.
  Each dimension answers only its own question; don't repeat the same content across dimensions.
  
  persona_sequence_summary
  
  After the nine narratives, write persona_sequence_summary: a single short paragraph that walks the non-null narratives in dimension order (1 to 9) into one flowing description of the person. Skip any dimension whose narrative is null. If every narrative is null, set the summary to null.
  
  Output
  
  Return ONLY a single JSON object — no markdown, no code fences, no commentary before or after. persona_narratives MUST be an ARRAY of exactly nine objects in dimension order. Use exactly this shape and key order; reproduce the fixed metadata verbatim and fill in only the narrative values and the summary:
  
  {
    "persona_id": "mp_{{$var.masterPerson.id}}",
    "is_simulated": false,
    "person": {
      "name": "{{$var.masterPerson.name}}",
      "location": "<from graph context, or null>",
      "snapshot_date": "{{'now'|date('Y-m-d')}}"
    },
    "persona_narratives": [
      { "dimension": "identity_profile", "dimension_order": 1, "question": "Who are they right now?", "role": "valence", "primary_source_signals": ["title","company","founder_status","headline","signature_project"], "evidence_coverage": "<rich|moderate|thin|none>", "narrative": "<text or null>" },
      { "dimension": "domain_focus", "dimension_order": 2, "question": "What do they know?", "role": "driver", "primary_source_signals": ["expertise_edges","industry_niche","company_focus","published_topics"], "evidence_coverage": "<rich|moderate|thin|none>", "narrative": "<text or null>" },
      { "dimension": "soft_skills", "dimension_order": 3, "question": "What can they do?", "role": "modifier", "primary_source_signals": ["recommendations","peer_mentions","collaboration_patterns","track_record_evidence"], "evidence_coverage": "<rich|moderate|thin|none>", "narrative": "<text or null>" },
      { "dimension": "track_record", "dimension_order": 4, "question": "What have they done?", "role": "valence", "primary_source_signals": ["employment_history","founding_history","product_launches","authorship","honors"], "evidence_coverage": "<rich|moderate|thin|none>", "narrative": "<text or null>" },
      { "dimension": "public_engagement", "dimension_order": 5, "question": "What do they talk about?", "role": "driver", "primary_source_signals": ["social_posts","podcast_topics","conference_talks","newsletter_themes"], "evidence_coverage": "<rich|moderate|thin|none>", "narrative": "<text or null>" },
      { "dimension": "communication_style", "dimension_order": 6, "question": "How do they talk about it?", "role": "modifier", "primary_source_signals": ["tone_analysis","writing_format","audience","content_corpus"], "evidence_coverage": "<rich|moderate|thin|none>", "narrative": "<text or null>" },
      { "dimension": "community_affiliation", "dimension_order": 7, "question": "Who are they with?", "role": "structural", "primary_source_signals": ["professional_networks","startup_ecosystems","industry_groups","open_source_or_advisory_roles"], "evidence_coverage": "<rich|moderate|thin|none>", "narrative": "<text or null>" },
      { "dimension": "passions_interests", "dimension_order": 8, "question": "What beyond work?", "role": "driver", "primary_source_signals": ["side_projects","causes","hobbies","advocacy","personal_content"], "evidence_coverage": "<rich|moderate|thin|none>", "narrative": "<text or null>" },
      { "dimension": "current_needs", "dimension_order": 9, "question": "What are they focused on and seeking right now?", "role": "driver", "primary_source_signals": ["recent_posts","stated_asks","current_role_initiatives","funding_activity","hiring_signals","career_transitions"], "evidence_coverage": "<rich|moderate|thin|none>", "narrative": "<text or null>" }
    ],
    "persona_sequence_summary": "<one-paragraph synthesis of non-null narratives, or null>",
    "coverage_note": "<one sentence describing which dimensions of this person are well-documented and which are thin or absent — phrased about the person's public footprint, never about the input data — or null>"
  }
  
  Final check before you emit: for each non-null narrative, confirm every entity, number, date, and claim appears in the input evidence — remove anything you cannot trace, and if that empties the narrative, set it to null. Confirm every null is the JSON literal null and its evidence_coverage is "none".
  ```
</Accordion>

### The LLM call (curl, all params)

The primary attempt exactly as #13349 sends it (kimi-k3 default). The user message is the three-section evidence envelope, JSON-stringified and bounded to 100,000 chars.

```bash
curl -s https://openrouter.ai/api/v1/chat/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $OPENROUTER_API_KEY" \
  -H "HTTP-Referer: https://app.xano.com" \
  -H "X-Title: Persona Build v0" \
  -d @- <<'JSON'
{
  "model": "moonshotai/kimi-k3",
  "temperature": 0.2,
  "reasoning_effort": "medium",
  "max_tokens": 8000,
  "response_format": {"type": "json_object"},
  "provider": {
    "data_collection": "deny",
    "zdr": true,
    "require_parameters": true,
    "sort": "throughput"
  },
  "messages": [
    {"role": "system", "content": "<the rendered system prompt above>"},
    {"role": "user", "content": "Evidence for <person name>:\n<JSON: {graphContext, deepBio, socialInsights} — bounded to 100k chars>"}
  ]
}
JSON
```

**Diverse retry variant** (fires only when the default-path primary is invalid; also the `relax_params` smoke-test profile): `"model": "anthropic/claude-sonnet-5"`, `"max_tokens": 16000`, `X-Title: Persona Build v0 Retry`, and **omit** `temperature`, `reasoning_effort`, and `response_format` (Sonnet 5 rejects sampling params, so `require_parameters` would filter every Anthropic endpoint). The provider block is identical.

## Model decision

**Decision: kimi-k3 primary, Claude Sonnet 5 as the diverse retry fallback** — implemented in v0.6, re-confirmed 2026-08-15 after a K2.6 head-to-head.

Rationale, compressed: across seven models smoke-tested on identical evidence, only kimi-k3 and Claude Sonnet 5 never fabricated beyond the evidence (four of seven failed a grounding canary); kimi-k3 completes the standard strict payload reliably at ~\$0.08–0.09/person, and Sonnet 5 retries with a disjoint failure surface when a primary attempt returns invalid JSON. An explicit `model` input disables the retry so future smoke tests measure models raw.

## Artifacts

<Card icon="play">
  ```text
  persona/build-persona — 13349
  ```

  **The builder** (v0.6). Input `master_person_id` (\+ optional `synthesize`, `persist`, `relax_params`, `model`). Default path: kimi-k3 primary with a one-shot `anthropic/claude-sonnet-5` diverse retry on invalid output; explicit `model` input runs raw (no retry). Returns `graphContext`, `persona`, `synthesis` telemetry (incl. `attempt`/`primary_reason`), `evidence_check`, and `persona_row_id`. The production rollup name `person/build-persona` stays reserved for the promoted version (vectors \+ `person_personas` upsert).
</Card>

<Card icon="layer-group">
  ```text
  mvp/node/update-person-node — 2758
  ```

  **v1.20** — projects `master_person.interests` (native array) and `social_media_influence` onto the Person node; null/empty values are not added (see Warning above). Rollback: `~/xano/persona-build/2758_update-person-node_original.xs`.
</Card>

<Card icon="globe">
  ```text
  POST qa/run-build-persona — api 8988 (Admin Backfill, canonical rlSvC4qe)
  ```

  **TEMP QA wrapper — unauthenticated — delete at promotion.** Exists because the metadata function-run endpoint cannot resolve nested `function.run` refs on branch v1. `update_node: true` runs #2758 first; `synthesize: false` stops after `graphContext`; `persist: false` skips the table upsert; `relax_params: true` \+ `model` for raw model smoke tests.
</Card>

<Card icon="database">
  ```text
  persona — table 754
  ```

  **The persisted persona row** — one per person, upserted by #13349. `master_person_id` (FK), `node_uuid` (from `master_person.node_uuid` at build time), 8 `*_narrative` columns, 8 `*_vector` columns (null until embedding), plus the columns `persona_sequence_summary`, `synthesis`, `graph_context`.
</Card>

<Card icon="cloud-arrow-up">
  ```text
  mvp/deep-research/deep-person-basic — 12833 (v4.18)
  ```

  **Live change:** optional `launch_mode` \+ `explicit_operator_authorized` passthrough to #12747 → reserve (#13301), enabling **operator replays** of people whose lifetime automatic Deep Bio reservation is terminal (`already_completed` = non-launchable). Absent inputs preserve automatic behavior for every existing caller. Rollback: `~/xano/persona-build/12833_deep-person-basic_original.xs`.
</Card>

<Card icon="globe">
  ```text
  POST qa/run-deep-bio — api 8989 · POST qa/run-deep-bio-sweeper — api 8990
  ```

  **TEMP deep-bio launchers — unauthenticated — delete at promotion.** 8989 launches one person's async Deep Bio through #12833 (Cloud Run researches, the callback lands in the active datasource); `operator_replay: true` relaunches a terminal reservation. 8990 wraps the retry sweeper #13297.
</Card>

<Card icon="play">
  ```text
  persona/build-simulated-person — 13372
  ```

  **The Simulated Person builder** (v0.4). Input `master_person_id` (\+ optional `model`, `relax_params`, `skip_persist`, `force`); reads the persona row, imagines 1–3 ideal helper archetypes for its `current_needs` (extra archetypes only when genuinely multi-need), upserts slot rows into `simulated_persona` (783) on valid output. kimi-k3 primary \+ sonnet-5 diverse retry. See the [Simulated Person builder](#simulated-person-builder--personabuild-simulated-person-13372) section below.
</Card>

<Card icon="database">
  ```text
  simulated_persona — 783
  ```

  **Simulated Person store** — mirror of `persona` (754); `master_person_id` = the leverage-loop target (unique, upsert). Text narratives now, vectors null until the embedding stage. Spec block below.
</Card>

<Card icon="globe">
  ```text
  qa/run-build-simulated-person — 8992
  ```

  **TEMP QA wrapper — unauthenticated — delete at promotion** (with 8988–8991). Passes `master_person_id`, `model`, `relax_params`, `skip_persist`, `force` through to #13372.
</Card>

### Run it

```bash
curl -X POST "https://xh2o-yths-38lt.n7c.xano.io/api:rlSvC4qe/qa/run-build-persona" \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: sandbox" \
  -H "X-Branch: v1" \
  -d '{"master_person_id": 9, "update_node": true, "synthesize": true}'
```

## Comparing and ranking — moved

Everything about **comparing personas and ranking scores** — the `compare_persona` (778) cache design and spec, the saved-raw-scores contract, and the match layers — lives under **Suggestion Recipes → loop\_generic**: [Persona Scoring](/guides/open-work/personas/scoring) (the three layers and the 778 spec), [loop\_generic](/guides/open-work/suggestion-recipes/loop-generic) (scoring theory), and [get-scores Contract](/guides/open-work/suggestion-recipes/get-scores-contract) (the call \+ save flow). This page owns the **build side** only.

### Table specs — copy/paste (Go port)

`persona` (table **754**) — verbatim live XanoScript:

```text
// Person narratives + embedding vectors (8 narrative/vector pairs). Modeled on conventions from investment_thesis (#709).
table persona {
  auth = false

  schema {
    int id
    timestamp created_at?=now {
      visibility = "private"
    }

    timestamp? updated_at?
    int? master_person_id? {
      table = "master_person"
    }

    text? node_uuid? filters=trim
    text? identity_profile_narrative?
    json? identity_profile_vector?
    text? domain_focus_narrative?
    json? domain_focus_vector?
    text? soft_skills_narrative?
    json? soft_skills_vector?
    text? track_record_narrative?
    json? track_record_vector?
    text? public_engagement_narrative?
    json? public_engagement_vector?
    text? communication_style_narrative?
    json? communication_style_vector?
    text? community_affiliation_narrative?
    json? community_affiliation_vector?
    text? passions_interests_narrative?
    json? passions_interests_vector?

    // One-paragraph synthesis walking the non-null narratives in dimension order (from persona/build-persona #13349)
    text? persona_sequence_summary?

    // Synthesis telemetry from persona/build-persona — valid/reason/model/usage/preview
    json? synthesis?

    // graphContext evidence that produced this persona (person node + 1-hop connections, embeddings stripped)
    json? graph_context?

    // Dimension 9 (locked into the canonical set 2026-08-15; introduced as experimental in workshop v0.4) — what they are focused on and seeking right now; time-anchored, null when nothing current
    text? current_needs_narrative?

    // Dimension 9 vector — null until embedding stage
    json? current_needs_vector?
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
  ]

  tags = ["person"]
}
```

`compare_persona` (table **778**): spec moved with the compare layer to [**Persona Scoring**](/guides/open-work/personas/scoring) under Suggestion Recipes → loop\_generic.

`queue_persona` (table **779** — the production-trigger queue from the callout at the top, built 2026-08-15):

```text
table "queue_persona" {
  description = "Persona generation queue — one row per master_person waiting on prerequisites (deep bio + social insights) before persona/build-persona can run. Upsert-only on master_person_id (house queue convention — never duplicated; re-requests bump count). Filled by the production persona POST trigger when a person has no fresh persona (< 90 days) and is missing a prerequisite; drained by a polling CRON that builds the persona and dequeues once both prerequisites exist."
  auth = false
  schema {
    int id { description = "Unique identifier for the queue row" }
    timestamp created_at?=now { description = "Enqueue timestamp" }
    timestamp updated_at?=now { description = "Last touch — bumped on re-requests (count++) and CRON claims" }
    int master_person_id {
      table = "master_person"
      description = "Person awaiting persona generation — UNIQUE (upsert-only, never duplicated)"
    }
    bool processing? { description = "CRON claim flag — true while a pass is building this persona" }
    int count?=1 { description = "Times persona generation was requested while queued — early-return paths still bump this" }
    bool deep_bio_initiated? { description = "Trigger endpoint launched Deep Bio at enqueue time (it was missing)" }
    bool social_insights_initiated? { description = "Trigger endpoint launched a social insights run at enqueue time (it was missing)" }
    timestamp last_checked_at? { description = "Last CRON prerequisite evaluation — stuck-row debugging" }
    text? source_function? filters=trim { description = "Which endpoint/function queued this person (e.g. the production persona POST trigger)" }
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
    {type: "unique", field: [{name: "master_person_id", op: "asc"}]}
  ]

  tags = ["person"]
}
```

## Simulated Person builder — `persona/build-simulated-person` (#13372)

The inversion of the builder above: instead of profiling a real person from evidence, it reads one **completed persona row** and **imagines the ideal helper** for that person's `current_needs` — the person-driven [Simulated Person](/guides/open-work/personas#the-simulated-person--to-build-in-go). Evidence is the persona row ONLY (nine narratives \+ sequence summary — no graph, no bio). Output is the same nine-dimension array shape, written **as if a real person of the helper type had been enriched**, so it embeds and matches against real personas channel-for-channel.

**Inputs:** `master_person_id` (the loop target — must have a persona row), `model?` (default `moonshotai/kimi-k3`), `relax_params?` (24k tokens, no sampling params — required for `anthropic/claude-sonnet-5`), `skip_persist?` (A/B runs), `force?` (bypass the `current_needs` requirement — the prompt infers stage-appropriate needs from the other eight narratives, conservatively, no invented specifics, and reports them in `inferred_brief`). On the default-model path an invalid primary **retries once on `anthropic/claude-sonnet-5`** with the relaxed payload (disjoint failure surface — same pattern as the persona builder); an explicit `model` disables the retry so smoke tests measure raw.

**Persistence (multi-archetype since v0.4):** on valid output the records are created — **one row per archetype, maximum three**, upserted into `simulated_persona` (783) on the composite unique `(master_person_id, archetype_slot)`, with **stale higher slots deleted** when a regeneration produces fewer archetypes. Text narratives only (**vector columns exist but stay null** until the embedding stage); each row carries its `archetype` label, `for_need` (the specific need it answers), and `inferred_brief` when the build was forced. The verbatim LLM item rides in the `simulated_persona` JSON column; `synthesis` carries the build receipt (`attempt`, `archetype_count`, `multi_need`, model, usage). `dimension_weights` is **no longer produced** — the harness owns weighting. Without `current_needs` (and no `force`), the function throws — same eligibility philosophy as the build gate.

**Prompt design points (v0.4):** the `current_needs` narrative is the brief; **one archetype unless the brief is genuinely multi-need** — then one per distinct need, maximum three, ordered by leverage, never padded, each a distinct role in a distinct world (reported in `multi_need` \+ per-item `archetype`/`for_need`); **mutual mirror** — each helper's `current_needs` describes what they are seeking such that the person is the natural answer; **anti-echo** — the helper is grounded in their OWN professional world's tools/venues/communities, borrowing the person's vocabulary only where the worlds overlap (a pure echo matches people _similar to_ the person instead of people who can _help_); **adjacent, not inside** the person's existing orbit; **no real names anywhere** in the output (the input person is only "the person"); every dimension filled — a simulated persona has no evidence gaps; on forced builds the **inferred brief is surfaced** (`inferred_brief`) and the mirror stays **capability-level, not event-level** (a guess anchored on a guess must not be hyper-specific).

<Accordion title="System prompt (v0.4, verbatim — spec for the Go port)">
  ```text
  You are Persona-Sim, an imagination engine. You read the completed nine-dimension persona of ONE real person and INVENT the ideal person who could help them with what they are seeking right now.
  
  The brief
  
  The input persona_narratives.current_needs is the brief: it describes what the real person is focused on and seeking. Imagine the person best positioned to help with exactly those needs — and for whom helping would also be valuable, because the best introductions are mutual.
  
  Archetype count: produce exactly ONE simulated persona unless the brief is genuinely MULTI-NEED — several distinct needs that different PEOPLE would solve. When it is, produce one simulated persona per distinct need, MAXIMUM THREE, ordered by leverage (the archetype whose help most advances the person comes first). Never pad: every additional archetype must answer a need the previous archetypes cannot, and each must be a distinct role in a distinct world. Never blend incompatible archetypes into one implausible person — a persona that could not exist as one real career matches nobody. Report your read of the brief in the multi_need output field.
  
  If current_needs is null or missing: derive the brief yourself. From the other eight narratives, infer what someone at this career stage, in this role and domain, most plausibly needs right now — stage-appropriate and conservative (a growth lever, a gap in their world, a natural next move), never invented specifics like named events, dates, or deals. Then imagine the helper for those inferred needs exactly as if they had been stated. Because an inferred brief is a considered guess, keep the mirror CAPABILITY-LEVEL rather than event-level: the simulated person's current_needs should seek the person's type and capabilities, never hyper-specific scenarios that depend on your inference being exactly right. State the brief you inferred in the inferred_brief output field.
  
  The other eight input narratives are context for fit: domain fluency (the simulated person must speak the real person's professional language), communication compatibility (a register that would land with how the person communicates — matched or deliberately complementary), community adjacency, and complementary rather than duplicate capability — the ideal helper fills what the real person lacks, not what they already have.
  
  Write a real profile, not a reflection
  
  These narratives will be embedded and matched, dimension for dimension, against the personas of real people — so write every narrative exactly as if a real person of this type had been enriched and profiled. Ground the simulated person in THEIR OWN professional world, with its own named tools, venues, communities, and career milestones — do not simply echo the input persona's vocabulary back. Borrow the person's terms only where the two worlds genuinely overlap; a simulated persona that merely mirrors the input matches people similar to the person instead of people who can help them.
  
  Adjacency: place the simulated person close enough to be plausibly reachable (shared conferences, adjacent communities, overlapping professional circuits) but NOT inside the person's existing orbit — not their colleagues, existing partners, or the communities they already lead. The value of an introduction is that it bridges.
  
  Rules
  
  - The simulated person is a TYPE, not a real individual. NEVER give them a name, and never describe a specific identifiable real person.
  - NO real person's name may appear ANYWHERE in your output — not the simulated person's, not the input person's, not anyone's. Refer to the input person only as "the person".
  - Plausibility is a hard constraint: one coherent career at one plausible seniority. Senior and impressive is fine; a composite of three different careers is not.
  - Naming real companies, products, technologies, scenes, and communities IS encouraged — concrete, specific language embeds and matches well (e.g. "a partner-track executive inside a major film studio's technology group", "runs developer relations for a generative-video startup"). Generic praise degrades matching.
  - Every narrative must be concrete and dense: name sectors, technologies, community types, deal shapes, and career stage.
  - The simulated person's current_needs must MIRROR the person's: describe what the simulated person is seeking right now such that the person is the natural answer to it. Mutual timing is the strongest match signal.
  - Third person, present tense, they/them, 2-4 sentences per narrative.
  - Every dimension is filled — a simulated persona has no evidence gaps.
  
  Output
  
  Return ONLY a single JSON object — no markdown, no code fences, no commentary. simulated_personas MUST be an ARRAY of one to three simulated-persona objects (one unless genuinely multi-need). Inside each, persona_narratives MUST be an ARRAY of exactly nine objects in dimension order. Reproduce the fixed metadata verbatim in every item; you write only the narrative values and the fields marked <>:
  
  {
    "persona_id": "simulated",
    "is_simulated": true,
    "simulated_for_master_person_id": <copy from the input JSON>,
    "multi_need": <true when the brief contains several distinct needs that different people would solve, else false>,
    "inferred_brief": "<ONLY when current_needs was null and you derived the brief yourself: one sentence stating the needs you inferred. Otherwise the JSON literal null>",
    "simulated_personas": [
      {
        "archetype": "<3-6 word label for this helper type, e.g. 'flagship studio technology chief'>",
        "for_need": "<one clause naming the specific need this archetype answers>",
        "persona_narratives": [
      { "dimension": "identity_profile", "dimension_order": 1, "question": "Who are they right now?", "role": "valence", "primary_source_signals": ["title","company","founder_status","headline","signature_project"], "narrative": "<text>" },
      { "dimension": "domain_focus", "dimension_order": 2, "question": "What do they know?", "role": "driver", "primary_source_signals": ["expertise_edges","industry_niche","company_focus","published_topics"], "narrative": "<text>" },
      { "dimension": "soft_skills", "dimension_order": 3, "question": "What can they do?", "role": "modifier", "primary_source_signals": ["recommendations","peer_mentions","collaboration_patterns","track_record_evidence"], "narrative": "<text>" },
      { "dimension": "track_record", "dimension_order": 4, "question": "What have they done?", "role": "valence", "primary_source_signals": ["employment_history","founding_history","product_launches","authorship","honors"], "narrative": "<text>" },
      { "dimension": "public_engagement", "dimension_order": 5, "question": "What do they talk about?", "role": "driver", "primary_source_signals": ["social_posts","podcast_topics","conference_talks","newsletter_themes"], "narrative": "<text>" },
      { "dimension": "communication_style", "dimension_order": 6, "question": "How do they talk about it?", "role": "modifier", "primary_source_signals": ["tone_analysis","writing_format","audience","content_corpus"], "narrative": "<text>" },
      { "dimension": "community_affiliation", "dimension_order": 7, "question": "Who are they with?", "role": "structural", "primary_source_signals": ["professional_networks","startup_ecosystems","industry_groups","open_source_or_advisory_roles"], "narrative": "<text>" },
      { "dimension": "passions_interests", "dimension_order": 8, "question": "What beyond work?", "role": "driver", "primary_source_signals": ["side_projects","causes","hobbies","advocacy","personal_content"], "narrative": "<text>" },
      { "dimension": "current_needs", "dimension_order": 9, "question": "What are they focused on and seeking right now?", "role": "driver", "primary_source_signals": ["recent_posts","stated_asks","current_role_initiatives","funding_activity","hiring_signals","career_transitions"], "narrative": "<text>" }
        ],
        "persona_sequence_summary": "<one flowing paragraph walking this archetype's nine narratives in order>"
      }
    ]
  }
  ```
</Accordion>

`simulated_persona` (table **783** — mirror of `persona` 754, built 2026-08-18):

```text
table "simulated_persona" {
  description = "Simulated Person store — one row per leverage-loop target: the LLM-imagined ideal helper for that person's current_needs. master_person_id = the REAL person the user ran the loop on (unique; regenerating upserts). Narrative/vector columns mirror persona (754) so matching stays channel-aligned; simulated_persona holds the verbatim LLM profile (dimension_weights, the nine-object array). Row is created when the narratives are created (persona/build-simulated-person). Vectors null until the embedding stage."
  auth = false
  schema {
    int id { description = "Unique identifier for the simulated persona row" }
    timestamp created_at?=now { description = "Row creation timestamp" }
    timestamp updated_at?=now { description = "Last regeneration timestamp" }
    int master_person_id {
      table = "master_person"
      description = "The REAL person this simulation was built for (the leverage-loop target) — UNIQUE, upsert key"
    }
    text node_uuid? filters=trim { description = "Graph uuid of the real person (copied from their persona row)" }
    int archetype_slot?=1 { description = "1-3: which archetype slot this row holds (1 = highest leverage). Composite unique with master_person_id" }
    text? archetype? { description = "3-6 word label for this helper type (from the LLM)" }
    text? for_need? { description = "One clause naming the specific need this archetype answers" }
    text? inferred_brief? { description = "Force mode only: the one-sentence brief the model inferred when current_needs was null" }
    text? identity_profile_narrative?
    json? identity_profile_vector?
    text? domain_focus_narrative?
    json? domain_focus_vector?
    text? soft_skills_narrative?
    json? soft_skills_vector?
    text? track_record_narrative?
    json? track_record_vector?
    text? public_engagement_narrative?
    json? public_engagement_vector?
    text? communication_style_narrative?
    json? communication_style_vector?
    text? community_affiliation_narrative?
    json? community_affiliation_vector?
    text? passions_interests_narrative?
    json? passions_interests_vector?
    text? current_needs_narrative? { description = "Mirrors the real person's needs so mutual timing scores" }
    json? current_needs_vector?
    text? persona_sequence_summary?
    json? synthesis? { description = "Build telemetry — valid/reason/model/usage" }
    json? simulated_persona? { description = "Verbatim LLM profile: dimension_weights, is_simulated, the nine-object persona_narratives array" }
  }

  index = [
    {type: "primary", field: [{name: "id"}]}
    {type: "btree", field: [{name: "created_at", op: "desc"}]}
    {type: "unique", field: [{name: "master_person_id", op: "asc"}, {name: "archetype_slot", op: "asc"}]}
  ]

  tags = ["person"]
}
```

**Validation (2026-08-18, sandbox):** six-person fleet on kimi-k3 (Kerris, Dixon, Jackson, Huang, Gallanty, Khan) — 6/6 valid at \$0.067–0.108, all name-clean, anti-echo held (1–4 echoed entities vs 17–34 novel per persona), archetypes needs-driven and varied. Model A/B on Kerris \+ Dixon (`skip_persist`): all of claude-sonnet-5 (relaxed), glm-5.2, and deepseek-v4-pro produced valid, name-clean output — **the grounding canary that disqualified glm/deepseek for real personas does not apply to simulation** (imagination is the job). Costs: glm \$0.009–0.024, deepseek \$0.017–0.021, sonnet \$0.053–0.062, kimi \$0.076–0.098; glm left `dimension_weights` as 0.0 placeholders in one of two runs; kimi hit its known ~stochastic invalid-JSON once across eight runs (retry fixed). Forced mode validated on the two null-`current_needs` personas (Mwangi, Shannon): both produced plausible, stage-appropriate helpers from inferred needs. **Decision: kimi-k3 default (one model family across build \+ simulate); deepseek-v4-pro flagged as the cost alternative at scale.**

**v0.4 re-fleet (2026-08-18, sandbox):** same six people \+ both forced — **8/8 valid first-try** (no retries fired). Multi-need discrimination worked: Kerris read as single-need (1 archetype), Dixon/Jackson/Gallanty/Khan produced 2, Huang 3 (infrastructure capital chief / open-AI policy convener / US advanced-manufacturing operator); slot rows landed as designed — table 783 now holds 12 rows across 6 people with distinct `archetype`/`for_need` per slot, all name-clean. Forced builds surfaced their `inferred_brief` and stayed capability-level. Multi-need runs cost \$0.16–0.21 (2–3 archetypes ≈ 2–3× a single). Watch item: forced (inferred-brief) builds both chose 2 archetypes — an inferred brief defaulting to multi-need is a guess on a guess; consider capping forced builds at one archetype if it over-triggers.

## Backlog — what's left

1. **Social posts are not in the graph** — stage 2 sees the synthesized `social_insights`/`social_media_influence`, not the first-party `social_post` corpus (table 776). Decide whether the promoted builder joins that evidence directly (it would make `public_engagement`/`communication_style` quotable and dated).
2. **Promotion path**: complete the embeddings (the compare cache backfills 1536-d vectors lazily into 754 as pairs are compared) \+ `person_personas` upsert \+ rename to `person/build-persona`; then delete temp QA endpoints 8988/8989/8990/8991/8992. For the Simulated Person builder: ~~wire the sonnet-5 diverse retry~~ (**done in v0.4**) and the embedding stage for `simulated_persona` (783) vectors.
3. **Production trigger build** — the POST endpoint \+ CRON drain from the callout at the top: 90-day freshness check against `persona` (754), prerequisite initiation, `queue_persona` (779) upsert. The queue table exists (spec above); the endpoint and CRON do not yet.

## Related

- [Personas](/guides/open-work/personas) — canonical 9-dimension design, target schema, promotion path
- [Build Persona](/guides/open-work/personas/build-persona) — original 8-dimension prompt (archived)
- [New Build Plan](/guides/open-work/personas/new-build-plan) — context-source scoping that led here
- [Social Insights v3 tests](/guides/open-work/enrichment-engine-v1-1/social-insights-v3-tests) — where `interests` and `social_media_influence` get written
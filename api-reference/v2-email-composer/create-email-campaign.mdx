---
title: "Create Email Campaign"
api: "POST https://xh2o-yths-38lt.n7c.xano.io/api:58PjJ6J9/email-campaigns"
description: "Create a new email campaign with multiple recipients."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField body="grant_id" type="string" required>Nylas grant ID to send from.</ParamField>
<ParamField body="subject" type="string" required>Email subject.</ParamField>
<ParamField body="from" type="string" required>Sender email.</ParamField>
<ParamField body="recipients" type="json" required>Array of recipient objects with name, email, and body.</ParamField>
<ParamField body="email_template_id" type="integer">Template ID if using a template.</ParamField>
<ParamField body="cc" type="array">CC addresses.</ParamField>
<ParamField body="bcc" type="array">BCC addresses.</ParamField>
<ParamField body="scheduled_at" type="timestamp">Optional scheduled send time.</ParamField>

<RequestExample>
```bash cURL
curl -X POST \
  'https://xh2o-yths-38lt.n7c.xano.io/api:58PjJ6J9/email-campaigns' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -d '{
    "grant_id": "grant_xyz",
    "subject": "Campaign Subject",
    "from": "me@example.com",
    "recipients": [{"name": "John", "email": "john@example.com", "body": "<p>Hi John!</p>"}]
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{}
```
</ResponseExample>

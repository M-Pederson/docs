---
title: 'Update Collections Reorder Favourites'
api: 'PATCH https://xh2o-yths-38lt.n7c.xano.io/api:U_4ewelb/collections/reorder-favourites'
description: 'Reorder the auth users favourite collections by submitting an ordered array of collection_ids Authentication: required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Request Body

<ParamField body="collection_ids" type="array">
  The collection_ids field.
</ParamField>


## Response

<ResponseField name="success" type="string">
  The success field.
</ResponseField>

<RequestExample>
```bash cURL
curl -X PATCH https://xh2o-yths-38lt.n7c.xano.io/api:U_4ewelb/collections/reorder-favourites \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "collection_ids": []
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "success": "example-success"
}
```
</ResponseExample>

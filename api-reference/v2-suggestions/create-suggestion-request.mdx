---
title: 'Create Suggestion Request'
api: 'POST https://xh2o-yths-38lt.n7c.xano.io/api:MkA4QsNh/suggestion-requests'
description: 'Add a new suggestion_request record'
---

## Overview

Create a new suggestion request record to initiate the suggestion workflow.

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Request Body

<ParamField body="dblink_0__" type="object" required>
  The suggestion request schema object containing the request details.
</ParamField>

## Response

<ResponseField name="id" type="integer">
  The ID of the newly created suggestion request.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST https://xh2o-yths-38lt.n7c.xano.io/api:MkA4QsNh/suggestion-requests \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "dblink_0__": {}
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1
}
```
</ResponseExample>

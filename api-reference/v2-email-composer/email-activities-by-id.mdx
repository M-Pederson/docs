---
title: 'Gets email activities by thread id'
api: 'GET https://xh2o-yths-38lt.n7c.xano.io/api:58PjJ6J9/email-activities/{email_thread_id}'
description: 'Gets email activities by thread id Authentication: required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Path Parameters

<ParamField path="email_thread_id" type="string" required>
  The email_thread_id parameter.
</ParamField>


## Response

<ResponseField name="itemsReceived" type="integer">
  The itemsReceived field.
</ResponseField>

<ResponseField name="curPage" type="integer">
  The curPage field.
</ResponseField>

<ResponseField name="nextPage" type="integer">
  The nextPage field. Can be `null`.
</ResponseField>

<ResponseField name="prevPage" type="integer">
  The prevPage field. Can be `null`.
</ResponseField>

<ResponseField name="offset" type="integer">
  The offset field.
</ResponseField>

<ResponseField name="perPage" type="integer">
  The perPage field.
</ResponseField>

<ResponseField name="itemsTotal" type="integer">
  The itemsTotal field.
</ResponseField>

<ResponseField name="pageTotal" type="integer">
  The pageTotal field.
</ResponseField>

<ResponseField name="items" type="array">
  The items field.
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET https://xh2o-yths-38lt.n7c.xano.io/api:58PjJ6J9/email-activities/1 \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN"
```
</RequestExample>

<ResponseExample>
```json 200
{
  "itemsReceived": 1,
  "curPage": 1,
  "nextPage": 1,
  "prevPage": 1,
  "offset": 1,
  "perPage": 1,
  "itemsTotal": 1,
  "pageTotal": 1,
  "items": []
}
```
</ResponseExample>

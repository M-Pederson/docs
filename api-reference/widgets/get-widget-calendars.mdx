---
title: "Get Widget Calendars"
api: "GET https://xh2o-yths-38lt.n7c.xano.io/api:3IvQ7jxX/widget/calendars"
description: "Get all calendars for the authenticated user."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ResponseField name="items" type="array">
  <Expandable>
    <ResponseField name="id" type="integer">Calendar ID.</ResponseField>
    <ResponseField name="external_id" type="string">External calendar ID.</ResponseField>
    <ResponseField name="name" type="string">Calendar name.</ResponseField>
    <ResponseField name="event_widget_preference" type="boolean">Whether shown in widget.</ResponseField>
  </Expandable>
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET \
  'https://xh2o-yths-38lt.n7c.xano.io/api:3IvQ7jxX/widget/calendars' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN'
```
</RequestExample>

<ResponseExample>
```json 200
[
  {
    "id": 1,
    "created_at": 1700000000,
    "external_id": "cal_abc",
    "name": "My Calendar",
    "event_widget_preference": true
  }
]
```
</ResponseExample>

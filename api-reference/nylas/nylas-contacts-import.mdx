---
title: 'Imports contacts for a specific nylas grant '
api: 'POST https://xh2o-yths-38lt.n7c.xano.io/api:s_Mv_Fpv/nylas/contacts/import'
description: 'Imports contacts for a specific nylas grant Authentication: required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Request Body

<ParamField body="grant_id" type="string" required>
  The grant_id field.
</ParamField>

<RequestExample>
```bash cURL
curl -X POST https://xh2o-yths-38lt.n7c.xano.io/api:s_Mv_Fpv/nylas/contacts/import \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "grant_id": "example-grant_id"
  }'
```
</RequestExample>

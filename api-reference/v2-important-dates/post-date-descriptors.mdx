---
title: 'Add or Get Date Descriptor'
api: 'POST https://xh2o-yths-38lt.n7c.xano.io/api:mGnfV_UB/date-descriptors'
description: 'Add or Get Date Descriptor Authentication: required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Request Body

<ParamField body="descriptor" type="string" required>
  The descriptor field.
</ParamField>


## Response

<ResponseField name="id" type="integer">
  The id field.
</ResponseField>

<ResponseField name="created_at" type="number">
  The created_at field.
</ResponseField>

<ResponseField name="descriptor" type="string">
  The descriptor field.
</ResponseField>

<ResponseField name="user_id" type="integer">
  The user_id field. Can be `null`.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST https://xh2o-yths-38lt.n7c.xano.io/api:mGnfV_UB/date-descriptors \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "descriptor": "example-descriptor"
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1,
  "descriptor": "example-descriptor",
  "user_id": 1
}
```
</ResponseExample>

---
title: 'Update email_draft record and sync to Nylas'
api: 'PUT https://xh2o-yths-38lt.n7c.xano.io/api:58PjJ6J9/email-drafts/{email_draft_id}'
description: 'Update email_draft record and sync to Nylas Authentication: required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Path Parameters

<ParamField path="email_draft_id" type="integer" required>
  The email_draft_id parameter.
</ParamField>


## Request Body

<ParamField body="subject" type="string">
  The subject field.
</ParamField>

<ParamField body="body" type="string">
  The body field.
</ParamField>

<ParamField body="to" type="array">
  The to field.
</ParamField>

<ParamField body="cc" type="array">
  The cc field.
</ParamField>

<ParamField body="bcc" type="array">
  The bcc field.
</ParamField>

<ParamField body="attachments" type="array">
  The attachments field.
</ParamField>

<ParamField body="deleted_attachment_ids" type="array">
  The deleted_attachment_ids field.
</ParamField>


## Response

<ResponseField name="id" type="integer">
  The id field.
</ResponseField>

<ResponseField name="created_at" type="number">
  The created_at field.
</ResponseField>

<ResponseField name="updated_at" type="number">
  The updated_at field. Can be `null`.
</ResponseField>

<ResponseField name="user_id" type="integer">
  User who owns this draft
</ResponseField>

<ResponseField name="grant_id" type="string">
  Nylas grant ID for the email account
</ResponseField>

<ResponseField name="nylas_draft_id" type="string">
  Draft ID from Nylas API Can be `null`.
</ResponseField>

<ResponseField name="thread_id" type="string">
  Thread ID if part of existing conversation Can be `null`.
</ResponseField>

<ResponseField name="reply_to_message_id" type="string">
  Message ID if replying to specific message Can be `null`.
</ResponseField>

<ResponseField name="subject" type="string">
  Email subject
</ResponseField>

<ResponseField name="body" type="string">
  Email body content
</ResponseField>

<ResponseField name="to" type="array">
  The to field.
</ResponseField>

<ResponseField name="cc" type="array">
  The cc field.
</ResponseField>

<ResponseField name="bcc" type="array">
  The bcc field.
</ResponseField>

<ResponseField name="last_synced_at" type="number">
  When draft was last synced to Nylas Can be `null`.
</ResponseField>

<RequestExample>
```bash cURL
curl -X PUT https://xh2o-yths-38lt.n7c.xano.io/api:58PjJ6J9/email-drafts/1 \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "subject": "example-subject",
      "body": "example-body",
      "to": [],
      "cc": [],
      "bcc": [],
      "attachments": [],
      "deleted_attachment_ids": []
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1,
  "updated_at": 1,
  "user_id": 1,
  "grant_id": "example-grant_id",
  "nylas_draft_id": "example-nylas_draft_id",
  "thread_id": "example-thread_id",
  "reply_to_message_id": "example-reply_to_message_id",
  "subject": "example-subject",
  "body": "example-body",
  "to": [],
  "cc": [],
  "bcc": [],
  "last_synced_at": 1
}
```
</ResponseExample>

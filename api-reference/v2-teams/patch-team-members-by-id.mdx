---
title: 'Update team member admin status'
api: 'PATCH https://xh2o-yths-38lt.n7c.xano.io/api:idMy4x2v/team-members/{team_member_id}'
description: 'Update team member admin status Authentication: required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Path Parameters

<ParamField path="team_member_id" type="integer" required>
  The team_member_id parameter.
</ParamField>


## Request Body

<ParamField body="is_admin" type="boolean" required>
  The is_admin field.
</ParamField>


## Response

<ResponseField name="id" type="integer">
  The id field.
</ResponseField>

<ResponseField name="created_at" type="number">
  The created_at field.
</ResponseField>

<ResponseField name="updated_at" type="number">
  The updated_at field. Can be `null`.
</ResponseField>

<ResponseField name="team_id" type="integer">
  The team_id field.
</ResponseField>

<ResponseField name="user_id" type="integer">
  The user_id field.
</ResponseField>

<ResponseField name="is_admin" type="boolean">
  The is_admin field.
</ResponseField>

<ResponseField name="status" type="string">
  Invitation status: pending, accepted, or rejected
</ResponseField>

<ResponseField name="invited_by_user_id" type="integer">
  User who sent the team invitation Can be `null`.
</ResponseField>

<ResponseField name="invited_at" type="number">
  When the invitation was sent Can be `null`.
</ResponseField>

<ResponseField name="responded_at" type="number">
  When the user accepted or rejected the invitation Can be `null`.
</ResponseField>

<ResponseField name="position" type="integer">
  The position field.
</ResponseField>

<RequestExample>
```bash cURL
curl -X PATCH https://xh2o-yths-38lt.n7c.xano.io/api:idMy4x2v/team-members/1 \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "is_admin": true
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1,
  "updated_at": 1,
  "team_id": 1,
  "user_id": 1,
  "is_admin": false,
  "status": "pending",
  "invited_by_user_id": 1,
  "invited_at": 1,
  "responded_at": 1,
  "position": 1
}
```
</ResponseExample>

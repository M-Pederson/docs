---
title: "Batch Fix Dub Links"
api: "GET https://xh2o-yths-38lt.n7c.xano.io/api:g7_LZ7hE/temp/batch-fix-dub-links"
description: "TEMPORARY: Batch create missing Dub links."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

### Query Parameters

<ParamField query="limit" type="integer">
  Total entries to process in this call (1-2000). Defaults to `500`.
</ParamField>

### Response

<ResponseField name="processed" type="string">
  Number of entries processed.
</ResponseField>

<ResponseField name="succeeded" type="string">
  Number of entries successfully fixed.
</ResponseField>

<ResponseField name="failed" type="string">
  Number of entries that failed.
</ResponseField>

<ResponseField name="remaining" type="string">
  Number of entries remaining to process.
</ResponseField>

<ResponseField name="message" type="string">
  Summary message.
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET \
  'https://xh2o-yths-38lt.n7c.xano.io/api:g7_LZ7hE/temp/batch-fix-dub-links?limit=500' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "processed": "500",
  "succeeded": "498",
  "failed": "2",
  "remaining": "0",
  "message": "Batch complete"
}
```
</ResponseExample>

---
title: 'Join the Orbiter waitlist with optional referral code'
api: 'POST https://xh2o-yths-38lt.n7c.xano.io/api:g7_LZ7hE/join'
description: 'Join the Orbiter waitlist with optional referral code Authentication: not required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

## Request Body

<ParamField body="email" type="string" required>
  Email address to join waitlist
</ParamField>

<ParamField body="ref" type="string">
  Optional referral code (e.g. ORB-A7X2B)
</ParamField>


## Response

<ResponseField name="success" type="string">
  The success field.
</ResponseField>

<ResponseField name="message" type="string">
  The message field.
</ResponseField>

<ResponseField name="position" type="string">
  The position field.
</ResponseField>

<ResponseField name="referral_link" type="string">
  The referral_link field.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST https://xh2o-yths-38lt.n7c.xano.io/api:g7_LZ7hE/join \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -d '{
      "email": "user@example.com",
      "ref": "example-ref"
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "success": "example-success",
  "message": "example-message",
  "position": "example-position",
  "referral_link": "example-referral_link"
}
```
</ResponseExample>

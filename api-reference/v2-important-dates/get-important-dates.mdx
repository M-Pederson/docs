---
title: "Get Important Dates"
api: "GET https://xh2o-yths-38lt.n7c.xano.io/api:mGnfV_UB/important-dates"
description: "Query all important date records for a master person."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField query="master_person_id" type="integer" required>
  The ID of the master person to query important dates for.
</ParamField>

<ResponseField name="itemsReceived" type="integer">
  Number of items in the current response.
</ResponseField>
<ResponseField name="curPage" type="integer">
  Current page number.
</ResponseField>
<ResponseField name="nextPage" type="integer">
  Next page number, or null if on the last page.
</ResponseField>
<ResponseField name="prevPage" type="integer">
  Previous page number, or null if on the first page.
</ResponseField>
<ResponseField name="itemsTotal" type="integer">
  Total number of items.
</ResponseField>
<ResponseField name="items" type="object[]">
  Array of important date objects.
</ResponseField>

<RequestExample>
```bash cURL
curl -G \
  https://xh2o-yths-38lt.n7c.xano.io/api:mGnfV_UB/important-dates \
  -H "X-Data-Source: live" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d "master_person_id=42"
```
</RequestExample>

<ResponseExample>
```json 200
{
  "itemsReceived": 1,
  "curPage": 1,
  "nextPage": null,
  "prevPage": null,
  "offset": 0,
  "perPage": 50,
  "itemsTotal": 1,
  "pageTotal": 1,
  "items": [
    {
      "id": 1,
      "created_at": 1700000000000,
      "updated_at": null,
      "my_person_id": 42,
      "date_descriptor_id": 1,
      "annual_recurring": true,
      "date": "2024-12-25",
      "note": "Christmas gathering",
      "reminder": 7
    }
  ]
}
```
</ResponseExample>

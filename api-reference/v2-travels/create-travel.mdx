---
title: "Create Travel"
api: "POST https://xh2o-yths-38lt.n7c.xano.io/api:WtHnIWRU/travels"
description: "Create a travel record."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

### Body Parameters

<ParamField body="departing_from" type="string" required>
  The departure location.
</ParamField>

<ParamField body="destination" type="string" required>
  The travel destination.
</ParamField>

<ParamField body="start_date" type="string" required>
  Start date of the travel (date format).
</ParamField>

<ParamField body="end_date" type="string">
  End date of the travel (date format).
</ParamField>

<ParamField body="note" type="string">
  Optional note about the travel.
</ParamField>

<ParamField body="share_to_users" type="boolean">
  Whether to share the travel with all users.
</ParamField>

<ParamField body="share_to_connections" type="boolean">
  Whether to share the travel with connections.
</ParamField>

<ParamField body="team_ids" type="array">
  List of team IDs to share the travel with.
</ParamField>

<ParamField body="formatted_address" type="string" required>
  The formatted address of the destination.
</ParamField>

<ParamField body="destination_longitude" type="number" required>
  Longitude coordinate of the destination.
</ParamField>

<ParamField body="destination_latitude" type="number" required>
  Latitude coordinate of the destination.
</ParamField>

<ParamField body="private" type="boolean">
  Whether this travel is private.
</ParamField>

### Response

<ResponseField name="id" type="integer">
  Unique identifier of the travel record.
</ResponseField>

<ResponseField name="created_at" type="timestamp">
  Timestamp when the record was created.
</ResponseField>

<ResponseField name="departing_from" type="string">
  The departure location.
</ResponseField>

<ResponseField name="destination" type="string">
  The travel destination.
</ResponseField>

<ResponseField name="start_date" type="string">
  Start date of the travel.
</ResponseField>

<ResponseField name="end_date" type="string">
  End date of the travel.
</ResponseField>

<ResponseField name="destination_longitude" type="number">
  Longitude coordinate of the destination.
</ResponseField>

<ResponseField name="destination_latitude" type="number">
  Latitude coordinate of the destination.
</ResponseField>

<ResponseField name="share_to_all" type="boolean">
  Whether shared with all users.
</ResponseField>

<ResponseField name="share_to_connections" type="boolean">
  Whether shared with connections.
</ResponseField>

<ResponseField name="private" type="boolean">
  Whether this travel is private.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST \
  'https://xh2o-yths-38lt.n7c.xano.io/api:WtHnIWRU/travels' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -d '{
    "departing_from": "New York, NY",
    "destination": "San Francisco, CA",
    "start_date": "2026-04-01",
    "end_date": "2026-04-05",
    "formatted_address": "San Francisco, CA, USA",
    "destination_longitude": -122.4194,
    "destination_latitude": 37.7749,
    "share_to_connections": true,
    "private": false
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1700000000000,
  "updated_at": null,
  "user_id": 42,
  "departing_from": "New York, NY",
  "destination": "San Francisco, CA",
  "start_date": "2026-04-01",
  "end_date": "2026-04-05",
  "destination_longitude": -122.4194,
  "destination_latitude": 37.7749,
  "note": "",
  "share_to_all": false,
  "share_to_connections": true,
  "private": false
}
```
</ResponseExample>

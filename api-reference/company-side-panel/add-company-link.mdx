---
title: "Add Company Link"
api: "POST https://xh2o-yths-38lt.n7c.xano.io/api:KdpaUs_r/add/my_company/link"
description: "Add a link to a company record."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField body="link_url" type="string" required>URL to add.</ParamField>
<ParamField body="my_company_id" type="integer" required>Company ID.</ParamField>

<ResponseField name="id" type="integer">Record ID.</ResponseField>
<ResponseField name="my_company_id" type="integer">Company ID.</ResponseField>
<ResponseField name="master_link_id" type="integer">Master link record ID.</ResponseField>

<RequestExample>
```bash cURL
curl -X POST \
  'https://xh2o-yths-38lt.n7c.xano.io/api:KdpaUs_r/add/my_company/link' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -d '{"link_url": "https://acme.com", "my_company_id": 1}'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1700000000,
  "my_company_id": 1,
  "master_link_id": 20
}
```
</ResponseExample>

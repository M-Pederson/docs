---
title: 'List My Persons'
api: 'GET https://xh2o-yths-38lt.n7c.xano.io/api:Et3oQAtI/my-persons'
description: 'Returns all my person records for the authenticated user'
---

## Overview

Returns a paginated list of all person records the authenticated user has added to their personal contacts.

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Query Parameters

<ParamField query="query" type="string">
  Search query to filter persons.
</ParamField>

<ParamField query="page" type="integer">
  Page number for pagination.
</ParamField>

<ParamField query="limit" type="integer">
  Number of items per page.
</ParamField>

<ParamField query="offset" type="integer">
  Number of items to skip.
</ParamField>

## Response

<ResponseField name="itemsReceived" type="integer">
  Number of items returned in the current response.
</ResponseField>

<ResponseField name="curPage" type="integer">
  The current page number.
</ResponseField>

<ResponseField name="nextPage" type="integer">
  The next page number, or null if no more pages.
</ResponseField>

<ResponseField name="prevPage" type="integer">
  The previous page number, or null if on the first page.
</ResponseField>

<ResponseField name="offset" type="integer">
  The offset used for the query.
</ResponseField>

<ResponseField name="itemsTotal" type="integer">
  Total number of items matching the query.
</ResponseField>

<ResponseField name="pageTotal" type="integer">
  Total number of pages.
</ResponseField>

<ResponseField name="items" type="array">
  Array of my_person records containing id, created_at, master_person_id, full_name, status_connected, and activity fields.
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET "https://xh2o-yths-38lt.n7c.xano.io/api:Et3oQAtI/my-persons?query=john&page=1&limit=20" \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_TOKEN"
```
</RequestExample>

<ResponseExample>
```json 200
{
  "itemsReceived": 1,
  "curPage": 1,
  "nextPage": null,
  "prevPage": null,
  "offset": 0,
  "itemsTotal": 1,
  "pageTotal": 1,
  "items": [
    {
      "id": 1,
      "created_at": 1700000000,
      "master_person_id": 50,
      "full_name": "John Doe",
      "status_connected": "non_user",
      "last_email_activity_id": null,
      "last_calendar_event_activity_id": null,
      "last_activity_at": null
    }
  ]
}
```
</ResponseExample>

---
title: "Upload LinkedIn CSV"
api: "POST https://xh2o-yths-38lt.n7c.xano.io/api:aNftW36n/linkedin_csv"
description: "Upload Connections.csv file from LinkedIn and add to linkedin_csv table and process."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField body="data" type="string">
  The CSV data content from the LinkedIn Connections.csv file.
</ParamField>

<ResponseField name="result" type="object">
  The processing result after importing the LinkedIn CSV data.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST \
  https://xh2o-yths-38lt.n7c.xano.io/api:aNftW36n/linkedin_csv \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: live" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "data": "First Name,Last Name,Email Address,Company,Position..."
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "status": "success"
}
```
</ResponseExample>

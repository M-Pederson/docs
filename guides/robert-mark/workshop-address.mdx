---
title: "Workshop Address"
description: "Workshop page — do we need changes to the address tables / functions? Current schemas of master_address and primary_location as the starting point."
---

<div style={{border: '2px solid #10B981', borderRadius: '12px', padding: '1.25rem 1.5rem', margin: '0 0 2rem 0'}}>

### Changelog

**Tables**

- ✅ `master_address` (164) — `longitude` / `latitude` → `decimal` · **applied 2026-07-15** (side effect: trim validators dropped table-wide)
- 🔲 `master_address` (164) — add `natural_key` (text) + `place_id` (text) · via Xano UI
- ⏳ Later — unique index `(master_person_id, master_company_id, natural_key)` · after app writers update
- ⏳ Later, independent — `primary_location` (385) unique index on `(city_id, region_id, country_id)` + city-lookup `OR` fix in 4690

**Company enrichment waterfall**

- 🔲 **NEW** `mvp/address/build-address-natural-key` — pure key helper (spec v2)
- 🔲 **EDIT** `mvp/address/add-company-address` (2451) — compute key on write · key-based dedupe · completeness floor · upgrade-in-place + ~15 km proximity fallback · downgrade guard
- 🔲 **EDIT** `mvp/maps/radar-maps-search-address` (2000) — one line: pin Radar response language
- ✅ **No change** — 2452 / 12989 / 12809 inherit via 2451 · person waterfall untouched (writes `primary_location` only)

**Monitoring**

- 🔲 **NEW** recurring collision-report task — safety net until the unique index lands

</div>

This page specifies the proposed address-dedupe solution and how we deploy it. The proposal comes first; the current table schemas (pulled live from Xano workspace 3, 2026-07-15), deployment runbook, and decision log follow as supporting material.

## Proposed: `natural_key` on master\_address — scope

<Note>
**Workshop scope only — no Xano changes made.** This section scopes the canonical-address dedupe key. It reuses the `natural_key` column already planned for `master_address` in the [Universe ↔ Backend sync design](/guides/open-work/backend-refactor-tables) (upsert key `(node_uuid, natural_key)`, one of the four sync columns), so we get one dedupe concept, not two.
</Note>

### Why — today's dedupe is broken in both directions

The two live writer paths use different, conflicting keys (verified against live XanoScript, 2026-07-15):

- `mvp/address/add-company-address` (2451) dedupes on **`(master_company_id, city, state_region)`** — massively **over-dedupes**: one row per company per city. A second office in the same city is silently swallowed (or overwrites the first via the update branch when `address_line_1` is empty).
- `mvp/address/create-person-address-join` (4654) dedupes on **exact raw `(master_person_id, formatted_address)`** — **under-dedupes**: "123 Main St" and "123 Main Street" both insert, since one string is user-typed and the other geocoder-formatted.

A computed `natural_key` with a unique index replaces both with one correct rule.

### Key definition

`natural_key` is a **computed, tier-prefixed normalized string** (not the raw `formatted_address`):

| Tier | Prefix | Value | When |
| --- | --- | --- | --- |
| 1 | `pid:` | geocoder place id | **Not available today** — Radar forward geocoding returns no durable place id (the retired `archive/details/add-address` 228 took a Google `place_id` input). Keep the optional `place_id` column from the sync design for a future geocoder upgrade. |
| 2 | `cmp:` | `country_code\|region_code\|city\|postal_code\|street\|number\|unit` | Row has geocoded components (the normal Radar path) |
| 3 | `fmt:` | normalized `formatted_address` string | Row was never geocoded (user-typed only) |

**Recommendation:** store the readable pipe-joined string, not a sha256 of it — deterministic either way, but debuggable when a merge goes wrong. Hash only if key length becomes a problem.

### Normalization spec (global) — v2

What makes the key work globally is that the **geocoder is the canonicalizer** (Radar expands "Str." → "Street", resolves ambiguous city names into components) and that **pipe positions are fixed** — a missing component leaves an empty slot and never shifts the others, so sparse address formats degrade gracefully instead of breaking.

Rules for tiers 2–3 (`cmp:` / `fmt:`):

1. **Fixed field order** `country_code|region_code|city|postal_code|street|number|unit`; missing component → empty string, position preserved.
2. **Lowercase everything;** trim; collapse internal whitespace to a single space in `street` / `city`.
3. **Strip punctuation and diacritics** (Unicode NFKD, drop combining marks) — applies to Latin scripts; CJK and other non-Latin scripts pass through intact (see rule 4).
4. **Pin the Radar response language** (query with `language=en`) so components always come back in one script. Without this, the same Beijing address geocoded from English input vs `中关村大街1号` returns different-script components → two keys for one building.
5. **`postal_code`: strip ALL whitespace**, keep alphanumerics only — UK postcodes ("SW1A 2AA") are written inconsistently with/without the internal space.
6. **No-postal-code countries** (and no-street-number regions): slots stay empty; the key degrades to street- or city-level granularity, which is acceptable because uniqueness is scoped per owner.
7. **`fmt:` tier gets rules 2–3 and 5 only** — no abbreviation expansion (that's the geocoder's job). A `fmt:` key never matches a `cmp:` key for the same address; convergence happens only when the row is geocoded.

### Worked examples

| Address | natural\_key |
| --- | --- |
| 133 West 22nd Str. New York, NY 10011 | `cmp:us\|ny\|new york\|10011\|west 22nd street\|133\|` |
| Same string, never geocoded | `fmt:133 west 22nd str new york ny 10011` |
| Kenyatta Avenue, Nairobi 00100, Kenya | `cmp:ke\|30\|nairobi\|00100\|kenyatta avenue\|\|` |
| 1 Zhongguancun Street, Haidian, Beijing 100080 | `cmp:cn\|bj\|beijing\|100080\|zhongguancun street\|1\|` |
| 10 Downing Street, London SW1A 2AA | `cmp:gb\|eng\|london\|sw1a2aa\|downing street\|10\|` |
| 10 Bayfront Avenue, Singapore 018956 | `cmp:sg\|\|singapore\|018956\|bayfront avenue\|10\|` |

Notes: the US example shows the geocoder fixing "Str."; Kenya shows sparse-slot degradation (no number/unit); Singapore shows a city-state with a legitimately empty region slot (postal code alone is building-precise there); London shows the space-stripped postcode.

### Completeness floor & upgrade rule

Dedupe alone doesn't stop junk rows: live example on `master_address` today — `neural frames` has row 1 `formatted_address = "DE"` (country only, from a sparse Fundable HQ concat) next to row 2 `"Donaustraße 44, 12043 Berlin, DE"`. Under `natural_key` those are *different* keys (`cmp:de||||||` vs the full Berlin key), so both would survive. Two additional rules in `add-company-address` (2451), enforced at the same choke point as the key:

1. **Completeness floor (reject at write).** If the geocode resolves to no city — country-only or region-only components — skip the insert and log it. A row like "DE" carries no address information; it should never be written.
2. **Upgrade in place (same address only).** A new write **supersedes** an existing row for the same owner when the existing row's non-empty components all match the new address AND the new address has strictly more populated slots (e.g. `cmp:de|be|berlin|||` upgraded by `cmp:de|be|berlin|12043|donaustrasse|44|`). The existing row is updated in place — `id` preserved, joins intact — not duplicated.
3. **Downgrade guard (ignore).** The reverse direction is a no-op: a less complete address never overwrites or joins alongside a more complete row it's consistent with.

"Consistent with" is strict: every non-empty slot of the coarser key must equal the finer key's value — a city-level Berlin row is upgradeable by a Berlin street address, but not by a Munich one, and a country-only row (which the floor now prevents) matches nothing.

**Proximity fallback for the consistency check.** Neighborhood-vs-city naming breaks strict slot matching: "Venice, Los Angeles, CA" may geocode to city = Los Angeles while "842 Flower Ave, Venice, CA 90291" geocodes to city = Venice — slots disagree, yet it's the same place. When a coarse **city-level** row fails the slot comparison, fall back to coordinates: treat it as consistent if its lat/long is within ~15 km of the finer address (both rows already carry Radar coordinates). Street-level rows never use the fallback — two different street addresses are different addresses regardless of distance.

### Live validation cases (from master\_address, 2026-07-15)

| Rows | Owner | Data | Handled by |
| --- | --- | --- | --- |
| 1 vs 2 | neural frames (140→id 1/2) | `"DE"` vs `"Donaustraße 44, 12043 Berlin, DE"` | **Completeness floor** — "DE" is rejected at write |
| 153 vs 154 | Warner Bros. Discovery (#140) | `"New York, NY US"` vs `"New York City, NY US"` | **Dedupe** — Radar canonicalizes both to city = New York → identical `cmp:` key → second write is a no-op |
| 176 vs 177 | supersogood (#174) | `"Venice, Los Angeles, CA US"` vs `"842 Flower Ave, Venice, CA 90291 US"` | **Upgrade + proximity fallback** — strict slots may disagree (LA vs Venice as city), coordinates agree → coarse row upgraded in place |

**Uniqueness:** composite unique index on `(master_person_id, master_company_id, natural_key)` — two people at the same address must remain legal. The null-vs-`0` FK defaults must be coalesced consistently (today `master_person_id` defaults to `0` but `master_company_id` to null) so blank owners actually collide.

### Field name — keep `natural_key`? (open, recommendation: keep)

- **Keep `natural_key` (recommended):** it is the locked column name in the [Universe ↔ Backend sync design](/guides/open-work/backend-refactor-tables) across all four facet tables (`master_email` / `master_phone` / `master_link` / `master_address`) — the generic upsert machinery keys on `(node_uuid, natural_key)`, and one uniform name is what makes the apply endpoint generic. It's also the standard DB term for a domain-derived key (vs the surrogate `id`).
- **`canonical_address`:** self-describing on this table, but per-table naming breaks the generic sync apply, and "canonical" already means something else on `master_link` (canonical URL normalization).
- **`dedupe_key` / `identity_key`:** clearer intent for app developers — viable only as a uniform rename across all four facet tables in the sync design, not an address-only name.

### Writer inventory — enrichment waterfalls only

<Note>
**Scope narrowed (2026-07-15):** only `master_address` writers reachable from the enrichment waterfalls are in scope. User-facing app paths (`create-person-address-join` 4654, `create-company-address-join` 4653, `POST /master-addresses` API 3110, `PUT /master-addresses/{id}` API 3112) and the merge orchestrators keep their current behavior — later phase.
</Note>

Verified against live XanoScript (2026-07-15): **every enrichment write to `master_address` funnels through one choke point — `mvp/address/add-company-address` (2451)**. And the person waterfall never writes `master_address` at all: `mvp/address/add-person-primary-location` (2766) geocodes via Radar and writes graph location nodes / `primary_location` via `mvp/node/primary-location-nodes-edges_v2` — so this change is **company-pipeline-only**.

| Writer | ID | Path into master\_address | Change |
| --- | --- | --- | --- |
| `mvp/address/build-address-natural-key` | **new** | called by 2451 | Create — single implementation of the key |
| `mvp/address/add-company-address` | 2451 | **the sink** — Radar geocode, then dedupe on `(company, city, state_region)` | **The only edit** — compute key from Radar components, upsert on it, recompute in the update branch |
| `mvp/address/bulk-add-company-address` | 2452 | foreach → 2451 | None (inherits) |
| `mvp/enrich/apply-company-llm-payload` | 12989 | Company\_LLM HQ + other locations → 2451 | None (inherits) |
| `mvp/enrich/process-company-phase-5` | 12809 | Fundable HQ string → 2451 (`source: "Fundable"`) | None (inherits) |

Implementation surface for this pass: **one new helper + one function edit.**

**DB unique index: deferred.** With user-facing writers out of scope, a table-wide unique constraint would make `PUT /master-addresses/{id}` (3112) and the join helpers fail on violation. This pass runs `natural_key` as an **application-level upsert key inside 2451** plus a recurring collision report; the DB constraint lands when the app writers are updated in the later phase.

### Open questions

- Field name: keep `natural_key` (recommended, see above) or rename uniformly across the four facet tables?
- Keep `unit` in the key? (Recommend yes — two units in one building are distinct addresses.)
- PO boxes — normalize as `street="po box", number=N`, or leave to the `fmt:` tier? (Countries without postal codes are now handled by spec rule 6 — empty slots degrade gracefully.)
- Does this land before or with the Universe sync's `ALTER TABLE` for the other three sync columns (`content_hash`, `source_run_id`, `locked`)?

## Current tables

<Card icon="database">
  ```text
  master_address — 164
  ```

  Master address records. Full street-level addresses attached to a `master_person` or `master_company`, with provenance (`data_source_id`, `source`, `created_by_user_id`) and graph-sync state (`path_triples_added`).
</Card>

<Card icon="database">
  ```text
  primary_location — 385
  ```

  Normalized coarse location (city / region / country lookups) with a formatted string, coordinates, and timezone. Part of the `location tables` group — references `city` (332), `region` (331), and `country` (173).
</Card>

## master\_address — schema (table 164)

<Warning>
**`natural_key` and `place_id` need to be ADDED to `master_address`.** Shown in the table below as proposed rows — they do not exist in Xano yet. Add them **via the Xano UI** (the MCP full-schema replace strips `trim` validators; `xano workspace push` wipes data-source records).
</Warning>

<Warning>
**`longitude` and `latitude` must be type `decimal`** (matching `primary_location`) — text storage accepts junk silently and blocks the proximity-fallback coordinate math. Status: applied on branch `v1` 2026-07-15 and verified by schema read; if your table view still shows `text`, check which branch/datasource you're viewing. Writers pass Radar's numeric values straight through, so no function edits were needed. Side effect of the applied schema replace: `trim` validators were dropped from every text column on this table — restore via the UI if needed; the `natural_key` helper normalizes at write time regardless.
</Warning>

| Column | Type | Nullable | Default | Notes |
| --- | --- | --- | --- | --- |
| `id` | int | no | — | Primary key |
| `created_at` | timestamp | no | `now` | Private access |
| `updated_at` | timestamp | yes | — |  |
| `master_company_id` | int → `master_company` (142) | yes | — |  |
| `master_person_id` | int → `master_person` (139) | yes | `0` |  |
| `address_label` | text | yes | — |  |
| `formatted_address` | text | no | — | Autocomplete field |
| `unit` | text | yes | — |  |
| `number` | text | yes | — |  |
| `address_line_1` | text | yes | — |  |
| `address_line_2` | text | yes | — |  |
| `city` | text | yes | — |  |
| `state_region` | text | yes | — |  |
| `region_code` | text | yes | — |  |
| `postal_code` | text | yes | — |  |
| `country` | text | no | — |  |
| `country_code` | text | no | — |  |
| `connected_only` | bool | no | — |  |
| `mailing_address` | bool | no | — |  |
| `data_source_id` | int → `data_source` (161) | yes | `0` |  |
| `longitude` | decimal | yes | — | Converted from text 2026-07-15 |
| `latitude` | decimal | yes | — | Converted from text 2026-07-15 |
| `timezone` | text | yes | — |  |
| `path_triples_added` | bool | no | — | Graph-sync flag |
| `created_by_user_id` | int → `user` (125) | yes | `0` |  |
| `source` | text | yes | — |  |
| `natural_key` | text | yes | — | 🆕 **PROPOSED — to be added.** Tier-prefixed dedupe key (see scope section) |
| `place_id` | text | yes | — | 🆕 **PROPOSED — to be added.** Geocoder place id, empty until a `pid:`-capable geocoder — reserved by the sync design |

**Indexes:** primary (`id`) · btree (`created_at` desc)

**Referenced by:** `my_address_notes` (293), `my_person_address_join` (310), `my_company_address_join` (314)

## primary\_location — schema (table 385)

| Column | Type | Nullable | Default | Notes |
| --- | --- | --- | --- | --- |
| `id` | int | no | — | Primary key |
| `created_at` | timestamp | no | `now` | Private access |
| `city_id` | int → `city` (332) | yes | — |  |
| `region_id` | int → `region` (331) | yes | — |  |
| `country_id` | int → `country` (173) | yes | — |  |
| `formatted_location` | text | yes | — |  |
| `longitude` | decimal | yes | — |  |
| `latitude` | decimal | yes | — |  |
| `timezone` | text | no | — |  |

**Indexes:** primary (`id`) · btree (`created_at` desc)

**Tags:** `location tables`

<Warning>
**Matching for primary\_location — logic exists in Xano today, verified 2026-07-15, two gaps found.** `primary_location` needs **no `natural_key` column**: its natural key is already the `(city_id, region_id, country_id)` FK triple, and the matching logic is live in `mvp/node/primary-location-nodes-edges_v2` (4690), called by `add-person-primary-location` (2766) from `process-person-phase-2` (12823). A bare "London" never reaches the table un-disambiguated — Radar geocodes it into components first. Current predicates: country matches on name or alpha2; region is scoped by `country_id`; `primary_location` is looked up on the exact triple and inserted if missing.

**Granularity note:** unlike `master_address` (which has a completeness floor), coarse rows are **valid** here — country-only ("Australia") and city-level ("Sydney, Australia") are both legitimate primary locations. A person's known location is often just that coarse. Consequence: the triple lookup must treat **null slots consistently** — a second country-only "Australia" person must reuse the existing `(null, null, AU)` row, not insert a duplicate. Verify null-equality behavior in the `db.query` when fixing the gaps below.

Two gaps to fix (planned — not yet applied in Xano):

1. **City lookup same-country collision bug.** The predicate is `city_name matches AND (region_id == $region OR country_id == $country)`. The `OR` collapses same-name cities within one country: London **Kentucky** reuses London **Ohio**'s city row (both match on `country_id == US`), and every Springfield/Portland/Columbus does the same. Fix: match on region when both sides have one; fall back to country only when region is absent.
2. **No unique index on the triple** — verified: `primary_location` (385) and `city` (332) carry only a primary key + `created_at` btree. The query-then-insert dedupe is race-prone (two concurrent phase-2 runs for the same city can double-insert). Add a unique index on `(city_id, region_id, country_id)` after a collision check.
</Warning>

## Points of comparison

A few asymmetries between the two tables worth discussing:

- ~~**Coordinate types differ**~~ — resolved 2026-07-15: both tables now store `longitude` / `latitude` as `decimal`.
- **Normalization differs** — `master_address` stores city / region / country as free text plus codes; `primary_location` references the normalized `city` / `region` / `country` lookup tables.
- **`updated_at`** exists on `master_address` but not on `primary_location`.
- **No uniqueness constraints** on either table beyond the primary key — nothing prevents duplicate addresses or locations for the same entity.

## Deployment — step by step

Ordered runbook to take the scoped `master_address` work live. Nothing below has been started; each step gates the next.

### Phase 0 — prep

1. ✅ **DONE 2026-07-15 — `master_address.longitude` / `latitude` → `decimal`** (decision #1, matching `primary_location`). Applied via MCP schema replace; writers pass Radar's numeric values straight through, so no function edits were needed. Side effect: `trim` validators dropped table-wide (see the callout in the schema section). This was a **prerequisite for the proximity fallback** (step 5), which needs numeric coordinate math.
2. **Save rollbacks**: export the current XanoScript of `add-company-address` (2451) to a rollback `.xs` file — `xano function edit` publishes live, there is no draft.

### Phase 1 — schema

3. **Add columns** `natural_key` (text) and `place_id` (text, empty for now) to `master_address` **via the Xano UI** — not the MCP full-schema replace (strips `trim` validators — confirmed live in step 1) and never `xano workspace push` (wipes data-source records).

### Phase 2 — code (callee before caller)

4. **Create `mvp/address/build-address-natural-key`** first — inputs: the Radar component set + a raw-string fallback; output: the tier-prefixed key. Pure function, no DB writes. Verify it against the worked-examples table (NY / Kenya / China / London / Singapore) and the `fmt:` fallback before touching any caller.
5. **Edit `add-company-address` (2451)** — single edit that lands all four behaviors: compute `natural_key` on write; replace the `(city, state_region)` dedupe with a key lookup; completeness floor (reject city-less geocodes, log the skip); upgrade-in-place with proximity fallback + downgrade guard. Pin the Radar response language in the same pass.
6. **Re-fetch the stored XanoScript** and confirm the `function.run "mvp/address/build-address-natural-key"` target stored non-empty (known serializer trap even when the callee exists).

### Phase 3 — validate

7. **Run the three live validation cases** in the STAGING data source: neural frames "DE" (floor rejects), WBD "New York City" (dedupe no-ops), supersogood Venice/LA (upgrade fires via proximity). Then re-run a normal company enrich end-to-end (Fundable 12809 + LLM payload 12989 paths) and confirm keys land.

### Phase 4 — monitor

8. **Recurring collision report** (weekly task or QA-agent check) — with no DB constraint yet, this is the safety net; also watch the floor-rejection log for sources that suddenly produce junk. Once the app writers are updated (later phase), the unique index can be created immediately.

### Later phase — explicitly not in this deployment

- User-facing writers (4654 / 4653 / `POST`+`PUT /master-addresses`) and merge-orchestrator compares.
- **DB unique index** on `(master_person_id, master_company_id, natural_key)` — only after the app writers are updated.
- The other three sync columns (`content_hash`, `source_run_id`, `locked`) — Universe sync work-package.
- `primary_location` fixes (independent, can ship any time): patch the city-lookup `OR` predicate in 4690, then unique index on `(city_id, region_id, country_id)` after a collision check.

## Workshop notes

**Decisions:**

1. ✅ **2026-07-15 — `master_address.longitude` / `latitude` → `decimal`. APPLIED in Xano** (the only change applied so far; everything else remains workshop-only). Writer inventory confirmed no function edits needed; `geo_point` remains a future consideration.
2. **2026-07-15 — `natural_key` dedupe column: scoped, pending sign-off.** See the scope section above — tiered normalized key, composite unique index, staged rollout. Also resolves the sync-design open question (place_id vs component hash → component hash now, `pid:` tier reserved).
3. **2026-07-15 — scope narrowed to enrichment-waterfall writers.** Only the company waterfall writes `master_address` (all through `add-company-address` 2451; the person waterfall writes `primary_location` instead). User-facing app paths and merge orchestrators deferred; DB unique constraint deferred with them — `natural_key` runs as an application-level upsert key in 2451 for this pass. Field name kept as `natural_key` pending sign-off (recommendation: keep, for sync-design uniformity).
4. **2026-07-15 — completeness floor + upgrade rule added to the 2451 scope.** Country-only/region-only geocodes are rejected at write; a more complete address of the *same* place upgrades the existing row in place (id + joins preserved); a less complete one is ignored. Motivated by live data: `neural frames` rows 1 ("DE") vs 2 (full Berlin address).
5. **2026-07-15 — `primary_location` gets no `natural_key` column.** Its FK triple `(city_id, region_id, country_id)` already is the natural key and the matching logic is live in 4690 (verified). Instead: fix the city-lookup same-country `OR` bug and add a unique index on the triple — see the callout in the primary\_location schema section.
6. **2026-07-15 — coarse granularity is valid for `primary_location`.** Country-only ("Australia") and city-level ("Sydney, Australia") rows are both legitimate — no completeness floor there (that's a `master_address`-only rule). Null slots in the triple must dedupe consistently.
7. **2026-07-15 — rules apply to fresh writes only.** Data is still being wiped and re-generated during testing, so there is no legacy data to migrate; the unique index is unblocked as soon as the app writers are updated.

_Further proposed changes, decisions, and open questions to be added as the workshop continues._
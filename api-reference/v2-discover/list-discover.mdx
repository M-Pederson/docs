---
title: 'List Discover Queries'
api: 'GET https://xh2o-yths-38lt.n7c.xano.io/api:rraxfaG7/discover'
description: 'Query all discover records'
---

## Overview

Returns a paginated list of all discover query records for the authenticated user.

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Query Parameters

<ParamField query="query" type="string">
  Search query to filter discover records.
</ParamField>

<ParamField query="page" type="integer">
  Page number for pagination.
</ParamField>

<ParamField query="limit" type="integer">
  Number of items per page.
</ParamField>

<ParamField query="offset" type="integer">
  Number of items to skip.
</ParamField>

## Response

<ResponseField name="itemsReceived" type="integer">
  Number of items returned in the current response.
</ResponseField>

<ResponseField name="curPage" type="integer">
  The current page number.
</ResponseField>

<ResponseField name="nextPage" type="integer">
  The next page number, or null if no more pages.
</ResponseField>

<ResponseField name="prevPage" type="integer">
  The previous page number, or null if on the first page.
</ResponseField>

<ResponseField name="items" type="array">
  Array of discover records containing id, created_at, updated_at, user_id, query, edges, answer, archived, processing, and sorted_updated_at.
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET "https://xh2o-yths-38lt.n7c.xano.io/api:rraxfaG7/discover?query=AI&page=1&limit=20" \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_TOKEN"
```
</RequestExample>

<ResponseExample>
```json 200
{
  "itemsReceived": 1,
  "curPage": 1,
  "nextPage": null,
  "prevPage": null,
  "offset": 0,
  "perPage": 20,
  "itemsTotal": 1,
  "pageTotal": 1,
  "items": [
    {
      "id": 1,
      "created_at": 1700000000,
      "updated_at": 1700000000,
      "user_id": 10,
      "query": "AI startups in fintech",
      "edges": null,
      "answer": "Based on the analysis...",
      "archived": false,
      "processing": false,
      "sorted_updated_at": null
    }
  ]
}
```
</ResponseExample>

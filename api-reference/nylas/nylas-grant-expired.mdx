---
title: 'Nylas sends expired grants created to this endpoint'
api: 'POST https://xh2o-yths-38lt.n7c.xano.io/api:s_Mv_Fpv/nylas/grant/expired'
description: 'Nylas sends expired grants created to this endpoint Authentication: not required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

## Request Body

<ParamField body="challenge" type="string">
  The challenge field.
</ParamField>


## Response

<RequestExample>
```bash cURL
curl -X POST https://xh2o-yths-38lt.n7c.xano.io/api:s_Mv_Fpv/nylas/grant/expired \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -d '{
      "challenge": "example-challenge"
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{}
```
</ResponseExample>

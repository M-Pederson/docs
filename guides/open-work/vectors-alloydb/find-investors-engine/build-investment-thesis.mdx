---
title: "build-investment-thesis function"
description: "Xano orchestrator that gathers investor context and hands off to the GCP Cloud Run backend to build and persist an investment thesis, with June 2026 QA handoff notes."
---

<div style={{padding: '20px', border: '3px solid #6B9FE8', borderRadius: '10px', background: 'rgba(107,159,232,0.08)', color: '#fff', margin: '16px 0'}}>

<div style={{color: '#9BBDE8', fontSize: '1.3em', fontWeight: 'bold', marginBottom: '8px'}}>PERSON-SIDE STATUS</div>

- **Person first:** #12911 v3.3 now forces a person-side Signal NFX/profile refresh before direct/manual `master_person_id` thesis context is trusted.
- **Profile table writes:** Scott Belsky sandbox QA confirmed `investor_profile_person` (#475) receives `derived_thesis_summary`.
- **Completion bar:** Scott Belsky sandbox QA confirmed `investment_thesis` (#709), #475, `master_person.full_enrich`, and closeout logs all land green.

</div>

<div style={{padding: '20px', border: '3px solid #E0245E', borderRadius: '10px', background: 'rgba(224,36,94,0.08)', color: '#fff', margin: '16px 0'}}>

<div style={{color: '#FF5C8A', fontSize: '1.3em', fontWeight: 'bold', marginBottom: '8px'}}>ACTIVE QA STATUS</div>

This function is still being iterated on. Person/angel thesis generation has passed sandbox QA. Company/fund enrichment is now bounded, but company thesis content is **not trusted** until the Cloud Run thesis builder stops returning mismatched firms.

**Current known state as of June 23, 2026:** person-side Scott Belsky QA works in sandbox: final Phase 9 builds the thesis, #709 receives a non-empty `derived_thesis_summary`, #475 mirrors it, `master_person.full_enrich` closes true, and downstream company work stays bounded. Company-side smoke runs now create/enrich seed orgs and write #709/#478, but Cloud Run frequently returns a different investor than the requested `master_company_id` (for example `Heroic Ventures` receiving `Long Journey Ventures`). #12978 v1.6 contains the damage by forcing canonical local `firm_name`, rejecting mismatched GCP output, and logging the mismatch to #542.

</div>

## Function

<Card icon="play">

```
thesis/build-investment-thesis-in-gcp — 12978
```

**Public entry point.** Gathers investor context in Xano, POSTs it to the GCP (Go / Cloud Run) backend which does the LLM + embedding work, then upserts the returned thesis metadata into the Xano `investment_theses` table. Workspace 3 (OrbiterV2).

</Card>

## June 23, 2026 Smoke-Run Findings

The 100-subject smoke task exposed two separate company-side issues:

| Area | Finding | Current state |
|---|---|---|
| Queue containment | `queue_enrich_person` (#582) and `queue_enrich_company` (#583) stayed at zero during the run. | Recursive queue fan-out is contained. |
| Sandbox cap | With kill switches at roughly `300/300`, person and company leaf expansion hit the company cap around `master_company` id `303`. | The active run is not a clean QA pass; rerun with a higher cap after wiping sandbox. |
| Task order | #185 v1.2 seeded people first, so person leaf-company creation raced ahead of the 50 seed orgs. | #185 v1.3 now seeds organizations first. |
| Org dispatch | Fresh seed orgs relied on nested async dispatch from `master-company-new`; request history made this hard to verify. | #185 v1.3 runs org get/add sync with `hold_downstream=true`, then explicitly dispatches `mvp/enrich/enrich-master-company` for the returned `master_company_id`. |
| Company thesis content | Cloud Run often returns a thesis for a lead/co-investor or unrelated firm instead of the requested company. | #12978 v1.6 rejects mismatched firm names and writes a safe diagnostic summary instead of corrupt content. |

Patch summary from this pass:

| Object | Patch | Why |
|---|---|---|
| `seed-invetment-thesis-smoke-test` task (#185) | v1.3 | Org-first deterministic smoke seed. Creates/resolves orgs with `hold_downstream=true`, explicitly dispatches company enrichment, then dispatches people. |
| `thesis/build-investment-thesis-in-gcp` (#12978) | v1.6 | Replaces optional OR upsert with explicit person/company branches, forces canonical local firm names, and rejects GCP company outputs whose `firm_name` does not match the requested company. |

Recommended next clean run:

1. Clear sandbox with #4657.
2. Set kill switches to at least `600/600`.
3. Run #185 v1.3.
4. Confirm #542 has `mode: "org-first-v1.3"` and `organization_enrich_dispatched: 50`.
5. Confirm company thesis rows use canonical `firm_name`; any `REJECTED: GCP thesis firm_name mismatch` logs are backend thesis-builder defects, not Xano identity/enrichment defects.

## June 22, 2026 QA Handoff

Company-side thesis generation passed in sandbox for FirstMark after the patches below:

| Check | Result |
|---|---|
| `master_company` (#142) | FirstMark sandbox row `id=1` ended with `is_vc: true`. |
| Signal NFX company scrape | `mvp/investor/get-signal-nfx-data-company` (#12991) succeeded and wrote `investor_profile_company` (#478). |
| `investment_thesis` (#709) | Row populated with `node_uuid`, `firm_name`, `investor_type`, `last_validated_date`, and `derived_thesis_summary`. |
| `investor_profile_company` (#478) | `derived_thesis_summary` mirrored from the GCP thesis response. |
| Live data source | Spot check showed no thesis/profile writes for live FirstMark (`master_company_id=421`), so the QA runs stayed in sandbox. |

Known good sandbox thesis summary written by #12978 v1.4:

> FirstMark is a thematic investor focused on AI-native infrastructure and data platforms, backing technical founders with deep domain expertise, typically at Series A stage, favoring companies with strong technical moats and product-led growth.

### Functions patched

| Function | Patch | Why |
|---|---|---|
| `mvp/investor/get-signal-nfx-data-company` (#12991) | v1.2 | Successful Signal NFX company scrapes now write `investor_profile_company` (#478), mark `master_company.is_vc = true`, and mirror `signal_page` / `signal_json` to `permalink_company`. |
| `mvp/enrich/enrich-master-company` (#12992) | v4.9 | If `fundable_org_id` already exists, copy `fundable_organizations.is_investor` into `master_company.is_vc` before Signal/thesis gates. Existing `company_enrich_data.signal_nfx_json` also counts as investor proof. |
| `mvp/enrich/enrich-master-company` (#12992) | v4.10 | Reassert `is_vc = true` after `update-company-node`, because later phases could overwrite the company row back to `is_vc: false`. |
| `mvp/enrich/enrich-master-company` (#12992) | v4.11 | Treat an existing `investment_thesis` row with empty `derived_thesis_summary` as incomplete and rebuild it. This prevents one failed/empty pre-patch run from permanently suppressing thesis generation. |
| `thesis/build-investment-thesis-in-gcp` (#12978) | v1.2 | Avoid local variable name `$response`; Xano serialized `$response|get...` into an invalid `$response[""]|get...`, causing `Unable to locate response:` crashes. |
| `thesis/build-investment-thesis-in-gcp` (#12978) | v1.3 | Added response-shape diagnostics and fallback parsing for alternate Cloud Run response wrappers. |
| `thesis/build-investment-thesis-in-gcp` (#12978) | v1.4 | Fixed the observed Cloud Run wrapper shape: `response.result = { data, error, meta }`. Now reads thesis fields from `response.result.data.*` and mirrors `derived_thesis_summary` to #478/#475 when present. |
| `thesis/build-investment-thesis-in-gcp` (#12978) | v1.6 | Company safety rails: explicit person/company upsert branches, canonical local `firm_name`, and GCP firm-name mismatch rejection to prevent corrupt company thesis rows. |
| `mvp/enrich/process-person-phase-9` (#12830) | v3.3 | Added a gated final-pass thesis build for angel/person investors after `investor_profile_person` has been refreshed. |
| `mvp/enrich/enrich-master-person` (#13040) | v3.18 | Calls early Phase 9 without thesis, then final Phase 9 with `build_investment_thesis: true` so the thesis sees complete person context. |
| `thesis/gather-investor-context-v3` (#12911) | v3.3 | Person branch forces Signal NFX and `person-extract-cb-signal` before reading `permalink_person` context, making direct #12978 person calls safer. |
| `thesis/build-investment-thesis-in-gcp` (#12978) | v1.5 | Defaults `last_validated_date` to today's `YYYY-MM-DD` when Cloud Run returns a successful thesis without that field. |
| `mvp/resolve/resolve-edges-certifications` (#12578) | v2.5 | Scoped every certification query to the input `master_person_id` so one person run cannot process unrelated certification backlog. |
| `mvp/resolve/resolve-edges-honor` (#4574) | v4 | Scoped honor issuer-resolution queries to the input `master_person_id` so Phase 7 stays bounded. |

Other related changes from this workstream:

- `mvp/firecrawl/scrape` (#4515) was updated earlier so Signal NFX scraping uses the current Firecrawl scrape API shape.
- Clear/reset function #4657 was patched earlier for fresher sandbox QA wipes; use it before repeating full person/company smoke tests.

### Root causes fixed

1. **Company investor classification was being lost.** FirstMark had a `fundable_org_id`, and the matching `fundable_organizations` row had `is_investor: true`, but the final `master_company` row was ending as `is_vc: false`. #12992 v4.9 repairs `is_vc` before Signal/thesis gates, and #12992 v4.10 reasserts it after `update-company-node`.
2. **Signal NFX wrote the profile, but thesis stayed empty.** #478 existed, but #709 contained an empty row. The company orchestrator skipped future thesis builds because it only checked row existence/staleness. #12992 v4.11 treats an empty `derived_thesis_summary` as incomplete and rebuilds.
3. **#12978 had response-handling failures.** The `$response` local variable name caused bad XanoScript serialization, and the Cloud Run payload was nested under `response.result.data`. #12978 v1.4 reads from the observed wrapper and writes `derived_thesis_summary` to #709 plus the relevant profile table.
4. **Person-side thesis was never called at the right point.** The early Phase 9 investor-profile pass ran before later person context finished, and no final thesis pass existed. #12830 v3.3 and #13040 v3.18 add a final thesis build after person context and current-role sync are complete.
5. **Phase 7 could process unrelated rows.** Certification and honor resolvers had unscoped table queries, which could make a Scott run chew through unrelated backlog before reaching final Phase 9. #12578 v2.5 and #4574 v4 scope those resolvers to the input person.
6. **Company Cloud Run thesis output can bind to the wrong investor.** June 23 smoke rows showed valid company contexts coming back with unrelated `firm_name` values such as `Long Journey Ventures`, `StepStone Group`, and `Greylock Partners`. #12978 v1.6 prevents corrupt writes, but the Cloud Run builder still needs a backend fix so summaries are generated for the target entity.

### Sandbox QA evidence

The final passing company test used:

```json
{
  "master_company_id": 1,
  "data_source_id": 0,
  "cascade_depth": 0
}
```

Expected table state after a passing company run:

| Table | Query | Expected |
|---|---|---|
| `master_company` (#142) | `id = 1` | `is_vc: true` |
| `investor_profile_company` (#478) | `master_company_id = 1` | Signal fields plus non-empty `derived_thesis_summary` |
| `investment_thesis` (#709) | `master_company_id = 1` | Non-empty `node_uuid`, `firm_name`, `investor_type`, `last_validated_date`, `derived_thesis_summary` |
| `log_crash` (#542) | `master_company_id = 1` | Latest #12992 log should be `qa_passed: true`; no new `WARN: GCP thesis response missing derived_thesis_summary` after v1.4 |
| `enrich_history_company` (#404) | `master_company_id = 1` | Latest `Base Company Enrich` row succeeds; Signal NFX row exists from the initial scrape |

Live spot check:

- Live FirstMark exists as `master_company_id = 421`.
- Live #709 and #478 were empty for `421` during this QA session.
- Live `master_company_id = 1` is Montana State University, not FirstMark.

### Person-side sandbox QA

Fresh Scott Belsky sandbox run on June 23, 2026 used `master_person_id = 1` and returned `node_uuid = 847fc772-da52-4050-a9ae-95dfb4577ca6`. The run completed after the Phase 7 resolver scoping patches and final person-thesis gate.

| Table | What to check |
|---|---|
| `master_person` | Scott Belsky row exists, `visibility = true`, `master_company_id = 2`, `current_title = Partner`, `full_enrich = true`. |
| `investor_profile_person` (#475) | Signal/Fundable investor fields are populated and `derived_thesis_summary` mirrors the #709 thesis summary. |
| `investment_thesis` (#709) | Row exists for `master_person_id = 1` with non-empty `node_uuid`, `firm_name`, `investor_type`, and `derived_thesis_summary`. The observed row was created before #12978 v1.5, so `last_validated_date` was null on that run; future successful builds default the date in Xano. |
| `log_crash` (#542) | Latest person logs show `investment thesis build passed`, final `phase-9`, `phase-10`, `phase-11`, `phase-12`, and `all phases passed`. |
| `enrich_history_person` | `Master Person Enrich` closed `processing = false`, `enrich_success = true`; Signal NFX and xAI succeeded; Crunchbase remains false/idle when no Crunchbase permalink exists. |
| Queues | Person queue (#582) empty. Company queue (#583) bounded to Scott-sourced leaf companies, all `processing = false`, `cascade_depth = 1`. |

## Smoke Test Subjects

Use these live Fundable-derived seed/enrich smoke-test fixtures for AI-related investment-thesis paths. People come from `fundable_people` (#667) through recent `fundable_angel_investments` (#679) attached to AI/agent/LLM-related deal rows and are listed by LinkedIn URL because `mvp/get-add/master-person-new` seeds from profile links. Organizations come from `fundable_organizations` (#666) through `fundable_institutional_investments` (#678) attached to Seed or pre-Seed AI-related deal rows and are listed by bare website domain because `mvp/get-add/master-company-new` seeds from domains.

The inactive live-data task `seed-invetment-thesis-smoke-test` (#185) mirrors these rows as object-array variables, then dispatches full top-level get-add calls with `queue=false` and `cascade_depth=0` for manual smoke runs.

### People

| Name | LinkedIn URL |
|---|---|
| David Bartman | https://www.linkedin.com/in/david-bartman-04062518/ |
| Fei-Fei Li | https://www.linkedin.com/in/fei-fei-li-4541247/ |
| Lukasz Kaiser | https://www.linkedin.com/in/lukaszkaiser/ |
| Vijay B. | https://www.linkedin.com/in/vbolina/ |
| Joe Sullivan | https://www.linkedin.com/in/joesu11ivan/ |
| Vincent Bouvier | https://www.linkedin.com/in/vincentbouvier/ |
| Felix Blossier | https://www.linkedin.com/in/felix-blossier/ |
| Quentin de Metz | https://www.linkedin.com/in/quentindemetz/ |
| Arthur Mensch | https://www.linkedin.com/in/arthur-mensch/ |
| Olivier Brourhant | https://www.linkedin.com/in/brourhant/ |
| Régis Samuel | https://www.linkedin.com/in/regis-samuel/ |
| Raphaël Nahum | https://www.linkedin.com/in/rnahum/ |
| David Ioannidis | https://www.linkedin.com/in/david-ioannidis-4491b0182/ |
| Mei-Shan Tan | https://www.linkedin.com/in/mei-shan-tan-ba7166a/ |
| Wai Ling Chan | https://www.linkedin.com/in/wai-ling-chan-75581714/ |
| Todd Persen | https://www.linkedin.com/in/toddpersen/ |
| Ben Sigelman | https://www.linkedin.com/in/bensigelman/ |
| Arman Suleimenov | https://www.linkedin.com/in/armansu/ |
| Ant Wilson | https://www.linkedin.com/in/ant-wilson-46179937/ |
| Nurdaulet Bazylbekov | https://www.linkedin.com/in/nurbaz/ |
| Paul Forster | https://www.linkedin.com/in/paul-forster/ |
| Jorn van Dijk | https://www.linkedin.com/in/jorn-vandijk/ |
| Koen Bok | https://www.linkedin.com/in/koen-bok-029592137/ |
| Brendan Foody | https://www.linkedin.com/in/brendan-foody-2995ab10b/ |
| Noam Brown | https://www.linkedin.com/in/noam-brown-8b785b62/ |
| Yash Patil | https://www.linkedin.com/in/yash-s-patil/ |
| Mark Strande | https://www.linkedin.com/in/markstrande/ |
| Mattias Miksche | https://www.linkedin.com/in/mattiasmiksche/ |
| Kyle Tibbitts | https://www.linkedin.com/in/kyletibbitts/ |
| Shuo Wang | https://www.linkedin.com/in/shuooo/ |
| Leo Polovets | https://www.linkedin.com/in/lpolovets/ |
| Julian Weisser | https://www.linkedin.com/in/julianweisser/ |
| Sean Williams | https://www.linkedin.com/in/sean-weareunknown/ |
| Sam Winward | https://www.linkedin.com/in/sam-winward/ |
| Hugo Rodger-Brown | https://www.linkedin.com/in/hugorodgerbrown/ |
| Nikesh Arora | https://www.linkedin.com/in/nikesh-arora-02894670/ |
| Amit Agarwal | https://www.linkedin.com/in/amitto/ |
| Jeff Dean | https://www.linkedin.com/in/jeff-dean-8b212555/ |
| Elad Gil | https://www.linkedin.com/in/eladgil/ |
| Mark Shmulevich | https://www.linkedin.com/in/mark-shmulevich/ |
| Alexey Dosovitskiy | https://www.linkedin.com/in/alexey-dosovitskiy-3694a1a9/ |
| Vadim Barshtak | https://www.linkedin.com/in/barshtak/ |
| Konstantin Vinogradov | https://www.linkedin.com/in/kvinogradov/ |
| David Peterson | https://www.linkedin.com/in/davidlpeterson3/ |
| Brad Peltz | https://www.linkedin.com/in/brad-peltz-149771129/ |
| JB Rudelle | https://www.linkedin.com/in/jbrudelle/ |
| Gary Anssens | https://www.linkedin.com/in/gary-anssens/ |
| Michael Benabou | https://www.linkedin.com/in/michael-benabou-a572936/ |
| Mike Knoop | https://www.linkedin.com/in/mikeknoop/ |
| Elias Torres | https://www.linkedin.com/in/eliast/ |

### Organizations

| Name | Domain |
|---|---|
| TTV Capital | ttvcapital.com |
| Correlation Ventures | correlationvc.com |
| Weekend Fund | weekend.fund |
| FJ Labs | fjlabs.com |
| HF0 | hf0.com |
| Mintaka Ventures | mintakaventures.com |
| Cambrian Ventures | cambrianhq.com |
| Bling Capital | blingcap.com |
| Sapphire Sport | sapphireventures.com |
| CEAS Investments | ceasinvestments.com |
| Betty and Will Ventures | bettyandwill.com |
| Will Ventures | willventures.com |
| A16Z Speedrun | a16z.com |
| LMNT Ventures | lmnt.vc |
| Penny Jar Capital | pennyjar.com |
| Telegraph Hill Capital | thcap.com |
| Hyundai Motor Company | hyundai.com |
| Collaborative Fund | collabfund.com |
| South Park Commons | southparkcommons.com |
| Naver | navercorp.com |
| Susa Ventures | susaventures.com |
| Repeat | repeat.vc |
| Tekton Ventures | tektonventures.com |
| People Ventures | people.ventures |
| Golden Sparrow Ventures | goldensparrow.vc |
| Creandum | creandum.com |
| Z21 Ventures | z21.ventures |
| Hersir Ventures | hersir.ventures |
| Leap435 | leap435.vc |
| Asterion Ventures | asterionventures.com |
| Zero Infinity Partners | zeroinfinitypartners.com |
| Heroic Ventures | heroicvc.com |
| EIT Urban Mobility | eiturbanmobility.eu |
| PeakBridge | peakbridge.vc |
| āltitude | altitude-vc.com |
| Gekko Capital Partners | gekkocapital.partners |
| seed + speed Ventures | seedandspeed.com |
| Flybridge Capital Partners | flybridge.com |
| NFX | nfx.com |
| Carao Ventures | caraov.com |
| Textbook VC | textbook.vc |
| Pear VC | pear.vc |
| Underscore VC | underscore.vc |
| Kleiner Perkins | kleinerperkins.com |
| NovaWave Capital | novawavecapital.com |
| Kima Ventures | kimaventures.com |
| Better Angle | better-angle.com |
| Super Capital | supercapital.club |
| another.vc | another.vc |
| Aonia Ventures | aonia.vc |

## `gather-investor-context-v3`

<Card icon="database">

```
thesis/gather-investor-context-v3 — 12911
```

**Context builder used by #12978.** This is the Xano-owned function that resolves the investor, pulls profile/deal context, and returns the payload that the GCP thesis builder uses as source material.

</Card>

### Inputs

| Input | Type | Default | Notes |
|---|---:|---:|---|
| `master_person_id` | int | — | Angel / individual investor path. Required when `master_company_id` is empty. |
| `master_company_id` | int | — | VC fund / firm path. Required when `master_person_id` is empty. |
| `max_deals` | int | `100` | Cap on returned investment examples. |
| `recency_months` | int | `36` | Deal date window used for the recent-deal sample. |

### Responsibilities

1. **Resolve identity.** Map the `master_person` or `master_company` row to the matching Fundable investor identity (`fundable_personnel_id` or `fundable_org_id`).
2. **Build `entity_context`.** Pull the investor profile context that the thesis LLM should trust: identity, bio/firm description, Signal NFX / Crunchbase-derived context, investment totals, and related profile data.
3. **Pull recent deals.** Fetch up to `max_deals` recent Fundable investment rows joined to the underlying `fundable_deals` facts.
4. **Filter by recency.** Keep the deal sample inside the `recency_months` window. Existing docs note this is done in-function because Xano's array/UUID filtering is unreliable for this shape.
5. **Return the thesis source bundle.** Shape is expected to be `{entity_type, entity_context, deals[], stats}`.

### Current build requirements

- **Signal attempt gate before context finalization.** Before returning `$context`, confirm Signal NFX has been attempted for the investor. If not, run the relevant Signal scraper, then refetch the Signal/profile fields before building the final `entity_context`.
- **Person path must enforce Signal before return.** For `master_person_id`, force the person-side Signal NFX attempt before this function's output is treated as thesis-ready.
- **Company path must prefer the new Signal location.** For `master_company_id`, read `company_enrich_data.signal_nfx_json` first, with legacy fallback to `permalink_company.signal_json`.
- **Profile tables must stay current.** Person/angel context should refresh `investor_profile_person` (#475); company/fund context should refresh `investor_profile_company` (#478).
- **Current QA boundary.** Company-side context worked in the FirstMark sandbox run. Person-side context still needs a fresh direct/person QA pass.

### Context finalization gate

This is the behavior we need before #12911 returns the final context bundle:

<Warning>
Today this guarantee is **path-dependent**. Company thesis builds reached through `mvp/enrich/enrich-master-company` (#12992) hit the Signal NFX gate before #12978 gathers context. Person thesis builds are only safe when called after person enrichment Phase 9 has completed. A direct/manual call to #12978 can still pull context before Signal has ever been attempted unless we add the gate below.
</Warning>

1. **Check attempt status, not only data presence.** A blank Signal payload does not tell us whether Signal was unavailable or whether we never tried. Use the history tables as the source of truth:
   - Person: `enrich_history_person` where `master_person_id == input.master_person_id` and `source == "Signal NFX"`.
   - Company: `enrich_history_company` where `master_company_id == input.master_company_id` and `source == "Signal NFX"`.
2. **If no Signal attempt exists, run the scraper before context is finalized.**
   - Person: call `mvp/investor/get-signal-nfx-data` (#2769), then run/confirm `person-extract-cb-signal` so `investor_profile_person` (#475) is refreshed.
   - Company: call `mvp/investor/get-signal-nfx-data-company` (#12991), then refresh/read the company-side profile context for `investor_profile_company` (#478).
3. **Refetch after the scrape attempt.** Do not keep using stale rows loaded before the scraper ran. Reload `permalink_person`, `company_enrich_data`, and the relevant investor profile table before constructing `entity_context`.
4. **Do not block forever when Signal has no match.** If a scraper attempt exists but produced no parsed Signal JSON, treat that as "attempted, no usable Signal data" and continue with Crunchbase/Fundable/profile context.

### Person enforcement to add here

For the person branch, do not rely on the caller having already completed Phase 9. If `master_person_id` is present, #12911 should enforce the Signal attempt itself before it builds the final `entity_context`.

Recommended implementation:

1. **Call the Signal scraper before loading final person context.** Run `mvp/investor/get-signal-nfx-data` (#2769) for `master_person_id`. This can be unconditional because #2769 already dedups on `enrich_history_person.source == "Signal NFX"` within the last 30 days.
2. **Refresh CB/Signal extraction.** After #2769 returns, query `permalink_person` for the same `master_person_id`. If a row exists, run `mvp/investor/person-extract-cb-signal` with `person_permalink_id`.
3. **Reload profile/context rows.** Refetch `permalink_person`, `investor_profile_person` (#475), and Fundable person/deal rows before assembling `entity_context`.
4. **Continue even when Signal has no profile.** The requirement is that Signal was attempted. If no Signal URL or parsed JSON exists after the attempt, continue with Crunchbase, Fundable, and existing profile data.

That makes direct/manual #12978 calls safe for person investors: **#12978 → #12911 → force person Signal attempt → refresh `investor_profile_person` → finalize context → GCP thesis build**.

## When Signal NFX scraping runs

### Company / fund path

Company Signal NFX scraping runs inside `mvp/enrich/enrich-master-company` (#12992), **Step 5**, before the company investment-thesis build/rebuild step.

It runs only when all of these are true:

- `master_company.fundable_org_id` is set.
- `master_company.is_vc` is true.
- `company_enrich_data.signal_nfx_json` is empty.
- The company Signal scraper has not already written a recent `enrich_history_company` row with `source == "Signal NFX"` in the last 30 days.

When it runs, `mvp/investor/get-signal-nfx-data-company` (#12991) does:

1. Find the Signal NFX company URL via Brave (`brave/get-signalnfx_domain`).
2. Scrape the Signal page via Firecrawl.
3. Write raw scrape output to `company_enrich_data.signal_nfx_scrape`.
4. Parse the markdown via `mvp/investor/parse-signal-nfx-company` (#12990).
5. Write parsed JSON to `company_enrich_data.signal_nfx_json`.
6. Write or refresh `investor_profile_company` (#478), mark `master_company.is_vc = true`, and mirror `signal_page` / `signal_json` to `permalink_company`.

Then #12992 Step 6 decides whether to run `thesis/build-investment-thesis-in-gcp` (#12978). That means the company path is ordered correctly: **Signal NFX first, thesis build second**.

### Person / angel path

Person Signal NFX scraping runs inside `process-person-phase-9` (#12830), but only after the person is already identified as an angel investor.

It runs only when:

- `master_person.is_angel` is true.
- `process-person-phase-9` is reached by the person enrichment orchestrator.
- `mvp/investor/get-signal-nfx-data` (#2769) has not already written a recent `enrich_history_person` row with `source == "Signal NFX"` in the last 30 days.

When it runs, #2769 does:

1. Find the Signal NFX person URL via `brave/get-signalnfx-person-url`.
2. Scrape the Signal page.
3. Write raw scrape output to `permalink_person.signal_person`.
4. Parse the markdown via `mvp/investor/parse-signal-nfx`.
5. Write parsed JSON to `permalink_person.signal_extract`.

After that, Phase 9 calls `mvp/investor/person-extract-cb-signal`, which writes/refreshes `investor_profile_person` (#475). Then the Fundable overlay fills gaps without overwriting Signal/CB-derived values.

### Important dependency for #12978

`thesis/build-investment-thesis-in-gcp` (#12978) does **not** scrape Signal NFX itself. It calls `thesis/gather-investor-context-v3` (#12911), which reads whatever Signal context already exists:

- Company path: `company_enrich_data.signal_nfx_json` preferred, fallback to `permalink_company.signal_json`.
- Person path: `permalink_person.signal_extract`.

Until the person enforcement above is implemented, a direct #12978 call for a person before Phase 9 has run can still miss Signal NFX and investor-profile enrichment. After the enforcement lands, direct calls are acceptable because #12911 itself forces the Signal attempt before finalizing context.

## Scraper → profile table audit

### Person side

`mvp/investor/get-signal-nfx-data` (#2769) **does not write `investor_profile_person` (#475) directly.** It writes:

- raw Signal scrape → `permalink_person.signal_person`
- parsed Signal JSON → `permalink_person.signal_extract`
- attempt/dedup history → `enrich_history_person.source == "Signal NFX"`

The profile-table write happens in `mvp/investor/person-extract-cb-signal` (#2687), which is called after the Signal scrape in Phase 9. #2687 upserts `investor_profile_person` by `master_person_id` and writes:

- `exits_summary`
- `board_advisory_summary`
- `investor_type`
- `investments`
- `location`
- `investment_range`
- `investment_sweet_spot`
- `sectors_stages`

### Company side

`mvp/investor/get-signal-nfx-data-company` (#12991) now writes `investor_profile_company` (#478) directly after a successful Signal NFX company scrape. It also writes:

- raw Signal scrape -> `company_enrich_data.signal_nfx_scrape`
- parsed Signal JSON -> `company_enrich_data.signal_nfx_json`
- attempt/dedup history -> `enrich_history_company.source == "Signal NFX"`
- compatibility copy -> `permalink_company.signal_page` / `permalink_company.signal_json`
- investor classification repair -> `master_company.is_vc = true`

`mvp/investor/parse-signal-nfx-company` (#12990) is parser-only: it returns JSON and does not write #478.

Current #478 schema:

| Column | Notes |
|---|---|
| `master_company_id` | row key |
| `location` | text |
| `exits` | text |
| `investment_range` | text |
| `investment_sweet_spot` | text |
| `investment_stages` | text |
| `derived_thesis_summary` | observed in staging row output; verify schema visibility before implementation |

## Pre-return profile summary write

#12978 v1.4 updates the relevant investor profile table **after the GCP response is parsed and before returning success**.

Required behavior:

1. Read `response.result.data.derived_thesis_summary` from the GCP response.
2. If `master_person_id` is present, upsert/update `investor_profile_person` (#475) for that person.
3. If `master_company_id` is present, upsert/update `investor_profile_company` (#478) for that company.
4. Write `updated_at: now` and store the derived thesis summary.
5. Do this before the final `return`, so the profile table is consistent with the generated thesis.

Schema note:

- #475 records already return `derived_thesis_summary` in table content, alongside the older `llm_investor_summary`.
- #478 records return `derived_thesis_summary` in the passing sandbox company test.
- Before extending this path to a new datasource, verify the field is visible in the Xano editor/schema for that datasource. If it is not visible, add/repair the column before writing to it.
- Prefer writing directly to `derived_thesis_summary`, not overloading #475's older `llm_investor_summary`.

## Firecrawl scrape audit

The shared wrapper `mvp/firecrawl/scrape` (#4515) was updated earlier for the current Firecrawl v2 scrape endpoint:

```text
POST https://api.firecrawl.dev/v2/scrape
```

Firecrawl's current [Scrape API docs](https://docs.firecrawl.dev/api-reference/endpoint/scrape) document `formats`, `onlyMainContent`, `maxAge`, `timeout`, `proxy`, `removeBase64Images`, and related scrape parameters.

Recommended Signal NFX scrape settings:

```json
{
  "formats": ["markdown", "links"],
  "onlyMainContent": true,
  "maxAge": 172800000,
  "proxy": "auto"
}
```

Notes for Signal NFX scrapes:

- **Use `proxy: "auto"`.** Firecrawl v2 documents `basic`, `enhanced`, and `auto`; `stealth` is not in the current v2 enum. `auto` starts cheaper and retries with enhanced proxy if needed.
- **Keep `formats: ["markdown", "links"]`.** The Signal parsers need markdown content, and the person path also preserves links as `linksOnPage`.
- **Prefer `onlyMainContent: true` for Signal pages.** This is the v2 default and should reduce nav/footer noise. If Signal profile fields disappear in testing, fall back to `false`.
- **Keep `maxAge: 172800000` unless we need fresher than two days.** The Xano-level Signal dedup is 30 days; Firecrawl's two-day cache is acceptable for an attempt gate and avoids unnecessary re-scrape cost.
- **Keep the 60s timeout.** It matches Firecrawl's documented default and is sufficient for a single Signal profile page.
- **Do not enable `onlyCleanContent` by default.** It is a beta LLM cleanup pass; useful for noisy pages, but unnecessary until Signal markdown quality proves to be a problem.

## What it does, step by step

1. **Validate input.** Requires at least one of `master_person_id` or `master_company_id` — fails with an `inputerror` precondition if both are null.

2. **Gather investor context.** Runs `thesis/gather-investor-context-v3` with `max_deals: 100` and `recency_months: 36` → `$context`. This is the Xano-owned layer (it has `master_*` and `fundable_*`); the gathered bundle is everything the thesis builder needs to reason about the investor. For `master_person_id`, this gather step must enforce the person Signal NFX attempt before returning final context.

3. **Fetch GCP credentials.** Runs `mvp/file/get-gcp-url-key` -> `$keys`, which supplies the Cloud Run `gcp_backend_url` and key material.

4. **Build the request body.** Assembles `$request_body` with `master_person_id` / `master_company_id` (only if non-empty), the full `$context`, and the current `data_source` from `$env`.

5. **POST to the Go Cloud Run backend.** Sends `$request_body` to `{gcp_backend_url}/v1/outcomes/investment-thesis/build` as JSON, with a 180s timeout. The backend runs the LLM orchestration + embeddings and returns the built thesis.

6. **Guard on success.** A precondition requires `status == 200`; otherwise it raises `badrequest` with the raw GCP response as the payload.

7. **Read the result.** Extracts thesis fields from `response.result.data.*`. This is the observed Cloud Run wrapper shape: `response.result = { data, error, meta }`.

8. **Look up any existing thesis.** Queries `investment_theses` for a row matching the same `master_person_id` or `master_company_id`, returning its `id` (or `0` if none).

9. **Upsert the thesis row.** `db.add_or_edit` on `investment_theses` keyed by that `id` writes `updated_at`, `node_uuid` (= the GCP `thesis_id`), `firm_name`, `investor_type`, `last_validated_date`, `derived_thesis_summary`, and the relevant master id.

10. **Update the investor profile summary.** Before returning, write `derived_thesis_summary` to `investor_profile_person` (#475) or `investor_profile_company` (#478), depending on whether this is a person or company thesis build.

11. **Return** the upserted `investment_theses` row.

## Related

- `thesis/gather-investor-context-v3` — the context-gather step (Xano-owned `master_*` / `fundable_*` layer).
- `mvp/file/get-gcp-url-key` — supplies the Cloud Run backend URL + key.
- [Find investors — AlloyDB build](/guides/open-work/vectors-alloydb/find-investors-engine) — the broader migration plan this function fits into. The earlier Xano-only builder was `thesis/build-investment-thesis-v21` (#12916).
- [Run Base Company Enrich](/guides/open-work/run-base-company-enrich)
- [Find Investors - Production Pipeline](/guides/open-work/suggestion-core-concepts/find-investors/production-pipeline)
- [Investment Thesis Schema](/guides/open-work/suggestion-core-concepts/find-investors/thesis-schema)

## Iteration log

_Progress notes land here as we iterate. Newest first._

- **June 23 smoke-task hardening + company thesis guard** — #185 v1.3 now seeds organizations first and explicitly dispatches company enrichment from the task. #12978 v1.6 prevents corrupt company thesis writes by forcing canonical local firm names, replacing optional OR upsert matching with explicit person/company branches, and rejecting mismatched Cloud Run firm output. The remaining company-side blocker is the Cloud Run thesis builder returning the wrong investor from otherwise valid company contexts.
- **June 23 person-side QA passed** — Scott Belsky sandbox run now closes `master_person.full_enrich = true`, writes `investment_thesis` (#709), mirrors `derived_thesis_summary` to #475, and keeps queues bounded. Published #12830 v3.3, #13040 v3.18, #12911 v3.3, #12978 v1.5, #12578 v2.5, and #4574 v4 during the pass.
- **June 22 QA handoff added** — company-side FirstMark sandbox test passed after #12991 v1.2, #12992 v4.9-v4.11, and #12978 v1.2-v1.4. Person-side investor QA remains open.
- **Fixed GCP response parsing** — #12978 v1.4 reads the observed wrapper from `response.result.data.*` and mirrors `derived_thesis_summary` to #478 or #475.
- **Fixed company thesis rebuild gating** — #12992 v4.11 treats an existing #709 row with empty `derived_thesis_summary` as incomplete, so failed pre-patch thesis rows do not suppress rebuilds.
- **Fixed company investor classification drift** — #12992 v4.9 and v4.10 repair/reassert `master_company.is_vc` from Fundable investor evidence before and after downstream company updates.
- **Added company profile-table write** — #12991 v1.2 writes `investor_profile_company` (#478) after successful Signal NFX company scrape.
- **Added scraper/profile-table audit** — original audit found #2769 writes Signal data to `permalink_person` and #2687 writes #475; the company-side #478 gap was closed later in #12991 v1.2.
- **Added pre-return profile summary write requirement** — #12978 needed to write `derived_thesis_summary` to #475 or #478 before returning; this landed in #12978 v1.4 using the `response.result.data.*` wrapper.
- **Added Firecrawl audit** — #4515 uses `/v2/scrape`; Signal NFX scrapes should use a documented v2 proxy value (`auto` recommended), not legacy `stealth`.
- **Strengthened person enforcement** — for `master_person_id`, #12911 should force `get-signal-nfx-data` (#2769), refresh CB/Signal extraction, refetch `investor_profile_person` (#475), then finalize context.
- **Added Signal attempt gate** — #12911 must check whether Signal NFX has ever been attempted before finalizing context; if not, run the relevant scraper, refetch context/profile rows, then return the source bundle.
- **Clarified guarantee boundary** — Signal is completed/attempted before context only on orchestrated paths today; direct #12978 calls still need an explicit pre-context gate.
- **Added Signal NFX timing section** — company scrape runs in #12992 Step 5 before thesis Step 6; person scrape runs in #12830 Phase 9 when `is_angel` is true; #12978 only reads existing Signal context through #12911.
- **Added `gather-investor-context-v3` section** — captured #12911 inputs, responsibilities, source-bundle shape, and the current person-first/profile-table requirements.
- **Added next-pass goals** — person-first Signal NFX scrape + deep research before thesis context is trusted, plus required writes to `investor_profile_person` (#475) and `investor_profile_company` (#478).
- **Initial doc** — captured the early shape of #12978 when it still stopped after context gathering and the GCP path had not been exercised end-to-end.

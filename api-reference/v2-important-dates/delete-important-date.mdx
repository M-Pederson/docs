---
title: "Delete Important Date"
api: "DELETE https://xh2o-yths-38lt.n7c.xano.io/api:mGnfV_UB/important-dates/{important_dates_id}"
description: "Delete an important date record."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField path="important_dates_id" type="integer" required>
  The ID of the important date to delete. Must be at least 1.
</ParamField>

<ResponseField name="result" type="object">
  Empty object on success.
</ResponseField>

<RequestExample>
```bash cURL
curl -X DELETE \
  https://xh2o-yths-38lt.n7c.xano.io/api:mGnfV_UB/important-dates/1 \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: live" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_TOKEN"
```
</RequestExample>

<ResponseExample>
```json 200
{}
```
</ResponseExample>

---
title: "Delete Birthday"
api: "DELETE https://xh2o-yths-38lt.n7c.xano.io/api:mGnfV_UB/birthdays/{birthday_join_id}"
description: "Delete a birthday record."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField path="birthday_join_id" type="integer" required>
  The ID of the birthday record to delete. Must be at least 1.
</ParamField>

<ResponseField name="result" type="object">
  Empty object on success.
</ResponseField>

<RequestExample>
```bash cURL
curl -X DELETE \
  https://xh2o-yths-38lt.n7c.xano.io/api:mGnfV_UB/birthdays/1 \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: live" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_TOKEN"
```
</RequestExample>

<ResponseExample>
```json 200
{}
```
</ResponseExample>

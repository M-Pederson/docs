---
title: "Disconnect Grant"
api: "DELETE https://xh2o-yths-38lt.n7c.xano.io/api:s_Mv_Fpv/nylas-grants/disconnect"
description: "Revoke a Nylas grant and remove the connected account."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField body="nylas_grant_id" type="integer" required>The grant ID to disconnect.</ParamField>

<RequestExample>
```bash cURL
curl -X DELETE \
  'https://xh2o-yths-38lt.n7c.xano.io/api:s_Mv_Fpv/nylas-grants/disconnect' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -d '{"nylas_grant_id": 1}'
```
</RequestExample>

<ResponseExample>
```json 200
{}
```
</ResponseExample>

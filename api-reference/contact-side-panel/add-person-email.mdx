---
title: "Add Person Email"
api: "POST https://xh2o-yths-38lt.n7c.xano.io/api:iTL91sBE/add/my_person/email"
description: "Create and add an email record to a person."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField body="my_person_id" type="integer" required>
  The person ID to add the email to.
</ParamField>
<ParamField body="email_address" type="string" required>
  The email address to add.
</ParamField>
<ParamField body="email_type" type="string">
  Type of email (e.g. `work`, `personal`).
</ParamField>
<ParamField body="active" type="boolean">
  Whether the email is active. Defaults to `true`.
</ParamField>
<ParamField body="email_note" type="string">
  Optional note about the email.
</ParamField>

<ResponseField name="id" type="integer">Record ID.</ResponseField>
<ResponseField name="my_person_id" type="integer">Person ID.</ResponseField>
<ResponseField name="master_email_id" type="integer">Master email record ID.</ResponseField>

<RequestExample>
```bash cURL
curl -X POST \
  'https://xh2o-yths-38lt.n7c.xano.io/api:iTL91sBE/add/my_person/email' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -d '{
    "my_person_id": 1,
    "email_address": "john@example.com",
    "email_type": "work"
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1700000000,
  "my_person_id": 1,
  "master_email_id": 10,
  "primary": false,
  "user_id": 5
}
```
</ResponseExample>

---
title: "Edit Person Email"
api: "PUT https://xh2o-yths-38lt.n7c.xano.io/api:iTL91sBE/edit/my_person/email"
description: "Edit a master email record for a person."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField body="master_email_id" type="integer">Master email record ID.</ParamField>
<ParamField body="email_address" type="string">Updated email address.</ParamField>
<ParamField body="email_type" type="string">Email type (e.g. `work`, `personal`).</ParamField>
<ParamField body="active" type="boolean">Whether the email is active.</ParamField>
<ParamField body="email_note" type="string">Optional note.</ParamField>
<ParamField body="is_primary" type="boolean">Set as primary email.</ParamField>

<RequestExample>
```bash cURL
curl -X PUT \
  'https://xh2o-yths-38lt.n7c.xano.io/api:iTL91sBE/edit/my_person/email' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -d '{"master_email_id": 10, "email_address": "john@newdomain.com"}'
```
</RequestExample>

<ResponseExample>
```json 200
{}
```
</ResponseExample>

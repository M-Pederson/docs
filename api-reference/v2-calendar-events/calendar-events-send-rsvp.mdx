---
title: 'Create Calendar Events Send Rsvp'
api: 'POST https://xh2o-yths-38lt.n7c.xano.io/api:LR2ywW7R/calendar-events/send-rsvp'
description: 'Authentication: required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Request Body

<ParamField body="event_id" type="string" required>
  The field id in the nylas response for events
</ParamField>

<ParamField body="calendar_id" type="string" required>
  The calendar_id field.
</ParamField>

<ParamField body="grant_id" type="string" required>
  The grant_id field.
</ParamField>

<ParamField body="status" type="string" required>
  The status field.
</ParamField>

<RequestExample>
```bash cURL
curl -X POST https://xh2o-yths-38lt.n7c.xano.io/api:LR2ywW7R/calendar-events/send-rsvp \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "event_id": "example-event_id",
      "calendar_id": "example-calendar_id",
      "grant_id": "example-grant_id",
      "status": "example-status"
  }'
```
</RequestExample>

---
title: 'Add leverage_loop_question record'
api: 'POST https://xh2o-yths-38lt.n7c.xano.io/api:MkA4QsNh/leverage-loop-questions'
description: 'Add leverage_loop_question record Authentication: required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Request Body

<ParamField body="leverage_loop_suggestion_id" type="integer" required>
  The leverage_loop_suggestion_id field.
</ParamField>

<ParamField body="query" type="string" required>
  The query field.
</ParamField>

<ParamField body="selected_nodes" type="array">
  The selected_nodes field.
</ParamField>

<ParamField body="deep_research" type="boolean">
  The deep_research field.
</ParamField>

<ParamField body="team_ids" type="array">
  The team_ids field.
</ParamField>


## Response

<ResponseField name="id" type="integer">
  The id field.
</ResponseField>

<ResponseField name="created_at" type="number">
  The created_at field.
</ResponseField>

<ResponseField name="user_id" type="integer">
  The user_id field. Can be `null`.
</ResponseField>

<ResponseField name="query" type="string">
  The query field. Can be `null`.
</ResponseField>

<ResponseField name="collection_id" type="integer">
  The collection_id field. Can be `null`.
</ResponseField>

<ResponseField name="discover_id" type="integer">
  The discover_id field. Can be `null`.
</ResponseField>

<ResponseField name="serendipity_suggestion_id" type="integer">
  The serendipity_suggestion_id field. Can be `null`.
</ResponseField>

<ResponseField name="leverage_loop_suggestion_id" type="integer">
  The leverage_loop_suggestion_id field. Can be `null`.
</ResponseField>

<ResponseField name="outcome_suggestion_id" type="integer">
  The outcome_suggestion_id field. Can be `null`.
</ResponseField>

<ResponseField name="selected_nodes" type="array">
  The selected_nodes field. Can be `null`.
</ResponseField>

<ResponseField name="uuid_for_path" type="array">
  The uuid_for_path field. Can be `null`.
</ResponseField>

<ResponseField name="deep_research" type="boolean">
  The deep_research field.
</ResponseField>

<ResponseField name="team_ids" type="array">
  The team_ids field. Can be `null`.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST https://xh2o-yths-38lt.n7c.xano.io/api:MkA4QsNh/leverage-loop-questions \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "leverage_loop_suggestion_id": 1,
      "query": "example-query",
      "selected_nodes": [],
      "deep_research": true,
      "team_ids": []
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1,
  "user_id": 1,
  "query": "example-query",
  "collection_id": 1,
  "discover_id": 1,
  "serendipity_suggestion_id": 1,
  "leverage_loop_suggestion_id": 1,
  "outcome_suggestion_id": 1,
  "selected_nodes": [],
  "uuid_for_path": [],
  "deep_research": false,
  "team_ids": []
}
```
</ResponseExample>

---
title: "Add Person to Contacts"
api: "POST https://xh2o-yths-38lt.n7c.xano.io/api:iTL91sBE/add/my_person"
description: "Add a master person into the user's contacts (my_person)."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField body="master_person_id" type="integer">
  The master person ID to add to contacts.
</ParamField>

<RequestExample>
```bash cURL
curl -X POST \
  'https://xh2o-yths-38lt.n7c.xano.io/api:iTL91sBE/add/my_person' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -d '{"master_person_id": 123}'
```
</RequestExample>

<ResponseExample>
```json 200
{}
```
</ResponseExample>

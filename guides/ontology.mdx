---
title: "Graph Ontology"
description: "Complete schema reference for all FalkorDB node types and edge relationships in the Orbiter knowledge graph"
---

The Orbiter knowledge graph is built on [FalkorDB](https://www.falkordb.com/) using the openCypher query language. This section documents every node type and edge relationship in the graph, with real data examples, property definitions, and weight conventions.

## Conventions

- **Most product graph nodes carry the `Entity` base label** plus one or more type-specific labels (e.g. `Entity:Person`, `Entity:Company:VC_Firm`, `Entity:Work:Publication`); legacy non-`Entity` labels are called out in the [Master Label List](/guides/ontology/nodes#master-label-list)
- **`uuid`** is the primary identifier on every node and edge (UUID v4, generated via `randomUUID()`)
- **`name_embedding`** is a `vecf32` vector embedding on most nodes, used for semantic search
- **Weight convention:** lower number = higher importance (e.g. `FOUNDED` = 5, `WORKS_AT` = 5-40, `VOLUNTEERED_AT` = 38-50)
- **Temporal convention:** currency is derived from `valid_to IS NULL` (no end date = currently active)
- **`display_label`** is a human-readable string stored on nodes for UI rendering

## Sub-pages

<Note>
  **Last reviewed & updated: 2026-07-09** — the ontology pages were audited against the live XanoScript graph writers (workspace 3, branch `v1`).
</Note>

<CardGroup cols={3}>
  <Card title="Node Types" icon="circle-nodes" href="/guides/ontology/nodes">
    Master label list plus node types grouped by domain — People, Organizations, Location, Film & TV, Expertise, Funding, MusicBrainz, Credentials, and Work Products.
  </Card>

  <Card title="Edge Types" icon="arrows-left-right" href="/guides/ontology/edges">
    All 30\+ edge relationship types with weights, properties, and real examples.
  </Card>

  <Card title="Edge Weights" icon="scale-balanced" href="/guides/ontology/edge-weights">
    The master weight scale (lower = stronger), the deployed weight inventory, the §3.6 weight-audit record, and the WORKS_AT seniority buckets.
  </Card>

  <Card title="Indexes" icon="magnifying-glass" href="/guides/ontology/all-indexes">
    Every scalar and vector index with the copy-paste Cypher to create them, plus HNSW semantic search patterns, health tooling, and the expertise/specialty matching pipeline.
  </Card>

  <Card title="Paths" icon="route" href="/guides/ontology/paths">
    The Discover relationship-paths pipeline — FalkorDB shortest-path Cypher, node_path / node_path_hop persistence, and realtime delivery.
  </Card>
</CardGroup>
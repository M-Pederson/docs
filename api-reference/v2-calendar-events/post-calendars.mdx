---
title: 'User chooses which calendars are active and which are not'
api: 'POST https://xh2o-yths-38lt.n7c.xano.io/api:LR2ywW7R/calendars'
description: 'User chooses which calendars are active and which are not Authentication: required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Request Body

<ParamField body="calendar_id" type="integer" required>
  The calendar_id field.
</ParamField>

<ParamField body="event_widget_preference" type="boolean" required>
  The event_widget_preference field.
</ParamField>


## Response

<ResponseField name="id" type="integer">
  The id field.
</ResponseField>

<ResponseField name="created_at" type="number">
  The created_at field.
</ResponseField>

<ResponseField name="updated_at" type="number">
  The updated_at field. Can be `null`.
</ResponseField>

<ResponseField name="user_id" type="integer">
  The user_id field.
</ResponseField>

<ResponseField name="external_id" type="string">
  The external_id field.
</ResponseField>

<ResponseField name="grant_id" type="string">
  The grant_id field.
</ResponseField>

<ResponseField name="name" type="string">
  The name field.
</ResponseField>

<ResponseField name="timezone" type="string">
  The timezone field.
</ResponseField>

<ResponseField name="is_primary" type="boolean">
  The is_primary field.
</ResponseField>

<ResponseField name="read_only" type="boolean">
  The read_only field.
</ResponseField>

<ResponseField name="is_owned_by_user" type="boolean">
  The is_owned_by_user field.
</ResponseField>

<ResponseField name="is_holiday_calendar" type="boolean">
  The is_holiday_calendar field.
</ResponseField>

<ResponseField name="event_widget_preference" type="boolean">
  The event_widget_preference field.
</ResponseField>

<ResponseField name="agenda_panel" type="boolean">
  The agenda_panel field.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST https://xh2o-yths-38lt.n7c.xano.io/api:LR2ywW7R/calendars \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "calendar_id": 1,
      "event_widget_preference": true
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1,
  "updated_at": 1,
  "user_id": 1,
  "external_id": "example-external_id",
  "grant_id": "example-grant_id",
  "name": "example-name",
  "timezone": "example-timezone",
  "is_primary": false,
  "read_only": false,
  "is_owned_by_user": false,
  "is_holiday_calendar": false,
  "event_widget_preference": false,
  "agenda_panel": false
}
```
</ResponseExample>

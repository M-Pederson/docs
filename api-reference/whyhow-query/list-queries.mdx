---
title: "List Queries"
api: "GET https://xh2o-yths-38lt.n7c.xano.io/api:8qSkh_B3/whyhow-query"
description: "Get a list of all user queries."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ResponseField name="items" type="object[]">
  Array of query summary objects.
</ResponseField>

<RequestExample>
```bash cURL
curl -G \
  https://xh2o-yths-38lt.n7c.xano.io/api:8qSkh_B3/whyhow-query \
  -H "X-Data-Source: live" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_TOKEN"
```
</RequestExample>

<ResponseExample>
```json 200
[
  {
    "id": 1,
    "created_at": 1700000000000,
    "query": "Who do I know at Google?"
  }
]
```
</ResponseExample>

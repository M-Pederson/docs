---
title: "Add Company Address"
api: "POST https://xh2o-yths-38lt.n7c.xano.io/api:KdpaUs_r/add/my_company/address"
description: "Create and add an address record to a company."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField body="my_company_id" type="integer">Company ID.</ParamField>
<ParamField body="formatted_address" type="string">Full formatted address.</ParamField>
<ParamField body="address_label" type="string">Address label.</ParamField>
<ParamField body="address_note" type="string">Optional address note.</ParamField>

<ResponseField name="id" type="integer">Record ID.</ResponseField>
<ResponseField name="my_company_id" type="integer">Company ID.</ResponseField>
<ResponseField name="master_address_id" type="integer">Master address record ID.</ResponseField>

<RequestExample>
```bash cURL
curl -X POST \
  'https://xh2o-yths-38lt.n7c.xano.io/api:KdpaUs_r/add/my_company/address' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -d '{"my_company_id": 1, "formatted_address": "456 Corp Ave, SF, CA 94105", "address_label": "HQ"}'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1700000000,
  "my_company_id": 1,
  "master_address_id": 20
}
```
</ResponseExample>

---
title: 'Delete Email Workspaces'
api: 'DELETE https://xh2o-yths-38lt.n7c.xano.io/api:dUJtZDlP/email-workspaces/{email_workspace_id}'
description: 'Delete an email workspace profile. Auth user must own the workspace. The linked nylas_grant is NOT deleted — only the wo'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Path Parameters

<ParamField path="email_workspace_id" type="integer" required>
  ID of the workspace to delete
</ParamField>


## Response

<ResponseField name="success" type="string">
  The success field.
</ResponseField>

<RequestExample>
```bash cURL
curl -X DELETE https://xh2o-yths-38lt.n7c.xano.io/api:dUJtZDlP/email-workspaces/1 \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN"
```
</RequestExample>

<ResponseExample>
```json 200
{
  "success": "example-success"
}
```
</ResponseExample>

---
title: 'Update Important Dates'
api: 'PUT https://xh2o-yths-38lt.n7c.xano.io/api:mGnfV_UB/important-dates/{important_date_id}'
description: 'Authentication: required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Path Parameters

<ParamField path="important_date_id" type="integer" required>
  The important_date_id parameter.
</ParamField>


## Request Body

<ParamField body="date_descriptor_id" type="integer" required>
  The date_descriptor_id field.
</ParamField>

<ParamField body="annual_recurring" type="boolean">
  The annual_recurring field.
</ParamField>

<ParamField body="date" type="string" required>
  The date field.
</ParamField>

<ParamField body="note" type="string">
  The note field.
</ParamField>

<ParamField body="reminder" type="integer">
  The reminder field.
</ParamField>


## Response

<ResponseField name="id" type="integer">
  The id field.
</ResponseField>

<ResponseField name="created_at" type="number">
  The created_at field.
</ResponseField>

<ResponseField name="updated_at" type="number">
  The updated_at field. Can be `null`.
</ResponseField>

<ResponseField name="my_person_id" type="integer">
  The my_person_id field.
</ResponseField>

<ResponseField name="date_descriptor_id" type="integer">
  The date_descriptor_id field.
</ResponseField>

<ResponseField name="annual_recurring" type="boolean">
  The annual_recurring field.
</ResponseField>

<ResponseField name="date" type="string">
  The date field. Can be `null`.
</ResponseField>

<ResponseField name="note" type="string">
  The note field.
</ResponseField>

<ResponseField name="reminder" type="integer">
  how many days before the important date to remind user Can be `null`.
</ResponseField>

<RequestExample>
```bash cURL
curl -X PUT https://xh2o-yths-38lt.n7c.xano.io/api:mGnfV_UB/important-dates/1 \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "date_descriptor_id": 1,
      "annual_recurring": true,
      "date": "example-date",
      "note": "example-note",
      "reminder": 1
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1,
  "updated_at": 1,
  "my_person_id": 1,
  "date_descriptor_id": 1,
  "annual_recurring": false,
  "date": "example-date",
  "note": "example-note",
  "reminder": 1
}
```
</ResponseExample>

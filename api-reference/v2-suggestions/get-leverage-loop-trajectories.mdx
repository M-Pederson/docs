---
title: 'Get Leverage Loop Trajectories'
api: 'GET https://xh2o-yths-38lt.n7c.xano.io/api:MkA4QsNh/leverage-loop-trajectories'
description: 'Query all leverage_loop_trajectory records'
---

## Overview

Retrieve all leverage loop trajectory records for a given leverage loop suggestion.

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Query Parameters

<ParamField body="leverage_loop_suggestion_id" type="integer" required>
  The ID of the leverage loop suggestion to get trajectories for.
</ParamField>

<ParamField body="page" type="integer">
  Page number for pagination.
</ParamField>

<ParamField body="offset" type="integer">
  Number of items to skip.
</ParamField>

<ParamField body="limit" type="integer">
  Number of items per page.
</ParamField>

## Response

<ResponseField name="itemsReceived" type="integer">
  Number of items returned in the current page.
</ResponseField>

<ResponseField name="curPage" type="integer">
  Current page number.
</ResponseField>

<ResponseField name="items" type="array">
  Array of leverage loop trajectory records.
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET "https://xh2o-yths-38lt.n7c.xano.io/api:MkA4QsNh/leverage-loop-trajectories?leverage_loop_suggestion_id=123&page=1" \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_TOKEN"
```
</RequestExample>

<ResponseExample>
```json 200
{
  "itemsReceived": 5,
  "curPage": 1,
  "nextPage": null,
  "prevPage": null,
  "items": []
}
```
</ResponseExample>

---
title: "Select Calendars"
api: "POST https://xh2o-yths-38lt.n7c.xano.io/api:_wjCLRXt/new-user/calendars"
description: "Select and configure calendars during onboarding."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField body="id" type="array">Array of calendar IDs to activate.</ParamField>
<ParamField body="active" type="boolean">Set calendars active or inactive.</ParamField>
<ParamField body="range" type="string">Sync range: `none`, `30 days ago`, or `all`.</ParamField>

<RequestExample>
```bash cURL
curl -X POST \
  'https://xh2o-yths-38lt.n7c.xano.io/api:_wjCLRXt/new-user/calendars' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -d '{"id": [1, 2], "active": true, "range": "30 days ago"}'
```
</RequestExample>

<ResponseExample>
```json 200
{}
```
</ResponseExample>

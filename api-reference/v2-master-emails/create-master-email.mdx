---
title: "Create Master Email"
api: "POST https://xh2o-yths-38lt.n7c.xano.io/api:ds4G40TG/master-emails"
description: "Add a master email record."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

### Body Parameters

<ParamField body="master_company_id" type="integer">
  The master company ID to associate with. Nullable.
</ParamField>

<ParamField body="master_person_id" type="integer">
  The master person ID to associate with. Nullable.
</ParamField>

<ParamField body="email_address" type="string" required>
  The email address to add.
</ParamField>

<ParamField body="email_type" type="string">
  Type of email (`personal`, `work`, `other`).
</ParamField>

<ParamField body="active" type="boolean">
  Whether the email is active.
</ParamField>

<ParamField body="email_note" type="string">
  Optional note for the email.
</ParamField>

### Response

Returns the created master email record.

<RequestExample>
```bash cURL
curl -X POST \
  'https://xh2o-yths-38lt.n7c.xano.io/api:ds4G40TG/master-emails' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -d '{
    "master_person_id": 1,
    "email_address": "contact@example.com",
    "email_type": "work",
    "active": true
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{}
```
</ResponseExample>

---
title: 'Create Team Questions'
api: 'POST https://xh2o-yths-38lt.n7c.xano.io/api:idMy4x2v/team-questions'
description: 'Create a team_question record Create a new team question. Optionally pass team_question_id to thread it under a parent q'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Request Body

<ParamField body="team_id" type="integer" required>
  The team_id field.
</ParamField>

<ParamField body="question" type="string" required>
  The question field.
</ParamField>

<ParamField body="team_question_id" type="integer" required>
  The team_question_id field.
</ParamField>

<ParamField body="selected_nodes" type="array">
  The selected_nodes field.
</ParamField>

<ParamField body="deep_research" type="boolean">
  The deep_research field.
</ParamField>

<ParamField body="team_ids" type="array">
  The team_ids field.
</ParamField>


## Response

<ResponseField name="id" type="integer">
  The id field.
</ResponseField>

<ResponseField name="created_at" type="number">
  The created_at field.
</ResponseField>

<ResponseField name="updated_at" type="number">
  Timestamp of the last update to the team question.
</ResponseField>

<ResponseField name="user_id" type="integer">
  The user who asked the team question.
</ResponseField>

<ResponseField name="team_id" type="integer">
  The team associated with this question.
</ResponseField>

<ResponseField name="question" type="string">
  The question asked by the user.
</ResponseField>

<ResponseField name="llm_response" type="string">
  The response generated by the LLM.
</ResponseField>

<ResponseField name="team_question_id" type="integer">
  Reference to a related collection question.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST https://xh2o-yths-38lt.n7c.xano.io/api:idMy4x2v/team-questions \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "team_id": 1,
      "question": "example-question",
      "team_question_id": 1,
      "selected_nodes": [],
      "deep_research": true,
      "team_ids": []
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1,
  "updated_at": 1,
  "user_id": 1,
  "team_id": 1,
  "question": "example-question",
  "llm_response": "example-llm_response",
  "team_question_id": 1
}
```
</ResponseExample>

---
title: 'Get Agent Run'
api: 'GET https://xh2o-yths-38lt.n7c.xano.io/api:5mQm30zj/agent_run/{agent_run_id}'
description: 'Get the details of the Agent run including Steps and Tools used. Authentication: required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Path Parameters

<ParamField path="agent_run_id" type="integer" required>
  The agent_run_id parameter.
</ParamField>


## Response

<ResponseField name="id" type="integer">
  The id field.
</ResponseField>

<ResponseField name="created_at" type="number">
  The created_at field.
</ResponseField>

<ResponseField name="user" type="integer">
  The user who initiated the agent run.
</ResponseField>

<ResponseField name="agent_id" type="integer">
  The agent_id field.
</ResponseField>

<ResponseField name="args" type="object">
  Input arguments for the agent run. Can be `null`.
</ResponseField>

<ResponseField name="final_response_text" type="string">
  The final response text from the agent run.
</ResponseField>

<ResponseField name="finish_reason" type="string">
  The reason the agent run finished (e.g., success, error).
</ResponseField>

<ResponseField name="total_usage" type="object">
  Usage statistics for the agent run. Can be `null`.
</ResponseField>

<ResponseField name="steps" type="array">
  The steps field.
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET https://xh2o-yths-38lt.n7c.xano.io/api:5mQm30zj/agent_run/1 \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN"
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1,
  "user": 1,
  "agent_id": 1,
  "args": "example-args",
  "final_response_text": "example-final_response_text",
  "finish_reason": "example-finish_reason",
  "total_usage": "example-total_usage",
  "steps": []
}
```
</ResponseExample>

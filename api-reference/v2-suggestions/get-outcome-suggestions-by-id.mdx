---
title: 'Get outcome_suggestion record'
api: 'GET https://xh2o-yths-38lt.n7c.xano.io/api:MkA4QsNh/outcome-suggestions/{outcome_suggestion_id}'
description: 'Get outcome_suggestion record Authentication: required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Path Parameters

<ParamField path="outcome_suggestion_id" type="integer" required>
  The outcome_suggestion_id parameter.
</ParamField>


## Response

<ResponseField name="id" type="integer">
  The id field.
</ResponseField>

<ResponseField name="created_at" type="number">
  The created_at field.
</ResponseField>

<ResponseField name="updated_at" type="number">
  The updated_at field. Can be `null`.
</ResponseField>

<ResponseField name="suggestion_request_id" type="integer">
  The suggestion_request_id field. Can be `null`.
</ResponseField>

<ResponseField name="user_id" type="integer">
  The user_id field. Can be `null`.
</ResponseField>

<ResponseField name="label_icon" type="string">
  The label_icon field. Can be `null`.
</ResponseField>

<ResponseField name="badge" type="string">
  The badge field. Can be `null`.
</ResponseField>

<ResponseField name="panel_description" type="string">
  The panel_description field. Can be `null`.
</ResponseField>

<ResponseField name="suggestion_context" type="string">
  The suggestion_context field. Can be `null`.
</ResponseField>

<ResponseField name="person_node_uuid" type="string">
  for panel  Can be `null`.
</ResponseField>

<ResponseField name="company_node_uuid" type="string">
  for panel display Can be `null`.
</ResponseField>

<ResponseField name="archived" type="boolean">
  The archived field.
</ResponseField>

<ResponseField name="outcome_trajectories" type="object">
  The outcome_trajectories field. Can be `null`.
</ResponseField>

<ResponseField name="outcome_questions" type="array">
  The outcome_questions field.
</ResponseField>

<ResponseField name="master_company" type="object">
  The master_company field. Can be `null`.
</ResponseField>

<ResponseField name="master_person" type="object">
  The master_person field. Can be `null`.
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET https://xh2o-yths-38lt.n7c.xano.io/api:MkA4QsNh/outcome-suggestions/1 \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN"
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1,
  "updated_at": 1,
  "suggestion_request_id": 1,
  "user_id": 1,
  "label_icon": "example-label_icon",
  "badge": "example-badge",
  "panel_description": "example-panel_description",
  "suggestion_context": "example-suggestion_context",
  "person_node_uuid": "example-person_node_uuid",
  "company_node_uuid": "example-company_node_uuid",
  "archived": false,
  "outcome_trajectories": "example-outcome_trajectories",
  "outcome_questions": [],
  "master_company": "example-master_company",
  "master_person": "example-master_person"
}
```
</ResponseExample>

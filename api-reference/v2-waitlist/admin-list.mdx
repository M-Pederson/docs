---
title: "Admin: List Waitlist Entries"
api: "GET https://xh2o-yths-38lt.n7c.xano.io/api:g7_LZ7hE/admin/list"
description: "Admin: Get paginated list of waitlist entries."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

### Query Parameters

<ParamField query="x_admin_key" type="string" required>
  Admin API key.
</ParamField>

<ParamField query="page" type="integer">
  Page number. Defaults to `1`.
</ParamField>

<ParamField query="per_page" type="integer">
  Items per page (max 100). Defaults to `25`.
</ParamField>

<ParamField query="status" type="string">
  Filter by status (`pending`, `invited`, `converted`, `rejected`).
</ParamField>

### Response

Returns a paginated list of waitlist entries.

<RequestExample>
```bash cURL
curl -X GET \
  'https://xh2o-yths-38lt.n7c.xano.io/api:g7_LZ7hE/admin/list?x_admin_key=YOUR_ADMIN_KEY&page=1&per_page=25' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "items": [],
  "curPage": 1,
  "nextPage": null,
  "prevPage": null
}
```
</ResponseExample>

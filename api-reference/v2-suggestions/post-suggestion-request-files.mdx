---
title: 'Create Suggestion Request Files'
api: 'POST https://xh2o-yths-38lt.n7c.xano.io/api:MkA4QsNh/suggestion-request-files'
description: 'Upload one or more files to a suggestion request. Runs each file through the GCS storage pipeline and creates a suggesti'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Request Body

<ParamField body="suggestion_request_id" type="integer" required>
  ID of the parent suggestion request
</ParamField>

<ParamField body="files" type="array" required>
  The files field.
</ParamField>

<ParamField body="mode" type="string">
  Section of the suggestion flow this file belongs to: suggestion_request, outcome_suggestion, outcome_suggestion_node, leverage_loop_suggestion, serendipity_suggestion, serendipity_suggestion_node
</ParamField>

<RequestExample>
```bash cURL
curl -X POST https://xh2o-yths-38lt.n7c.xano.io/api:MkA4QsNh/suggestion-request-files \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "suggestion_request_id": 1,
      "files": [],
      "mode": "example-mode"
  }'
```
</RequestExample>

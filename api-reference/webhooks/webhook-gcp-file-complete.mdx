---
title: 'Create Webhook Gcp File Complete'
api: 'POST https://xh2o-yths-38lt.n7c.xano.io/api:nFBFWRKy/webhook/gcp-file-complete'
description: 'Webhook endpoint that receives file processing results from the Go Cloud Run backend. When xano_file_id is present it up'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

## Response

<ResponseField name="received" type="string">
  The received field.
</ResponseField>

<ResponseField name="xano_file_id" type="string">
  The xano_file_id field.
</ResponseField>

<ResponseField name="status" type="string">
  The status field.
</ResponseField>

<ResponseField name="data_source" type="string">
  The data_source field.
</ResponseField>

<ResponseField name="persisted" type="string">
  The persisted field.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST https://xh2o-yths-38lt.n7c.xano.io/api:nFBFWRKy/webhook/gcp-file-complete \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1"
```
</RequestExample>

<ResponseExample>
```json 200
{
  "received": "example-received",
  "xano_file_id": "example-xano_file_id",
  "status": "example-status",
  "data_source": "example-data_source",
  "persisted": "example-persisted"
}
```
</ResponseExample>

---
title: "Person Waterfall Functions"
description: "Every function and tool in the live person enrichment waterfall, including the independent non-bio terminal contract, optional Deep Bio branch, edge resolvers, current-company dispatch, and bounded company queues."
---

<Note>
  **Last full live audit: 2026-07-28** — live workspace 3 / branch `v1`, traced by BFS from all ten execution roots (entry gate #13039, C-suite helper #12997, orchestrator #13040, FullEnrich processor #13049, Deep Bio callback ingress #13296, both retry sweeps #13260 / #13297, queue drain #12816, queue upsert #12854, seed #12853). The live person call graph is **201 functions**; this audit added the 132 that had never been listed — chiefly the terminal-receipt / lease checkpoint layer, the Cloud Run Deep Bio callback chain, semantic-state finalization, and the record-writer + link/social leaf layer. `enrich-master-person` is now **#13040 v3.85 (2026-07-28)**. The selected sparse current company still direct-dispatches #12992 at depth 0. `resolve-edges-education` #12560 verifies `ATTENDED` first and then directly dispatches school company enrichment asynchronously at depth 1 without joining the result.
</Note>

Every function and task in the live person waterfall, grouped by role. Every path \+ ID is one-click copyable.

## Quick copy — all active paths

```text All active person paths
# Entry gate + stages (full walkthrough: Person Waterfall Entry Points)
mvp/get-add/master-person-new                 #13039
mvp/enrich/cascade-person-data                #13037
api/exa/fiber-person-email                    #13179
mvp/resolve/match-master-person               #13038
mvp/format/name-format                        #2649
mvp/stop/check-kill-switch-v2                 #4617
mvp/format/set-person-names                   #12820
mvp/node/register-node-registry               #13183
# Orchestrator
mvp/enrich/enrich-master-person               #13040
mvp/enrich/coordinate-person-terminal-barrier #13196
mvp/enrich/resolve-person-terminal-company-dependencies #13199
mvp/expertise/reconcile-person-expertise-recency #13192
# Phase chain (0-12)
mvp/enrich/process-person-phase-0             #12857
mvp/enrich/process-person-phase-1             #12822
mvp/enrich/process-person-phase-2             #12823
mvp/enrich/process-person-phase-3             #12824
mvp/enrich/process-person-phase-4             #12825
mvp/enrich/process-enrich-layer               #12615
mvp/enrich/process-person-exa-profile         #13031
mvp/enrich/process-person-phase-5             #12826
mvp/enrich/process-person-phase-6             #12827
mvp/enrich/process-person-phase-7             #12828
mvp/enrich/process-person-phase-8             #12829
mvp/enrich/process-person-phase-9             #12830
mvp/enrich/process-person-phase-10            #12831
mvp/enrich/process-person-phase-11            #12832
mvp/enrich/complete-person-enrich             #12590
mvp/format/resolve-temp-name                  #12890
# Phase-8 expertise chain
mvp/expertise/llm-identify-person-expertise   #12666
mvp/expertise/resolve-person-expertise-v2     #12926
mvp/expertise/upsert-person-has-expertise     #12925
enrich/write-expertise-edge-description-to-graph  #13020
enrich/create-expertise-edge-description-from-graph  #13019
mvp/expertise/attach-subdomain-parent         #13142
mvp/expertise/resolve-parent-domain           #13141
# Edge resolvers (phase 7) + current-role sync
mvp/resolve/resolve-edges-education           #12560
mvp/resolve/resolve-edges-work                #12562
mvp/resolve/resolve-edges-certifications      #12578
mvp/resolve/resolve-edges-projects-publications  #12579
mvp/resolve/resolve-edges-honor               #4574
mvp/resolve/resolve-edges-volunteering        #4575
mvp/work/choose-best-current-role             #12621
# Deal machinery (phase 9 writes direct angel edges; #12856/#12702/#12701 run from the company side)
mvp/investor/cascade-deal-participants        #12856
mvp/fundable/resolve-investors-edges          #12702
mvp/funding/add-fundable-deal-node            #12701
mvp/funding/add-all-fundable-deals            #12703
# Investor overlay (phase 9)
mvp/investor/get-signal-nfx-data              #2769
# Cross-waterfall bridges (phase 8b + expertise re-runs)
mb/reverse/detect-and-enter-artist            #13171
mvp/enrich/kickoff-cross-waterfalls-from-expertise  #13185
# C-suite mode (company-seed people)
mvp/enrich/enrich-master-company              #12992
mvp/get-add/master-person-from-exa            #12997
# Graph writers
mvp/node/add-person-node                      #2601
mvp/node/update-person-node                   #2758
mvp/edges/create-work-edges                   #2800
mvp/node/primary-location-nodes-edges_v2      #4690
mvp/enrich/find-avatar                        #12846
mvp/avatar/create-placeholder-avatar          #1832
mvp/avatar/replace-avatar                     #237
# Bios & insights
mvp/bios/add-person-biography                 #2536
mvp/bios/create-llm-person-bios               #2537
mvp/social/social-insights/get-social-insights  #12669
mvp/deep-research/deep-person-basic           #12833
mvp/enrich/deep-research-person-or-company    #12747
# FullEnrich (async contact tier)
enrich/full-enrich-reverse-email-lookup       #13036
mvp/enrich/process-full-enrich                #13049
webhook/contact-finished                      #8594
# Fundable substrate (person side)
mvp/fundable/link-fundable-person             #12793
mvp/investor/person-extract-cb-signal         #2687
batch-seed-fundable-person                    #12853
# Contact + format helpers
mvp/link/create-master-link                   #2541
mvp/email/add-person-email                    #2784
mvp/phone/add-person-phone                    #2783
mvp/phones/llm-phone-format                   #2540
mvp/address/add-person-primary-location       #2766
# Terminal receipts + retry sweeps (checkpoint/lease layer, 2026-07)
mvp/enrich/seed-person-terminal-run           #13205
mvp/enrich/record-person-terminal-stage       #13203
mvp/enrich/run-person-orchestrator-terminal   #13207
mvp/enrich/run-person-non-bio-terminal-continuation #13235
mvp/enrich/run-person-callback-terminal       #13208
mvp/enrich/sweep-person-non-bio-terminal-retries #13260
mvp/enrich/ensure-person-terminal-current-company #13206
mvp/enrich/plan-person-current-company-depth0 #13252
mvp/enrich/retry-person-current-company-dependency #13277
mvp/enrich/repair-and-verify-person-current-role #13219
mvp/enrich/verify-person-music-core-readiness #13267
system/error-to-text                          #13299
# Deep Bio job machinery (Cloud Run provider callback)
mvp/enrich/reserve-person-deep-bio            #13301
mvp/enrich/ingest-deep-bio-provider-callback  #13296
mvp/enrich/run-person-deep-bio-provider-worker #13295
mvp/enrich/process-deep-bio-provider-callback #13294
mvp/enrich/process-legacy-deep-bio-provider-callback #13293
mvp/enrich/transition-person-deep-bio-job-terminal #13305
mvp/enrich/sweep-person-deep-bio-retries      #13297
mvp/bios/validate-deep-research-payload       #13209
mvp/bios/llm-repair-deep-research-payload     #13278
mvp/bios/repair-unsupported-current-role-payload #13218
mvp/bios/enforce-current-role                 #13189
# Semantic-state finalization (phase-7 gate owner)
mvp/enrich/finalize-person-semantic-state     #13194
mvp/expertise/decide-semantic-policy          #13300
mvp/expertise/project-person-expertise-array  #13306
mvp/education/reconcile-school-company-identity #13264
mvp/company/reconcile-school-classification   #13233
mvp/company/classify-school-evidence          #13232
mvp/company/classify-investor-evidence        #13200
mvp/company/select-cross-surface-candidate    #13201
mvp/company/reconcile-lifecycle-from-llm      #13249
mvp/queue/process-imdb-award-graph            #13245
# Person alias subsystem
mvp/person/upsert-person-alias                #13187
mvp/person/select-verified-alias-candidates   #13202
# Provider fetchers (called by the cascade)
api/enrich_layer/person-email                 #12734
api/enrich_layer/person-linkedin              #12612
mvp/enrich/get-exa-profile                    #12936
mvp/firecrawl/scrape                          #4515
# Record writers (Enrich Layer / PDL materialization)
mvp/education/add-education-record            #2788
mvp/work/add-position-record                  #12608
mvp/work/add-roles-to-join                    #2242
mvp/roles/add-roles                           #10510
mvp/skills/add-person-skills                  #1459
mvp/honors/add-honor-record                   #12609
mvp/project/add-project-record                #12610
mvp/publications/add-publication-record       #12611
mvp/volunteering/add-volunteering-record      #12574
mvp/certifications/add-certification-record   #2787
mvp/interest/add-person-interest              #12557
mvp/language/add-language-array               #2094
mvp/industry/add-industry                     #137
mvp/linkedin/add-followers                    #2785
mvp/details/add-person-links                  #318
# Work reconciliation + current-role selection
mvp/work/reconcile-source-duplicates          #13191
mvp/work/collapse-work-experience             #1860
mvp/work/best-current-role                    #4540
mvp/work/llm-roles-reduction                  #2243
mvp/work/resolve-employer-from-email-domain   #13304
mvp/format/pdl-date-format                    #12565
# Edge description writers + graph transport
enrich/write-work-edge-description-to-graph   #13008
enrich/create-work-edge-description-from-graph #13003
enrich/write-relationship-edge-description-to-graph #13010
enrich/create-relationship-edge-description-from-graph #13009
mvp/edges/create-education-edges              #1898
mvp/education/get-school-domain               #12561
mvp/honor/find-honor-issuer                   #4586
mvp/llm/check-openrouter                      #13136
mvp/falkor/send-cypher                        #2815
mvp/context/full-person-context               #12667
# Links, socials + permalinks (via create-master-link #2541)
mvp/link/add-enrichment-link                  #2539
mvp/link/scrape-instagram                     #2108
mvp/link/scrape-twitter-person                #2106
mvp/link/scrapecreator-person-linkedin        #12556
mvp/link/scrapecreator-person-youtube         #12670
mvp/linkedin/link-enrich-linkedin             #2775
mvp/links/re-direct-url                       #4543
mvp/permalink/add-person-permalink            #2762
mvp/social/add-instagram-handle               #2109
mvp/social/add-twitter-person-join            #2107
mvp/social/parse-profile-contact-text         #13254
mvp/social/openrouter-grok45-social-call      #13190
mvp/scrape_creators/instagram                 #2487
mvp/scrape_creators/twitter                   #2491
mvp/identity/canonicalize-linkedin-organization-url #13195
mvp/email/classify-email_v2                   #2756
mvp/email/archive-past-work-emails            #13186
# Crunchbase parsing (person + company pages)
mvp/crunchbase/parse-person-page              #2760
mvp/crunchbase/process-person-profile         #2765
mvp/crunchbase/parse-cb-person                #2677
mvp/crunchbase/parse-company-page             #2647
mvp/crunchbase/company-page-enrich            #2812
mvp/investor/parse-signal-nfx                 #2668
# Avatar + object storage
mvp/avatar/convert-image-url-to-webp          #12756
mvp/image/save-image                          #322
mvp/google-storage/upload-file                #12725
mvp/google-storage/delete-file                #12731
mvp/google-storage/generate-avatar-file-path  #12738
mvp/google-storage/get-file-path-from-url     #12733
mvp/google-storage/get-google-storage-keys    #12728
mvp/tools/generate-random-values              #12739
mvp/format/remove-emojis                      #2523
# Embeddings (dual-write on #2601 / #2758)
vectors/create-vectors-string                 #4676
vectors/create-gemini-vectors-string          #13125
vectors/create-gemini-vectors                 #13114
# Company-side machinery reached from the person path (owned by the company waterfall — listed for rebuild completeness)
merge/company-v2                              #12956
merge/helpers/snapshot-company                #12953
merge/helpers/detect-conflicts-company        #12969
merge/helpers/aggregate-scalars-company       #12954
merge/helpers/merge-enrich-data-company       #12955
merge/helpers/repoint-long-tail-company       #12957
merge/helpers/fold-my-companies-on-survivor   #12962
merge/helpers/fold-master-dates-on-survivor   #13229
merge/helpers/dedup-experience-after-company-merge #12971
merge/helpers/graph-merge-company             #12958
merge/helpers/resync-company-node-uuid        #12963
mvp/company-website-enrich                    #4519
mvp/enrich/scrape_extract_website_data        #2814
mvp/revenue/revenue-employees-ownership       #2811
mvp/revenue/get-company-revenue               #2544
mvp/permalink/add-company-permalink           #2656
mvp/email/add-company-email                   #13257
mvp/twitter/scrape-twitter-company            #2448
mvp/twitter/get-website                       #4521
mvp/scrape_creators/add-company_instagram     #2449
mvp/social/add-instagram-company-join         #13258
mvp/social/add-twitter-company-join           #2759
# Shared date substrate (via ensure-company-founded-date)
dates/ensure-company-founded-date             #13234
dates/project-master-dates                    #13228
dates/upsert-master-date                      #13224
dates/parse-date-parts                        #13222
dates/build-date-natural-key                  #13221
# Queue + gate infrastructure
mvp/queue/upsert-enrich-person                #12854
mvp/queue/process-enrichment-queue            #12816
# Crons (tasks, not functions — see Enrichment Crons)
task:sweep-person-non-bio-terminal-retries          Task #192   ACTIVE  live     60 s -> fn #13260
task:sweep-person-non-bio-terminal-retries-sandbox  Task #193   ACTIVE  sandbox  60 s -> fn #13260
task:process-person-queue                           Task #126   inactive live   600 s -> fn #13040
```

## LLM calls at a glance

Every LLM-bearing function and its live model. Full temp / token / timeout / pricing config lives on the [Person model summary](/guides/enrichment/person-pipeline/model-summary) and the prompts on [Person system prompts](/guides/enrichment/person-pipeline/llm-system-prompts) — linked, not duplicated.

| Function | Live model | Purpose |
| --- | --- | --- |
| `name-format` #2649 | `google/gemini-3.1-flash-lite` (strict `json_schema` \+ response-healing, temp 0, 500 tok) | full-name → first/middle/last/suffix/nickname split (entry gate, #12890, #13049) |
| `llm-phone-format` #2540 | `google/gemini-3.1-flash-lite` (temp 0.2 / 30 s; one same-model retry) | raw phone → E.164 — the `master_phone` dedup key (via #13038 \+ #2783) |
| `create-llm-person-bios` #2537 | `moonshotai/kimi-k2.5` (temp 0.2, two calls \+ v4.8 backup-model retry) | short 229-char \+ long 500-char bios, debounced \+ thesis-fallback |
| `resolve-edges-education` #12560 | `deepseek/deepseek-v4-flash` (v1.12 tie-breaker) | narrow same-person school-identity tie-break for similar non-exact school names — must return high-confidence `same_school` or the resolver fails closed |
| `llm-identify-person-expertise` #12666 | `qwen/qwen3.7-plus` (reasoning off, 2500 tok) | expertise signal extraction → #12926 (clears \+ rewrites `HAS_EXPERTISE`) |
| `resolve-person-expertise-v2` #12926 | `qwen/qwen3.7-plus` (×1–2 per signal) | blurb generation \+ guard-band merge adjudication (0.15–0.25); Gemini embeddings still power KNN |
| `resolve-parent-domain` #13141 (via #13142) | `google/gemini-3.1-flash-lite` (reasoning off, 3-attempt retry, fail-loud) | parent DomainExpertise pick over the curated roster (0 = NONE, never auto-creates) |
| `create-expertise-edge-description-from-graph` #13019 (via #13020) | `deepseek/deepseek-v4-flash` (reasoning off, token cap; 402 throws via #13136) | `HAS_EXPERTISE` edge NL description — GOOD-or-null |
| `create-education-edges` #1898 (via #12560) | `openai/gpt-oss-120b` (throughput-sorted providers) | inline ATTENDED-family edge descriptions (phase 7) |
| `resolve-edges-certifications` #12578 | `openai/gpt-oss-120b` \+ `openrouter:web_search` | certification descriptions \+ issuer-domain discovery |
| `verify-imdb-url` #12677 | `google/gemini-3-flash-preview` (temp 0.1, 60 s) | IMDB identity disambiguation over Exa IMDb candidates \+ full person context; links only on `confidence: high` (phase 8) |
| `get-social-insights` #12669 v1.26 | `x-ai/grok-4.5` via #13190 v2.1 (temp 0.2; 1600-token/90 s primary, 1100-token/75 s retry) | Two bounded Exa evidence searches → tool-free, semantically validated social insight synthesis and audited profile URL discovery |
| `llm-roles-reduction` #2243 | `google/gemini-2.5-flash` (temp 0, 500 tok) | noisy job-title strings → seniority-normalized role labels |
| `best-current-role` #4540 (via #12621) | `$env.LLM_MODEL_JSON_PARSE` (env-selected, temp 0, 200 tok) | primary-role tiebreak — LLM fallback behind the v1.1 deterministic pre-selector |
| `parse-cb-person` #2677 | `google/gemini-3.1-flash-lite` primary → `qwen/qwen3.7-plus` failure-only retry (temp 0.1, 4000 tok, 45 s each, reasoning disabled) | guarded Crunchbase markdown → structured JSON (feeds `cb_extract`, read by #2687) |
| `deep-person-basic` #12833 | `moonshotai/kimi-k2.6` pinned `["moonshotai", "fireworks"]`, `allow_fallbacks: false`, \+ `openrouter:web_search`/`web_fetch` | deep-research biography — async via #12747; spec on [Deep Research Bio](/guides/enrichment/waterfall/deep-research-bio) |

Embeddings provenance: person nodes carry OpenAI `text-embedding-3-small` (1536, `name_embedding`) \+ `google/gemini-embedding-001` (1536, `embeddings`) dual-writes via helper #13125 on #2601/#2758; the expertise KNN searches Gemini `expert_embeddings`.


## Entry gate

THE entry point — `mvp/get-add/master-person-new` #13039 v1.21 (2026-07-09) — is documented on [Person Waterfall Entry Points](/guides/enrichment/waterfall/person-entry-points): the calling contract, the kill-switch → pre-API-dedup → cascade → match → write-loops → dispatch walkthrough, and the PDL → Enrich Layer → Exa Fiber → Exa fallback tiers. The public HTTP entry is the V2 Master Persons `POST /master-persons` endpoint #7977 (group 350, `api:WKgay2AU`), repointed to #13039 on the 2026-06-01 migration day. The stage sub-functions and gate helpers #13039 composes are cataloged below.

## Entry-gate stages \+ gate helpers

<Card icon="cloud-arrow-up">
  ```text
  mvp/enrich/cascade-person-data — #13037
  ```

  **v1.9 (2026-07-06) — the pure data-fetch cascade** (stage 1 of the gate; runs only when the pre-API dedup misses AND `cascade_depth == 0`). Order: PDL `/v5/person/enrich` → Enrich Layer Tier 2 (email path `api/enrich_layer/person-email` #12734, LinkedIn path `api/enrich_layer/person-linkedin` #12612) → **Exa Fiber verified-email fallback** `api/exa/fiber-person-email` #13179 (v1.5 — fires when PDL is thin, EL surfaced no usable LinkedIn, and both email \+ `full_name` are present; a promoted Fiber LinkedIn immediately re-runs EL `person-linkedin`, v1.9) → PDL retry with the EL-/Fiber-derived LinkedIn URL (promoted only on `status == 200` AND `likelihood > 5`) → Exa Tier 3 `get-exa-profile` #12936. Returns one object: `pdl`, `pdl_status`, `pdl_email_404`, `el`, `el_linkedin_url`, `el_links` (LinkedIn \+ Twitter \+ Facebook URLs, deduped), `el_profile_pic`, `exa`, `linkedin_url`, `input_email_source`, `needs_full_enrich` (true when the email path surfaced no LinkedIn URL — the caller's trigger for the async [FullEnrich tier](#fullenrich-async-contact-tier)), plus the auditable `exa_fiber_attempt` object and `exa_fiber_candidate_links` (v1.6/v1.7 — weak Fiber candidates become evidence-only `master_link` rows; `promoted_pending_review` outcomes get a `human_review_required` row). A PDL 200 with empty `data.experience` is treated as thin data, so the ladder continues instead of stopping. Zero DB writes except `log_crash` on a caught exception; every tier is try\_catch-isolated so an EL failure never blocks Exa.
</Card>

<Card icon="filter">
  ```text
  mvp/resolve/match-master-person — #13038
  ```

  **v1.0 (2026-05-26, unchanged since creation) — the 4-tier dedup resolver** (stage 2). Walks `master_link.link_url` (normalized) → `master_email.email_address` → `master_phone.phone_number` (per-number LLM formatting via `llm-phone-format` #2540, **name-guarded** — first OR last name must agree — with a phone-conflict error path) → direct `master_person.linkedin_url` fallback (catches people who have the column set but no `master_link` row). Returns `{master_person_id, match_via, status, message, formatted_phones}` — every supplied phone is formatted even after a match lands, so the caller reuses `formatted_phones` in the write loops without double-LLMing. No graph writes. The `match_via` values are the observability contract on every dedup `log_crash` row (see [Entry Points](/guides/enrichment/waterfall/person-entry-points)).
</Card>

<Card icon="cloud-arrow-up">
  ```text
  mvp/format/name-format — #2649
  ```

  **v3.4 (2026-05-30) — LLM name splitter.** Parses a `full_name` into `first_name` / `middle_name` / `last_name` / `suffix` / `nickname` on `google/gemini-3.1-flash-lite` via OpenRouter with **strict `json_schema` structured output \+ the response-healing plugin** (temperature 0, `max_tokens` 500, `require_parameters: true`). v3.4 fixed the v3.2/v3.3 array-literal schema regression by rebuilding the schema arrays with `|push` chains. Called early in #13039 (so the cascade gets structured name parts for PDL), from `resolve-temp-name` #12890, and from `process-full-enrich` #13049's temp-name repair. _(The old phases page's "Groq Llama 3.3 with Gemini 2.5 Pro fallback" claim was two model generations stale — the live call is the v3.4 structured-output Gemini path.)_
</Card>

<Card icon="wrench">
  ```text
  mvp/stop/check-kill-switch-v2 — #4617
  ```

  **Headerless (description comment only; unchanged since 2026-03-19) — the person-creation kill switch.** Returns `true` when the live `master_person` row COUNT is \>= the `kill_switch_person` row in the `environment_variables` TABLE (per-datasource threshold — not a Xano `$env` var; live threshold 10,000 per the #12997 inline note); any failure in the check is caught, logged to `log_crash`, and defaults the switch **OFF** — creation proceeds (fail-open). When ON, #13039 answers from local-only lookups (links / emails / phones / `linkedin_url`) and blocks new-person creation with zero external API spend; #12997 calls it twice (company-resolve containment \+ person-create gate); #12816 skips its person section entirely. Since #13039 **v1.13 (2026-06-30)** blocked people are audited in `log_crash` #542 **only** — the `kill_switch_blocked_people` #611 side-write is gone and the table is deprecated (no drainer ever existed; see [deviations](#deviations-deprecated--orphaned)). _(The old core-concepts name `check-kill-switch-person` was wrong — the live name is `check-kill-switch-v2`; the legacy predecessor `mvp/stop/check-kill-switch` #2774 still exists but is not the pipeline gate.)_
</Card>

<Card icon="wrench">
  ```text
  mvp/format/set-person-names — #12820
  ```

  **v1.0 (2026-04-13, unchanged):** tiny isolation helper — `db.edit` of the `master_person` name fields, but only when `person_id > 0` and at least first or last name is non-empty. Its inputs are named `fn` / `ln` / `mn` / `sfx` / `nick` instead of `first_name` / `last_name` to dodge the Xano naming-collision bug where data keys that shadow a parent function's input parameters silently resolve to the (empty) inputs. **Xano artifact — do not reimplement:** in a port this is a plain guarded UPDATE; the helper exists only for the collision workaround. Single writer for person name fields — #13039 and #12890 both route through it.
</Card>

## Orchestrator

<Card icon="layer-group">
  ```text
  mvp/enrich/enrich-master-person — #13040
  ```

  **v3.85 (2026-07-28) — the person orchestrator** (phases 0–12, with phase 9 run twice). Dispatched async-shared by `master-person-new` #13039 (new, non-queued depth-0 people), `process-full-enrich` #13049 (`force_run: true` re-run after a FullEnrich writeback), the company orchestrator's C-suite mode (#12992 Step 7 via #12997), and the queue drains (Task #126 / #12816). Inputs: `master_person_id`, `deep_research`, `cascade_depth`, `force_run` (v3.10), `current_company_only` (v3.13), `allow_investor_network_cascade` (v3.14). Pre-flight: a **stale-dispatch guard** returns early with a `log_crash` entry when the `master_person` row was deleted since the async fire; then the **60 s source-keyed debounce** — any `enrich_history_person` row with the same `master_person_id` \+ `source == "Master Person Enrich"` \+ `updated_at` inside the window logs `"DUPLICATE TRIGGER, debounced (60s window)"` and returns null (debounce rows log `qa_passed: true`); `force_run: true` bypasses it. The setup row is upsert-keyed on `(master_person_id, source)` — source **text**, not the legacy numeric `data_source_id` — so the debounce key is `updated_at` (touched every re-run), not `created_at`.

  **Live chain:** 0 → 1 → 2 → 3 → 4 → 4b (Exa) → 5 → **EARLY phase 9** (`build_investment_thesis: false`, immediately after Fundable linking) → 6 → **best-current-role sync** via `mvp/work/choose-best-current-role` #12621 → safety close → 7 → safety close → 8 → **phase 8b** (the reverse music bridge `detect-and-enter-artist` #13171, gated on `$env.reverse_music_bridge == "on"`) → **phase 9 AGAIN** (`build_investment_thesis: true`) → 10 (no-op stub, still called) → 11 → 12 → final `enrich_history_person.processing = false` close → closeout aggregate → **post-thesis best-effort retry** (re-runs `create-llm-person-bios` #2537 \+ `update-person-node` #2758 so a thesis built late in the run still reaches the bio and the graph node) → **exit repairs \+ current-company dispatch** (below). Every phase runs in its own `try_catch` writing per-phase `qa_passed` / `CRASH: phase-N` rows to `log_crash` #542 (strict `phase` enum — see [Core Concepts → Per-Phase Crash Logging](/guides/enrichment/waterfall/core-concepts#per-phase-crash-logging)); failures are logged but never block later phases.

  **Controlled-waterfall exit:** the closeout repairs `master_person.master_company_id` from the selected current work row and, for root runs, direct-dispatches a sparse selected current company to `enrich-master-company` #12992 at depth 0. A depth-1 queue record does not satisfy that dependency. Schools follow a different bounded contract: #12560 verifies their table/graph education state, then directly dispatches company enrichment in detached `async-shared` mode at depth 1 with no captured result and no sleep.

  **Optional Deep Bio boundary (v3.58, 2026-07-17):** `deep_research` is normalized once. Provider launch, profile-job creation, company-dependency rows for that profile job, and the durable Deep Bio barrier execute only when it is true. False goes directly to `finalize-person-semantic-state` #13194 with `require_deep_bio=false`; stale profile-job rows cannot reactivate the optional barrier. Non-bio success still requires work/date/tense/UUID parity, education, leaf surfaces, current role, expertise, classifications, owner traceability, and graph readback.

  **Terminal checkpoint layer (v3.76 onward):** #13040 calls `seed-person-terminal-run` #13205 to lay down a deterministic checkpoint set **before phase 7**, then records each stage through `record-person-terminal-stage` #13203 and closes via `run-person-orchestrator-terminal` #13207. Seeding the receipt pre-phase-7 is what makes an uncatchable phase-7 death observable instead of silent. It also calls `plan-person-current-company-depth0` #13252, `finalize-person-semantic-state` #13194, `reconcile-source-duplicates` #13191, `resolve-employer-from-email-domain` #13304, and `enforce-current-role` #13298 directly. **v3.84/v3.85 (2026-07-28)** classify the phase-8 final catch so a concurrent deep-bio expertise write cannot mask a real phase-8 failure. Full inventory: [Terminal receipts \+ retry sweeps](#terminal-receipts-retry-sweeps).

  **The terminal company-dependency contract (v3.51, 2026-07-14):** when root closeout selects a sparse current company, #13040 records `person_terminal_company_dependency` #760 before dispatching #12992 at depth 0. Company closeout calls #13199, which succeeds the dependency only after fresh company table/history evidence newer than `required_after`; a depth-1 queue or resolver row cannot satisfy it. #13196 holds person success until provider, callback, orchestrator, and current-company writers agree. Phase 8 recovery re-reads committed expertise and calls #13192 before accepting a previous write, so count-only or stale graph state cannot suppress a needed retry.
</Card>

## Phase chain (orchestrator phases 0–12)

Each phase is an isolated sub-function invoked by #13040 in the chain order above — crash-isolated, independently loggable, and shared by no other orchestrator (the legacy #12554 that once shared them is deleted). Phase 7's six edge resolvers and phase 9's deal cascade have their own sections below.

<Card icon="layer-group">
  ```text
  mvp/enrich/process-person-phase-0 — #12857
  ```

  **v1.0 (2026-04-20; WHERE-predicate parser normalization 2026-06-04) — PDL backfill.** Calls PDL only when `person_enrich_data.people_data_labs` is empty — the repair lane for cascade-seeded people (created at depth \> 0 with external APIs skipped) that never got a PDL payload. Defensively creates the `person_enrich_data` row if missing and writes the `ds = 91` `enrich_history_person` row.
</Card>

<Card icon="cloud-arrow-up">
  ```text
  mvp/enrich/process-person-phase-1 — #12822
  ```

  **v1.5 (2026-06-25) — Enrich Layer API phase.** LinkedIn path with email fallback, 30-day dedup, `source: "Enrich Layer"` history record. Post-docs it aggressively **skips the paid EL lookup**: for `current_company_only` Exa leaf people, when PDL already returned work history (v1.4), and when the stashed Exa profile is already card-ready (v1.5). Empty EL responses are **non-fatal optional-source misses** logged `qa_passed: true` (v1.3) so the `full_enrich` closeout is never blocked by a provider that simply had nothing.
</Card>

<Card icon="layer-group">
  ```text
  mvp/enrich/process-person-phase-2 — #12823
  ```

  **Headerless (unchanged since 2026-04-14) — primary location.** Runs only when `master_person.primary_location_id` is null; cascades `enrich_layer_data.location_str` → `linkedin_profile.location` → PDL `location_name` (stripping "Metropolitan Area"), then hands the winning string to `add-person-primary-location` #2766 (Radar geocode → location nodes \+ edges via #4690).
</Card>

<Card icon="layer-group">
  ```text
  mvp/enrich/process-person-phase-3 — #12824
  ```

  **v3.10 (2026-07-06) — PDL processing.** Fans the stashed PDL payload out: skills, interests, languages, education, work, certifications, bios, and the PDL avatar (since v3.1). **v3.3:** `current_company_only` filters `experience[]` to current roles before company resolution. **v3.5 controlled waterfall (2026-07-04):** a root run (`cascade_depth = 0`) keeps all PDL work rows, **fully enriches the selected current company**, and leaves historical employers for `resolve-edges-work` to queue as depth-1 leaf companies; non-root and C-suite runs still resolve the employer local-only with `hold_downstream: true`. **v3.6:** once the selected current role links to a `master_company_id`, the work-edge writer runs immediately (`write_descriptions: false` at this call site) so `current_company_only` depth-1 people get their person→company edge without opening new downstream enrichment. **v3.7:** C-suite depth-1 runs clean up matching phase-3 company-queue residue when the linked company already has completed-enrichment evidence. **v3.8/v3.9:** when a stronger company match changes a work row's `master_company_id`, the stale work-derived graph edges (paired `WORKS_AT` \+ `FOUNDED`) are deleted and `edge_uuid` cleared so `create-work-edges` re-targets the resolved company. **v3.10:** "Advisor to Founder/Co-Founder"-style titles no longer classify as founder roles during primary-role selection.
</Card>

<Card icon="layer-group">
  ```text
  mvp/enrich/process-person-phase-4 — #12825
  ```

  **v1.3 (2026-06-24) — Enrich Layer processing wrapper.** Calls `process-enrich-layer` #12615 and closes the `ds = 94` EL history row. v1.2 passes `current_company_only` through; **v1.3** closes the EL history row as **failed** before rethrowing when #12615 throws — no more dangling `processing: true` EL rows after a crash.
</Card>

<Card icon="wrench">
  ```text
  mvp/enrich/process-enrich-layer — #12615
  ```

  **v1.5 (2026-06-24) — the EL data lander.** Persists the full Enrich Layer person surface: name, avatar, bios, location, skills, gender, followers, languages, education, positions, certifications, volunteering, projects, publications, honors, interests. v1.2 (2026-06-09) filters positions to current roles under `current_company_only`; v1.3–v1.5 (2026-06-23/24) are a run of null-safety fixes for nullable text fields, `location_str`, and certification dates/authorities.
</Card>

<Card icon="layer-group">
  ```text
  mvp/enrich/process-person-exa-profile — #13031
  ```

  **v1.14 (2026-07-07) — phase 4b (Exa profile).** Turns `person_enrich_data.exa` into bio rows, an avatar replacement, and `work_experience` rows; **idempotent** — a no-op when `exa` is empty, so it is safe on every run. v1.4 filters `workHistory[]` to current roles under `current_company_only`. **v1.7 (2026-07-06):** Exa `workHistory` routes through `mvp/work/add-position-record` #12608 so mixed-source duplicate roles update existing rows instead of leaving no-edge `work_experience` residue. **v1.8/v1.9 controlled waterfall:** root runs once again **queue resolved non-primary work-history companies** — local-only get-add (`hold_downstream: true`) followed by an explicit `upsert-enrich-company` #12855 write at `cascade_depth: 1`, so sparse employers become leaf enrichment work without recursive fanout. **v1.10/v1.11:** the phase mirrors phase-3's primary-role selection first so the selected current company is never queued as a historical leaf, and advisor-to-founder titles don't classify as founder roles. **v1.12 (2026-07-07):** materializes Exa structured `educationHistory` into `education_experience` and runs the education resolver inline, so school nodes/`ATTENDED` edges don't depend on a later LinkedIn scrape callback. **v1.13/v1.14:** `current_company_only` work rows must resolve to a real company before `add-position-record` runs, and the selected current company may materialize when Exa provides strong domain/LinkedIn evidence (still `queue: false` \+ `hold_downstream: true`). This is the phase that makes C-suite-mode people whole — the seed-company Exa payload stashed by #12997 becomes work rows inside the dispatched run.
</Card>

<Card icon="layer-group">
  ```text
  mvp/enrich/process-person-phase-5 — #12826
  ```

  **v1.4 (2026-04-15, unchanged since April) — Fundable people linking.** Matches the person to a BigQuery-sourced `fundable_people` row via LinkedIn / Crunchbase / X URLs and links `fundable_people.master_person_id`. The link is what arms the early phase-9 investor overlay that follows it in the chain.
</Card>

<Card icon="layer-group">
  ```text
  mvp/enrich/process-person-phase-6 — #12827
  ```

  **Updated 2026-07-08 — LLM bios \+ deep research.** Runs `create-llm-person-bios` #2537, then conditionally `deep-person-basic` #12833 when `deep_research = true` AND `master_person.bio` is non-empty. This removes the former Fly.io launcher from the phase-6 path; #12833 launches through the Cloud Run transport #12747. After this phase, #13040 syncs the best current role via #12621 and safety-closes the history row (v3.21).
</Card>

<Card icon="layer-group">
  ```text
  mvp/enrich/process-person-phase-7 — #12828
  ```

  **v1.3 (2026-07-04) — resolve edges.** Runs all six `resolve-edges-*` resolvers (education, work, certifications, projects/publications, honors, volunteering — [catalog below](#edge-resolvers-phase-7)). **v1.2:** `current_company_only` people skip broad edge resolution — phase 3 already resolved their primary employer, and skipping stops company-born people from spawning a new graph wave. **v1.3:** `current_company_only` people **still run education resolution** so their schools can full-enrich; work, certification, project, honor, and volunteering resolvers stay suppressed for them. v1.1 added explicit failed crash markers per resolver.
</Card>

<Card icon="layer-group">
  ```text
  mvp/enrich/process-person-phase-8 — #12829
  ```

  **v1.1 (2026-06-04) — expertise, IMDB & angel detection.** The phase itself is thin: it calls `llm-identify-person-expertise` #12666 (the full chain is in the [next section](#phase-8-expertise-chain)); when the returned `film_industry` flag is true it runs the IMDB verify/link path — `mvp/imdb/verify-imdb-url` #12677 (Exa IMDb candidates \+ full-person-context LLM disambiguation) **→ and only when that returns `confidence: "high"`** → `create-master-link` #2541 → `mvp/imdb/add-imdb-to-master-person` #10509 (the IMDB-family attach; see [IMDB Waterfall Functions](/guides/enrichment/waterfall/imdb-functions)); then sets `is_angel` from the **legacy `expertise_join` table (#469), domain 22** — a _domain_ check on the old relational join, **not** migrated to `person_has_expertise` #711. The music-side counterpart runs as **phase 8b inside #13040** (env-gated `reverse_music_bridge`), and expertise re-runs outside the orchestrator route the same film/music pair through the shared bridge `kickoff-cross-waterfalls-from-expertise` #13185.
</Card>

<Card icon="layer-group">
  ```text
  mvp/enrich/process-person-phase-9 — #12830
  ```

  **v3.3 \+ the 2026-07-02 D-series rework — investor pipeline \+ direct angel portfolio edges.** Builds the investor surface: Signal NFX scrape via the person-side `mvp/investor/get-signal-nfx-data` #2769 (v1.8 — Serper discovery \+ identity validation, unlike its company twin), Crunchbase signal extraction via `person-extract-cb-signal` #2687, and the Fundable investor overlay into `investor_profile_person` #475. **The seed-angel investment fanout is now DIRECT-EDGES-ONLY (D1/D2, 2026-07-02):** per `fundable_angel_investments` row the phase resolves the deal → `fundable_organizations` → `master_company_node_uuid` and MERGEs `(Person)-[:INVESTED_IN w25]->(Company)` **by the canonical company node uuid** (ON CREATE stamps a self-healing stub; no fundable-id fallback, so never an orphan) — `cascade-deal-participants` #12856 is **no longer called from the person side**, no `Funding_Round` is materialized on this path, and there is no co-investor/VC/partner fanout. **D3: the gate now DEFAULTS ON for depth-0 angels** — `allow_investor_network_cascade|first_notnull:true && cascade_depth == 0`; pass `false` to suppress (depth \> 0 still blocks it entirely). **D4:** each direct edge gets an LLM description via the shared helper with a deterministic fallback. **v3.3** adds the `build_investment_thesis` final pass writing the person side of `investment_thesis` #709 — and #13040 invokes this phase **twice**: early (`thesis: false`, right after Fundable linking, so investor context exists before bios) and again after phase 8 (`thesis: true`, once expertise and edges are in place).
</Card>

<Card icon="layer-group">
  ```text
  mvp/enrich/process-person-phase-10 — #12831
  ```

  **v2.0 (2026-04-19) — NO-OP STUB, still called.** The XanoScript stack is literally empty (`{}`), response null, unchanged since 2026-04-19 — deal fanout is fully owned by phase 9. #13040 v3.33 **still calls it** with its own try/catch \+ phase-10 qa log; the "safe to remove from the orchestrator in a later pass" cleanup never happened (see [deviations](#deviations-deprecated--orphaned)).
</Card>

<Card icon="layer-group">
  ```text
  mvp/enrich/process-person-phase-11 — #12832
  ```

  **Headerless (updated 2026-06-02 without a version bump) — completion dispatch.** Fetches the `source == "Master Person Enrich"` `enrich_history_person` row and calls `complete-person-enrich` #12590 with its `created_at`/`id`. **Xano artifact — do not reimplement:** its `db.query` where clause is stored as a backtick template string with `&amp;&amp;` entities — the known inline-serializer artifact (CLAUDE.md trap #1); a port writes a normal predicate.
</Card>

<Card icon="wrench">
  ```text
  mvp/enrich/complete-person-enrich — #12590
  ```

  **v1.3 (2026-06-09) — the finalizer.** Flips `master_person.visibility = true`, **always** runs `update-person-node` #2758 (v1.3 — since #2758 v1.2\+ falls back `bio_500` → `bio` → `name` for embedding text, Exa C-suite people get visible graph nodes before a long bio exists), marks the enrichment complete, and cleans the person's `queue_enrich_person` row.
</Card>

<Card icon="wrench">
  ```text
  mvp/format/resolve-temp-name — #12890
  ```

  **v1.5 (2026-04-24; parser normalization 2026-06-04) — phase 12, temp-name repair.** When `master_person.name` (or `first_name`) still starts with `temp_`, scans **successful** `enrich_history_person` rows in source priority **PDL (91) → Enrich Layer (94) → Twitter (7) → Fundable (89) → Crunchbase (8)**, falls back to parsing the LinkedIn URL slug, formats the candidate through `name-format` #2649, and writes via `set-person-names` #12820. **v1.5** sanity-checks alpha-character overlap between input and LLM output — transient LLM garbage is rejected instead of overwriting the placeholder with noise. Also called by `find-avatar` #12846 (so avatar searches never query a `temp_` string) and `process-full-enrich` #13049.
</Card>

## Phase-8 expertise chain

<Card icon="cloud-arrow-up">
  ```text
  mvp/expertise/llm-identify-person-expertise — #12666
  ```

  **Header 2026-06-19 (changelog-style, no vN.N) \+ 2026-07-03 model swap \+ 2026-07-04 film\_industry \+ music\_industry rework \+ 2026-07-06/07 hardening — expertise signal extraction.** `qwen/qwen3.7-plus` with `reasoning: {enabled: false}` \+ `max_tokens` 2500; the earlier `effort: low` Gemini config caused intermittent empty content (CLAUDE.md trap #13), and Qwen replaced Gemini 3.1 Flash Lite after dry-run A/B tests on 2026-07-03. The v7 prompt emits per-signal `start_year`/`end_year` (duration-aware `expertise_strength`) **and top-level `film_industry` \+ `music_industry` booleans**; parsing is a safe `indexOf`/`slice` JSON extract. It **DELETEs all existing `HAS_EXPERTISE` edges for the person**, then calls `resolve-person-expertise-v2` #12926 per signal — **not** the legacy `resolve-person-expertise` #12668, which is uncalled since this header (see [deviations](#deviations-deprecated--orphaned)). The **`film_industry` gate (phase 8's IMDB trigger) is now the LLM's own top-level judgment** — the model reads the full person context and decides — replacing the old substring keyword scan (which false-positived on `JavaScript`, `producer`, `scoring`, gene `editing`, …); a **tokenized whole-word scan** over signals \+ resolved labels \+ context survives only as a **fallback** for when the LLM omits the field. The gate deliberately spans film, TV, streaming **and video games / interactive entertainment** (IMDb catalogs games — cf. the `Video_Game` subtype in [IMDB Nodes](/guides/enrichment/waterfall/imdb-nodes)). A parallel **`music_industry`** boolean (same full-context LLM judgment, explicitly FP-guarded against a devops "orchestrator" / PHP "Composer") is emitted the same way; **both flags are persisted into `llm_expertise_raw`** on `expertise_identification_log` #660, where the music reverse-bridge (`detect-and-enter-artist` #13171, phase 8b) reads `music_industry` as its primary detection signal. **2026-07-06 (f):** on re-resolution the function now clears the person's `person_has_expertise` rows alongside the `HAS_EXPERTISE` graph-edge delete, so table #711 mirrors the current edge set after deep-research re-runs. **2026-07-07 (g):** robust OpenRouter response extraction (accepts `response.result` / `result` / `choices` shapes without throwing). Second caller: the deep-research webhook #8303 re-runs it after a deep bio lands — safe because the downstream chain is idempotent, and since #8303 v1.5 (2026-07-09) that re-run is followed by the cross-waterfall bridge #13185.
</Card>

<Card icon="cloud-arrow-up">
  ```text
  mvp/expertise/resolve-person-expertise-v2 — #12926
  ```

  **v2.8 (2026-07-08) — LOCKED expertise-match spec** (promoted 2026-06-18 from the v3 experiment #13137; OpenRouter chat model on `qwen/qwen3.7-plus` with `reasoning: {enabled: false}` \+ 2500-token cap since the 2026-07-03 root-cause timeout fix). Per signal: a blurb LLM writes a short domain description, `"{name}: {blurb}"` is Gemini-embedded and KNN-searched against the `expert_embeddings` index, and a **3-way guard band** decides (score is a distance — lower = closer): `< 0.15` auto-match, `0.15–0.25` Qwen LLM adjudication, `> 0.25` create. Matched edges MERGE at `weight = min(round(10 + match_score × 160), 50)`; **created** edges get flat weight **10** (a new node perfectly represents its domain). `r.expertise_strength` is a **separate 0–100 property** (depth-dominant \+ recency \+ duration from the per-signal dates × confidence) — decoupled from the path-distance weight. **v2.8:** the `HAS_EXPERTISE` MERGE now stamps the full edge-metadata envelope on both branches — `uuid`, `created_at`, `updated_at`, `source: "llm_identify_person_expertise"`, plus the match/evidence fields (the description writer #13020 still owns LLM prose \+ `description_generated_at`). Every MERGE dual-writes `person_has_expertise` #711 via #12925 and generates the edge description via #13020; the CREATE branch calls `attach-subdomain-parent` #13142 → `resolve-parent-domain` #13141. **v2.4 hardening:** exact-name guard before create (stale/rebuilding vector indexes can no longer mint duplicate nodes), create-verify (`create_failed` instead of dangling rows), `created_at` stamp on the `sub_domain_expertise` insert, and relational `domain_expertise_id` FK dual-write by name lookup; **v2.5/v2.6** fixed the exact-name-branch `match_score` null crashes (exact hits carry `match_score 0` / weight 10); **v2.7** gives matched-parent repair a description fallback. The music-side MB-credit emitter #13160 reuses this same chain post-bridge.

  <Warning>
    Do **not** copy the `× 80` weight formula floating in older docs and in #12925's input description — the live matched-edge formula is `min(round(10 + match_score × 160), 50)`, and created edges are flat 10. `expertise_strength` (0–100, higher = stronger) is a different property with the opposite polarity.
  </Warning>
</Card>

<Card icon="wrench">
  ```text
  mvp/expertise/upsert-person-has-expertise — #12925
  ```

  **v1.2 (2026-07-02) — relational dual-write.** Mirrors every `HAS_EXPERTISE` MERGE into `person_has_expertise` #711 — one row per `(master_person_id, expertise_node_uuid)` pair (unique-indexed, unlike the queue tables). v1.2 fixed the two-arg `|concat` mis-render (CLAUDE.md serializer trap) and added CONTAINS dedupe guards so resolver and MB-emitter re-runs stop bloating evidence rows. Its `weight` input's description text still says `*80` — stale; trust #12926's header (see the Warning above).
</Card>

<Card icon="wrench">
  ```text
  enrich/write-expertise-edge-description-to-graph — #13020
  enrich/create-expertise-edge-description-from-graph — #13019
  ```

  **The HAS\_EXPERTISE description pair — reworked 2026-07-07/08.** **#13020 v2.3 (2026-07-08)** is the idempotent writer called inline from **both** branches of #12926 after each MERGE: keyed on `(person_uuid, expertise_uuid)` because `HAS_EXPERTISE` edges are matched by endpoint UUIDs. The v2.x line made it graph-native and diagnostic-proof: **v2.0** builds the deterministic fallback sentence from graph properties in Cypher (Xano parser diagnostics can no longer land as description text), **v2.1** treats a 0-row SET as a transient/deferred edge-write instead of a hard QA crash, **v2.2** restores LLM prose by delegating to #13019 and writing the normalized result, and **v2.3** stamps `source` alongside the timestamp metadata. **#13019 v1.6 (2026-07-08)** generates the sentence on `deepseek/deepseek-v4-flash` with `reasoning: {enabled: false}` \+ a `max_tokens` cap; v1.4/v1.5 moved prompt construction to `api.lambda` JSON (rejecting parser diagnostics) with an undirected UUID match, and v1.6 tightened the prose style rules. Responses route through the `check-openrouter` guard #13136: `empty_content`/`llm_error` degrade to null (GOOD-or-null — the edge stays retry-eligible), but **out-of-credits (402) throws loudly**. The batch drain for pre-existing edges is `backfill-has-expertise-descriptions-v2` #13046 via **Task #177** (inactive, 20 edges/batch — replaced deleted buggy Task #176).
</Card>

<Card icon="wrench">
  ```text
  mvp/expertise/attach-subdomain-parent — #13142
  mvp/expertise/resolve-parent-domain — #13141
  ```

  **Taxonomy parent attach (shared with the company pipeline).** **#13142 v1.4 (2026-07-08)** is called from the CREATE branch of both #12926 (person) and `resolve-company-specialties` #12746 (company), and since v1.3 also idempotently repairs **matched/reused** SubDomainExpertise nodes (an existing relational FK → repair the graph `HAS_SUBDOMAIN` edge without re-running the LLM; no FK → resolve, write the edge, dual-write `sub_domain_expertise.domain_expertise_id`; non-relational uuids no-op); v1.4 falls back to the relational row description/name when `sub_description` is missing. It sync-runs **#13141 v1.6 (2026-07-08)** — an LLM pick (`google/gemini-3.1-flash-lite`, reasoning off) over the curated DomainExpertise roster fetched live from the graph (`0 = NONE`, **never auto-creates a domain**; v1.3 added a 3-attempt retry that THROWS on exhaustion so a failed LLM can't mislabel orphans; v1.4 refreshed the prompt for the expanded 136-domain roster; v1.5/v1.6 route industry-qualified leadership and business-administration/operations-strategy signals to their closest covered domains instead of NONE). A hit MERGEs `HAS_SUBDOMAIN {weight: 100}` — deliberate hub damping for path-distance semantics, not closeness; a NONE verdict flags the node `parent_unresolved` for human review. Full as-built caveats on [Company Waterfall Functions](/guides/enrichment/waterfall/company-functions) — linked, not duplicated.
</Card>

## Edge resolvers (phase 7)

All six resolvers accept `cascade_depth` and stamp source metadata for traceability. Schools direct-dispatch as detached depth-1 company work only after `ATTENDED` verification; past employers use the company queue. Certification/honor/volunteering issuers plus publishers follow their resolver-specific policy. Depth \> 0 keeps company C-suite/founder expansion gated off.

<Card icon="cloud-arrow-up">
  ```text
  mvp/resolve/resolve-edges-education — #12560
  ```

  **v2.11 (2026-07-17):** resolves structured education to canonical school companies, creates and terminally audits `ATTENDED`, then directly calls `enrich-master-company` in `async-shared` mode at depth 1 without capturing a result. The person run does not sleep or wait for that detached company waterfall, and no school queue row is created. Strong evidence gates keep company-looking narrative noise out. Cross-provider identity/date merges preserve the surviving edge UUID and fail closed on conflicting UUIDs. The narrow ambiguous-school LLM tie-breaker is explicit opt-in; ordinary terminal completion never waits on its retry budget.
</Card>

<Card icon="cloud-arrow-up">
  ```text
  mvp/resolve/resolve-edges-work — #12562
  ```

  **v1.13 (2026-07-07):** resolves `work_experience` rows to companies and writes the work edge set via `create-work-edges` #2800. **v1.5** delegates current-role selection to the shared `choose-best-current-role` #12621; **v1.6** sets the best current role BEFORE edge creation. **v1.8 controlled waterfall (2026-07-04):** the current-role fallback stays local-only, while **past work employers mint company shells and enqueue depth-1 leaf company enrichment** (`upsert-enrich-company` #12855). The July hardening wave: **v1.8/v1.11** generic role/company placeholders ("Angel Investor", "Board Member", "Stealth Startup") are captured as work rows but never mint company shells, edges, or queue rows; **v1.9** stronger domain/LinkedIn evidence updates-and-queues the already-linked company instead of minting a duplicate; **v1.10** unresolved LinkedIn-backed and name-only rows materialize as depth-1 leaf shells so non-primary advisor/producer rows don't stay `edge_uuid = null`; **v1.12** LinkedIn-backed rows also reuse an existing `master_company.linkedin_url` match when `master_link` is missing; **v1.13** duplicate source rows copy the existing same-person/same-company `edge_uuid` so weaker rows can't overwrite the canonical edge's title/history.
</Card>

<Card icon="cloud-arrow-up">
  ```text
  mvp/resolve/resolve-edges-certifications — #12578
  ```

  **v2.6 (2026-07-07):** certification nodes \+ edges (`HAS_CERTIFICATION` 15/20, `GRANTED_BY` 10, `CERTIFIED_BY` 25 — unchanged), with **LLM certification descriptions on `openai/gpt-oss-120b` \+ `openrouter:web_search`** for issuer-domain discovery. **v2.5** scopes every certification query to the input `master_person_id`; issuer companies are always `cascade_depth = 1` leaves. **v2.6** normalizes certification descriptions after LLM output and before the graph edge writes; all three edges now carry `uuid` / `created_at` / `updated_at` / `description` in the live Cypher.
</Card>

<Card icon="cloud-arrow-up">
  ```text
  mvp/resolve/resolve-edges-projects-publications — #12579
  ```

  **v8 (2026-07-07):** projects (`CONTRIBUTED_TO`) \+ publications (`AUTHORED`, `PUBLISHED_BY`). Contributor resolution is **seed-person-only** (v4); **v5** makes publisher companies leaf/local-only — `queue: false` \+ `hold_downstream: true`. **v6 (2026-07-04)** was a structural fix: the resolver now **processes existing project rows instead of self-skipping** (no more node_uuid-null-only gating for projects), fixes project response parsing, and stamps descriptions \+ `display_label` on project/publication nodes and `AUTHORED` edges — project nodes are created as `Entity:Work:Project` now. **v7** treats missing contributors as `[]`; **v8** normalizes project/publication text before node/edge descriptions are written.
</Card>

<Card icon="cloud-arrow-up">
  ```text
  mvp/resolve/resolve-edges-honor — #4574
  ```

  **v4 (2026-06-23):** honor issuer resolution (domain or LinkedIn URL) → `Honor` nodes \+ `RECEIVED_HONOR` / `ISSUED_BY` edges. v4 scoped issuer-resolution queries to the input `master_person_id`; issuers are depth-1 leaves (v3).
</Card>

<Card icon="cloud-arrow-up">
  ```text
  mvp/resolve/resolve-edges-volunteering — #4575
  ```

  **v4 (2026-06-04):** `VOLUNTEERED_AT` edges via education cross-reference or LinkedIn URL matching. v4 scoped all queries to the input person and dropped a read of the missing `item.source` field; volunteer organizations resolve as depth-1 leaf companies.
</Card>

<Card icon="wrench">
  ```text
  mvp/work/choose-best-current-role — #12621
  mvp/work/add-employment-current — #4552
  ```

  **The shared current-role selector \+ writer.** **#12621 v1.4 (2026-07-04):** queries **resolved** active work rows (`master_company_id` set — v1.1/v1.2; since **v1.3** a row counts as active when `work_experience.current` is true OR its end-date parts are empty, because upstream date normalizers can emit zero-like end parts on current roles), picks the primary role (single row → direct; multiple → LLM tiebreak via `mvp/work/best-current-role`), and writes through **#4552 v1.1 (2026-06-23)**, which guards against empty writes and against investor/advisor/board titles clobbering an operating role. **v1.4** persists the selected company/title back onto `master_person` so late completion paths can't leave `master_company_id` null. Called from **three** places: `resolve-edges-work` v1.5\+ (pre-edge), the orchestrator's post-phase-6 sync, and the orchestrator's post-thesis exit repair (v3.28) — the pairing that keeps `master_person.master_company_id` / `current_title` stable across the run.
</Card>

## Deal machinery (company-side cascade \+ person-side direct edges)

Since the 2026-07-02 D-series rework, the person waterfall no longer drives the full deal cascade: **phase 9 writes direct `(Person)-[:INVESTED_IN w25]->(Company)` portfolio edges only** (see the phase-9 card above — default-on at depth 0 for angels, suppressible with `allow_investor_network_cascade: false`). The cascade helper below remains the single source of truth for **company-side** deal fanout (`process-company-phase-7` #12813 → #12703 → #12856), which is where `Funding_Round` nodes, `RAISED`, and the investor edge set are materialized — and where co-angels / VC partners are queued into `queue_enrich_person` at depth 1 / tier 1. The shared pattern is owned by [Core Concepts → Deal Cascade Pattern](/guides/enrichment/waterfall/core-concepts#deal-cascade-pattern); company-side callers and gates are on [Company Waterfall Functions](/guides/enrichment/waterfall/company-functions) — linked, not duplicated.

<Card icon="layer-group">
  ```text
  mvp/investor/cascade-deal-participants — #12856
  ```

  **v1.10 (2026-07-07):** cascades one `fundable_deals` row end-to-end; called per deal from #12703 (company side — the person phase 9 no longer calls it). Order: **v1.2 staleness gate** (skip when `processed_at` is set and nothing newer touched the deal or its investment rows; v1.7 fixed the query so newly-added investors actually trigger a re-cascade) → ensure the portfolio company exists (sync `master-company-new`, `queue: false`, `hold_downstream: true`, tier 2, depth `effectiveDepth+1` — created local-only, **not** queued) → IPO signal (`company_financial.is_public` false→true when `ipo_status == "public"`) → **#12701** Funding\_Round \+ RAISED (all depths) → gated **#12702** → stamp `processed_at`. **v1.8:** `processed_at` is stamped **only when the Funding_Round node actually materialized** — node-write failures stay reprocessable and log a crash row (ops can still null `processed_at` to force a re-cascade). **v1.9 scope gate:** when the origin company is itself an investor, its portfolio rounds (deal raiser ≠ origin company) are skipped at depth 0 instead of minting dangling co-investor rosters. **v1.6 depth repair:** `cascade_depth` is a nullable input read via `first_notnull` — an explicit seed `0` survives and the depth-0 gate is satisfiable. **v1.10:** scalarized ids/errors so diagnostics can't mask the original failure.
</Card>

<Card icon="cloud-arrow-up">
  ```text
  mvp/fundable/resolve-investors-edges — #12702
  ```

  **v1.17 (2026-07-07):** the investor half of the cascade. For one deal's `Funding_Round` node it resolves every institutional investor org (bounded local-only creation, then queued **depth 1 / tier 1** via `upsert-enrich-company` #12855 — v1.14, 2026-07-03, dropped firms from tier 2 to the drain-eligible tier 1), each investment's deal partners and every co-angel (local-only `master-person-new`, then `upsert-enrich-person` #12854 at depth 1 / tier 1 — note it unconditionally overwrites an existing partner's `master_person.master_company_id` / `current_title` with this deal's firm context), and MERGEs the investor edge set with `VC_Firm` / `Angel` label writes: `LEAD_INVESTED_IN` w15 · `INVESTED_IN` w20 institutional / w25 angel · `FOLLOW_ON_INVESTED_IN` w20 · `INVESTMENT_PARTNER_IN` w15 · `INVESTMENT_PARTNER_AT` w20 — every edge stamping `uuid` / `created_at` / `updated_at` \+ an LLM-or-deterministic description. Investor people/orgs are **queued leaves, not inline full-enriched**. **v1.12** repaired the `first_notempty` depth-guard self-skip; **v1.13** floors the ms→s date division; **v1.15–v1.17 (2026-07-07)** JSON-encode description variables directly in Twig, repair Fundable-linked participant node names while rewriting edges, and fix the partner-template `WITH p` clause FalkorDB requires. Catalog: [Person Edges](/guides/enrichment/waterfall/person-edges).
</Card>

<Card icon="cloud-arrow-up">
  ```text
  mvp/funding/add-fundable-deal-node — #12701
  ```

  **v1.17 (2026-07-09):** graph writer for one deal — MERGEs the `Funding_Round` node keyed `uuid = fundable_deals.id` (now labeled **`:Entity` \+ `visibility: true`**, v1.10), and (when the org's Company node uuid exists) MERGEs `(Company)-[:RAISED]->(Funding_Round)` at **weight 10** with a deterministic edge uuid, timestamps, and a description that prefers the LLM helper and falls back to the deal's own round description (v1.11/v1.16). The v1.9 hardening JSON-encodes every Cypher string prop (the old single-quoted templates broke on apostrophes), guards the embedding splice, and logs a crash row when the node write silently returns no uuid; **v1.12** upserts `company_funding_round` with the node uuid \+ RAISED edge id; **v1.13–v1.15** scalarize Fundable source values before Xano text filters can crash on them; **v1.17 (2026-07-09)** registers materialized `Funding_Round` uuids in `node_registry` (`entitiy_type: "funding_round"`). Weights: [edge weights](/guides/ontology/edge-weights) (lower = stronger).
</Card>

<Card icon="wrench">
  ```text
  mvp/funding/add-all-fundable-deals — #12703
  ```

  **v2.3 (2026-07-02) — the company-side deal walker** (cross-referenced here because it dispatches into the same #12856). Walks both sides of a fundable org's deal graph in per-deal try\_catch. **v2.1 fan-out governor:** **25 target-side \+ 25 investor-side deals per invocation** — caps arrive via `first_notempty:25`, so 0/null/omitted all coerce to 25; overflow writes a `qa_passed: true` "fanout capped" log row with no continuation mechanism. **v2.2** fixed a paging+projection combination that made both deal-walk queries silently return zero rows; **v2.3** dispatches target deals in chronological order so #12702's earlier-round `FOLLOW_ON_INVESTED_IN` detection sees prior rounds already in the graph. Full card on [Company Waterfall Functions](/guides/enrichment/waterfall/company-functions).
</Card>

## C-suite mode (`current_company_only`)

<Card icon="layer-group">
  ```text
  mvp/enrich/enrich-master-company — #12992   (Step 7 only)
  ```

  **v4.24 (2026-07-08) — the company orchestrator's people-facing step** (the other steps: [Company Waterfall Functions](/guides/enrichment/waterfall/company-functions) — linked, not duplicated). Step 7 runs the Exa C-suite/founder search for a seed company and is **depth-0-gated (v4.2)** and **non-recursive (v4.3)**: deeper companies save the Exa response but never expand people. **v4.21–v4.24 (2026-07-05/08):** when Exa misses a verified founder, Step 7 can synthesize seed-scoped founder/executive matches from `company_llm.data.founders`, Fundable, or LinkedIn text (a title phrase like "Chairman Jason Peterson" is required — employee cards alone are not enough), and the Exa query prefers canonical Fundable/LinkedIn names over domain-like seed strings. Accepted matches do **not** land in `queue_enrich_person` — each goes through #12997, then a **direct async dispatch** of `enrich-master-person` #13040 with `{cascade_depth: depth+1, deep_research: false, current_company_only: true}` (since v4.5). In that mode phases 1/3/4/4b and the FullEnrich applier filter employment to **current roles**, phase 7 runs education only (schools full-enrich; other resolvers skipped), and the person's work rows \+ `WORKS_AT`-family edges **materialize inside the dispatched run** (phase 3 PDL \+ phase 4b reading the stashed Exa payload; edges via #2800). That is the runaway guard: the Exa people wave enriches people and links their current companies, but downstream companies never create another people wave.
</Card>

<Card icon="play">
  ```text
  mvp/get-add/master-person-from-exa — #12997
  ```

  **v2.5 (2026-07-07) — the C-suite person upsert.** Dedup on normalized `linkedin_url`, plus a seed-company \+ first/last-name fallback dedupe for LinkedIn slug variants (v2.0); hit → non-destructive `first_notempty` merge \+ `is_c_suite: true`; miss with the person kill switch ON → `blocked_by_kill_switch`; else `db.add` with **`visibility: false`** (v1.7 — created ≠ card-ready; #12590/#2758 flip it after the enrichment run), `add-person-node` #2601 \+ `create-master-link` #2541. **v1.8:** company-context employer resolution is **lookup-only** (`allow_create: false`, `queue: false`, `hold_downstream: true`) — it may link an existing company, never mints one. **v1.9:** guarantees a `person_enrich_data` shell and backfills an empty `exa` column from the match, so phase 4b always has material. **v2.2/v2.3 (2026-07-06):** immediately runs Enrich Layer `person-linkedin` on the Exa LinkedIn (stores the rich EL profile \+ EL-discovered links without fanout) and reconciles cached ScrapeCreators employee cards — a matched card's `/in/` URL becomes the canonical LinkedIn and its image goes to `replace-avatar`, so C-suite leaves don't keep placeholder avatars. **v2.1/v2.4:** seed-company matching accepts only exact domain/root-domain, exact LinkedIn, exact name, or generic legal suffixes — "Knock" can no longer match "Knock AI". **v2.5:** name normalization strips zero-width characters. It writes **no work rows and no `WORKS_AT`-family edges** — those come from the dispatched #13040 run (the work-edge writer #2800 emits `WORKS_AT`; legacy `WORKED_AT` edges linger in the graph from the pre-#2800 writer, and no `HAS_WORKED_AT` edge type exists).
</Card>

## Graph writers

<Card icon="cloud-arrow-up">
  ```text
  mvp/node/add-person-node — #2601
  ```

  **v2.1 (2026-06-25) — the Person node creator.** `MERGE` keyed on `master_person_id` (idempotent on retries — contrast the company writer's raw CREATE), sets the `:Entity` base label, and dual-embeds: OpenAI 1536 `name_embedding` \+ Gemini 1536 `embeddings` (3072 before 2026-07-06) via the shared helper #13125, with the Gemini line **gated in the template** so a Gemini miss still creates the node. Avatar chain: Serper `find-avatar` #12846 → `create-placeholder-avatar` #1832 fallback when EL/PDL returned nothing. **v2.0 (2026-06-22)** made the Serper `replace-avatar` call best-effort (try/catch, non-blocking); **v2.1** returns a safe `$nodeUuid` var so a Falkor timeout can't throw after a logged non-fatal miss; the Falkor write runs a **3-attempt retry loop**, and the function seeds an idempotent `person_enrich_data` shell. Called from #13039 (new-person path) and #12997. Envelope: [Person Nodes](/guides/enrichment/waterfall/person-nodes) · Cypher: [Cyphers → Person pipeline](/guides/cyphers/person-pipeline).
</Card>

<Card icon="cloud-arrow-up">
  ```text
  mvp/node/update-person-node — #2758
  ```

  **v1.11 (2026-07-09) — the Person node sync.** Full property SET on the existing node with the investor-profile fields; forces `master_person.visibility = true`. Embedding text falls back `bio_500` → `bio` → `name`, so sparse people still sync; dual-embeds OpenAI 1536 `name_embedding` \+ Gemini 1536 `embeddings` via the shared helper #13125, gated in the Twig template so a Gemini miss still syncs (the v1.8 rebuild dropped this; restored in v1.9). **v1.8:** pre-sync repair of an empty `master_person.master_company_id` from resolved current work, so late deep-research webhook updates can't leave the FK or graph current-company fields blank. **v1.10 (2026-07-07):** stamps a scalar `Person.linkedin_url` on the node (from `master_person` or the canonical `/in/` master link) so graph QA projections keep it. **v1.11 (2026-07-09):** a non-fatal preflight inactivates active `master_email` rows whose domains match past-only work-experience company domains before the sync. **Open item:** the dynamic `:Angel` / `:C_Suite` label writing that v1.5 carried was lost in the v1.8 rebuild and has **not** been restored — today the only live `:Angel` label writer is #12702's investor branches, and nothing stamps `:C_Suite`. Graph string properties (including `short_biography` / `long_biography`) are stripped of control characters and newlines so node props stay one-line and Cypher-safe — the formatted bios keep their line breaks in Xano. **Three callers:** `complete-person-enrich` #12590 (phase 11), the deep-research webhook #8303 fan-out, and the orchestrator's post-thesis retry.
</Card>

<Card icon="cloud-arrow-up">
  ```text
  mvp/edges/create-work-edges — #2800
  ```

  **v2.12 (2026-07-08) — the work-edge writer, body-verified.** One Cypher MERGEs Person \+ Company stubs (coalesce-stable timestamps) and up to **six** edge types with the exact weights: `FOUNDED` **5**, `BOARD_MEMBER_OF` **8**, `ADVISOR_TO` **12**, `INVESTED_IN` **15** _(branch retained but effectively dead — see below)_, `MEMBER_OF` **20**, `WORKS_AT` **= seniority\_rank, \+20 if past** — every edge stamping `uuid` / `created_at` / `updated_at` \+ a deterministic fallback description. Title routing (§3.3/§3.7): advisory / board / membership titles are excluded from `WORKS_AT`, unions route to `MEMBER_OF`, and generic placeholder companies ("Angel Investor", "Board Member", "Stealth Startup" — v2.4/v2.7) are blocked from receiving edges at all. **v2.5 (2026-07-05): work-history titles no longer classify as `INVESTED_IN`** — the classifier hardcodes `isInvestor: false`, so investment edges come only from the explicit deal writers ("Angel Investor at Leeds Angels" is now a work-history relationship). **The v2.x founder overhaul:** `FOUNDED` OR-ins Fundable structured `is_founder` (v1.9/v2.1 — exec-titled founders like "Chairman and CEO" get `FOUNDED` w5), the **phantom-founder guard** (v2.0) drops title-only founder claims when authoritative Fundable founder lists exclude the person (they downgrade to `WORKS_AT`), Company\_LLM founder-fallback rows and explicit Co-Founder work history count as real evidence (v2.10/v2.11), and "Founder in Residence" / "Advisor to Founder" titles never classify as founders (v2.3/v2.6). **v2.12: edge descriptions are LLM-standard** — the `write_descriptions` input is retained for old callers but **ignored**; the work/relationship description writers (#13008/#13010, plus the shared FOUNDED writer since v2.9) always run after graph creation, try\_catch-wrapped so an LLM failure never blocks edge creation. `edge_uuid` writes back to `work_experience.edge_uuid` (v2.2). There is **no `HAS_WORKED_AT` edge type**; legacy `WORKED_AT` edges linger from the pre-#2800 writer. Catalog: [Person Edges](/guides/enrichment/waterfall/person-edges).
</Card>

<Card icon="cloud-arrow-up">
  ```text
  mvp/node/primary-location-nodes-edges_v2 — #4690
  ```

  **v1.5 (2026-07-04) — the person-side location graph writer.** Consumes the structured Radar geocode result handed over by #2766: gets-or-creates `Country` / `Region` / `City` nodes and writes one of four edge variants — `LOCATED_IN` w70 (to city or region) or **w80 direct-to-country**, `IN_REGION` w25, `IN_COUNTRY` w50 — anchored on the Person node, all edges stamping `created_at` / `updated_at`. **v1.3–v1.5 (2026-07-04):** the location nodes now write a canonical `description` property (City alongside `summary`; Region/State `description` instead of `summary`; Country from `country.description`) for graph QA. Weights and patterns: [Person Edges](/guides/enrichment/waterfall/person-edges). (The company twin is `location-nodes-edges-company` #4528, documented on [Company Waterfall Functions](/guides/enrichment/waterfall/company-functions).)
</Card>

<Card icon="cloud-arrow-up">
  ```text
  mvp/enrich/find-avatar — #12846
  ```

  **v1.3 — Serper.dev image search for person avatars.** Called by `add-person-node` #2601 (v1.8\+) and the avatar backfill task family; resolves `temp_` placeholder names via #12890 **first**, so the image query never searches a uuid string. A hit routes through `replace-avatar` #237; a miss falls through to the placeholder generator.
</Card>

<Card icon="wrench">
  ```text
  mvp/avatar/create-placeholder-avatar — #1832
  ```

  **v1.3 (2026-05-25) — initials placeholder.** Builds a ui-avatars.com initials image, converts to webp, uploads to GCS, and inserts `master_avatar` with text `source = "UI Avatars"` (the #237 v3\+ text-attribution scheme). Dedups by `original_source_url` and returns the existing URL instead of throwing on re-runs. The body carries an inline comment labeled v1.4 on the source write, but the header stops at v1.3 — trust the header.
</Card>

<Card icon="wrench">
  ```text
  mvp/avatar/replace-avatar — #237
  ```

  **v4.12 (2026-07-08) — priority-based avatar replacement, text-source-only.** v4.2 dropped the `data_source` int input entirely — attribution is the text `source` string, and the live priority ladder (header-documented) is **Signal NFX \> LinkedIn / ScrapeCreators LinkedIn \> Serper \> Fundable / other \> Twitter**. The v4.5–v4.8 wave (2026-07-04) made duplicate-URL source attribution priority-aware (higher-confidence sources can repair provenance, lower ones can't overwrite it); **v4.9–v4.12 (2026-07-06/08)** block provider default/placeholder images, LinkedIn cover/background banners, and LinkedIn company-logo images from becoming main person avatars — an existing banner/logo main is treated as replaceable bad provenance, and main selection compares incoming vs current source priority (Fundable can no longer demote Serper). Called from #13039 (EL `profile_pic_url`), #2601 v2.0\+ (Serper result, best-effort), phase 4b #13031 (Exa avatar), and #12997 v2.3 (ScrapeCreators employee-card image). Do not resurrect the int-based scheme in a port.
</Card>

## Bios & insights

<Card icon="wrench">
  ```text
  mvp/bios/add-person-biography — #2536
  ```

  **The biography source-row writer.** Inserts raw biography material into `about_person` #365 — the feedstock table `create-llm-person-bios` reads. Callers: the provider phases (PDL bios in phase 3, EL bios in phase 4, Exa bios in phase 4b), the deep-research webhook #8303 fan-out (the joined deep-bio text becomes a source row), and the music person-profile helper #13159.
</Card>

<Card icon="cloud-arrow-up">
  ```text
  mvp/bios/create-llm-person-bios — #2537
  ```

  **v4.8 (2026-07-08) — short \+ long bio generation on `moonshotai/kimi-k2.5`** (two calls: ≤229-char `bio`, ≤500-char two-paragraph `bio_500`; Kimi since v3). The v4.x run: LLM-Biography history rows closed on **all** paths; **v4.2** newest-first lookup \+ recent-success **debounce** (a fresh successful bio short-circuits a duplicate regeneration); **v4.4** clean optional-skip when the `about_person` context is missing; **v4.5** falls back to investment-thesis / deal context as the bio source — the half that pairs with the orchestrator's post-thesis retry, giving thesis-only investors a real bio; **v4.6** removed example-name leakage from the prompts; **v4.7** uses a successful half as a conservative fallback when only one of the two generations succeeds; **v4.8** adds a final backup-model retry and closes still-empty optional bios as a `qa_passed` SKIP instead of a hard waterfall failure.
</Card>

<Card icon="cloud-arrow-up">
  ```text
  mvp/social/social-insights/get-social-insights — #12669
  ```

  **v1.26 (2026-07-11) — evidence-backed social insights on `x-ai/grok-4.5`.** #12669 prefetches two bounded Exa evidence sets (profile candidates and recent activity), then calls shared helper #13190 v2.1 for tool-free JSON synthesis. The primary uses temperature 0.2, medium reasoning, 1,600 output tokens, and a 90-second timeout; one compressed low-reasoning 1,100-token/75-second retry follows only after semantic validation fails. Strict ZDR, data-collection denial, `require_parameters`, provider usage logging, and explicit terminal history remain enabled. Discovered URLs still require two person-binding evidence points; only high-confidence URLs persist, while held candidates are audited with reasons. Called from the deep-research webhook #8303 fan-out.
</Card>

<Card icon="cloud-arrow-up">
  ```text
  mvp/deep-research/deep-person-basic — #12833
  ```

  **v4.1 / phase-6 route updated 2026-07-08 — the active deep-research launcher.** Builds the researcher prompt from the person's verified context and calls `deep-research-person-or-company` #12747, which POSTs to the Cloud Run `orbiter-enrich` service. The former #4578 Fly.io launcher is no longer called by phase 6 and has been reduced to a compatibility wrapper that delegates here.
</Card>

The **Cloud Run deep-research chain** — `deep-person-basic` #12833 v4.1 (`moonshotai/kimi-k2.6`, provider-pinned `["moonshotai", "fireworks"]`, `allow_fallbacks: false`, `openrouter:web_search` \+ `web_fetch`), the LITE variant #12863, the transport `deep-research-person-or-company` #12747, the `profile_enrichment_job` #686 ledger, and the `webhooks/enrich-profile` **#8303 v1.5 callback (2026-07-09 — six-function fan-out: #2536 → #2537 → #12669 → #2758 → the #12666 expertise re-run → the cross-waterfall bridge #13185**; since v1.4 the webhook stores `deep_bio` as narrative context only and no longer materializes education rows/nodes/edges from deep-research credentials) — is documented on [Deep Research Bio](/guides/enrichment/waterfall/deep-research-bio) — linked, not duplicated.

<Card icon="wrench">
  ```text
  mvp/enrich/kickoff-cross-waterfalls-from-expertise — #13185
  ```

  **Unversioned (2026-07-09) — the reusable film/music bridge.** Takes `master_person_id` \+ the `film_industry` / `music_industry` flags: the film branch runs `verify-imdb-url` #12677 and, **only on `confidence: "high"`**, stores the URL via `create-master-link` (`source: "IMDb Verify"`) and attaches via `add-imdb-to-master-person` #10509; the music branch runs `detect-and-enter-artist` #13171, gated on `$env.reverse_music_bridge != "off"`. Both branches are try\_catch-isolated and audit to `log_crash` (default `phase: "phase-8"`). Callers: the deep-research webhook #8303 v1.5, the IMDB person orchestrator #12880, and the music person-resolution orchestrator #13121 — so an expertise re-run from any waterfall can light up the other two.
</Card>

## FullEnrich (async contact tier)

<Card icon="cloud-arrow-up">
  ```text
  enrich/full-enrich-reverse-email-lookup — #13036
  ```

  **v1.4 (2026-06-09) — the async FullEnrich dispatcher.** Fired by #13039 on the new-person path only, when the cascade reports `needs_full_enrich: true` (EL-via-email produced no LinkedIn URL) and an input email exists. POSTs to FullEnrich's bulk reverse-email endpoint with our webhook URL, encoding `person_enrich_data_id` \+ `data_source` in BOTH the `name` field — live format `ped__{id}__{data_source}__{email}` — and the `custom` object; **v1.4** also carries `current_company_only` \+ `cascade_depth` in `custom` so bounded C-suite cascades survive the async hop. Returns immediately with FullEnrich's enrichment id; the profile arrives minutes later at #8594. The `data_source` threading exists because the webhook is a fresh HTTP request that would otherwise default to `live` — sandbox QA writebacks must not land in live.
</Card>

<Card icon="layer-group">
  ```text
  mvp/enrich/process-full-enrich — #13049
  ```

  **v1.10 (2026-07-04) — the FullEnrich applier.** Runs after the orchestrator closes (the webhook waits for `processing = false`): `linkedin_url` backfill \+ a FullEnrich `master_link`, `temp_`-name repair via #2649, PDL re-fire \+ EL fallback stashes (stored, not processed), a title-at-employer bio, `employment.all[]` → company resolution via `master-company-new` \+ deduped `work_experience` rows, then an async re-dispatch of `enrich-master-person` #13040 with **`force_run: true`** to bypass the debounce. **v1.9:** `current_company_only` filters `employment.all[]` to current roles before company resolution — the C-suite containment holds through the async tier. **v1.10:** repairs `master_person.master_company_id` from the resolved current work row before the async rerun.
</Card>

<Card icon="globe">
  ```text
  webhook/contact-finished — #8594   (API group Full Enrich, 1280, api:8hTTyqsU)
  ```

  **v1.9 (2026-06-09) — the FullEnrich webhook receiver.** Per item: read `custom.person_enrich_data_id` (fall back to parsing `name`), literal-dispatch `db.set_datasource` from `custom.data_source`, write the payload into `person_enrich_data.full_enrich`, then invoke #13049. **v1.6 (2026-05-30) dropped the `email_enrich_fallback` branch** — no table by that name exists in workspace 3. **v1.7** waits for the orchestrator's `enrich_history_person.processing = false` flag before applying (30 s grace polls inside a 30-minute deadline); **v1.8** is the emergency guard from the Carl→Robert live-corruption incident — any item with an **empty `custom.data_source` is refused**; **v1.9** threads `custom.current_company_only` \+ `cascade_depth` through to #13049. Response counters: `{matched_ped, processed, wait_timeouts, skipped, refused_no_data_source}`.
</Card>

## Fundable substrate (person side)

The company-side Fundable substrate — `fundable-lookup` #12698, the BigQuery mirror ingester #12689 with `_bq-execute` #12690 / `_upsert-org` #12691 / `_upsert-person` #12692, and `link-fundable-org` #12794 — is documented on [Company Waterfall Functions](/guides/enrichment/waterfall/company-functions) — linked, not duplicated. The person-side pieces:

<Card icon="wrench">
  ```text
  mvp/fundable/link-fundable-person — #12793
  ```

  **Headerless (unchanged since 2026-04-12):** one-step `db.edit` setting `fundable_people.master_person_id` via caller-supplied `lookup_field`/`lookup_value`. **Xano artifact — do not reimplement:** the function exists only to dodge a Xano `db.edit` UUID/scope bug on inline edits of `fundable_people`; in a port it is a one-line UPDATE. Called from #13039 when a Fundable match resolves and from phase 5.
</Card>

<Card icon="wrench">
  ```text
  mvp/investor/person-extract-cb-signal — #2687
  ```

  **Headerless (updated 2026-06-02, description-only) — the phase-9 CB/Signal overlay.** Reads `permalink_person.cb_extract` \+ `signal_extract` (the Crunchbase side is parsed upstream by `parse-cb-person` #2677) and upserts `investor_profile_person` #475: master links, merged `investor_type`, CB bios, primary location, investments (**Signal preferred over CB**), sector/stage summary, Signal avatar, investment range/sweet spot — then stamps `permalink_person.last_investor_enrich_at`. Also force-refreshed by the GCP thesis context builder's person branch (#12911, company pipeline).
</Card>

<Card icon="wrench">
  ```text
  batch-seed-fundable-person — #12853
  ```

  **v1.1 (2026-04-19) — the per-person Fundable seeder**, called from the one-shot `seed-fundable-people` **Task #155** (inactive). Creates the `master_person`, writes the Fundable linkback, and queues to `queue_enrich_person` — **v1.1** routes the queue write through `upsert-enrich-person` #12854 so re-seeding the same Fundable person cannot duplicate queue rows.
</Card>

## Contact \+ format helpers

<Card icon="wrench">
  ```text
  mvp/link/create-master-link — #2541
  ```

  **v1.8 (2026-07-08) — URL normalizer \+ `master_link` creator.** Dedups on the **lowercased** normalized URL (v1.7), **scoped by owner** (v1.8 — the same website can attach to a Person and separately to a Company), and branches per domain — x.com / instagram / linkedin / crunchbase / facebook / imdb / youtube for persons (LinkedIn also sets `master_person.linkedin_url`; Crunchbase adds the permalink), crunchbase / x / youtube / linkedin for companies — each branch launching async scraper enrichment. **v1.3 adds `suppress_downstream`, guarding all 9 async scraper dispatch sites**, and #13039 v1.10 passes it whenever `hold_downstream` or `cascade_depth > 0` — scraper fan-out is suppressed on contained paths. **The IMDb branch grew up in v1.4–v1.6 (2026-07-06/08):** person `imdb.com/name` links now dispatch `add-imdb-to-master-person` #10509 **synchronously** (data-source-safe) so EL/PDL/input-discovered IMDb links enter the controlled IMDb waterfall, and Deep-Research-Bio IMDb citations are subordinate to the dedicated resolver — a high-confidence `verify-imdb-url` #12677 result replaces the incoming citation URL before dedupe/attach. The Twitter branch's scraper is `scrape-twitter-person` #2106 **v1.11 (2026-07-05)** — Twitter-expanded URLs are person-link evidence only; no company creation or queueing. Source attribution rules (EL-first wins, `source` vs `data_source`): [Person Waterfall Entry Points](/guides/enrichment/waterfall/person-entry-points).
</Card>

<Card icon="wrench">
  ```text
  mvp/email/add-person-email — #2784
  ```

  **v1.2 (2026-05-27):** dedup-guarded `master_email` insert with `classify-email_v2` typing. v1.1 added `source` provenance; **v1.2** added the `orbiter_verified` bool — only caller-supplied input emails are marked `true`; PDL-discovered emails always stay `false` (they are not user-verified).
</Card>

<Card icon="wrench">
  ```text
  mvp/phone/add-person-phone — #2783
  ```

  **v1.1 (2026-05-24):** formats each phone via `llm-phone-format` #2540 (E.164 \+ local display, country context), dedups against `master_phone`, inserts with `source` provenance.
</Card>

<Card icon="cloud-arrow-up">
  ```text
  mvp/phones/llm-phone-format — #2540
  ```

  **Unversioned (active prompt `// v4`) — the LLM phone normalizer.** One OpenRouter call on `google/gemini-3.1-flash-lite` (temp 0.2, 30 s, system-message-only) returning `{country_calling_code, phone (E.164), phone_local_display}`; the catch logs and retries once on the **same** model. The E.164 output is the `master_phone` dedup key, so idempotency rests on near-deterministic formatting. Shared with the company pipeline — full as-built caveats (missing-key empty-input shape, stale Kimi tag) on [Company Waterfall Functions](/guides/enrichment/waterfall/company-functions).
</Card>

<Card icon="wrench">
  ```text
  mvp/address/add-person-primary-location — #2766
  ```

  **Headerless (unchanged since 2026-03-19):** geocodes a location string via `radar-maps-search-address` #2000 and hands the structured result (city / state / country / codes / lat-lng / timezone) to `primary-location-nodes-edges_v2` #4690 to create the location nodes and edges. Failures go to `log_crash` and it **always responds `"success"`** — location is optional enrichment by design.
</Card>

## Queue \+ gate infrastructure

<Card icon="wrench">
  ```text
  mvp/queue/upsert-enrich-person — #12854
  ```

  **v1.1 (2026-06-03) — single source of truth for ALL `queue_enrich_person` writes.** Callers never branch on "already queued?" — that logic lives here. One row per `master_person_id`, enforced **app-level only**: the table has no unique index on it, so idempotency lives in this function, not the DB (a concurrency race can still duplicate — flagged on [Person Waterfall Tables](/guides/enrichment/waterfall/person-tables)). Update path: `count`\+\+ on every duplicate request, `cascade_depth` only ever improves (min), `priority_tier` upgrades only, and **`deep_research` is OR-promoted** — a deep-research request survives an earlier shallow queue row. Insert path: `processing: false, count: 1` with the caller's source\_\* metadata. Callers: #13039 (queue path), #12702 (co-angels/partners), #12853, and the backfill tasks.
</Card>

<Card icon="clock-rotate-left">
  ```text
  mvp/queue/process-enrichment-queue — #12816
  ```

  **v1.2 (2026-07-04) — the operator batch drain serving BOTH queues** (`entity_type: "person" | "company" | "both"`; the person batch runs to completion first). Checks both kill switches (#4617 person, #12789 company) — an active switch skips that queue's section. Person section: candidate rows selected with **deterministic Xano-native filters** (v1.2 replaced the string-built lambda filter; `min_count` now defaults to 0, so the old "always pass `min_count: 0`" trap is gone), sorted `priority_tier / cascade_depth / id` → `max_items` batch → per row: lock → **sync** #13040 → delete on success; the catch releases the lock, bumps the error counter, and logs the per-row failure so stranded priority-1 school queues are visible. `dry_run: true` is read-only. **v1.1** routed dispatch through the gated orchestrators so `cascade_depth` gates apply to queued leaves. With Task #126 inactive, this function (or Run Now on the task) is how `queue_enrich_person` actually drains today.
</Card>

## Crons

Three tasks touch the person waterfall. Deployment policy for all of them lives on [Enrichment Crons](/guides/enrichment/waterfall/enrichment-crons).

| Task | Function | Data source | Freq | State |
| :-: | --- | --- | ---: | --- |
| **#192** | `mvp/enrich/sweep-person-non-bio-terminal-retries` #13260 | live | 60 s | **ACTIVE — load-bearing.** Nothing else executes a reserved terminal receipt; without it every person's receipt stays pending forever. |
| **#193** | same, sandbox twin | sandbox | 60 s | **ACTIVE.** v1.7 (2026-07-24) classifies status-0/timeouts as indeterminate and escalates only consecutive indeterminate cycles with no committed progress. |
| **#126** | `process-person-queue` → #13040 | live | 600 s | **`active = false`** — entity-throughput drain, must remain disabled. Gated to `priority_tier <= 1` since 2026-07-03. Its queue table is listed under [Build Drain Crons](/guides/enrichment/waterfall/enrichment-crons#build-drain-crons). |

In practice the queue is drained manually via `mvp/queue/process-enrichment-queue` #12816 or "Run Now" on Task #126.

---

## Terminal receipts \+ retry sweeps

The checkpoint/lease layer that decides whether a person run may be called finished. #13040 seeds a deterministic checkpoint set at the top of the run and records each stage against it; the sweeps are the durable fallback when an async dispatch is lost. This layer is what makes a person run terminate rather than hang — Task #192 is load-bearing on live (see [Enrichment Crons](/guides/enrichment/waterfall/enrichment-crons)).

<Card icon="flag-checkered">
  ```text
  mvp/enrich/seed-person-terminal-run — #13205
  ```

  Seeds one deterministic checkpoint set for a person enrichment run. Called by #13040 before phase 7 so the terminal receipt exists even if a later phase dies uncatchably — the fix for the phase-7 silent-kill class of failure, where a run ended with `enrich_success=false` and no crash log at all.
</Card>

<Card icon="clipboard-check">
  ```text
  mvp/enrich/record-person-terminal-stage — #13203
  ```

  **v-2026-07-27** — records a stage transition against the seeded checkpoint set. Sanctions `success -> running` for the auxiliary preflight generation stage, so a re-entrant preflight does not corrupt an already-successful receipt.
</Card>

<Card icon="circle-check">
  ```text
  mvp/enrich/run-person-orchestrator-terminal — #13207
  ```

  **v1.3 (2026-07-24)** — the orchestrator-side terminal. Settles the orchestrator barrier immediately after terminal materialization and before shared coordination, so a slow coordinating peer cannot hold the barrier open. Calls #13206 to guarantee the selected current company exists.
</Card>

<Card icon="rotate">
  ```text
  mvp/enrich/run-person-non-bio-terminal-continuation — #13235
  ```

  **v1.36 (2026-07-27)** — the independent non-bio continuation. The canonical run id (`$terminalRunId`) emits the company-less path, so a person with no resolvable employer still reaches a real terminal state instead of stalling on a dependency that can never be satisfied.
</Card>

<Card icon="broom">
  ```text
  mvp/enrich/sweep-person-non-bio-terminal-retries — #13260
  ```

  **v1.31 (2026-07-27) — driven by Task #192 (ACTIVE, live, 60 s) and Task #193 (ACTIVE, sandbox).** Reclaims due non-bio terminal retry receipts. A fresh-frame preflight fails only the `selected == dependency` case. Nothing else executes these receipts: without #192 every person's terminal receipt stays pending forever. It is the durable fallback for missed async terminal retry dispatches, and it re-enters the resolver chain (#12560, #12562, #12621, #13194) directly.
</Card>

| Path | ID | Role |
| --- | :-: | --- |
| `mvp/enrich/ensure-person-terminal-current-company` | **#13206** | v1.1 (2026-07-16) — derives the selected company from the canonical current-role row and guarantees it exists before terminal evaluation. |
| `mvp/enrich/plan-person-current-company-depth0` | **#13252** | v1.2 (2026-07-19) — plans the depth-0 current-company dispatch; reusable proof may belong to any real profile job. |
| `mvp/enrich/retry-person-current-company-dependency` | **#13277** | v1.2 (2026-07-22) — a claimed retry that fails closes through its owned lease token instead of leaving a running receipt stranded. |
| `mvp/enrich/repair-and-verify-person-current-role` | **#13219** | v1.0 (2026-07-16) — reusable terminal current-role repair \+ verification. |
| `mvp/enrich/verify-person-music-core-readiness` | **#13267** | v1.1 (2026-07-24) — makes negative music evidence a first-class successful outcome rather than a blocking gap. |
| `system/error-to-text` | **#13276** | Shared error-to-string normalizer used by the sweep's durable audit rows. |

## Deep Bio job machinery

The optional `deep_research=true` branch. Launch is reserved once (#13301), the Cloud Run provider calls back into #13296, and an isolated leased worker processes the payload — so a duplicate delivery acknowledges without overwriting the canonical payload. Spec: [Deep Research Bio](/guides/enrichment/waterfall/deep-research-bio).

<Card icon="inbox">
  ```text
  mvp/enrich/ingest-deep-bio-provider-callback — #13296
  ```

  **v1.0 (2026-07-24)** — fast durable ingress for Cloud Run Deep Bio callbacks. Immutable callback capture plus a random-token worker claim; duplicate deliveries ack without overwriting the canonical payload. This is an **API-driven root** — no function calls it.
</Card>

| Path | ID | Role |
| --- | :-: | --- |
| `mvp/enrich/reserve-person-deep-bio` | **#13301** | v1.2 (2026-07-24) — single-flight launch reservation; terminal automatic reservations return `already_completed` and stay non-launchable. Called from #12747. |
| `mvp/enrich/run-person-deep-bio-provider-worker` | **#13295** | Isolated leased Deep Bio provider callback worker. |
| `mvp/enrich/process-deep-bio-provider-callback` | **#13294** | v1.60-draft (2026-07-24) — persists an immutable Deep Bio validation envelope. |
| `mvp/enrich/process-legacy-deep-bio-provider-callback` | **#13293** | Legacy path — updates the matching `profile_enrichment_job` row from the Cloud Run result. |
| `mvp/enrich/run-person-callback-terminal` | **#13208** | v5.0-draft (2026-07-24) — verifies stored bytes against the immutable provider payload, then runs the callback-side terminal (aliases via #13202, social insights via #12669). |
| `mvp/enrich/transition-person-deep-bio-job-terminal` | **#13305** | v1.2 (2026-07-25) — returns the committed terminal transition directly. Also called by the barrier #13196. |
| `mvp/enrich/sweep-person-deep-bio-retries` | **#13297** | Bounded stranded-state driver for isolated Deep Bio receipts. **No cron and no function caller — currently undriven** (see Deviations). |
| `mvp/bios/enforce-current-role` | **#13298** | Called directly by #13040; validates the deep-research payload's current-role claims. |
| `mvp/bios/validate-deep-research-payload` | **#13299** | Structural validation of the provider payload before it is trusted. |
| `mvp/bios/llm-repair-deep-research-payload` | **#13291** | LLM repair pass for a payload that fails validation. |
| `mvp/bios/repair-unsupported-current-role-payload` | **#13292** | Repairs a payload whose asserted current role is unsupported by evidence. |

## Semantic-state finalization

`finalize-person-semantic-state` #13194 owns the phase-7 success gate: work/date/tense/UUID parity, education, leaf surfaces, current role, expertise, classifications, owner traceability, and graph readback. It is where a non-bio run proves it is complete, and it re-enters the edge resolvers itself.

<Card icon="scale-balanced">
  ```text
  mvp/enrich/finalize-person-semantic-state — #13194
  ```

  **v-2026-07-26** — all three bounded-preflight generation guards resolve the claimed row by ID. Requires exact relational-table ↔ graph parity for every award carrying a `node_uuid`, with no queue tolerance on that branch — which is why IMDB award-graph Task #191 is load-bearing for person terminal readback and why #13245 is reachable from here.
</Card>

| Path | ID | Role |
| --- | :-: | --- |
| `mvp/expertise/decide-semantic-policy` | **#13300** | v3.0-draft (2026-07-24) — authoritative version-bound semantic decision; non-role industry expertise is current unless contradicted. |
| `mvp/expertise/project-person-expertise-array` | **#13306** | v1.1 (2026-07-24) — list projection reads direct row fields before exact table/graph UUID parity. |
| `mvp/education/reconcile-school-company-identity` | **#13264** | v1.0 (2026-07-21) — post-enrichment canonical school identity reconciliation. |
| `mvp/company/reconcile-school-classification` | **#13233** | v1.0 (2026-07-17) — reconciles hard school evidence across `master_company` and the graph. |
| `mvp/company/classify-school-evidence` | **#13232** | v1.0 (2026-07-17) — deterministic, conservative school-evidence classifier. |
| `mvp/company/classify-investor-evidence` | **#13200** | v1.8 (2026-07-16) — requires affiliate-scope alignment before shared-LinkedIn investor inference. |
| `mvp/company/select-cross-surface-candidate` | **#13201** | v1.0 (2026-07-14) — deterministic person-scoped company candidate selector. |
| `mvp/company/reconcile-lifecycle-from-llm` | **#13249** | v1.0 (2026-07-18) — deterministically reconciles explicit `Company_LLM` closure signals. |
| `mvp/queue/process-imdb-award-graph` | **#13245** | v1.2 (2026-07-19) — drains the award-graph queue inline so the terminal readback can see exact parity. |

## Person alias subsystem

| Path | ID | Role |
| --- | :-: | --- |
| `mvp/person/upsert-person-alias` | **#13187** | v1.4 (2026-07-14) — returns the committed graph alias list and requires exact parity. Called directly by the entry gate #13039. |
| `mvp/person/select-verified-alias-candidates` | **#13202** | v1.0 (2026-07-14) — selects verified deep-research name aliases from the union of provider evidence. Called by the callback terminal #13208. |

## Record writers \+ leaf helpers

The materialization layer beneath the phases — every one is dedup/idempotent-keyed, and all are reached from `process-enrich-layer` #12615, phase 3 #12824, or `create-master-link` #2541. Listed here because a rebuild has to port them; they carry no orchestration logic of their own.

| Group | Functions |
| --- | --- |
| **Record writers** | `add-education-record` #2788 · `add-position-record` #12608 · `add-roles-to-join` #2242 · `add-roles` #10510 · `add-person-skills` #1459 · `add-honor-record` #12609 · `add-project-record` #12610 · `add-publication-record` #12611 · `add-volunteering-record` #12574 · `add-certification-record` #2787 · `add-person-interest` #12557 · `add-language-array` #2094 · `add-industry` #137 · `add-followers` #2785 · `add-person-links` #318 |
| **Work reconciliation** | `reconcile-source-duplicates` #13191 · `collapse-work-experience` #1860 · `best-current-role` #4540 · `llm-roles-reduction` #2243 · `resolve-employer-from-email-domain` #13304 · `pdl-date-format` #12565 |
| **Edge descriptions \+ transport** | `write-work-edge-description-to-graph` #13008 · `create-work-edge-description-from-graph` #13003 · `write-relationship-edge-description-to-graph` #13010 · `create-relationship-edge-description-from-graph` #13009 · `create-education-edges` #1898 · `get-school-domain` #12561 · `find-honor-issuer` #4586 · `check-openrouter` #13136 · `send-cypher` #2815 · `full-person-context` #12667 |
| **Provider fetchers** | `person-email` #12734 · `person-linkedin` #12612 · `get-exa-profile` #12936 · `scrape` #4515 |
| **Links, socials \+ permalinks** | `add-enrichment-link` #2539 · `scrape-instagram` #2108 · `scrape-twitter-person` #2106 · `scrapecreator-person-linkedin` #12556 · `scrapecreator-person-youtube` #12670 · `link-enrich-linkedin` #2775 · `re-direct-url` #4543 · `add-person-permalink` #2762 · `add-instagram-handle` #2109 · `add-twitter-person-join` #2107 · `parse-profile-contact-text` #13254 · `openrouter-grok45-social-call` #13190 · `instagram` #2487 · `twitter` #2491 · `canonicalize-linkedin-organization-url` #13195 · `classify-email_v2` #2756 · `archive-past-work-emails` #13186 |
| **Crunchbase** | `parse-person-page` #2760 · `process-person-profile` #2765 · `parse-cb-person` #2677 · `parse-company-page` #2647 · `company-page-enrich` #2812 · `parse-signal-nfx` #2668 |
| **Avatar \+ storage** | `convert-image-url-to-webp` #12756 · `save-image` #322 · `upload-file` #12725 · `delete-file` #12731 · `generate-avatar-file-path` #12738 · `get-file-path-from-url` #12733 · `get-google-storage-keys` #12728 · `generate-random-values` #12739 · `remove-emojis` #2523 |
| **Embeddings** | `create-vectors-string` #4676 · `create-gemini-vectors-string` #13125 · `create-gemini-vectors` #13114 |

## Company-side machinery reached from the person path

Owned by the [Company Waterfall](/guides/enrichment/waterfall/company-functions), but a person run reaches it directly — so a person rebuild must account for it. `reconcile-source-duplicates` #13191 can trigger a full company merge, and the shared date substrate is entered through `ensure-company-founded-date` #13234 during the terminal sweep.

| Group | Functions |
| --- | --- |
| **Company merge (via `reconcile-source-duplicates` #13191)** | `company-v2` #12956 · `snapshot-company` #12953 · `detect-conflicts-company` #12969 · `aggregate-scalars-company` #12954 · `merge-enrich-data-company` #12955 · `repoint-long-tail-company` #12957 · `fold-my-companies-on-survivor` #12962 · `fold-master-dates-on-survivor` #13229 · `dedup-experience-after-company-merge` #12971 · `graph-merge-company` #12958 · `resync-company-node-uuid` #12963 |
| **Shared date substrate** | `ensure-company-founded-date` #13234 · `project-master-dates` #13228 · `upsert-master-date` #13224 · `parse-date-parts` #13222 · `build-date-natural-key` #13221 |
| **Company enrich helpers (via `create-master-link` #2541)** | `company-website-enrich` #4519 · `scrape_extract_website_data` #2814 · `revenue-employees-ownership` #2811 · `get-company-revenue` #2544 · `add-company-permalink` #2656 · `add-company-email` #13257 · `scrape-twitter-company` #2448 · `get-website` #4521 · `add-company_instagram` #2449 · `add-instagram-company-join` #13258 · `add-twitter-company-join` #2759 |

## Deviations, deprecated & orphaned

Everything below is (or was) visible in workspace 3 but must NOT be treated as part of the active waterfall contract — deleted predecessors, deprecated chains, the surviving stub, legacy tables, and stale metadata a rebuild should not port.

| Path | ID | Status | Note |
| --- | :-: | --- | --- |
| `mvp/get-add/master-person` | **#12553** | **DELETED** | The legacy monolithic entry point was retired 2026-06-01 and has since been **deleted from workspace 3** — nothing can call it. The public HTTP entry is `POST /master-persons` endpoint #7977 (group 350, `api:WKgay2AU`) → #13039. The QA group holds no person endpoint. |
| `mvp/enrich/run-base-person-enrich` | **#12554** | **DELETED** | The legacy orchestrator is gone; only frozen copies remain — `run-base-person-enrich_BACKUP2` #12821 (v2.1, 2026-04-13) and `_BACKUP` #12727 (2026-03-31), archaeology only. #13040 is the **only** person orchestrator. |
| Work-derived `INVESTED_IN` | **#2800 v2.5** | Dead branch — still in the Cypher | The classifier hardcodes `isInvestor: false` (2026-07-05), so the `INVESTED_IN` w15 FOREACH in the work-edge Cypher can no longer fire from work-history titles. Investment edges come only from the deal writers (#12830 direct angel edges, #12702 round edges). Existing w15 work-derived edges linger in the graph. |
| Dynamic `:Angel` / `:C_Suite` labels | **#2758** | Dropped — open item | The v1.8 (2026-07-05) rebuild of `update-person-node` lost the dynamic label lambda and it has not been restored. Today `:Angel` is stamped only by #12702's investor branches, and **no live writer stamps `:C_Suite`** (the label survives on legacy nodes and keeps its `C_Suite(uuid)` index). |
| RocketReach chain | **#13032 / #13033** | Deleted (2026-05-30) | `get-rocketreach-person` \+ `process-person-rocket-reach` (the old Phase 4c) were removed with orchestrator v3.7; the `person_enrich_data.rocket_reach` column was dropped the same day. Do not port. |
| `mvp/enrich/process-person-phase-10` | **#12831** | No-op stub — **still called** | Empty stack (`{}`) since v2.0 (2026-04-19); phase 9 owns the deal fanout. #13040 v3.33 still invokes it with its own try/catch \+ phase-10 qa log — the planned orchestrator cleanup never happened. Removing the call site is safe whenever someone next touches #13040. |
| `mvp/expertise/resolve-person-expertise` | **#12668** | Legacy — uncalled | Out of the call path since #12666's 2026-06-19 header (which calls `resolve-person-expertise-v2` #12926 instead); last touched 2026-06-04. The old llm-system-prompts §3 consumer note still points at it. |
| `mvp/expertise/upsert-person-has-expertise` (weight input doc) | **#12925** | Stale comment (live function) | The `weight` input description still says `*80` — the live matched-edge formula is `min(round(10 + match_score × 160), 50)`, created edges flat 10. Trust #12926's header; do not propagate the `*80` text into docs or ports. |
| `mvp/expertise/llm-identify-person-expertise-v3` | **#13140** | Sandbox experiment — not wired | v3.1 (2026-06-18) clone of #12666 adding FOUNDED/C-suite company context; calls live #12926 but phase 8 does not call it (its v3 siblings #13137/#13138/#13139 were promoted into the v2 locked specs on 2026-06-18). |
| `webhooks/deep-research-bio-webhook` | **#3082** | Deprecated (tagged) | The first-generation deep-research callback, keyed on the older `deep_biography` #591 table. Still routable; nothing launches jobs against it. Superseded by `enrich-profile` #8303 \+ `profile_enrichment_job` #686 — see [Deep Research Bio](/guides/enrichment/waterfall/deep-research-bio). `deep_biography` is retained as historical data. |
| `data_source` (table) | **#161** | Legacy — no longer an entry-point input | #13039 v1.4 dropped the `data_source_id` input that referenced it. The table itself still exists and older tables (`master_link`, `master_email`, `email_activity`, …) still carry FK columns to it — legacy-referenced, not removed. Business attribution is the text `source` field; the environment selector is the text `data_source` input. |
| `kill_switch_blocked_people` (table) | **#611** | **Deprecated 2026-06-30** | #13039 v1.13 removed the blocked-person side-write; `log_crash` #542 is the sole kill-switch audit trail. No reprocessing task exists or **ever** drained this table — it is retained only as historical data. |
| `mvp/stop/check-kill-switch` | **#2774** | Legacy predecessor | The pre-v2 person kill switch, still present live but not the pipeline gate (`check-kill-switch-v2` #4617 is). Do not wire it into anything new. |
| `mvp/work/add-employment-current` | **#4552** | Misfiled — not in the person waterfall | Listed in the roster under current-role sync, but a whole-workspace caller search (all 1,057 functions, comments stripped) finds its only caller is `mvp/work/resolve-current-work` #2799, which is called only by `mvp/signature/email-signature-process-only` #10519. It belongs to the **email-signature** flow, not person enrichment. No person execution root reaches it. |
| `mvp/enrich/sweep-person-deep-bio-retries` | **#13297** | Built, **undriven** | The bounded stranded-state driver for isolated Deep Bio receipts has **no cron and no function caller** — all 49 workspace tasks were checked. Its sibling #13260 has Task #192; this one has no equivalent. Live Deep Bio recovery therefore depends entirely on the provider calling back into #13296; a receipt stranded before that callback has no sweeper. |
| `mvp/investor/cascade-deal-participants` | **#12856** | Company-side only | Reached only from `add-all-fundable-deals` #12703 and `tool/backfill-fundable-org-companies` #13066. Phase 9 #12830 removed the cascade call at D1/D2 (2026-07-02) — the remaining mentions in #12830/#12831 are version-history comments, not code. |
| `mvp/funding/add-all-fundable-deals` | **#12703** | Company-side only | Sole live caller is `process-company-phase-7` #12813. No person root reaches it. |
| `Project` node label | **#12579 v6** | Relabeled 2026-07-04 | Project nodes are now created as `Entity:Work:Project` (with `display_label: "Project"`), joining publications under the unified `Work` parent. Pre-v6 bare `:Project` nodes linger in the graph. Update queries that match on the bare label. |

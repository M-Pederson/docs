---
title: "Create My Note"
api: "POST https://xh2o-yths-38lt.n7c.xano.io/api:iuc7DvAP/my-notes"
description: "Create a new note or document."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField body="title" type="string">
  Title of the note.
</ParamField>
<ParamField body="note" type="string">
  Content of the note.
</ParamField>
<ParamField body="tiptap_reference" type="string" required>
  Auto-generated Tiptap reference for docs.
</ParamField>
<ParamField body="my_person_id" type="integer">
  Associate the note with a person.
</ParamField>
<ParamField body="my_company_id" type="integer">
  Associate the note with a company.
</ParamField>
<ParamField body="my_person_mentions" type="array">
  Array of person IDs mentioned in the note.
</ParamField>
<ParamField body="my_company_mentions" type="array">
  Array of company IDs mentioned in the note.
</ParamField>
<ParamField body="is_document" type="boolean">
  Whether this is a document (true) or a simple note (false).
</ParamField>

<ResponseField name="id" type="integer">
  The ID of the newly created note.
</ResponseField>
<ResponseField name="created_at" type="timestamp">
  Creation timestamp.
</ResponseField>
<ResponseField name="user_id" type="integer">
  The ID of the user who created the note.
</ResponseField>
<ResponseField name="title" type="string">
  Title of the note.
</ResponseField>
<ResponseField name="note" type="string">
  Content of the note.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST \
  'https://xh2o-yths-38lt.n7c.xano.io/api:iuc7DvAP/my-notes' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -d '{
    "title": "Meeting Notes",
    "note": "Discussed Q4 targets...",
    "tiptap_reference": "abc123",
    "is_document": false
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1700000000,
  "updated_at": null,
  "user_id": 10,
  "title": "Meeting Notes",
  "note": "Discussed Q4 targets..."
}
```
</ResponseExample>

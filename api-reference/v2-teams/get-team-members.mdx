---
title: 'Query all team_member records'
api: 'GET https://xh2o-yths-38lt.n7c.xano.io/api:idMy4x2v/team-members'
description: 'Query all team_member records Authentication: required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Query Parameters

<ParamField query="team_id" type="integer" required>
  The team_id parameter.
</ParamField>

<ParamField query="page" type="integer">
  The page parameter.
</ParamField>

<ParamField query="offset" type="integer">
  The offset parameter.
</ParamField>

<ParamField query="limit" type="integer">
  The limit parameter.
</ParamField>


## Response

<ResponseField name="itemsReceived" type="integer">
  The itemsReceived field.
</ResponseField>

<ResponseField name="curPage" type="integer">
  The curPage field.
</ResponseField>

<ResponseField name="nextPage" type="integer">
  The nextPage field. Can be `null`.
</ResponseField>

<ResponseField name="prevPage" type="integer">
  The prevPage field. Can be `null`.
</ResponseField>

<ResponseField name="offset" type="integer">
  The offset field.
</ResponseField>

<ResponseField name="perPage" type="integer">
  The perPage field.
</ResponseField>

<ResponseField name="itemsTotal" type="integer">
  The itemsTotal field.
</ResponseField>

<ResponseField name="pageTotal" type="integer">
  The pageTotal field.
</ResponseField>

<ResponseField name="items" type="array">
  The items field.
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET https://xh2o-yths-38lt.n7c.xano.io/api:idMy4x2v/team-members \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN"
```
</RequestExample>

<ResponseExample>
```json 200
{
  "itemsReceived": 1,
  "curPage": 1,
  "nextPage": 1,
  "prevPage": 1,
  "offset": 1,
  "perPage": 1,
  "itemsTotal": 1,
  "pageTotal": 1,
  "items": []
}
```
</ResponseExample>

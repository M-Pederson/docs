---
title: 'Add note_collaborator_comment record'
api: 'POST https://xh2o-yths-38lt.n7c.xano.io/api:iuc7DvAP/my-note-collaborator-comments'
description: 'Add note_collaborator_comment record Authentication: required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Request Body

<ParamField body="updated_at" type="number">
  The updated_at field.
</ParamField>

<ParamField body="my_note_id" type="integer" required>
  The my_note_id field.
</ParamField>

<ParamField body="comment" type="string" required>
  The comment field.
</ParamField>

<ParamField body="getstream_message_id" type="string">
  GetStream message ID for webhook sync
</ParamField>


## Response

<ResponseField name="id" type="integer">
  The id field.
</ResponseField>

<ResponseField name="created_at" type="number">
  The created_at field.
</ResponseField>

<ResponseField name="updated_at" type="number">
  The updated_at field. Can be `null`.
</ResponseField>

<ResponseField name="user_id" type="integer">
  The user_id field.
</ResponseField>

<ResponseField name="my_note_id" type="integer">
  The my_note_id field.
</ResponseField>

<ResponseField name="comment" type="string">
  The comment field.
</ResponseField>

<ResponseField name="getstream_message_id" type="string">
  GetStream message ID for webhook sync Can be `null`.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST https://xh2o-yths-38lt.n7c.xano.io/api:iuc7DvAP/my-note-collaborator-comments \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "updated_at": 1,
      "my_note_id": 1,
      "comment": "example-comment",
      "getstream_message_id": "example-getstream_message_id"
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1,
  "updated_at": 1,
  "user_id": 1,
  "my_note_id": 1,
  "comment": "example-comment",
  "getstream_message_id": "example-getstream_message_id"
}
```
</ResponseExample>

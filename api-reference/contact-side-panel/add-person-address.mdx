---
title: "Add Person Address"
api: "POST https://xh2o-yths-38lt.n7c.xano.io/api:iTL91sBE/add/my_person/address"
description: "Create and add an address record to a person."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField body="my_person_id" type="integer" required>
  The person ID.
</ParamField>
<ParamField body="formatted_address" type="string" required>
  The full formatted address string.
</ParamField>
<ParamField body="address_label" type="string">
  Label for the address (e.g. `home`, `office`).
</ParamField>
<ParamField body="address_note" type="string">
  Optional note about the address.
</ParamField>

<ResponseField name="id" type="integer">Record ID.</ResponseField>
<ResponseField name="my_person_id" type="integer">Person ID.</ResponseField>
<ResponseField name="master_address_id" type="integer">Master address record ID.</ResponseField>

<RequestExample>
```bash cURL
curl -X POST \
  'https://xh2o-yths-38lt.n7c.xano.io/api:iTL91sBE/add/my_person/address' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -d '{
    "my_person_id": 1,
    "formatted_address": "123 Main St, New York, NY 10001",
    "address_label": "office"
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1700000000,
  "my_person_id": 1,
  "master_address_id": 10
}
```
</ResponseExample>

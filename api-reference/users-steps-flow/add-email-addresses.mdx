---
title: "Add Email Addresses"
api: "POST https://xh2o-yths-38lt.n7c.xano.io/api:_wjCLRXt/new-user/email-addresses"
description: "Add email addresses during the onboarding flow."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField body="email_address" type="string">Email address to add.</ParamField>
<ParamField body="email_type" type="string" required>Type (e.g. `work`, `personal`).</ParamField>
<ParamField body="active" type="boolean">Whether the email is active.</ParamField>

<RequestExample>
```bash cURL
curl -X POST \
  'https://xh2o-yths-38lt.n7c.xano.io/api:_wjCLRXt/new-user/email-addresses' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -d '{"email_address": "john@work.com", "email_type": "work", "active": true}'
```
</RequestExample>

<ResponseExample>
```json 200
{}
```
</ResponseExample>

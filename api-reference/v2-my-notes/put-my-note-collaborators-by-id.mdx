---
title: 'Edit''s permission of a person to share the note/document to.'
api: 'PUT https://xh2o-yths-38lt.n7c.xano.io/api:iuc7DvAP/my-note-collaborators/{my_note_collaborator_id}'
description: 'Edit''s permission of a person to share the note/document to. Authentication: required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Path Parameters

<ParamField path="my_note_collaborator_id" type="integer" required>
  The my_note_collaborator_id parameter.
</ParamField>


## Request Body

<ParamField body="permission_level" type="string" required>
  The permission_level field.
</ParamField>


## Response

<ResponseField name="id" type="integer">
  The id field.
</ResponseField>

<ResponseField name="created_at" type="number">
  The created_at field.
</ResponseField>

<ResponseField name="updated_at" type="number">
  The updated_at field. Can be `null`.
</ResponseField>

<ResponseField name="my_note_id" type="integer">
  References the `my_note` table to link to the specific note being shared.
</ResponseField>

<ResponseField name="shared_with_user" type="integer">
  References the `user` table, indicating which user the note is shared with.
</ResponseField>

<ResponseField name="permission_level" type="string">
  Defines the access level for the shared note (e.g., read-only, editor).
</ResponseField>

<RequestExample>
```bash cURL
curl -X PUT https://xh2o-yths-38lt.n7c.xano.io/api:iuc7DvAP/my-note-collaborators/1 \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "permission_level": "example-permission_level"
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1,
  "updated_at": 1,
  "my_note_id": 1,
  "shared_with_user": 1,
  "permission_level": "viewer"
}
```
</ResponseExample>

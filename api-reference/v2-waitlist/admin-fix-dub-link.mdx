---
title: 'Admin: Fix/recreate Dub link for a specific entry'
api: 'POST https://xh2o-yths-38lt.n7c.xano.io/api:g7_LZ7hE/admin/fix-dub-link'
description: 'Admin: Fix/recreate Dub link for a specific entry Authentication: not required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

## Request Body

<ParamField body="x_admin_key" type="string" required>
  Admin API key
</ParamField>

<ParamField body="email" type="string" required>
  Email of entry to fix
</ParamField>


## Response

<ResponseField name="success" type="string">
  The success field.
</ResponseField>

<ResponseField name="email" type="string">
  The email field.
</ResponseField>

<ResponseField name="referral_code" type="string">
  The referral_code field.
</ResponseField>

<ResponseField name="referral_link" type="string">
  The referral_link field.
</ResponseField>

<ResponseField name="dub_link_id" type="string">
  The dub_link_id field.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST https://xh2o-yths-38lt.n7c.xano.io/api:g7_LZ7hE/admin/fix-dub-link \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -d '{
      "x_admin_key": "example-x_admin_key",
      "email": "user@example.com"
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "success": "example-success",
  "email": "example-email",
  "referral_code": "example-referral_code",
  "referral_link": "example-referral_link",
  "dub_link_id": "example-dub_link_id"
}
```
</ResponseExample>

---
title: 'Admin: Get aggregate waitlist statistics'
api: 'GET https://xh2o-yths-38lt.n7c.xano.io/api:g7_LZ7hE/admin/stats'
description: 'Admin: Get aggregate waitlist statistics Authentication: not required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

## Query Parameters

<ParamField query="x_admin_key" type="string" required>
  Admin API key (X-Admin-Key header)
</ParamField>


## Response

<ResponseField name="total" type="string">
  The total field.
</ResponseField>

<ResponseField name="by_status" type="string">
  The by_status field.
</ResponseField>

<ResponseField name="referrals" type="string">
  The referrals field.
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET https://xh2o-yths-38lt.n7c.xano.io/api:g7_LZ7hE/admin/stats \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1"
```
</RequestExample>

<ResponseExample>
```json 200
{
  "total": "example-total",
  "by_status": "example-by_status",
  "referrals": "example-referrals"
}
```
</ResponseExample>

---
title: "Get Calendar Events"
api: "GET https://xh2o-yths-38lt.n7c.xano.io/api:LR2ywW7R/calendar-events"
description: "Gets all events for a user."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

### Response

<ResponseField name="events" type="array">
  List of calendar event objects for the authenticated user.
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET \
  'https://xh2o-yths-38lt.n7c.xano.io/api:LR2ywW7R/calendar-events' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "events": []
}
```
</ResponseExample>

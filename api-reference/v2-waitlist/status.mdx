---
title: 'Check waitlist status by email'
api: 'GET https://xh2o-yths-38lt.n7c.xano.io/api:g7_LZ7hE/status'
description: 'Check waitlist status by email Authentication: not required'
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

## Query Parameters

<ParamField query="email" type="string" required>
  Email address to check
</ParamField>


## Response

<ResponseField name="email" type="string">
  The email field.
</ResponseField>

<ResponseField name="position" type="string">
  The position field.
</ResponseField>

<ResponseField name="people_ahead" type="string">
  The people_ahead field.
</ResponseField>

<ResponseField name="status" type="string">
  The status field.
</ResponseField>

<ResponseField name="referral_code" type="string">
  The referral_code field.
</ResponseField>

<ResponseField name="referral_link" type="string">
  The referral_link field.
</ResponseField>

<ResponseField name="referral_count" type="string">
  The referral_count field.
</ResponseField>

<ResponseField name="link_clicks" type="string">
  The link_clicks field.
</ResponseField>

<ResponseField name="joined_at" type="string">
  The joined_at field.
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET https://xh2o-yths-38lt.n7c.xano.io/api:g7_LZ7hE/status \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1"
```
</RequestExample>

<ResponseExample>
```json 200
{
  "email": "example-email",
  "position": "example-position",
  "people_ahead": "example-people_ahead",
  "status": "example-status",
  "referral_code": "example-referral_code",
  "referral_link": "example-referral_link",
  "referral_count": "example-referral_count",
  "link_clicks": "example-link_clicks",
  "joined_at": "example-joined_at"
}
```
</ResponseExample>

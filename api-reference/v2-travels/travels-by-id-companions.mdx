---
title: 'Create Travels Companions'
api: 'POST https://xh2o-yths-38lt.n7c.xano.io/api:WtHnIWRU/travels/{travel_id}/companions'
description: 'Add a single companion (travel_companion) to a travel. If the companion''s my_person record links to a user, also create '
---

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Path Parameters

<ParamField path="travel_id" type="integer" required>
  The travel_id parameter.
</ParamField>


## Request Body

<ParamField body="my_person_id" type="integer" required>
  The my_person_id field.
</ParamField>


## Response

<ResponseField name="id" type="integer">
  The id field.
</ResponseField>

<ResponseField name="created_at" type="number">
  The created_at field.
</ResponseField>

<ResponseField name="updated_at" type="number">
  The updated_at field. Can be `null`.
</ResponseField>

<ResponseField name="travel_id" type="integer">
  The travel_id field.
</ResponseField>

<ResponseField name="my_person_id" type="integer">
  The my_person_id field.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST https://xh2o-yths-38lt.n7c.xano.io/api:WtHnIWRU/travels/1/companions \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_AUTH_TOKEN" \
  -d '{
      "my_person_id": 1
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1,
  "updated_at": 1,
  "travel_id": 1,
  "my_person_id": 1
}
```
</ResponseExample>

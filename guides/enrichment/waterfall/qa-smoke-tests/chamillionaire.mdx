---
title: "Chamillionaire (Hakeem Seriki) — LinkedIn only, triggers IMDb + Music waterfalls"
description: "Person waterfall QA stress test fixture."
---

# Chamillionaire (Hakeem Seriki) — LinkedIn only, triggers IMDb + Music waterfalls

<Info>
  **Person Waterfall · Stress test**
</Info>

[Back to QA Smoke Tests](/guides/enrichment/waterfall/qa-smoke-tests) · [Original parity QA page](/guides/enrichment/waterfall/person-parity-qa-smoke-tests)

**Passed QA** — **certified 2026-07-26.** The terminal-preflight generation deadlock that previously blocked certification is **fixed and verified** in `#13194` — see [Terminal preflight generation deadlock](/guides/open-work/terminal-preflight-generation-deadlock). The certifying run **hit the selected-company retarget** (company 1 → 4), the exact condition that used to deadlock, and completed anyway: `terminal_preflight_generation` `success` on both passes, `full_enrich=true`, ~25 min, 9 `success` + 1 `superseded` receipts (the superseded row is the v3.76 pre-phase-7 reservation placeholder, correctly superseded by the real materialization). Music queues banded correctly and did **not** collapse: `queue_mb_recording` 15 rows all at tier 1, `queue_mb_release_group` 5 at tier 1 / 12 at tier 2 despite `count=7` re-adds each, `queue_mb_event` 3 at tier 2. `queue_mb_artist` drained to 0 during the run via task `194`. Graph parity: `Company` 5/5 and `Film_TV_Award` 5/5 exact; `Film_TV` 2 materialized against 16 still queued; `Person` = 2 by design (a `Music_Artist`-labelled stage-name identity linked `SAME_AS` to the canonical Person). Caveats: he generates **no label or work rows**, so the `13083`/`13086` debounce port is deployed but unexercised; and his IMDb footprint is thin (16 titles, **0 queued awards**), so the IMDb equalization, under-banding and zero-aggregation checks come back **vacuous** — pair with Ryan Reynolds for those.

Notes:

- Grammy-winning rapper.
- Angel investor in Maker Studios, Ring, Cruise, Lyft, and Dropbox; ex-EIR at Upfront Ventures; partner at Supreme Partners Fund.

```json
{
  "links": [
    "https://www.linkedin.com/in/chamillionaire/"
  ],
  "phone_numbers": [],
  "email": [],
  "company_name": null,
  "title": null,
  "user_id": 0,
  "full_name": "Chamillionaire",
  "queue": false,
  "deep_research": true,
  "cascade_depth": 0,
  "priority_tier": 0,
  "source_function": null,
  "source_entity_id": 0,
  "source_entity_uuid": null,
  "source_entity_type": null,
  "is_c_suite": false,
  "source": null,
  "hold_downstream": false,
  "data_source": null,
  "orbiter_verified": false
}
```

## Sandbox Test Results:

**100% PASS** — 2026-07-28, chain run 13 (19.8 min settle, zero real crashes, health clean).

- Company retarget (company 1 → 4) certified through receipts.
- **Defect found by this fixture**: the enrich-vs-merge race — person-resolution merged a deep-bio duplicate away mid-run; every downstream failure of the doomed run was a symptom. Fixed as typed `person_merged_away` classification (#13040 v3.86), root-caused via Xano function request history.

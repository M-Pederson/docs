---
title: "Delete Person Address"
api: "POST https://xh2o-yths-38lt.n7c.xano.io/api:iTL91sBE/delete/my_person/address"
description: "Remove an address from a person record."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField body="master_address_id" type="integer" required>Master address record ID.</ParamField>
<ParamField body="my_person_id" type="integer" required>Person ID.</ParamField>

<RequestExample>
```bash cURL
curl -X POST \
  'https://xh2o-yths-38lt.n7c.xano.io/api:iTL91sBE/delete/my_person/address' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -d '{"master_address_id": 10, "my_person_id": 1}'
```
</RequestExample>

<ResponseExample>
```json 200
{}
```
</ResponseExample>

---
title: "Add Company Email"
api: "POST https://xh2o-yths-38lt.n7c.xano.io/api:KdpaUs_r/add/my_company/email"
description: "Add an email to a company record."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField body="my_company_id" type="integer" required>Company ID.</ParamField>
<ParamField body="email_address" type="string" required>Email address.</ParamField>
<ParamField body="email_type" type="string">Email type.</ParamField>
<ParamField body="active" type="boolean">Whether the email is active.</ParamField>
<ParamField body="email_note" type="string">Optional note.</ParamField>

<ResponseField name="id" type="integer">Record ID.</ResponseField>
<ResponseField name="my_company_id" type="integer">Company ID.</ResponseField>
<ResponseField name="master_email_id" type="integer">Master email record ID.</ResponseField>

<RequestExample>
```bash cURL
curl -X POST \
  'https://xh2o-yths-38lt.n7c.xano.io/api:KdpaUs_r/add/my_company/email' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -d '{"my_company_id": 1, "email_address": "info@acme.com"}'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": 1,
  "created_at": 1700000000,
  "my_company_id": 1,
  "master_email_id": 20
}
```
</ResponseExample>

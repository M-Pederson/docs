---
title: "Snooze Thread"
api: "PUT https://xh2o-yths-38lt.n7c.xano.io/api:58PjJ6J9/email-threads/{thread_id}/snooze"
description: "Snooze a thread by moving it to the Snoozed folder with restore metadata."
---

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>
<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>
<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

<ParamField path="thread_id" type="string" required>Thread ID.</ParamField>
<ParamField body="nylas_grant_id" type="integer" required>Nylas grant ID.</ParamField>
<ParamField body="snooze_until" type="timestamp" required>When to unsnooze.</ParamField>
<ParamField body="snoozed_folder_id" type="string" required>The snoozed folder ID.</ParamField>

<RequestExample>
```bash cURL
curl -X PUT \
  'https://xh2o-yths-38lt.n7c.xano.io/api:58PjJ6J9/email-threads/thread_abc/snooze' \
  -H 'Content-Type: application/json' \
  -H 'X-Data-Source: live' \
  -H 'X-Branch: v1' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -d '{"nylas_grant_id": 1, "snooze_until": 1700200000, "snoozed_folder_id": "folder_123"}'
```
</RequestExample>

<ResponseExample>
```json 200
{}
```
</ResponseExample>

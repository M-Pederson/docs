---
title: 'List Master Companies'
api: 'GET https://xh2o-yths-38lt.n7c.xano.io/api:10Em-CSF/master-companies'
description: 'Query all master company records'
---

## Overview

Returns a paginated list of all master company records, with optional search filtering.

## Headers

<ParamField header="X-Data-Source" type="string" required>
  Identifies the data source for the request.
</ParamField>

<ParamField header="X-Branch" type="string" required>
  The Xano branch to target (e.g. `v1`, `staging`, `dev`).
</ParamField>

<ParamField header="Authorization" type="string" required>
  Bearer token obtained from login or signup.
</ParamField>

## Query Parameters

<ParamField query="query" type="string">
  Search query to filter companies.
</ParamField>

<ParamField query="page" type="integer">
  Page number for pagination.
</ParamField>

<ParamField query="limit" type="integer">
  Number of items per page.
</ParamField>

<ParamField query="offset" type="integer">
  Number of items to skip.
</ParamField>

## Response

<ResponseField name="itemsReceived" type="integer">
  Number of items returned in the current response.
</ResponseField>

<ResponseField name="curPage" type="integer">
  The current page number.
</ResponseField>

<ResponseField name="nextPage" type="integer">
  The next page number, or null if no more pages.
</ResponseField>

<ResponseField name="prevPage" type="integer">
  The previous page number, or null if on the first page.
</ResponseField>

<ResponseField name="offset" type="integer">
  The offset used for the query.
</ResponseField>

<ResponseField name="itemsTotal" type="integer">
  Total number of items matching the query.
</ResponseField>

<ResponseField name="pageTotal" type="integer">
  Total number of pages.
</ResponseField>

<ResponseField name="items" type="array">
  Array of master company records containing id, company_name, logo, tagline, about, about_500, and is_school.
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET "https://xh2o-yths-38lt.n7c.xano.io/api:10Em-CSF/master-companies?query=acme&page=1&limit=20" \
  -H "Content-Type: application/json" \
  -H "X-Data-Source: YOUR_DATA_SOURCE" \
  -H "X-Branch: v1" \
  -H "Authorization: Bearer YOUR_TOKEN"
```
</RequestExample>

<ResponseExample>
```json 200
{
  "itemsReceived": 1,
  "curPage": 1,
  "nextPage": null,
  "prevPage": null,
  "offset": 0,
  "itemsTotal": 1,
  "pageTotal": 1,
  "items": [
    {
      "id": 100,
      "company_name": "Acme Corp",
      "logo": "https://example.com/logo.png",
      "tagline": "Innovation at its best",
      "about": "A leading technology company...",
      "about_500": "A leading technology company specializing in...",
      "is_school": false
    }
  ]
}
```
</ResponseExample>
